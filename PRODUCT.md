# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

한 사람 — 저장소 주인. 이더리움 선물을 **매매하는 동안 실시간으로** 띄워 두고 수급·청산·포지션을 읽고, 그 자리에서 바로 주문한다. 데스크톱과 휴대폰 둘 다에서 본다. 공유용이 아니다.

## Product Purpose

ETH 무기한 선물 매매를 위한 개인 작업대. 시장 미시구조(체결 수급·풋프린트·청산·OI·호가벽)와 계좌 상태를 한 화면에 모으고, 같은 화면에서 주문(진입·청산·TP/SL)을 낸다.

성공 기준(사용자 확인):
- **수수료를 이기는 매매** — 화면이 비용을 넘는 판단과 체결(메이커 우선)을 돕는다. 사용자 원칙: «수수료를 못 이기면 트레이딩이 아니다». 수수료 할인은 가정하지 않는다.
- **빠른 판단** — 몇 초 안에 지금 상황(수급·청산·포지션)을 읽고 행동할 수 있다.

## Operating Context

- 매매 중 상시 띄워 두는 화면. 급할 때는 바이낸스 앱/웹에서 직접 주문하기도 한다 — 계좌·성과 집계는 대시보드 밖에서 낸 주문도 포함해야 한다.
- 거래는 ETHUSDC(메이커 0bp · 테이커 4bp), 분석은 거래량이 풍부한 ETHUSDT. 합산 수급·OI 는 바이낸스 선물 + OKX.
- 서버는 집 WSL 머신에서 돌고 브라우저로 접속한다. 알림은 PWA 웹푸시.

## Capabilities and Constraints

- 스택: Python aiohttp 서버(`dashboard/server.py`) + 빌드 없는 바닐라 JS/CSS(`dashboard/live/app.js`, `styles.css`, `index.html`). 다크/라이트 테마 토큰이 `styles.css :root` 에 있다.
- 배포: main 에 푸시하면 서버 워처가 ~10분 안에 끌어와 대시보드를 재시작한다(`check_deploy_drift.sh` 선행).
- 실주문 경로(수동 진입·청산·청산맵 TP/SL)가 화면에 있다 — 길게 누르기·30초 확인·상한(증거금 50%·위험모델)으로 보호한다.
- 화면에 올리는 신호는 연구 측정을 통과한 것이 원칙이다(표본외·대조군). 미결정: 장기 디자인 시스템 문서(DESIGN.md) 없음.

## Product Principles

1. **수수료를 이기는 쪽으로 기운다.** 체결·크기·표시 모두 비용을 넘는 결정을 돕는 방향으로 — 메이커 체결을 기본으로 두고 테이커가 되는 경로는 그 사실을 보인다.
2. **몇 초 안에 읽힌다.** 매매 중에 보는 화면이라, 지금 행동에 필요한 값이 가장 먼저·가장 크게 읽힌다.
3. **밀도가 장식을 이긴다.** 한 화면에 많이 담고 스캔 속도를 지킨다. 장식은 정보를 밀어내지 않는 한에서만.
4. **한국어 화면.** 모든 사용자 문구는 한국어다.

## Accessibility & Inclusion

특정 요구사항은 정해지지 않았다. 기존 코드가 지키는 대비(보조 문구 4.5:1 이상 주석)·줄인 모션 존중·키보드 포커스는 유지한다.
