"""Unified training pipeline entrypoints.

This package adds a thin orchestration layer on top of the existing scripts so
the project can move toward a simpler 3-step pipeline without breaking the
current workflow.
"""

