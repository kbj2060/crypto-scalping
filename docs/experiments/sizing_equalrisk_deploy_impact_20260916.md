# 「동일위험 1/safeMAE」를 배포하면 무엇이 변하는가 (2026-09-16)

- 요청: 사용자 *"동일위험 1/safeMAE 가 배포되면 어떻게 변하는지 자세히 알려줘"*
- 스크립트 `scripts/research_sizing_equalrisk_deploy_impact_20260916.py` · 산출 `tmp/eth_equalrisk_deploy_20260916/`
- 무작위 진입 **120,000건** · 보유 4h · 비용 5.88bp · 평균명목 정규화

## ⭐먼저 — **이미 배포돼 있다**

`live_eth_risk_sizing_policy_20260913::survival_leverage` = `MAX_DRAWDOWN × 100 / safe_mae_pct`
= **정확히 1/safeMAE** 다. 그런데 실계좌 72왕복에서 모델 상한이 묶은 건 **1건**뿐이었다.
진짜 질문은 «배포하면»이 아니라 **«왜 안 보이나»** 다.

## A 왜 안 보이나 — 순자산 상한 6배가 **거의 항상 이긴다**

| | p1 | p10 | p25 | **p50** | p75 | p90 | p99 |
|---|---:|---:|---:|---:|---:|---:|---:|
| safe_mae(%) | 2.75 | 4.85 | 6.34 | **8.29** | 10.83 | 13.72 | 22.75 |
| survival 배수 | 4.40 | 7.29 | 9.23 | **12.06** | 15.77 | 20.62 | 36.35 |

- survival 중앙 **12.06배** vs 순자산 상한 **6.0배**
- survival 이 6배보다 작은 비율 = **4.16%**(safe_mae > 16.67% 인 봉)
- ⇒ **95.84% 에서 순자산 상한이 이긴다.** 실계좌 72건 중 1건만 모델이 묶은 이유가 이것이다.
- 켈리(`kelly_robust_leverage`) = **35.5배** 로 아예 구속력이 없다(dead).
  `HARD_CAP_X` 25배는 safe_mae < 4% 일 때만 묶는다.

**현행 배포 `min(survival, 6배)` 의 실효 가중 CV = 0.05.** 즉 **지금 사이징은 사실상 고정이다**
— SD −3.3% · 하위1% −3.3% · 청산율 −1.3%. 거의 아무 일도 하고 있지 않다.

## B 상한을 올리는 길 — **권하지 않는다**

| EQUITY_X | 모델이 묶는 비율 | 실효 배수 중앙 | **평균 명목(현재=1.0)** |
|---:|---:|---:|---:|
| 6 (현행) | 4.16% | 6.00 | 1.00 |
| 8 | 14.55% | 8.00 | **1.31** |
| 10 | 32.09% | 10.00 | **1.57** |
| 12 | 49.48% | 12.00 | **1.76** |
| 25 | 94.90% | 12.06 | **2.18** |

모델이 «보이기» 시작하지만 **평균 명목이 1.3~2.2배 커진다** — 위험 개선이 아니라 **더 크게 걸기**다.
경주의 «평균명목 동일» 전제가 깨지므로 그 결과를 근거로 쓸 수 없다.

## C ⭐답 — **평균명목 보존형**: 상한은 그대로, 모양만 1/safeMAE

권고 명목에 `중앙safeMAE / safeMAE` 배수를 곱한다. 총 노출은 불변, **배분만 바뀐다.**

| 판 | 가중CV | 가중 범위 | SD | vs기준 | 하위1% | vs기준 | **50배 청산율** | vs기준 |
|---|---:|---|---:|---:|---:|---:|---:|---:|
| 현행 고정(기준) | 0.00 | [1.00, 1.00] | 145.3 | — | −430.4 | — | 13.31% | — |
| **현행 배포** min(surv,6배) | **0.05** | [0.20, 1.01] | 140.5 | −3.3% | −416.1 | −3.3% | 13.14% | −1.3% |
| 보존형 (클립 없음) | 0.47 | [0.09, **8.38**] | 112.2 | −22.7% | −308.3 | −28.4% | **7.74%** | **−41.9%** |
| **보존형 (±2배 클립)** ← 권장 | 0.38 | [0.46, 1.86] | 115.9 | **−20.2%** | −321.0 | **−25.4%** | **8.43%** | **−36.7%** |
| 보존형 (±1.5배 클립) | 0.29 | [0.64, 1.44] | 121.8 | −16.2% | −342.6 | −20.4% | 9.69% | −27.2% |

클립 없이 쓰면 가중이 **8.38배**까지 벌어진다 — 극단 봉 하나가 계좌를 좌우한다. **±2배 클립**이
개선의 대부분(청산율 −36.7% vs −41.9%)을 가져오면서 극단을 없앤다.

## 구체적으로 무엇이 바뀌나

- **저변동 봉**: 권고 수량이 최대 **2배**로 커진다(지금은 6배 상한에 눌려 못 키운다)
- **고변동 봉**: 최대 **절반**으로 줄어든다
- **평균 명목**: 그대로. 총 노출·증거금 사용량이 늘지 않는다
- **상한 3종**(원장중앙×2 · 순자산×6 · 모델): **그대로 유지** — 파산 방어는 손대지 않는다
- **얻는 것**: 50배 기준 청산 도달률 **13.31% → 8.43%**, 일손익 SD −20%, 하위1% 손실 −25%

## 한계 — 정직하게

1. **수익 개선이 아니다.** 무작위 진입의 평균은 −비용 그대로다. 사이징은 **분포를 고르게** 할 뿐이다
   (실력 e 가정별 로그성장에서 1위이긴 하지만 그건 e 를 외생으로 준 계산이다).
2. **실계좌 22일에서는 이 규칙이 졌다.** 그 창에서 고변동이 3.5배 벌었기 때문인데,
   전체 1,717일에서 그 효과는 **ρ +0.0037 (p .20)** 으로 사라진다. 그래서 전체판을 근거로 삼는다.
3. `safe_mae` 모델이 틀리면 반대로 간다. 다만 눈금은 검증돼 있다 —
   q=0.9 의 실제 초과율 9.92%(목표 10%).
4. 🔴**이 자리는 09-15 에 E\|r\| 배수를 얹었다가 «켜기 전에 철회»한 바로 그 자리다**
   (`assert "evr_mult" not in e` 재부착 방지까지 있다). 다만 **근거의 성질이 다르다** —
   E\|r\| 배수는 원장 72왕복(증분의 84%가 상위 5건)이었고, 이건 **무작위 진입 120,000건 · 1,717일**이다.
5. 이 문서는 **측정이지 배포 제안서가 아니다.** 실제로 켜려면 `dashboard/**` 를 고쳐야 하고,
   그 경로는 이 저장소에서 두 번 장애를 냈다(커밋 → `check_deploy_drift.sh` → 머지 → 워처 순서).

---

## 🔴배포 중단 — 동시 세션이 **정반대 기울기**를 같은 원장에서 찾았다 (2026-09-16)

사용자가 «(c) 절충(상한 8배)» 을 선택해 구현 직전, 배포 규약대로 `check_deploy_drift.sh`(exit 0 ✅)
와 서버 md5 대조를 돌리다 **서버 HEAD 가 `77269ab` → `3a0fa15` 로 전진**한 것을 발견했다.
내 워크트리의 `dashboard/server.py` 가 서버와 달랐다(다른 세션이 −325줄 수정).
`git fetch` 후 확인하니 **같은 자리를 다른 세션이 이미 작업 중**이었다.

### 커밋 `eb774b7` — *"크기 축은 살아있다 — 다만 E\|r\| 모델이 아니라 «변동성 비례」가"*
같은 실계좌 **72왕복**에서:
- 같은 노출로 정규화한 뒤 **변동성 백분위에 «비례해» 크게** 걸면 `플랫×q − 플랫` **+194.45$**,
  CI [+0.70,+5.79], P=0.001 · **날짜블록 순열 p=0.0000 · 백분위 100%**
- 모델은 불필요 — 1시간 실현변동성 백분위가 E\|r\| 와 같은 일을 한다(둘 차이 CI 0 포함)
- 부수: **명목 편차만 없애도(플랫)** 수익 +328 → **+653**, MDD 549 → **155**, 수익/MDD 0.60 → **4.21**

### ⚠️기울기 방향이 내 제안과 **정반대**다

| | 근거 | 기울기 |
|---|---|---|
| 동시 세션 | 실원장 **72왕복 · 22일** | 고변동에 **크게** |
| 이 문서 | 무작위 진입 **120,000건 · 1,717일** | 고변동에 **작게**(1/safeMAE) |

둘은 실제로 **같은 사실의 양면**이다 — 내 전체데이터판이 이미 보였듯 원장의
«고변동이 3.5배 벌었다»(ρ +0.243)는 전체에서 **ρ +0.0037 (p .20)** 으로 사라진다.
그래서 «수익 기울기»는 22일 표본에서만 강하고, «위험 기울기»는 1,717일에서 강하다.
**어느 쪽으로 기울일지는 이 두 표본으로 갈리지 않는다.**

⇒ **기울기를 배포하지 않는다.** 사용자 승인은 «(c) 절충」에 대한 것이었고, 그 승인은
동시 세션의 반대 방향 증거를 모르는 상태에서 나온 것이다. 실계좌 노출을 반대로 기울이는
변경을 그 상태로 켜면 안 된다.

### ⭐네 분석이 **전부 일치하는 것 하나** — 그것만 먼저 하면 된다

| 분석 | 재량(들쭉날쭉) vs 일정하게 |
|---|---|
| 동시 세션(원장 72) | 플랫만으로 MDD 549 → **155** · 수익/MDD 0.60 → **4.21** |
| 이 세션 원장판(72) | 재량 샤프 **0.220**(꼴찌) vs 고정 0.760(1위) |
| 이 세션 전체판(120,000) | 재량 SD **+46.8%** · 하위1% **+57.7%**(고정보다 나쁘다) |
| 09-11(82,167) | «재량 분포가 최악 — 고정보다도 나쁘다» |

**기울기의 방향은 갈려도, «명목 편차를 없애라」는 네 분석이 전부 같은 말을 한다.**
그리고 그건 상한도 배수도 안 건드리는 변경이다.
