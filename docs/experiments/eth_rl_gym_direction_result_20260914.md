# 방향 RL 결과 — gym 은 섰고, 게이트는 못 넘었다 (2026-09-14)

- 사용자 지시: *"이 강화모델 에이전트는 롱숏홀드의 행동만 갖고 진입 및 청산 로직을 따르며 학습하는거야."* → *"그렇게 진행해줘"*(권고안 확정).
- 설계: `docs/eth_rl_gym_control_agent_design_20260914.md` v2. 게이트는 **실행 전에** 사전등록했다.
- 코드: `scripts/rl_gym_direction_env_20260914.py`(gym) · `research_eth_rl_gym_direction_ppo_20260914.py`(PPO·대조군·쌍둥이·게이트) · `research_eth_rl_gym_direction_ic_20260914.py`(방향 정보 측정) · `research_eth_rl_gym_feature_stability_20260914.py`(피쳐 분석).
- 산출: `data/research/eth_rl_gym_direction_20260914/`.
- `fresh_forward_bar_by_bar=true · trade_ledgers_used_as_input=false · saved_parent_exit_timestamps_used=false · future_rows_used_for_entry=false`

## 0. 한 문단

gym 은 제대로 섰다(배포 하네스와 1e-9 일치). 양성대조도 살아 있다(OOS AUC .800). 그 위에서 PPO 는 **세 팔 전부 «관망」으로 수렴**했고, 같은 피쳐의 지도학습 쌍둥이는 세 창 전부 −5.5~−6.3bp 였다. 모델을 안 거치고 직접 재니 **적중률이 세 창 모두 동전 아래**(47.0~48.5%)인데 손익분기는 53.9~54.8% 다. 피쳐 자체는 죽어 있지 않다 — 43열 중 16열이 세 창에서 부호를 지키고(우연 기대 5.4), anti-stable 점검도 통과했다(+0.40/+0.58/+0.07/+0.85). **그런데 크기가 모자란다: 최대 단일피쳐 |IC| 0.030 vs 손익분기 IC 0.098~0.119 = 필요치의 27%.** 그리고 43열을 GBM 으로 **합치면 오히려 나빠진다**(표본내 IC +0.204 → 표본외 −0.009~−0.070).

## 1. 무엇을 세웠나

**gym**(`rl_gym_direction_env_20260914.py`): 배포 스택 위에서 {홀드, 롱, 숏}만 받는다. 크기(MAE 분위 × 상한 6배)·지평(`planning_hold`)·손절 3%(봉내)·예산 사다리·만기·펀딩·세 조각 비용은 배포 함수를 **import** 한다. 빈 슬롯에서만 결정하고 보유 중에는 결정이 없다(semi-MDP).

**자체점검이 그 사실을 증명한다.** 무작위 진입 하네스(`research_fresh_forward_random_entry_stack_20260914.walk`)와 같은 난수 순서로 행동을 뽑는 정책을 넣으면 네 셀(VAL·OOS × acc 0.5·0.6) 전부 계좌배수·손절수·사다리수·시간적분 노출이 **1e-9 안에서 일치**한다. 결정 전이의 보상 합 = 로그 계좌배수도 1e-9 안이다. 상태 43D, 절단 불변 통과.

**상태 43D**: 연속 재료 31(패널의 `ev_*`/`trg_*` 이진 발동 플래그 제외 — 09-12 지시) + 실현변동성 4(rv12/48/288·Parkinson48) + 레짐 원핫 8. 정규화 통계는 학습창에서 동결.

## 2. G1 양성대조 — 통과

같은 상태·같은 PPO 루프에 타깃만 «앞으로 48봉 변동성 확장」으로 바꾸면:

| | VAL | OOS | TEST |
|---|---|---|---|
| AUC(P롱−P숏 vs volexp) | .816 | **.800** | .795 |

⭐**이 실험의 전부가 여기 달려 있다.** 딥러닝이 실패하면 «정보 없음」과 «내 코드가 망가짐」이 겉보기가 같다. 대조가 .80 으로 살아 있으므로 아래 방향 결과는 **정보 탓**이다.

## 3. PPO — 세 팔 전부 관망으로 수렴

| 팔 | 비용 | 엔트로피 | 결과 |
|---|---|---|---|
| main v1 · taker v1 | peg / 테이커 | 고정 0.01·0.003 | 창당 800~1,600 거래, −4~−11bp. **G2 0/3 · G3 0/3 · G4 0/3 · DSR 2e-5** |
| taker2 | 테이커 | 고정 0.001 | 창당 **0~3 거래**(전 창 n=0) |
| main_anneal · taker_anneal | peg / 테이커 | 0.02 → 0.001 감쇠 | 넓게 탐색(창당 1,198 거래 @ −6.1bp) → **2 거래로 수렴** |

🔴**v1 은 눈금 결함이 있었다.** 보상이 ln(순자산비)라 거래당 ±0.005 인데 엔트로피 계수 0.01 이 결정마다 붙어 **«무작위로 거래하는 값」이 «거래 손실」보다 컸다**. 열 씨드 전부 엔트로피 0.6~1.0 나트에 머물며 거래를 멈출 유인이 없었다. 보상을 100배(퍼센트 로그)로 고쳤다. ⭐교훈: **보상 눈금과 정형화 계수의 비를 실행 전에 손으로 계산한다** — 결정당 기대보상 ≈ 0.5 × (거래 800 / 결정 17,000) ≈ 0.023 인데 엔트로피 보너스가 0.011 이었다.

⭐**감쇠 팔이 «조기 수렴」과 «엣지 없음」을 가른다.** 첫 30~60회 반복 동안 창당 800~1,200 거래로 넓게 탐색했고 그 구간의 순수익이 일관되게 −6bp 였다. 탐색이 먼저 죽은 게 아니라 **탐색해 보니 전부 손해**라서 관망한 것이다. 관망은 이 gym 의 지배 대조군이다(홀드 = 0bp).

## 4. 왜 −6bp 인가 — 비용 그 자체다

| 창 | 항상롱 | 항상숏 | 평균 = −비용 | 절반차 = 표류 |
|---|---|---|---|---|
| VAL | −8.50 | −4.54 | **−6.52** | −1.98 |
| OOS | −8.40 | −3.46 | **−5.93** | −2.47 |
| TEST | −4.94 | −7.61 | **−6.28** | **+1.34** |

⭐**비용 전 방향 수익이 0 이다.** 두 측면의 평균이 정확히 왕복 비용(peg 5.88 + 손절추가 + 펀딩)이고, 남는 표류항은 창마다 **부호가 바뀐다**(VAL·OOS 숏 유리, TEST 롱 유리). 드리프트를 타는 정책은 창을 넘기면 죽는다 — 09-12 DSAC 가 겪은 그 구조다.

## 5. 지도학습 쌍둥이 — RL 이 아니라 정보가 문제다

같은 피쳐·같은 창·같은 비용으로 «부호 있는 순손익 bp 회귀 + |예측| > c 기권」(09-12 목적함수). 임계 c 는 VAL 에서 {0,5,10,20,40}bp 를 쓸어 고른다.

| 창 | 쌍둥이 순bp | 일군집 t | Δ(정책−쌍둥이) |
|---|---|---|---|
| VAL | **−6.26** | −1.03 | −2.30 ± 2.36 |
| OOS | **−5.51** | −0.92 | +0.29 ± 2.79 |
| TEST | **−6.32** | −1.14 | +2.17 ± 1.83 |
| 2022~23 | −8.44 | −2.61 | +2.74 ± 1.48 |

⭐**«확신할 때만 베팅」해도 비용을 못 넘는다.** 선택적 예측은 이 저장소가 여러 번 시도한 마지막 우회로인데 여기서도 닫혔다. 그리고 정책이 쌍둥이를 이기지 못하므로(G3 0/3) **RL 이 더한 값은 0** 이다 — 보상이 청산 시 한 번 관측되는 문제라 예상대로였다.

## 6. 방향 정보를 직접 재다 (모델 밖)

라벨 `y = ½(r_long − r_short)·1e4` = **비용이 상쇄된 순수 가격 이동 bp**. 같은 봉에서 양측면을 각각 한 번 넣어 배포 스택이 청산할 때까지 굴린 결과다. 학습창 3만9,528건 · 평균 **+0.03bp**(편향 없음) · SD 97.8bp.

| 창 | n | 표본외 IC | p | 적중률 | 손익분기 | 상위10% 순bp |
|---|---|---|---|---|---|---|
| VAL | 2,928 | −0.013 | .468 | **48.5%** | 54.8% | −2.42 |
| OOS | 2,160 | −0.009 | .677 | **48.2%** | 53.9% | +17.50 |
| TEST | 3,408 | **−0.070** | **.000** | **47.0%** | 54.5% | −30.55 |
| 2022~23 | 16,785 | −0.019 | .014 | 48.4% | 54.1% | −2.84 |

🔴**적중률이 네 창 모두 동전 아래다.** 표본내 IC 는 +0.204 인데 표본외는 전부 음수이고 TEST 는 유의하게 음수다. OOS 의 상위10% +17.50 은 TEST 의 −30.55 와 부호가 반대이고 크기가 비슷하다 — **잡음**이다. (라벨이 최대 288봉 겹치므로 이 칸들의 t 는 읽지 않는다.)

## 7. 피쳐 분석 — 정보는 있는데 4배 모자란다

### 7.1 anti-stable 점검은 **통과**했다
학습창 IC 와 평가창 IC 의 스피어만 상관:

| TRAIN ↔ | VAL | OOS | TEST | 2022~23 |
|---|---|---|---|---|
| | **+0.399** | **+0.577** | +0.070 | **+0.853** |

⭐사전등록 중단조건(음수면 중단)에 걸리지 않았다. **피쳐는 죽어 있지 않다** — SOL 에서 본 −0.38 과 다른 그림이다. 43열 중 **16열이 세 평가창 전부에서 학습창 부호를 지킨다**(우연 기대 5.4열). 0/3 은 6열뿐이다.

### 7.2 그런데 크기가 모자란다 — 산술
부호 베팅의 기대 방향수익 ≈ IC · E|y| · √(2/π) 이므로 **손익분기 IC ≈ 비용 / (E|y| · 0.798)**:

| 창 | 비용 | E\|이동\| | **손익분기 IC** |
|---|---|---|---|
| VAL | 6.52bp | 68.5bp | **0.119** |
| OOS | 5.93bp | 75.8bp | **0.098** |
| TEST | 6.28bp | 69.7bp | **0.113** |

**관측된 최대 단일피쳐 |학습창 IC| = 0.030(rv48) = 필요치의 27%.** 이게 이 문서의 핵심 숫자다. 이전 종결들은 «AUC .50~.55」라고만 적었는데, 여기서는 **얼마나 모자란지**가 나온다.

### 7.3 두 계열이 정반대로 행동한다
| 피쳐 | TRAIN | VAL | OOS | TEST | 부호 | 십분위 스프레드 |
|---|---|---|---|---|---|---|
| rv48 | +.0302 | −.0188 | +.0298 | −.0732 | 1/3 | 12.5bp |
| park48 | +.0234 | −.0099 | +.0334 | −.0754 | 1/3 | 5.3 |
| **dist_hi48** | +.0195 | +.0205 | +.0290 | **+.0638** | **3/3** | 6.8 |
| **p_slow** | −.0195 | −.0249 | −.0047 | −.0411 | **3/3** | 6.7 |
| **ret12** | −.0183 | −.0304 | −.0132 | −.0465 | **3/3** | 7.7 |
| rv12 | +.0174 | −.0123 | +.0400 | −.0581 | 1/3 | 6.7 |
| atr_pct | +.0166 | −.0099 | +.0463 | −.0709 | 1/3 | 4.9 |
| **dem** | −.0154 | −.0186 | −.0112 | −.0417 | **3/3** | 8.3 |

⭐**변동성 계열(rv·park·atr)은 방향에서 부호가 뒤집힌다.** 크기 축에서 그렇게 잘 듣던 바로 그 피쳐들이다 — [[eth_rl_state_feature_family_ablation_20260912]] 의 «타깃이 정한다」가 여기서 다시 확인된다. **위치·모멘텀 계열(dist_hi·ret12·p_slow·dem)은 세 창 부호를 지키고 TEST 에서 오히려 커진다**(|IC| .04~.06). 다만 그 .06 조차 필요치 0.11 의 절반이다.

### 7.4 합치면 나빠진다
| | 표본내 IC | 표본외 IC |
|---|---|---|
| 단일 최고 피쳐 | .030 | +.020~.064(부호 유지 계열) |
| **GBM 43열** | **+.204** | **−.009 ~ −.070** |

🔴**43열을 비선형으로 합치면 표본내 IC 가 7배 오르고 표본외는 음수가 된다.** 이 저장소의 반복 패턴(«정규화 비율은 그 분모에 진다» · «57피쳐 vs atr_pct 단독 +0.001»)이 방향 축에서 가장 뚜렷하게 나온 판이다.

## 8. 게이트 판정

| 게이트 | 결과 |
|---|---|
| G1 양성대조 ≥ .65 | ✅ **PASS** (.800) |
| G2 순수익 3창 중 ≥2 | ❌ **FAIL** 0/3 (전 팔) |
| G3 쌍둥이 대비 +2SE | ❌ FAIL 0/3 |
| G4 역방향·같은측면 | ❌ FAIL 0/3 |
| G5 2022~23 부호 일치 | ✅ 일치(둘 다 음수) |
| G6 씨드 부호일치 / DSR | 부호일치 ✅ (전부 음수) · DSR 2e-5 · 0.057 |
| G7 붕괴 2축 | 새 눈금에서 **관망 붕괴**(거래 < 10) |
| G8 누수 신호 | ✅ 없음 — 순열 중요도 상위(rv48·atr_pct·p_slow)의 단변량 효과가 평평하지 않고, 모델이 1피쳐 규칙을 이기지도 못한다 |
| G9 테이커 10bp | ❌ 같은 결과(더 나쁨) |

**사전등록대로 G2 미달 ⇒ 방향 RL 축 종결.** 이 gym 에서 {롱, 숏, 홀드}로 비용을 넘는 정책은 찾지 못했다.

## 9. 내가 틀린 것 (재발 방지)

1. **보상 눈금 대 엔트로피 계수**(§3). 실행 전에 «결정당 기대보상 대 정형화 보너스」 비를 손으로 계산했어야 했다. 첫 20 씨드가 낭비됐다.
2. **anti-stable 점검이 전부 NaN 이었는데 그대로 출력했다.** 창 안에서 상수인 레짐 원핫 열의 IC 가 NaN 이라 상관 전체를 오염시켰다. `+nan` 을 보고서야 알았다 — **«점검을 돌렸다»와 «점검이 값을 냈다»는 다르다.** 이제 유효 열 수를 같이 찍는다(41/43).
3. **첫 보고에서 OOS 상위10% +17.5bp 를 그대로 전했다.** TEST 가 −30.6 으로 오면서 잡음임이 드러났다. 겹치는 라벨에서 한 창의 분위 수치를 단독으로 인용하지 않는다.

## 10. 무엇이 답을 바꿀 수 있나

- **5분봉 해상도에서는 남은 표현이 없다.** 재료 31열 + 변동성 + 레짐을 연속값으로 넣고, 선형·비선형·강화학습 셋 다 돌렸다. [[eth_scale_in_axis_closed_six_variants_20260914]] 가 분할 축에서 내린 결론과 같은 자리다.
- **필요한 것은 IC 0.03 → 0.11.** 모델을 바꿔서 되는 크기가 아니다(합치면 오히려 음수). 정보원을 바꿔야 한다 — 주문단위 L2 연속수집이 유일하게 열려 있고 그건 달력 대기다.
- **비용을 반으로 줄이면 손익분기 IC 가 0.11 → 0.055 다.** 여전히 관측치의 1.8배지만 거리가 절반이 된다. peg 실측 5.88bp 를 더 줄일 여지는 09-11 에 2.2bp 로 측정됐다.
- **위치·모멘텀 계열이 3/3 부호를 지킨 것**은 이 판의 유일한 양성 신호다. 크기는 못 되지만 **다른 타깃**(크기·변동성)에서는 이미 값을 하고 있고, 방향 축에서 이 계열만 따로 긴 지평으로 미는 것은 09-12·09-13 에 이미 닫혔다(H=144 는 독립블록이 225년 필요).

## 11. 후속 — 보상을 바꿔 거래를 유도 (2026-09-14, 사용자 지시 *"보상함수를 바꿔서 거래를 유도하고 실패와 성공을 경험시켜"* · *"내가 이전에 만든 dsac 모델을 참고해도 좋아"*)

**우려 한 문장**: 보상을 바꾸면 에이전트는 돈이 아니라 바뀐 보상을 푼다. 그래서 유도 장치는 학습에만 쓰고 **판정은 바뀌지 않은 순손익과 «같은 측면 무작위 대비 Δ」로만** 한다.

네 모드(각 5씨드 · 150반복 · peg 비용 · 평가는 실제 비용):
| 모드 | 무엇 | 출처 |
|---|---|---|
| `forced` | 홀드를 행동에서 제거. 빈 슬롯마다 롱/숏 강제 — 실패·성공을 빠짐없이 겪는다 | — |
| `holdpen` | 홀드 결정마다 −λ(0.02, 보상 눈금) | DSAC `r5_idle`(specialist_idle_penalty) |
| `gross` | 학습 환경만 수수료 0(방향 자체를 배우게). 평가는 실제 비용 | — |
| `antiflat` | 액터 손실에 λ·relu(0.05 − P거래), λ 0.08 → 0 선형 감쇠 | DSAC `anti_flat_lambda/min_abs/anneal`(`train_rl_dsac_agent.py:868-870, 1036-1047`) |

DSAC 의 나머지 보상 6항(drawdown·quality·time_decay·adverse_hold·kelly_regime·terminal)은 09-12 에 «손튜닝 항이 많으면 에이전트가 시장이 아니라 보상 설계를 푼다」로 중립화됐던 것이라 넣지 않는다.

**사전등록 판정** (실행 전 고정):
- P1 **Δ(정책 − 같은 측면 무작위)** 순bp, 씨드 5 짝지음, > +2SE 가 3창 중 ≥2. 같은 측면 무작위는 정책의 행동 빈도를 그대로 두고 시점만 뒤섞으므로 비용·측면 편향이 같다 — 남는 차이가 «시점 선택 실력」이다.
- P2 **적중률(비용 전 부호) > 손익분기**(VAL 54.8 · OOS 53.9 · TEST 54.5%) 가 ≥2/3 창.
- P3 역방향 < 정책 · G5 구간 부호 · G6 씨드 부호 · G8 누수 신호는 그대로.
- 정직한 예상: §6 의 직접 측정(적중 47.0~48.5%, IC 음수)이 이미 «강제로 거래시키면 얼마나 맞히나」에 답했다. 네 모드는 그 답을 RL 경로로 재현하거나 뒤집을 것이고, 뒤집히면 §6 을 다시 본다.

### 결과 (7팔 · 각 5씨드 · 150반복 · 평가는 실제 peg 비용) — 사용자 질문 *"이전 DSAC −60% 보다 나은가"* 포함

| 팔 | 유도 | 창당 거래 | 순bp VAL/OOS/TEST | 적중률 | 측면 | Δ(vs 같은측면 무작위) VAL/OOS/TEST | G2 | G7 |
|---|---|---|---|---|---|---|---|---|
| forced | 홀드 제거 | 1,160~1,627 | −7.75/−7.51/−5.44 | 49.2~50.4% | **1.00** | +0.04±.04 / +0.01±.01 / −0.06±.06 | 0/3 | 측면 붕괴 3/3 |
| forced_bal | + 균형 2.0 | 1,162~1,625 | −5.62/−5.88/−6.70 | 49.6~50.5% | 0.96~0.98 | +0.05±.15 / −0.09±.08 / −0.28±.60 | 0/3 | 3/3 |
| forced_ent | + 균형 2.0 + 엔트로피 0.01 | 1,162~1,624 | −5.78/−5.49/−6.19 | 49.7~50.7% | 0.99~1.00 | +0.36±.22 / +0.01±.11 / +0.24±.09 | 0/3 | 3/3 |
| holdpen λ=0.02 | DSAC r5_idle | **0** | 0 | — | — | 0 | 0/3 | 관망 3/3 |
| **holdpen λ=0.1** | DSAC r5_idle ×5 | 1,163~1,625 | −5.99/−4.43/−7.40 | 48.9~50.3% | 0.57~0.76 | −0.39±.50 / **+3.56±1.42** / −0.39±1.13 | 0/3 | **없음** |
| gross | 학습만 수수료 0 | 252~327(씨드 편차 큼) | +107/−18.5/−22.4 | 50.2 / **35.7 / 31.5**% | 0.80 | +105±56 / +16±20 / −13±9 | 0/3 | 씨드 부호 불일치 |
| antiflat | DSAC 액터 항(감쇠) | **0** | 0 | — | — | 0 | 0/3 | 관망 3/3 |

**판정: P1 0/7 팔(≥2/3 창에서 +2SE 없음) · P2 0/7(적중률 전부 손익분기 53.9~54.8% 아래) · P3 역방향은 전 팔에서 더 나쁨(−4~−8bp, 정책이 «드리프트 측면」을 골랐다는 뜻이지 실력이 아님).** ⇒ 유도 보상 축도 닫힌다.

무엇이 보였나:
- **유도는 됐다.** 홀드 벌점 0.1 은 거래를 창당 1,600 건으로 올리고 측면도 0.57~0.76 으로 섞였다 — «실패와 성공을 경험」하는 상태를 실제로 만들었다. 그 위에서 배운 정책의 적중률이 48.9~50.3% 다.
- **강제 거래는 세 변형 전부 «항상 한 측면」으로 붕괴했다**(씨드마다 롱/숏이 갈림). 홀드 로짓을 −1e9 로 막으면 소프트맥스가 포화돼 균형 항·엔트로피 기울기가 0 이 된다 — 균형 계수를 10배, 엔트로피를 10배 올려도 같았다. 상태 의존 신호가 상수 편향(펀딩이 숏에 유리, 창별 드리프트)보다 작다는 뜻이다.
- **점수 순위 판독**(`research_eth_rl_gym_forced_score_readout_20260914.py`: 롱−숏 로짓을 창 안에서 순위화, 상위 10% 롱·하위 10% 숏)으로 측면을 구조적으로 균형시키면 forced_bal 이 **2씨드에서 +3.50/+7.60/+3.76bp**(vs 같은측면 무작위)로 보였다가 **5씨드에서 +0.07±1.54 / +2.97±2.32 / −1.17±2.14 로 사라졌다.** [[feedback_paired_seeds_required_when_side_is_injected_20260914]] 의 정확한 재현이고, 그날 오전 «2씨드 값을 그대로 보고」했던 것을 이번엔 «2씨드라 SE 를 못 믿는다」고 적어 두었다가 확인했다.
- **OOS(2026-01~03)만 세 번째로 혼자 양수다**(holdpen01 Δ+3.56 · 상위10% IC 판독 +17.5 · gross +16). 세 창 중 한 창은 우연 기대치 안이고 이 저장소가 «시기 한정」을 세 번 본 그 모양이다.
- DSAC 이식 두 항은 **둘 다 DSAC 에서 실패한 이유 그대로 실패했다**: r5_idle 은 크기가 거래 손실보다 작으면 무력(0.02 → 0거래, 0.1 → 거래하되 실력 0), anti_flat 은 감쇠가 끝나면 관망으로 돌아간다.

**DSAC −60% 와의 비교**(사용자 질문): 계좌 기준 강제 팔은 −97~−99.9%/창(6배 × 창당 1,600 거래 강제)이고 관망 팔은 0% 다 — 둘 다 «더 잘 맞혔다」가 아니다. 거래당으로 보면 DSAC 순 ≈ −14bp(테이커 왕복 14) · 이번 ≈ −6bp(peg 5.88) 로 **차이는 전부 집행 비용**이고 비용 전 방향 수익은 둘 다 0, 적중률 둘 다 동전이다. 이번 판이 DSAC 보다 나은 점은 성과가 아니라 **진단**이다 — semi-MDP 로 회전율 실패를 걷어내자 그 밑의 원인(손익분기 IC 의 27%)이 드러났고, DSAC 에 없던 «같은 측면 무작위」 대조군이 «드리프트 측면 선택」과 «시점 실력」을 갈랐다.

## 12. 후속 — 피쳐 확장 (사용자 *"피쳐를 좀 더 추가해보자"*, 2026-09-14)

`research_eth_rl_gym_feature_expansion_20260914.py`. PPO 전에 **캐시된 gym 라벨 위에서 모델 없이** 거른다.
후보 8군 54열, 전부 klines 5분봉에 인과 정렬(봉 마감 t+5분까지의 정보만, 절단검사 통과):
deriv(OI·롱숏비·테이커비 10) · tape(1분 체결 테이프 16) · book(30초 호가 깊이 7, `bd_ok` 필터) · dvol(4, 2026-08-04 까지) ·
horizon(긴 지평 7) · deployed(안전MAE·전방변동성 3) · btc 3 · time 4.
**선별 규칙(실행 전 고정)**: (a) |TRAIN IC| ≥ 0.05 & 부호유지 3/3 열이 있거나 (b) 43열 대비 쌍둥이 표본외 IC 증분이 OOS·TEST **둘 다** ≥ +0.01.

| 군 | 열 | max\|TRAIN IC\| | 3/3 | 쌍둥이 증분 VAL/OOS/TEST | 판정 |
|---|---|---|---|---|---|
| **deriv** | 10 | .021 (dv_glob_ls) | 1 | +.018 / **+.087** / **+.038** | ⭐선택(b) |
| tape | 16 | .024 (tp_impact) | 3 | +.007 / −.007 / +.052 | 탈락 |
| book | 7 | .032 (bk_depth1_log) | 2 | +.039 / −.009 / +.060 | 탈락 |
| dvol | 4 | .022 | 1 | −.016 / −.020 / +.073 | 탈락 |
| horizon | 7 | **.036** (hz_ret288) | 0 | +.074 / +.001 / +.058 | 탈락 |
| **deployed** | 3 | .025 (dp_safe_mae240) | 0 | +.021 / **+.025** / **+.047** | ⭐선택(b) |
| btc | 3 | .019 | 0 | +.002 / +.006 / −.002 | 탈락 |
| time | 4 | .022 (hour_cos) | 0 | +.028 / −.017 / +.033 | 탈락 |
| all | 54 | — | — | +.046 / +.057 / +.092 | 민감도 팔 |

- **단일 피쳐로 0.05 를 넘는 열은 없다**(최대 0.036). 3/3 부호유지는 bk_depth1_log(+.032 → +.024/+.021/+.033) · tp_impact · tp_kyle 셋뿐인데 셋 다 |IC| .02~.03.
- ⭐**43열 쌍둥이의 표본외 IC 는 세 창 음수(−.013/−.009/−.070)였는데 54열을 전부 더하면 +.033/+.048/+.022 로 처음 세 창 양수**가 된다. 손익분기 0.10~0.12 의 절반 이하지만 부호가 바뀌었으므로 PPO 로 확인한다.
- ⚠️증분이 TEST 에서 크게 보이는 이유의 절반은 **기준 43열이 TEST 에서 −.070 으로 반대로 갔기 때문**이다(«덜 틀림」이 증분으로 잡힌다). 규칙 (b) 가 OOS·TEST 둘을 요구하는 이유다.
- 규칙 (b) 는 GBM 1씨드·8군 다중검정에 노출돼 있다 — 선별은 «PPO 에 넣을 후보」를 정할 뿐 결론이 아니다.

PPO 팔(각 5씨드·150반복·peg 비용·평가 실제 비용): `ext_pnl`(43+13=56D, 순손익 보상) · `ext_holdpen01`(56D, 홀드 벌점 0.1 유도) ·
`extall_holdpen01`(43+54=97D, 사전등록 외 민감도). 판정은 §11 과 같다(P1 Δ vs 같은측면 무작위 ≥2/3 창 +2SE · P2 적중률 > 손익분기).
(결과는 실행 후 기입)

## 13. 후속 — 170컬럼에서 골라 넣기 (사용자 *"150개 피쳐들이 있었는데 그 중 골라서 피쳐 추가해보는건 어때?"*, 2026-09-14)

09-12 절제에 쓴 `rl_training_*_unified.csv`(170열)를 gym 라벨로 다시 걸렀다.
스크립트 `research_eth_rl_gym_unified170_screen_20260914.py` · `research_eth_rl_gym_tape_rebuild_20260914.py`.

### 13.1 자료의 한계가 먼저다
커버리지 **2024-01-01 ~ 2026-02-28**. 내 창 기준 TRAIN 100% · VAL 100% · **OOS 65%** · **TEST 0%**.
⇒ §12 의 3창 규칙을 못 쓴다. 이 자료는 **선별 전용**이고 판정은 재생성 뒤에 한다.
CSV 는 2026-05-08 생성 = M7 제거 커밋(4c46d20) 이전 화석이라 `m7_*`(38) · `ai_*`(15) · 신경망출력(11)은
**지금 코드로 재생산 불가** — 통과해도 라이브에 못 쓴다.

### 13.2 🔴내 선별 규칙 (a) 가 잘못 잡혀 있었다
(a) 를 「단일 |학습창 IC| ≥ 0.05」로 걸었는데 **기존 43열의 최댓값이 0.030** 이다. 이 저장소가 한 번도
도달한 적 없는 선을 필수 조건으로 걸어, (a) AND (b) 는 재생성 가능한 군에 대해 **통과 불가능**했다.
⇒ 규칙 (b)(쌍둥이 표본외 IC 증분 두 창 ≥ +0.01) 단독 결과를 따로 낸다. **사후 완화임을 명시한다.**

### 13.3 화석이 1등인데 그게 오염의 교과서다
`m7_expected_ret`/`m7_q50` 의 **|학습창 IC| = 0.300** — 기존 최댓값의 10배이고 손익분기 0.11 의 3배다.
그런데 VAL +0.023 · OOS −0.004. **표본내만 거대하고 표본외는 0.** m7 은 이 구간에서 학습된 모델의
출력이므로 전형적인 in-sample 오염이고, 재생산도 불가라 애초에 쓸 수 없다.

### 13.4 규칙 (b) 단독 — 재생성 가능 5군이 통과한다 (기준 43열 표본외 IC = −0.013/−0.009)
| 군 | 열 | 증분 VAL/OOS | 절대 IC |
|---|---|---|---|
| **주문흐름·고래** | 12 | **+0.040 / +0.107** | +0.027 / **+0.088** |
| sig규칙 | 6 | +0.022 / +0.062 | +0.009 / +0.053 |
| 점프·꼬리 | 6 | +0.054 / +0.034 | +0.041 / +0.025 |
| OI·포지셔닝 | 4 | +0.026 / +0.046 | +0.012 / +0.037 |
| 구조 | 9 | +0.026 / +0.020 | +0.013 / +0.012 |

주문흐름 절대 OOS IC +0.088 은 손익분기 0.098 에 근접한다. 그래서 세 가지를 차례로 배제했다.

### 13.5 대안 설명 세 개를 차례로 배제 — 그리고 마지막에 무너졌다
1. **결측 인공물?** 새 열은 2026-03-01부터 전부 결측이고 OOS 는 1~3월이다. GBM 이 «비었는가」를
   시간 지표로 쓸 수 있다. → OOS 를 **덮인 65% 로 제한**해 재니 오히려 커진다(+0.040/**+0.107**). 배제.
   ⚠️처음엔 «결측 지시자 1열만 추가」로 검사해 증분 0.0000 을 얻었는데 **그 검사는 무의미했다** —
   학습창에서 그 열이 전부 존재해 분산 0 이라 분기가 불가능하다. 검사가 0 을 내면 «효과 없음」인지
   **«검사가 작동 못 함»** 인지 먼저 본다.
2. **한 봉 미래참조?** 라벨 지평이 48~288봉이라 피쳐를 1봉(5분) 늦춰도 거의 무해해야 한다.
   → L=1 · L=12 에서 증분 유지(주문흐름 +0.045/+0.087 · +0.057/+0.108). 배제.
3. **어느 열에 있나?** 테이프 재생성 가능 7열 **+0.047/+0.085** vs 구성식 불명 5열 +0.010/+0.010.
   ⇒ 이득은 **내가 다시 만들 수 있는 쪽**에 있다. 내 기존 `tape` 16열엔 없던 모양 셋이 거기 있었다:
   **고래/소매 비율 · 1차 차분(가속도) · Amihud 비유동성**.

### 13.6 🔴재생성 — 그리고 +0.107 이 +0.007 로 줄었다
그 개념들을 테이프 parquet(2024-04-20~2026-09-05, **TEST 포함**)에서 9열로 다시 만들었다(절단검사 통과,
전 창 커버리지 100%). 사전등록 규칙(OOS·TEST 둘 다 ≥ +0.01)으로 판정:

| | VAL | OOS | TEST |
|---|---|---|---|
| 기준 43열 | −0.0134 | −0.0090 | −0.0702 |
| + 재생성 9열 | −0.0099 | −0.0032 | −0.0630 |
| **증분** | **+0.0035** | **+0.0058** | **+0.0072** |

**탈락.** 화석 CSV 에서 +0.107 이던 것이 인과적으로 재생성해 TEST 까지 포함하면 **+0.007** 이다.
3/3 부호 유지는 `tr_intensity` 하나뿐이고 그 |IC| 도 ≤ 0.039 다.

### 13.7 판정
**170컬럼에서 추가할 것은 없다.** 통과한 것은 재생산 불가 화석뿐이고, 재생성 가능한 쪽의 이득은
다시 만들자 사라졌다. ⚠️정직한 한계: 내 재생성은 **개념 근사**이지 바이트 단위 복제가 아니다.
그래서 «unified 열이 오염됐다」고는 말하지 않는다 — 말할 수 있는 것은 **«그 개념을 인과적으로
다시 만들어 TEST 까지 재면 +0.007 이다»** 까지다.
⭐이 판의 값: 「헤드라인 수치를 다른 경로로 한 번 더 계산하기 전엔 그 위에 쌓지 않는다」가
**+0.107 → +0.007** 로 실측됐다.

## 14. 제품 변경

**없다.** 배포된 진입·청산 스택은 손대지 않았다(gym 이 그 함수를 import 만 한다). 방향은 계속 사람이 넣는다.
