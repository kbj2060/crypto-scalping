# 교차자산 지속 규칙 섀도우 (XRP·SOL 추가) — 사전등록 (2026-09-05)

**섀도우를 켜기 전에 이 문서를 고정한다.** 러너 `scripts/live_eth_fire_cont_shadow_runner_20260904.py --asset {xrp,sol}`,
supervisor `scripts/ops/supervisor_{xrp,sol}_fire_cont_shadow.sh`,
원장 `data/live/fire_cont_shadow_state_{xrp,sol}.json`.
근거: `docs/experiments/eth_composite_direction_trend_pullback_results_20260905.md` §7·§8·§10, 호메로스 §5.29.

## 0. 왜 이것인가 (다른 걸 하지 않는 이유)
2026-09-05 세션에서 **ETH 안에서 R(지속 규칙)을 개선하는 시도 116팔이 전부 실패**했다 — 방향(다중도 합의)·
추세(정보칩 상태 필터 27축)·되돌림(지정가 진입)·조기 손절·확신도 사이징·표결식 청산(144팔). 같은 정보를
재배열하는 400+셀이 전부 gross ≈ 0을 보존했다. **정보집합을 바꾸는 축만 남았고, 그중 즉시 가능한 것이 자산 수다.**

## 1. 규칙 — ETH와 **완전히 동일**(자유도 0)

    모집단  8종 raw 첫발동(GAP=12, 신호·측면별, 뒤만 봄) 합집합, (봉,측면) 중복 제거
    발동    `live_evidence_signal_dashboard_20260823.compute_signals(kl, btc_df=BTCUSDT)` — 라이브 정본, 자산 공유
            (funding 미전달 → orthogonal_combo 바닥 다리가 delta_z 단독으로 degrade — **ETH 러너도 동일**)
    방향    지속(신호 반대) — 바닥 발동 → 숏, 천장 발동 → 롱
    진입    발동 봉 마감 직후 마크가격(다음 봉 시가 근사), 직전 완결봉 발동만(백로그 금지)
    청산    5.0 SL / 1.5 ARM / 0.1 trail (×ATR14), 200봉 타임아웃, 완결 봉 고가/저가 기준
    비용    원장에 테이커 10bp·메이커 7.8bp 병기. 판정은 10bp
    스킵    같은 봉 양측 첫발동이면 둘 다 스킵

**셀을 자산별로 바꾸지 않는다.** TRAIN 64셀 재선정(XRP 4.0/1.0/0.1, SOL 6.0/1.0/0.1)이 TRAIN에서는 나았지만
(합산 샤프 6.63 vs 5.59) **VAL에서 뒤집혔다**(3.23 [−0.87, 13.82] vs **4.45 [+2.40, +15.54]**) — TRAIN 과적합이다.
⇒ **ETH 배포 셀 5.0/1.5/0.1을 3자산 공통으로 쓴다. 조정 가능한 파라미터가 하나도 없다.**

## 2. 자산별로 다른 것 — 동시보유 한도뿐

| 자산 | 심볼 | 셀 | 동시보유 | 상태 파일 | 레짐 태그 |
|---|---|---|---|---|---|
| ETH | ETHUSDT | 5.0/1.5/0.1 | **5 (현행 유지, 무변경)** | `fire_cont_shadow_state.json` | `/api/regime-wide24` |
| XRP | XRPUSDT | 5.0/1.5/0.1 | **2** | `..._xrp.json` | 없음 |
| SOL | SOLUSDT | 5.0/1.5/0.1 | **2** | `..._sol.json` | 없음 |

**XRP/SOL이 2인 이유는 백테스트가 아니라 위험 계약이다.** 위험 0.4%/건 · `notional = 0.004/(5·atr_pct)` 상한 0.5에서
중앙 notional은 ETH 0.286 · XRP 0.274 · SOL 0.233이다. ETH 혼자 cap5로 이미 **1.43**을 쓰고
A4 크로스심볼 캡은 **총 1.5**다. cap2씩만 더해도 총 2.44로 캡을 넘으므로 그 이상은 의미가 없다.
(TRAIN 합산 샤프도 cap2 5.16 > cap5 5.08로 같은 방향이다.)

## 3. 백테스트 기대 범위 (판정 근거가 아니라 섀도우가 재현해야 할 범위)
가격 bp, 10bp 차감, 각 자산 자기 cap. HOLDOUT 미접촉.

| 자산 | TRAIN exp · /일 | VAL exp · /일 | OOS exp · /일 | 승률 |
|---|---|---|---|---|
| ETH (cap5) | +4.84 · 25.5 | +4.44 · 24.4 | +6.87 · 24.0 | 0.75~0.77 |
| XRP (cap2) | +8.87 · 17.3 | +2.19 · 17.6 | +4.50 · 17.5 | 0.77 |
| SOL (cap2) | +5.72 · 18.5 | +7.94 · 17.4 | +9.56 · 17.0 | 0.76~0.77 |

**합산(자산별 위험정규화 자기자본 bp, 동일가중, ETH cap5 + XRP/SOL cap2)**:

| 창 | 합산 샤프 | ETH 단독 | 일평균 | 일 CI95 | 양의 날 | 최악일 | 자산 간 상관 |
|---|---|---|---|---|---|---|---|
| TRAIN | 5.16 | 4.44 | +7.53 | [+5.15, +9.81] | 0.586 | −85.3 | 0.04~0.22 |
| VAL | **5.07** | 3.35 | +7.35 | **[+2.36, +12.44]** | 0.574 | −50.8 | 0.14~0.31 |
| OOS | **5.59** | 5.02 | +8.23 | **[+2.63, +13.81]** | 0.611 | −55.5 | −0.03~0.33 |

## 4. 판정 (결과 보기 전 고정)
- **30일 계측 점검**(승격 판정 아님): 자산별 발동 빈도가 기대(XRP 17.6/일 · SOL 17.4/일)의 **±30%** 안인가 ·
  진입 지연 ≤ 1봉 · 놓친 봉 수 · 마크가격 실패율 · 메이커/테이커 실현 비용 격차.
  ⚠️라이브 1500봉 실측 발동률은 백테스트 평균의 0.86~0.92배였고 **ETH 자신도 0.91배**였다 — 이 편차는 정상이다.
- **90일 판정(주)**: 세 원장의 **자산별 위험정규화 일손익을 동일가중 합산**한 시계열의 **일군집 CI 하한 > 0**.
- **90일 판정(부)**: 합산 샤프 > ETH 단독 샤프. 세 창 백테스트에서 전부 성립했으므로 전진에서도 요구한다.
- **집행 가능성 보고(판정 아님)**: 세 원장을 시각순으로 합쳐 **총 notional ≤ 1.5(A4)** 제약을 적용한 변형의 일손익.
  위 백테스트 수치는 이 제약을 넣지 않은 값이다 — 캡이 얼마나 자주 무는지를 전진 데이터로 처음 잰다.
- **킬 스위치**(자산별): 최근 30일 평균 < −10bp/건 ∧ 일군집 CI 상한 < 0 → 그 자산 정지. 나머지는 계속.
- 측면별(바닥/천장)로 따로 보고하되, **반드시 같은 측면 무작위 진입 귀무와 함께** 본다(§7-1, 하락장 드리프트 함정).

## 5. 하지 않는 것
- **ETH 러너·셀·cap·상태 파일은 건드리지 않는다.** 09-04 시작한 ETH 30/90일 판정이 그대로 진행된다.
- **실주문 없음.** 가상 원장만 기록한다. `BINANCE_EXECUTION_ENABLED`는 여전히 False이고 봇 전체가 페이퍼다.
- BTC는 넣지 않는다 — 실행가능 하한 아래 적격 셀 0/64였고, 같은 창의 **무작위 진입도 −2.2~−4.2bp**였다.
- 자산별 셀·임계값·필터 추가 탐색 금지(§1). 90일 판정 전에는 규칙을 바꾸지 않는다.

## 6. 알려진 한계 (사후합리화 방지)
1. **cap 선택은 위험 계약이 주 근거이고 백테스트도 같은 방향이지만, 나는 VAL/OOS를 본 뒤에 이 문서를 썼다.**
   TRAIN 단독으로도 cap2 ≥ cap5였다는 점만이 사전적 근거다.
2. 자산 단독으로는 약하다 — XRP VAL 샤프 2.25 · OOS 2.88(위험정규화 기준). **합산이 되는 건 셋이 서로 무관하기 때문**(상관 0.03~0.33)이고,
   그 무상관성이 전진에서 유지된다는 보장은 없다.
3. VAL/OOS 두 창 모두 4자산 전부 하락장이었다(−19~−38%). 상승장에서의 거동은 TRAIN(+46% 구간)에서만 관측됐다.
4. 이 문서의 어떤 수치도 승격 근거가 아니다. 승격은 전진 90일 판정 + 별도 실주문 결정이다.
