# 청산맵 S/R 을 돌파/되돌림 배리어로 (2026-09-11)

사용자: *"돌파와 되돌림에서 중요한 건 저항과 지지를 돌파하는가 돌아오는가."*
철회된 돌파/되돌림은 배리어가 **±0.8×ATR 대칭**이었다. 이 가설은 배리어를 **실제 S/R** 로 바꾸자는 것.

빌더 `scripts/build_eth_liqmap_sr_panel_5m_20260911.py` · 분석 `scripts/research_eth_liqmap_sr_breakout_20260911.py`.

## 패널 — 5분봉마다의 지지·저항

라이브 함수 `compute_spliced_levels`(2026-08-26 배포판: 지지=mid 패스 · 저항=close 패스)를
**그대로 호출**했다. 식을 두 벌 두지 않았다.

- **487,846행** · 2021-12-08 ~ 2026-08-28 · 측면당 최근접 3개(가격·강도)
- 양쪽 레벨 존재율 **1.000** · 저항 거리 중앙 **1.70%** · 지지 **1.90%**
- 인과성: 룩백 1시간봉은 **그 5분봉이 시작하기 전에 닫힌 것만**(`T+1h ≤ t_i`), 기준가는 그 봉 **종가**.

## 검정 1 — 배리어를 S/R 로 바꾸면 방향이 예측되는가

라벨: 종가 진입, **다음 봉부터** 1시간, 저항 먼저(1) vs 지지 먼저(0). 해소율 0.189.

| 팔 | VAL | OOS | 최근 |
|---|---|---|---|
| **거리비 단독(모델 없음)** | **.8915** | **.8803** | **.9263** |
| 모델·기하만 | .9129 | .9016 | .9478 |
| 모델·기하+레벨강도 | .9132 | .9038 | .9461 |
| 모델·기하+강도+가격 | .9155 | .9016 | .9437 |
| **플라시보·자기기하만**(순환이동 거리) | **.8964** | **.8873** | **.9275** |

🔴**AUC 0.9 는 실력이 아니라 산수다.** 모델 없이 «가까운 쪽이 먼저 닿는다»만으로 .88~.93 이 나온다.
🔴**레벨 강도는 기하 위에 아무것도 안 얹는다**: +.0003 / +.0022 / −.0017.
🔴**아무 거리나 뽑은 플라시보 배리어가 실제와 거의 같은 AUC**(.896/.887/.928 vs .913/.902/.948).
즉 청산맵 레벨은 이 축에서 «가격선 두 개» 이상이 아니다.

## 검정 2 — 저항에 다가갔을 때 돌파인가 반등인가 (사용자 질문 그대로)

근접 0.15% · 판정 ±0.2% · 1시간.

| | 근접 건수 | 돌파율 |
|---|---|---|
| **실제 저항** | 11,485 | **0.240** |
| 플라시보 라인(순환이동 거리) | 11,485 | 0.271 |

⭐**격차 3.1pp 는 두 대조군을 다 통과한다** — 월 매칭 −2.21pp(56개월 중 36개월 음수),
일군집 부트(2000회) 95%CI **[−4.58, −1.74]pp**. 즉 «거기에 레벨이 있다»는 사실 자체는
같은 거리의 아무 선보다 실제로 덜 뚫린다.

🔴그런데 **«얼마나 강한가»는 무정보이고 부호도 반대다**: 강도 사분위별 돌파율
약 .231 · 중하 .240 · 중상 .243 · 강 .245. 폭 1.4pp(≈1.7 SE) 이고 방향은 가설의 반대다.

### 경제성 — 2~4pp 는 비용을 못 넘는다

| 진입 방식 | 건당 |
|---|---|
| 종가 진입(테이커 10bp) | **−8.36bp** (일군집 95%CI [−8.70, −8.02], 하루 8.6건) |
| 레벨 지정가 진입(메이커 7.8bp, 체결 0.91, **체결봉 제외**) | **−9.84bp** |
| ↳ 같은 방식 플라시보 | −8.26bp |

승률 76%인데 지는 이유는 **진입가가 레벨보다 아래**라서다. 종가에서 익절선(레벨−0.2%)까지는
평균 5bp 남짓인데 손절선(레벨+0.2%)까지는 35bp다. 이긴 76%가 작게 먹고 진 24%가 크게 잃는다.

반론 «레벨에 지정가를 걸면 대칭 아닌가»도 안 된다. **체결 봉을 정직하게 빼면**(같은 봉 해소는
철회된 돌파/되돌림이 저지른 바로 그 죄) −9.84bp 로, **플라시보(−8.26bp)보다도 나쁘다**.
즉 76% 반등률의 상당 부분이 «다가간 그 봉 안에서 이미 끝난 것»이었다.

## 판정

**배리어를 S/R 로 바꾸는 것으로는 돌파/되돌림이 살아나지 않는다.**
- 방향 예측: 레벨은 기하 이상을 안 준다(플라시보와 동률).
- 돌파/반등: 레벨의 **존재**는 2~4pp 진짜 정보지만 **강도**는 무정보이고, 체결 규약을 지키면 경제적으로 −9.8bp.

선행 결론과 같은 자리로 수렴한다 — 청산맵 S/R 축은 2026-08-24~25 십수 각도로 이미 기각됐다
(`eth_liquidation_map_sr_research_2026_rollup`). 이번엔 «레벨이 유효한가»가 아니라
«레벨을 배리어로 쓰면»을 물었고, 답은 같다.

⚠️격자를 쓸지 않았다. δ(근접)·τ(판정)·H(지평)를 쓸면 더 좋은 셀이 나올 수 있으나,
그 경우 **무작위 부분표집 귀무(B=200)** 가 필요하다(`feedback_grid_pass_count_needs_random_subsample_null_20260902`).
지금 셀에서 −8~−10bp 이고 엣지의 정체가 «체결 봉»이라, 격자로 뒤집힐 여지는 낮다고 본다.


---

# 재질문 (사용자): *"돈이 되나가 아니라, S/R 이 돌파/되돌림 **예측에 정보를 주냐**"*

앞의 «배리어 교체» 검정은 이 질문에 **불공정했다** — 라벨이 «두 배리어 중 어느 쪽에 먼저 닿나»라
거리비만으로 AUC 가 .89~.93 까지 차서 레벨이 기여할 여지가 없었다. 셋으로 갈라 다시 물었다.
스크립트 `scripts/research_eth_liqmap_sr_information_20260911.py`. 읽는 것은 **ΔAUC 하나**다.
(CTX = 레벨을 전혀 모르는 가격·변동성·BTC·시간 12피쳐)

## 검정 3 — 은퇴 신호의 자기 라벨(±0.8×ATR)에 S/R 을 얹으면 [표본 최대]

창당 **25,727~42,743행**. 셋 중 유일하게 표본이 충분하다.

| 팔 | VAL | OOS | 최근 |
|---|---|---|---|
| 맥락만(레벨 모름) | .5102 | .5113 | .5123 |
| **S/R 만** | **.5073** | **.5094** | **.5046** |
| 맥락+S/R | .5091 | .5148 | .5134 |

🔴**ΔAUC = −.0011 / +.0035 / +.0011 = 0.** S/R 단독은 .505~.509 로 동전이다.
은퇴한 신호가 못 푼 그 질문에 청산맵 S/R 은 아무것도 보태지 않는다.

## 검정 2 — 레벨에 다가갔을 때 돌파/반등 [표본 부족, 판정 불가]

| 이벤트 | ΔAUC (맥락 → 맥락+S/R) |
|---|---|
| 실제 저항 | +.078 / −.027 / +.055 |
| 플라시보 저항 | −.035 / +.040 / −.030 |
| 실제 지지 | −.002 / +.030 / −.037 |
| 플라시보 지지 | −.048 / +.035 / −.006 |

창당 n=494~938. **플라시보가 같은 크기(|Δ|≤.048)로 요동친다** — 실제와 못 가른다.

## 검정 1 — 기하가 답을 못 정하는 구간(지지·저항 등거리, 전체의 19.5%)

여기서 유일하게 신호처럼 보였다. 그래서 두 단계로 더 팠다.

| 팔 | VAL | OOS | 최근 |
|---|---|---|---|
| 맥락만 | .5870 | .5077 | .5264 |
| 맥락+S/R | .6175 | .5564 | .5702 |
| 맥락+**플라시보** S/R | .5659 | .5279 | .4880 |
| S/R 만(모델, 거리 포함) | .5658 | .5558 | .5805 |
| **잔여 ratio 단독(모델 없음)** | **.5724** | **.5524** | **.5636** |
| **강도·2차레벨만(기하 완전 배제)** | **.5101** | **.4840** | **.5287** |

실제 S/R 은 플라시보를 세 창 모두 이긴다(+.052/+.029/+.082). **그런데 그 이긴 몫의 정체가
잔여 기하다** — 등거리로 잘라도 `ratio` 가 ±0.05 남아 있고, **손으로 계산한 잔여 ratio 하나가
전 피쳐를 쓴 모델과 동률**이다(.572/.552/.564 vs .566/.556/.581).
🔴**거리를 완전히 빼고 강도·2차레벨만 주면 .510/.484/.529 — 0.5 주변이고 한 창은 아래다.**

## 최종 판정

**S/R 이 주는 정보는 «거리» 하나뿐이고, 그건 S/R 의 정보가 아니다.**
- 어느 가격선이든 가까운 쪽이 먼저 닿는다 — 플라시보 배리어가 실제와 같은 AUC(.896/.887/.928)를 낸다.
- 레벨의 **강도·정체·2차 구조**는 세 검정 어디에서도 0.5를 벗어나지 못한다.
- 은퇴 신호의 자기 라벨에 얹어도 ΔAUC ≈ 0 (표본 2.5~4.3만).

즉 «저항을 돌파하는가 되돌아오는가»라는 물음 자체는 옳지만, **청산맵이 그 저항이 어디인지를
쓸 만하게 알려주지 못한다**. 다른 출처의 레벨(스윙 고저, 거래량 프로파일, 라운드 넘버 등)로
같은 질문을 다시 물을 수는 있고, 그때 이 문서의 대조군 3종(거리비 단독 · 플라시보 배리어 ·
기하 배제 팔)을 그대로 쓰면 된다.


---

# 재질문 (사용자): *"차트를 보면 지지·저항에서 되돌아가는 경우가 많다고 느꼈는데"*

스크립트 `scripts/research_eth_liqmap_sr_bounce_perception_20260911.py`.

## 왜 앞의 «반등 76%» 를 그대로 믿으면 안 됐나

앞 검정의 «근접» 정의는 **비대칭**이었다 -- 관측 기준이 종가(레벨 아래)인데 반등선(레벨−0.2%)은
가깝고 돌파선(레벨+0.2%)은 멀다. 가까운 쪽이 더 자주 먼저 닿는 건 산수다.
그래서 **레벨 기준 대칭**으로 다시 정의했다:

    레벨 R 은 봉 i−1 종가 시점에 이미 확정 · 봉 i 의 고가(저가)가 R 을 실제로 **터치**
    · 봉 **i+1 부터** R×(1+t) 와 R×(1−t) 중 먼저 닿는 쪽

## ⭐무조건부 기준선이 0.5 가 아니다 (이게 착시의 절반이다)

| ±t | 아무 봉에서나 **아래가 먼저** | n |
|---|---|---|
| ±0.1% | **0.5759** | 484,120 |
| ±0.2% | 0.5215 | 453,619 |
| ±0.5% | 0.5099 | 281,315 |

ETH 5분봉은 **아래로 더 빨리 움직인다**(음의 왜도 -- 하락은 캐스케이드로 튀고 상승은 갈아 오른다).
드리프트 때문이 아니다(구간 전체 −41.7%, 5분 평균 −0.011bp 로 이 크기를 못 만든다).
레벨 숫자를 이 기준선과 비교하지 않으면 «되돌아간다»가 전부 착시로 읽힌다.

## 결과 -- 느낌은 **저항에서만** 맞다

τ 를 8점으로 쓸어 감쇠를 재었다. 반전율(저항=아래로 되돌아감 · 지지=위로 되돌아감),
기준선 초과 / 거리 십분위 층화 후 플라시보 대비:

| ±τ | 저항 반전 | 기준선 | 저항 층화초과 | 지지 반전 | 기준선 | 지지 층화초과 | 해소율 |
|---|---|---|---|---|---|---|---|
| 0.05% | .721 | .682 | +2.81pp | .293 | .318 | −3.59pp | 1.000 |
| **0.10%** | **.666** | .576 | **+4.79pp** | **.356** | .424 | **−4.76pp** | 1.000 |
| 0.15% | .616 | .537 | +4.26pp | .401 | .463 | −4.13pp | 1.000 |
| 0.20% | .580 | .522 | +3.17pp | .436 | .478 | −3.14pp | 0.999 |
| 0.30% | .541 | .511 | +1.19pp | .474 | .489 | −1.25pp | 0.989 |
| 0.50% | .516 | .510 | +0.70pp | .496 | .490 | +0.39pp | 0.922 |
| 0.75% | .514 | .516 | +1.44pp | .503 | .484 | +1.73pp | 0.766 |
| 1.00% | .512 | .522 | +1.70pp | .491 | .478 | +1.46pp | 0.609 |

⭐**0.10% 에서 최대(+4.79 / −4.76)이고 0.50% 에서 사라진다.** 0.05%~0.30% 구간에서 두 값이
거울상으로 움직이는 게 핵심이다. 일군집 부트 95%CI 는 ±0.1% 에서 저항 [.658,.673] · 지지 [.347,.366].
터치 건수가 실제/플라시보로 달라(17,105 vs 10,823) 거리 분포가 어긋나므로 **십분위 층화**로 교란을
제거했고, 층화 전후 숫자가 거의 같다.

⚠️**τ ≥ 0.5% 는 주장하지 않는다** -- 해소율이 92%→61% 로 떨어져 결과로 걸러진 부분집합이 되기
시작한다. 그 구간에서 양쪽 부호가 **둘 다 +1.4~+1.7pp** 로 같아지는데(거울상이 깨진다) 이는
신호가 아니라 표본이 얇아진 자리다.

그림: 발행 아티팩트 «지지·저항 반전 감쇠».

## 판정 -- «되돌아간다»가 아니라 «아래로 간다»

⭐**+4.79pp 와 −4.76pp 는 거울상이다.** 저항이든 지지든 **실제 레벨을 건드리면 아무 선을 건드릴 때보다
아래로 간다** -- 저항에서는 그게 «반등»으로, 지지에서는 «이탈»로 보일 뿐 같은 하나의 현상이다.
따라서 «레벨이 가격을 밀어낸다(repel)»는 설명은 틀렸다. 방향성 효과다.

- **느낌은 절반 맞다**: 저항에서는 실제로 되돌아간다(기준선 대비 +9pp, 플라시보 대비 +4.8pp).
- **지지에서는 반대다**: 되돌아감이 35.6%뿐 -- 아무 선보다 **더 잘 뚫린다**.
- **±0.5%면 사라진다**(+0.70 / +0.39pp). 이 효과는 **10~20bp 눈금에서만** 산다.

앞 문단들(정보량 검정)과 모순되지 않는다: 레벨의 **강도·정체**는 여전히 무정보이고, 여기서 살아난 건
«실제 레벨을 터치했다»는 **사건** 자체의 방향 편향이다. 예측 피쳐로는 앞에서 이미 0 으로 나왔다.


---

# 재질문 (사용자): *"지지·저항은 호가창이나 체결속도로 알 수 있을 것 같은데"*

빌더 `scripts/build_eth_volprofile_sr_panel_5m_20260911.py` · 검정은 앞의 반전 스크립트에
패널 경로 인자를 추가해 **그대로 재사용**했다.

## 먼저 데이터 재고 -- 호가로는 «레벨 위치»를 못 짚는다

| 자원 | 가격 단위? | 역사 |
|---|---|---|
| `eth_bookdepth_30s_20260908.parquet` 244만행 | ❌ **누적 밴드**(±0.2/1/2/3/4/5%) | 869일 |
| `data/live/l2_anomaly_snapshots.duckdb` (depth20 raw) | ✅ | 🔴 **58,843행 · 2026-08-26~27 이틀**, 이상 이벤트 주변만 |
| klines `trades`(중앙 2,333/분)·`taker_buy_base` | ❌ **시간축** | 4.7년 전량 |

«±1% 안에 얼마»는 알아도 «4,120에 벽»은 모른다. **호가 축은 미검정으로 남는다** --
depth20@100ms 연속 수집(하루 ~86만행)을 다시 켜야 하고, 오늘 켜도 VAL/OOS 는 몇 달 뒤다.

## 체결 축은 지금 검정할 수 있다 -- 거래량 프로파일

1분봉 1,357,436개(2023-12~)를 로그가격 격자(0.1%)에 쌓았다. 1분봉 고저폭 중앙 **0.082%** 라
«한 봉 = 한 빈» 근사가 성립한다. 룩백 168h·반감기 240h·빈 폭·측면당 6개·최대거리 5%·최소지분 5% 는
**청산맵 상수를 import 해서** 맞췄다 -- 바뀌는 건 **빈의 무게**뿐이다(가상 청산가 부피 →
실제 체결 대금 / 체결 건수). 네 판: 대금·건수 × 덩어리·봉우리(HVN, ±0.5% 국소 최댓값).

| 판 | 행수 | 양쪽 레벨 | 저항거리 중앙 | 터치 |
|---|---|---|---|---|
| 청산맵 | 487,846 | 1.000 | 1.70% | 17,105 |
| 거래량 덩어리 | 269,472 | 0.987 | **0.13%** | 92,308 |
| 거래량 **HVN** | 269,472 | 0.849 | **0.62%** | 28,811 |

⚠️첫 판(덩어리)은 «전역 최대의 5% 이상» 조건이 현재가 주변 연속 덩어리를 통째로 통과시켜
레벨이 가격에 달라붙었다(0.13%). 가격에 붙은 선은 정의상 위치 정보가 없다. 그래서 **HVN 판을
추가**했고, 아래 판정은 HVN 을 기준으로 한다.

## 결과 -- 층화 후 플라시보 대비 초과 반전율(pp)

해소율 96% 이상 구간만 읽는다.

| 레벨 출처 | 저항 ±0.05 / 0.10 / 0.15 / 0.20% | 지지 ±0.05 / 0.10 / 0.15 / 0.20% |
|---|---|---|
| **청산맵** | +2.81 / **+4.79** / +4.26 / +3.17 | −3.59 / **−4.76** / −4.13 / −3.14 |
| 거래량 덩어리(대금) | −0.97 / −0.93 / −0.65 / −0.28 | +0.60 / +0.63 / +0.29 / +0.08 |
| 거래량 덩어리(건수) | −0.83 / −0.84 / −0.51 / −0.03 | +0.50 / +0.45 / +0.16 / +0.06 |
| **거래량 HVN(대금)** | +0.44 / **+0.67** / +0.47 / +0.59 | +0.14 / **−0.04** / −0.18 / −0.47 |
| **거래량 HVN(건수)** | +0.31 / **+0.13** / — | +0.11 / **+0.30** / +0.12 / −0.35 |

🔴**체결로 만든 레벨은 청산맵의 1/7~1/10 이다.** HVN 으로 제대로 만들어 거리를 0.62% 까지 벌려도
저항 +0.5pp 안팎, 지지 ~0 이고 **거울상이 없다**. 청산맵의 ±4.8pp 거울상과 비교가 안 된다.
🔴**체결 건수(≈체결속도)가 대금보다 나은 게 없다** -- 네 쌍 전부 차이가 0.2~0.3pp 안이다.
사용자 가설의 «체결속도» 쪽은 이것으로 답이 된다.

## 왜 반대 방향이었나 (기전)

청산가는 **강제 주문**이 놓인 자리다. 강제 흐름은 방향을 갖는다(롱 청산 = 강제 매도).
거래량 노드는 **거래가 일어난** 자리이고 매수·매도가 이미 짝지어진 대칭 사건이다.
앞 문단에서 청산맵의 **강도는 무정보인데 터치 사건은 −4.8pp** 였던 것과 맞물린다 --
정보를 담은 것은 «거기 물량이 많다»가 아니라 «거기에 **강제로 나올 주문**이 있다»였다.

## 남는 것

- **체결 축: 기각.** 거래량 프로파일(대금·건수, 덩어리·HVN 4판) 전부 청산맵에 못 미친다.
- **호가 축: 미검정.** 데이터가 없다. 하려면 depth20@100ms 수집기를 다시 켜고 몇 달 기다려야 한다.
  켤 값어치는 «강제 주문 vs 자발 주문» 기전이 시사한다 -- 호가의 **거대 지정가 벽**은 자발적이라
  거래량 노드 쪽에 가깝고, 오히려 `forceOrder` 스트림(실제 청산 체결)이 청산맵의 개선판일 수 있다.
