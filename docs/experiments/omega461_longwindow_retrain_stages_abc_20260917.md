# Omega4.6.1 최장창 재학습 — A~C 완료, D 는 라벨 제약 (2026-09-17)

사용자 제안: *"Omega4.6.1 부모 모델을 차용해서 입력 피쳐를 이번에 우리가 새로 가져온 것들로
재학습해보는건 어때?"* → 조사 결과 **새 피쳐는 증분이 없고(§1), 대신 학습창 깊이가 실재하는
기회**로 밝혀져(§2) 그쪽으로 진행했다.

## 0. 한 줄

**A~C 완료**(136열 프레임 · walk-forward 레짐, 2022-01~2026-09, 495,072행). **D 는 2024 부터만
가능** — `zigzag_action` 라벨이 2024·2025·2026 뿐이고 2022~2023 은 저장소 전체에 없다(전수 확인).

## 1. 먼저 기각된 것 — 새 라이브 피쳐의 증분

Omega base 96 위에 2026-05-03 이후 수집한 라이브 피쳐 15개(microstructure)를 얹은 ridge 증분,
평가창 2026-05-03~06-30(60 독립일, **07-01~09-30 single-touch OOS 미접촉**):

| 지평 | 검정력 | ρ(Omega) | ρ(+새) | Δρ | CI |
|---|---|---|---|---|---|
| **1h** | MDE 4.55bp < 벽 5.52 ✅ | +0.1329 | +0.1385 | **+0.0056** | [−0.0171,+0.0301] 🔴0포함 |
| 4h | MDE 16.6bp ❌ | +0.1552 | +0.1431 | −0.0121 | 검정력 없음 |

🔴청산 8개는 이 창에서 **전부 퇴화**(`@forceOrder` 결함으로 05-03~07-18 전량 0) — 자동 제외됐고
이 게이트는 청산 피쳐를 판정하지 않는다. 별도로 09-16 B2 게이트에서 Δρ −0.0065 였다.

⭐**벤치마크가 약화된 조건에서도 유의하지 않다**: BV 패널에 `quote_volume`/`taker_buy_quote` 가
없어 근사 복원했으므로 Omega 벤치마크가 실제보다 약하다 ⇒ 새 피쳐에 **유리한** 편향인데도
CI 가 0 을 포함한다. 같은 피쳐군에 대한 **네 번째 음수**다(708셀 규칙 0 · 99셀 단변량 0 ·
청산 B2 −0.0065 · 이번 +0.0056 CI 0포함).

## 2. 그래서 바뀐 목표 — 학습창 깊이

⭐**Omega 부모의 실제 학습창을 확인했더니 2025 한 해였다**(번들엔 기록이 없어 예측 CSV 로 확인):
| | 기간 | 봉 |
|---|---|---|
| TRAIN | 2025-01-01 ~ 2025-09-30 | 78,509 |
| VAL | 2025-10-01 ~ 2025-12-31 | 26,496 |
| OOS | 2026-01-01 ~ 2026-02-28 | 16,897 |

**2021~2024 를 한 봉도 안 썼다.** 반면 저장소엔 `eth_5m_2021_2023_archive.csv`(2021-12~2023-12,
완전 OHLCV)가 있다.

## 3. A단계 — 136열 완비 프레임 ✅

`tmp/omega461_longwindow_20260917/features_136_2022_2026.parquet` · **495,072행 × 158열**
(2022-01-01~2026-09-15) · balnobb 136 중 **136 생성** · NaN 잔존 **0**.

- 원시: 아카이브 CSV + BV 패널(`open`=close[t-1] · `quote_volume`≈volume×typical 복원)
- `state7_*`/`state12_*` 8개는 `_with_raw_state12()` 한 줄 — **새 원천 불요, 기존 열의 tanh 변환**
- 🔴**중앙값 채움**(사용자 결정): `last_funding_rate`·`funding_abs`·`funding_pressure` 각
  **324,577행 (65.6%)** · `sum_toptrader_long_short_ratio` 100,842 (20.4%) · 나머지 2개 소량.
  ⇒ **재학습 모델은 표본의 2/3 에서 펀딩을 상수로 본다.** 펀딩 중요도를 과소평가할 수 있고,
  그건 D단계 해석에 반드시 함께 읽어야 한다.

## 4. B단계 — balnobb 2022~2023 재적합 ✅

배포 balnobb 은 2024-01~2026-06 적합이라 2022~23 에 쓰면 **미래를 본 모델이 과거를 판정**한다.
같은 라벨(`balancedish_adx16_slope15_NO_BB_OVERRIDE`)·같은 config(HGB depth10/lr.04/iter400/l2 2.0)로
2022-01~2023-12 재적합. → `tmp/omega461_longwindow_20260917/regime_balnobb_refit2022.joblib`

**2024 구간 105,408봉에서 배포본과 나란히**:
| | 재적합본(표본외) | 배포본(표본내) |
|---|---|---|
| **예측 일치율** | **96.23%** (기준선: 다른 라벨끼리 80.3%) | |
| bull/bear/chop | 26.7 / 24.8 / 48.5% | 26.5 / 24.9 / 48.6% |
| 결정적 라벨 대비 정확도 | 94.29% | 95.60% |

🔴**첫 실행은 라벨이 100% chop 이었다** — 원본 `_current_labels3` 의 **ADX 폴백**(`adx_14` 가
없으면 `_adx()` 로 직접 계산)을 안 옮기고 `fillna(0)` 으로 때웠더니 `adx < 12` 가 항상 참이 됐다.
그 상태의 "일치율 48.63%" 를 **이음매 문제로 읽을 뻔했다.** 이후 라벨 퇴화 `assert` 를 넣었다.

## 5. C단계 — walk-forward 레짐 6열 ✅

`features_with_regime_2022_2026.parquet` · 495,072행 × 164열.
2022-01~2023-12 → 재적합본 · 2024-01~ → 배포본. 6열 산출은 라이브
(`omega4_6_2_source_parent_live.py:230-236`)를 그대로: `_entropy` 는 **log(3) 정규화**.
⚠️라이브는 HMM(`filter_proba`+`state_class_matrix`), 여기는 GBM(`predict_proba`) — 확률 경로만
다르고 6열 변환은 동일.

**이음매(2023-12-29~2024-01-03, 모델 교체 지점)**: confidence Δ **−0.0024** · margin Δ −0.0046 ·
entropy Δ +0.0091 · 전이율 11.12%→10.43%. ⇒ **부모가 이음매를 학습할 위험 없음.**

연도별: 2022 bull29.3/bear29.2/chop41.5 · 2023 17.5/16.6/**65.9**(전이율 7.42%, 횡보장) ·
2024 26.5/24.9/48.6 · 2025 29.7/27.7/42.6 · 2026 23.0/23.8/53.2.

## 6. 🔴D단계 제약 — 라벨이 2024 부터다

부모 학습은 `zigzag_action` 을 요구한다(`train_omega1_direction_head_direction_only_20260602.py::
_add_labels` → `LABEL_DIR/zigzag_action_labels_{year}.csv`).
**정본 `tmp/causal_regen_20260516/zigzag_action_labels_20260531/` 에 2024·2025·2026 만 있고,
저장소 전체 전수 검색에서도 2021~2023 은 0건**이다.

**역공학을 시도했고 관문에서 실패했다.** 감사 파일에 파라미터 9개(min_wave_bars 8 ·
transition_buffer 2 · atr_window 14 · zigzag_reversal_pct 0.01 · mae_penalty 1.25 ·
softmax_temperature 1.75 …)가 있으나 **적용 순서·방식이 없다**. 2024 재현 관문
(counts {0:10335,1:50512,2:44533} · segments 1843 · buffer 9965)에 대해 내 구현은
**피벗 0개 · 전부 CASH · 일치율 9.81%**(= CASH 비중과 같음, 우연) 로 실패.

⇒ **D 는 2024 부터.** 2022~2023 레짐(C단계 산출)은 버리지 않는다 — **라벨만 생기면 즉시 쓸 수
있는 상태**로 남는다. ⚠️다음 세션 주의: `features_with_regime_2022_2026.parquet` 만 보고
"2022 부터 학습 가능" 으로 읽으면 안 된다. **레짐은 있고 라벨이 없다.**

## 7. D단계 설계 (미실행)

| 판 | TRAIN | Omega 현재 대비 |
|---|---|---|
| **2024판** | 2024-01 ~ 2026-02 (약 790일) | **2.9배** |
| **2025판** | 2025-01 ~ 2026-02 (약 425일) | 1.6배 |

공통 VAL 2026-03-01~06-30(**07-01~09-30 single-touch OOS 밖**, 단 08-20 결정으로 TRAIN 편입된
이미 소진 구간). **판정은 VAL 까지만** — 최종 판정은 09-30 이후 새 OOS
([[oos_window_usage_ledger]]).

## 8. 이 작업에서 내가 틀린 것

| # | 주장 | 실제 | 원인 |
|---|---|---|---|
| 1 | "R 지속 규칙 = 배포본" | 09-07 정지한 섀도우 | 실행 상태 미확인 |
| 2 | "evr_gate 는 닫힌 축" | app.js 가 소비 중 · 미증명이지 반증 아님 | 소비처 미확인 |
| 3 | "실계좌 +31.25bp = Omega 성과" | **Omega 는 실주문 0건**(`exchange_execution_enabled=False` 26/26) | 주문 경로 미확인 |
| 4 | "대시보드 레짐 = balgbm" | **balnobb**(09-10) | 코드 대신 문서만 봄 |
| 5 | balnobb 재적합 라벨 정상 | **100% chop** | 원본 ADX 폴백 누락 |
| 6 | 섀도우 카드 "프런트 미사용" | app.js 는 `/api/shadows` 일반 엔드포인트만 부름 | 이름 검색으로 판정 |

⭐**1·2·3·4·6 은 「기존 시스템의 상태를 확인 안 함」, 5 는 「기존 코드를 일부만 옮김」.** 전부
같은 뿌리 — **원본을 끝까지 안 읽었다.** 여섯 건 모두 사용자가 되물어서 잡혔다.
⇒ 그래서 A~C 각 단계에 `assert`(136/136 · NaN 0 · 라벨 퇴화 · 계약 일치)를 넣었고, D 의 라벨
관문도 그 연장이다. **내가 틀려도 다음 단계로 안 넘어가게** 하는 것이 목적이다.
