# ETH 지정가 페이드 진입 모델 — 연구·동결 (2026-09-03)

상태: **동결 v1 완료** (`tmp/eth_entry_limit_fade_v1_20260903/`, sha256 `d611e31b7752a379`).
⚠️**승격이 아니다.** 홀드아웃은 소진됐고 신선한 검정은 전진 섀도우로만 벌 수 있다.

발단: 2026-09-03 사용자 "오메가를 잊어버리고 우리가 새로 진입 모델부터 차근차근 만들어보자."
설계: `docs/eth_entry_model_clean_slate_design_20260903.md`

---

## 0. 확정 구성

```
트리거   8종 raw (compute_signals, ⚠️cluster_dedup 금지 — 앵커 선택이 미래참조)
진입     양팔 지정가 depth 3.0×ATR · 대기 6봉 · 미체결 취소
필터     HistGradientBoostingRegressor(squared_error) 5시드 평균 · 161피쳐 · 예측 > 40bp
슬롯     4
청산     트레일링 SL3.0/ARM1.0/Trail0.1 · horizon은 신호별
비용     왕복 10bp (peg-maker 실측 대기)
```

| | VAL | OOS | HOLDOUT |
|---|---|---|---|
| **정책** | **+62.29bp** (n206, PF 7.41) | **+63.18** (n224, PF 7.12) | **+59.66** (n107, PF 9.23) |
| 무필터 | +24.36 | +30.33 | +15.64 |
| 무작위 필터(동일 비율) | +30.93 | +31.52 | +20.05 |

---

## 1. 문제 정식화가 두 번 바뀌었다

**처음**: "신호가 제안한 방향을 받을지 거절할지" (수락/기권 필터).

**1차 전환** — 사용자가 "방향을 고르는 모델 아니었나"라고 지적. 방향 모델(안 B)의 오라클 천장을
재려다 **계산 결함**을 발견했다. v1은 **롱 체결가에서 숏 수익**을 쟀는데, 지정가는 한쪽으로만
걸리므로(bottom 신호 → 3 ATR 아래 매수) 체결되면 롱이고 그 가격에서 숏이 될 수 없다.
실현 불가능한 반사실이었다.

v2에서 두 팔을 **각자 다른 체결 사건**으로 다시 계산했다(신호방향 = 아래 매수 / 역방향 = 위 매도).
그러자 부호가 뒤집혔다 — v1에서 −8~−18bp였던 "뒤집기"가 **+7~13bp로 수익**이 됐다.

⭐**두 팔 다 "3 ATR excursion 페이드"다.** bottom 신호에서 아래 매수는 더 빠진 걸 사고, 위 매도는
튄 걸 판다. **엣지의 출처는 신호의 방향이 아니라 excursion 자체**이고, 신호는 "그런 움직임이
나오고 되돌릴 만한 순간"을 짚어줄 뿐이다.

| 정책 (트리거당) | VAL | OOS | HOLDOUT |
|---|---|---|---|
| 신호방향만 | +20.92 | +22.69 | +14.47 |
| 역방향만 | +12.62 | +9.47 | +7.01 |
| **양쪽 다 걸기 (모델 없음)** | **+33.54** | **+32.16** | **+21.48** |
| 오라클A (방향 완벽선택) | +26.69 | +30.88 | +20.60 |
| 오라클B (양팔+기권 완벽) | +41.02 | +44.57 | +29.23 |

**"양쪽 다 걸기"가 방향을 완벽히 아는 오라클A를 이긴다. 모델 없이.** 그래서 기준선이 바뀌었고,
트리거당 총 체결이 1.01~1.02건이라(양쪽 동시 체결 0.9%) 주문은 둘이어도 포지션은 사실상 하나다.

**2차 전환** — 무제한 자본에서는 필터가 **원리상 손해**다(거의 모든 팔이 양수라 거르면 이익을
버린다). B0-a에서 τ를 올릴수록 단조 악화했고 시드 0/5였다. 사용자가 "잣대가 틀렸다"고 지적해
**슬롯 제약**으로 옮기자 정반대가 됐다 — 1슬롯에서 필터가 약 2배 이득.

⚠️이 과정에서 **역선택 오진을 한 번 했다.** τ=−5bp의 제외군(n=119)에서 유지 16.50 vs 제외 26.70을
보고 "역선택"이라고 했는데, CI가 [14.6, 38.0]으로 완전히 겹치는 노이즈였다. 의미 있는 크기
(τ=+5bp, 제외 n=1,551)에서는 유지 20.83 vs 제외 8.95로 **정상 판별**이었다. 작은 부분집합의
평균을 결론으로 쓰지 말 것.

---

## 2. 표본이 병목이었고, 확장이 풀었다

τ를 올리면 성과가 오르는데 n이 32~67, 독립 블록 9일로 무너져 검정력이 없었다.
깊이 {2.0,2.5,3.0,3.5} × 대기 {6,12}로 확장해 **체결 11,311 → 138,005건**으로 늘렸다.
(대기는 상위집합이라 wait=12로 한 번 시뮬하고 `btf<=6`으로 걸러 파생)

그 결과 최적점 n이 107~224, 독립 블록 17~25일이 됐고 3창이 +59~63bp로 수렴했다.

⭐**순위 배분(M 상위만 살리기)은 실패했다** — 표본은 지켰지만(n 201~297) 무작위 M 대조군을
못 이겼다(p=0.25~0.55). 개선분 전부가 "동시 대기 주문 수를 줄인 것" 자체에서 나왔다.
대기 중인 주문들끼리는 서로 비슷해 상대 순위에 정보가 없고, **전체에서 상위 12%를 뽑는 건
정보가 있다**. 그래서 τ 임계값 방식을 유지했다.

---

## 3. 대조군 (전부 통과)

| 대조군 | 결과 |
|---|---|
| **모멘텀 뒤집기** (지정가를 반대편에 = 추격) | VAL −6.42 / OOS −13.95 / HOLDOUT −11.47 — **3창 손실**. 페이드 방향성이 진짜 |
| **무작위 필터** (같은 유지비율) | 3창 **p=0.000** (실제 +62/+63/+60 vs 무작위 +31/+32/+20) |
| 무작위 봉 | 백분위 100% 양 창 |
| 순환이동 플라시보 | 양 창 p=0.000 |
| 무작위 5시드 | 3창 **5/5** |
| 시간블록(일) 군집 부트스트랩 | CI 하한이 3창 전부 무필터 위 |
| DSR / PBO | **1.0000 / 0.0437** (172 trial) |

⚠️**단, 무작위 봉도 +16~21bp를 낸다.** "아무 봉에서나 양쪽에 3 ATR 지정가를 걸고 1슬롯"만으로
대부분의 수익이 나오고, **트리거가 더하는 건 +5~7bp(약 30%)뿐**이다. 엣지의 상당 부분이
신호가 아니라 **기계**(양팔 페이드 + 슬롯 배분)에 있다.

---

## 4. 선택 축 — 사전등록으로 닫았고, 전부 현행이 남았다

### 피쳐 (B2 → B8)
확장 표본(행/피쳐 **504**)에서 재선별. **k=15~161이 전부 +54~62bp로 평평**하고 뚜렷한 봉우리가
없다. 선별본 최고(k=25)가 VAL 동률·OOS −3.0bp로 뒤져 **161개 동결**.
*판정 규칙: 선별의 목적은 단순화이지 성능 향상이 아니므로, 동등하면 이미 대조군을 통과한 쪽.*

### 아키텍처 (B9 → B15)
| 후보 | 결과 |
|---|---|
| absolute_error / quantile(0.5) | VAL 동률, OOS −7.0 |
| **quantile(0.6)** | VAL +3.02 · **OOS −2.35** · HOLDOUT +5.51 |
| winsorize(p1/p99) | VAL −1.97 · OOS −8.61 |
| 분류 `y > 40bp` | VAL −5.08 · **OOS +4.13** · HOLDOUT +7.37 |
| 분류 `y > 0` | 3창 −25bp 이상 |
| TabPFN 회귀 (18k×4) | 3창 −0.08~−2.39 |
| **TabPFN 분류 (18k×5)** | 양 창 승리 ✅ · **순열검정 VAL p=0.139 / OOS p=0.052 ❌** · 대조군 ✅ |

⭐**여러 후보가 서로 다른 한 창에서만 이긴다** — quantile(0.6)은 VAL/HOLDOUT, 분류는 OOS/HOLDOUT.
이건 대안이 진짜 나은 게 아니라 **창별 노이즈가 ±5bp 수준**이라는 뜻이고, "양 창 동시 승리"를
기준으로 잡은 이유다. 한 창만 봤으면 여러 번 잘못 결론냈을 것이다.

### ⭐데이터량 vs 학습기 분리 (B14)
TabPFN을 서브샘플로 돌리니 "데이터를 78% 버려서 진 것"이라는 가설이 생겼다. HGB도 **같은 18k
서브샘플**로 돌려 분리했다:

```
데이터량의 값어치 (HGB-full − HGB-sub) : VAL −0.82 · OOS +0.89 · HOLDOUT −0.33
동일 데이터 학습기 차이 (TabPFN − HGB) : VAL +0.91 · OOS +1.79 · HOLDOUT +5.52
```

**데이터를 100%→22%로 줄여도 성능이 안 변한다(±0.9bp).** 81,168행이 필요 없고 18,000행이면
포화다. 그러면 "컨텍스트 제약이 TabPFN을 죽인다"는 이 저장소의 기존 법칙은
**표본이 포화되지 않았을 때만 참**이다 — 정정이 필요하다.
그리고 TabPFN **분류기**는 같은 데이터에서 HGB를 3창 전부 앞선다(+0.91/+1.79/+5.52).

⚠️**즉 TabPFN 분류가 진짜로 조금 나을 가능성이 실재한다.** 다만 10번째 후보의 경계선 결과
(OOS p=0.0518)라 다중검정에 당하지 않으려면 채택 불가다. **표본이 더 쌓이면 재검정할 것.**


### TabPFN 공식 권고 적용 (B15, 진행 중)

`docs.priorlabs.ai/improving-performance` · `/capabilities/embeddings`를 읽고 적용했다.
지금까지의 TabPFN 비교가 불공정했을 가능성을 확인하기 위해서다.

| 문서 권고 | 우리 상황 | 판정 |
|---|---|---|
| "데이터를 최대한 raw로. **결측 대체는 성능을 해친다**" | `fillna(median)` 사용 | ⭐**무효** — 실측 결측률이 **0.0000%**라 대체가 아무것도 안 했다 |
| "피쳐 **100개 이상이면 선별**해서 attention 효율을 높여라" | 161개 | ⚠️**유효한 지렛대** — HGB는 피쳐 수에 둔감했지만(B8) TabPFN은 문서상 불리 |
| `n_estimators`(기본 auto) · `ignore_pretraining_limits`(기본 False) | 미조정 | 스윕 대상 |
| **임베딩 → 경량 모델**(`TabPFNEmbedding`, n_fold≥2 OOF) | — | ⛔`tabpfn_extensions` 미설치. quant_ai는 **라이브 공유 env**라 설치하지 않았다(TabFM 때 별도 env를 쓴 것과 같은 이유) |

즉 두 의심 중 하나(결측 대체)는 사실이 아니었고, **남은 진짜 지렛대는 피쳐 선별 하나**다.
스크린 14조합(회귀·분류 각 7) × 2멤버 → 최선 조합으로 5멤버 확정 + 순열검정 + 대조군.

⚠️운영 메모: TabPFN이 18k×161피쳐에서 **GPU 메모리를 7.9/8.2GB까지 쓴다**(사용률 89%).
`top30` 조합은 훨씬 가볍다. 그리고 `pgrep`이 래퍼 셸을 잡으므로 실제 python은
`/proc/<pid>/comm`으로 골라야 한다(이 저장소에서 반복되는 함정).

**결과 — 권고 3개가 전부 실패했고, 스크린 승자는 아무것도 안 바꾼 T0였다.**

| 1단계 스크린 (멤버2) | 분류 VAL/OOS | 회귀 VAL/OOS |
|---|---|---|
| **T0 기준(161, 대체)** | **+63.21 / +59.72** ⭐ | +63.63 / +56.47 |
| T1 raw결측(161) | +63.21 / +59.72 | +63.63 / +56.47 |
| T2·T3 선별30 | +59.79 / +58.75 | +53.27 / +59.90 |
| T4 선별50 | +64.40 / +57.67 | +57.25 / +57.58 |
| T5 선별30+est8 · T6 +ipl | +59.79 / +58.75 | +53.27 / +59.90 |

- **T0 = T1 소수점까지 동일** → 결측 대체는 진짜 no-op였다(권고 무효 확인)
- ⭐**피쳐 선별이 오히려 해로웠다** — 분류 −3.4bp, 회귀 −10.4bp(VAL). 문서의 "100개 이상이면 선별"이
  이 데이터에는 전이되지 않는다. B8에서 HGB도 k=15~161이 평평했으니 **학습기와 무관하게
  이 피쳐 집합에는 잘라낼 잉여가 없다**
- T2=T3=T5=T6 완전 동일: `n_estimators=8`·`ignore_pretraining_limits` 둘 다 효과 0.
  (배선은 확인함 — `kw["n_estimators"]`가 실제로 전달된다. `auto`가 8로 풀렸거나 서브샘플이
  한계 이하라 무효. 게다가 `pol`이 4슬롯으로 자르므로 순위가 흔들려도 채택 집합이 잘 안 바뀐다)

**2단계 확정 (분류 T0, 5멤버)**

| | VAL | OOS | HOLDOUT |
|---|---|---|---|
| HGB-full (동결) | +60.24 | +58.72 | +59.66 |
| TabPFN-tuned | **+61.97** | **+59.62** | **+65.51** |
| 차 | +1.73 | +0.90 | +5.85 |

시드별 순열검정 **VAL p=0.1391 · OOS p=0.0518**. 대조군은 3창 전부 p=0.000.

⭐**사전등록 판정: ①양창승리 ✅ ②순열 p<0.05 ❌ ③대조군 ✅ → HGB 회귀 동결 유지.**

⚠️p값이 B14와 **소수점까지 일치**한다(0.1391 / 0.0518). 버그가 아니라 승자 T0가 B12/B14와
같은 구성·같은 시드라 결정론적으로 같은 계산이 된 것이고, **튜닝이 문자 그대로 아무것도
바꾸지 못했다는 증거**다.

⭐**진짜 병목은 모델이 아니라 검정력이다.** TabPFN 분류는 3창 전부 수치가 앞서고 대조군도
통과하는데, 시드 5개 · 창별 노이즈 ±5bp라 p를 0.05 아래로 못 민다. B14에서 **18k면 표본이
포화**라는 걸 확인했으므로 학습기 용량을 더 키워도 이 벽은 안 뚫린다.
→ **임베딩 경로(`TabPFNEmbedding`)는 지금 만들 값어치가 없다.** 별도 env가 필요한데, 그게
고칠 수 있는 문제(모델 표현력)가 애초에 병목이 아니다. **체결 표본이 더 쌓인 뒤 재검정할 것.**

### 라이브 추론 실측 — 운영 비용이 약 4,800배 (결정타)

"이미 다른 모델에서 검증된 TabPFN을 쓰는 게 낫지 않나"는 물음에, 통계가 아니라 **운영 쪽**을
숫자로 갈랐다. `scripts/measure_eth_entry_live_inference_cost_20260903.py`, 서버 GPU.

| | HGB 동결본 | TabPFN 5멤버 |
|---|---|---|
| 적합 | 14.45초 | 4.18초 |
| 상주 GPU | — (CPU) | **1.065GB** ✅ |
| **채점 n=1** | **p50 15.4ms · p95 18.7ms** | **p50 68,701ms · p95 86,076ms** |
| 채점 n=4 / n=16 | 14.3ms / 12.3ms | *(판정 확정 후 중단)* |

⭐**한 행 채점에 68.7초, 최악 86.1초 = 약 4,800배.** 그 시간 내내 GPU가 99%에 붙는다.

⚠️**정정(2026-09-03, 커밋 23539b5 직후)**: 처음에 "5분마다 규칙적으로 일어난다"고 썼는데
**약 288배 과장이었다.** 후보 팔 발생률을 실측하니 **하루 17~19건 · 전체 봉의 6%**이고,
양팔이 같은 봉에 걸리므로 **후보가 있는 봉은 하루 약 9개**다. 라이브는 매 봉 트리거만 계산하고
(값싸다) **트리거가 뜬 봉에서만 모델을 부른다.** 게다가 TabPFN 비용은 컨텍스트가 지배하므로
그 봉의 후보 전부를 **한 번에 배치 채점**할 수 있다.

| | 처음 주장 | 1차 정정 | **실측(2026-09-03)** |
|---|---|---|---|
| 채점 호출 | 288회/일 (매 봉) | 9~10회/일 | **41.2회/일** (전체봉의 14.3%) |
| GPU 포화 | 봉의 23~29% 상시 | 하루 10~15분 | **하루 약 47분** (듀티 3.3%) |

⚠️**1차 정정도 틀렸다.** 내가 "후보 팔 17~19건/일"이라 부른 값은 fills.csv 행수 = **체결된 팔**이지
제출된 팔이 아니었고, 채점 빈도는 **트리거가 뜬 봉 수**로 정해진다. 라이브 최근 2000봉 실측:
트리거 발동 77.3건/일 · **채점 대상 봉 41.2봉/일** · 제출 팔 154.7개/일 · **체결률 11.9%**.
같은 숫자를 두 번 틀린 뒤에야 실측했다 -- 추정으로 답하지 말았어야 했다.

**따라서 운영 장벽은 실재하지만 내가 말한 것보다 훨씬 작다.** 남는 실제 비용은 두 가지다 —
①하루 10여 분의 GPU 포화(대시보드·섀도우 러너와 경합) ②**주문 제출이 68.7초 늦어짐**
(지정가가 3 ATR 떨어져 있고 대기창이 6봉=30분이라 치명적이진 않지만, 기준가가 1분 낡는다).

⭐**그래서 판정의 무게가 옮겨간다.** HGB 동결 유지의 근거는 이제 **거의 전적으로 통계**다
(+0.90bp, p=0.0518, 10번째 후보). 운영은 보조 근거일 뿐이다.
그리고 이 정정은 **섀도우 동시 채점을 더 분명히 옳게 만든다** — 하루 10분 GPU면 섀도우로
치르기에 충분히 싸다.

⚠️**상주 메모리는 통과였다 — 이건 내가 틀렸다.** "8.2GB 중 7.9GB 점유"라고 본 건 b15 자신이
쓰던 것이고, 유휴 시 여유는 7.44GB다. 병목은 메모리가 아니라 **순전파 시간**이다. TabPFN은 매
호출마다 18,000행 컨텍스트를 다시 통과시키므로 **질의가 1행이든 16행이든 비용이 거의 같다**
(HGB는 n이 커질수록 오히려 행당 비용이 준다 — 성격이 반대다).

우회로 셋이 있는데 전부 **논쟁의 대상 자체를 없앤다**: 1멤버 축소(~13.7초, 그런데 +0.90bp를 낸 게
5멤버 앙상블) · 컨텍스트 18k→4k(B14가 18k 포화를 보였으니 근거가 사라짐) · 사전 캐시(후보는
봉 마감에야 정해짐).

⭐**결론: 약 4,800배의 운영 비용을, 통계가 확인조차 못 하는 이득(+0.90bp, p=0.0518, 10번째
후보)에 치르는 셈이다. HGB 동결 유지.**
다만 닫지는 않는다 — **섀도우에서 둘 다 채점**은 유효하다(주문을 안 내니 봉당 지연이 무의미하고,
HOLDOUT 소진 후 전진 데이터가 유일하게 깨끗한 증거다). 거기서 분명히 앞서면 그때 엔지니어링
(전용 GPU 예산, 컨텍스트 축소 재검정)에 값어치가 생긴다.

⚠️측정 주의: 대시보드·섀도우 러너가 함께 도는 **실제 경합 상태**에서 잰 값이라 다소 부풀려졌을
수 있다. 다만 1/3이어도 23초라 결론은 안 바뀐다. 그리고 이 측정 자체가 GPU를 물어서 대시보드를
느리게 만들 수 있으므로, n=1 판정이 나온 시점에 중단하고 8787 리슨·응답(302, 0.7ms)을 확인했다.

### 레짐 (B1 → B13)
- **레짐 분류기 예측값**(clean S12_K3/S24_K3 OOF): 기여 0 (A+B+C OOS 0.0998 → +D 0.0952) → 제외
- **레짐 136 피쳐 패널**: ⭐**OOS 상관을 0.103 → 0.204로 두 배**. 펀딩 9 · OI 3 · BTC 교차자산 11 등
  **klines에 없는 정보**가 핵심. "klines 파생 축 소진"이라 해놓고 정작 확보된 비-klines를 안 쓰고 있었다
- **정책 하드 게이트**(chop일 때만 진입): 양 창 동시 승리 없음(ETH chop은 OOS만, BTC chop은 VAL만),
  HOLDOUT n이 11~26으로 붕괴 → 기각. 레짐 원재료 20개가 이미 피쳐에 있어 게이트는 자유도만 뺏는다

---

## 5. 누수 방지 두 겹

**OOF 교차적합** — 재료 `_pct`는 메타라벨 TRAIN이 필터 TRAIN과 **100% 겹쳐** 모든 행이 in-sample
이었다. 청산 설계의 해법(TRAIN 롤백)은 발동 모집단이 2024-01에 시작해 밀 공간이 없어 안 통한다.
시계열 확장창 K-fold OOF로 해결(워밍업 2024-01~04 제외, fold k는 그 앞 구간만 학습).
레짐도 같은 방식으로 fold별 재훈련(임계값도 fold 학습창에서만 백분위 보정).

⚠️결과적으로 재료와 레짐 예측값은 **둘 다 기여 0**으로 확인돼 최종 피쳐에서 빠졌다.
누수를 막아놓고 보니 애초에 쓸 게 없었다 — **이 재료의 세 번째 0**이다.

---

## 6. 라이브 파리티 (통과)

161피쳐 중 **156개가 `FeatureEngineer().process()` + `_with_raw_state12()` 경로로 재현**되고
**불일치 0건**(완전일치 47 / 수치오차 수준 109). 나머지 5개는 정책 파생값(`arm`/`sig_id`/`depth`)과
이름 중복 사본(`rsi_r136`/`realized_vol_ratio_r136`)이다.

⚠️**라이브는 klines만으로 안 된다.** 바이낸스 파생 엔드포인트 4개가 필요하다 —
`openInterestHist` · `topLongShortPositionRatio` · `globalLongShortAccountRatio` · 펀딩 이력.
라이브 레짐 스코어러가 이미 프로덕션에서 호출하므로 신규 배선은 없지만,
**의존성이 늘었다** — 하나라도 실패하면 진입 모델이 점수를 못 낸다. 섀도우에 실패 처리 필요.

---

## 7. 남은 것

| | |
|---|---|
| 비용 | 왕복 10bp 가정. **≥2026-09-04 peg-maker 실측** 반영 필요. 청산은 taker 고정이라 절감은 진입 다리뿐 |
| 체결 모델 | "저가≤지정가면 지정가 체결" 미검증. 3 ATR 깊이는 대개 급속 이동 중이라 역선택 우려 — 섀도우 체결통계로 대조 |
| 홀드아웃 | **소진**. 8종이 각자 1회 + 깊이/τ 선별에도 참고 → 진단 전용 |
| 주문 상태기계 · 섀도우 | 미구현 |
| TabPFN 분류 | 표본 증가 후 재검정 대상(OOS p=0.0518) |


---

## 8. 섀도우 러너 (2026-09-03 구축)

`scripts/live_eth_entry_limit_fade_signal_20260903.py` (후보 생성·채점) +
`scripts/live_eth_entry_limit_fade_shadow_runner_20260903.py` (가상 원장). 주문 함수를
import하지 않는다.

### ⭐구축 중 발견한 라이브 경로 결함 — 파생거래소 이력 500행

바이낸스 `/futures/data/*`(openInterestHist·topLongShortPositionRatio·globalLongShortAccountRatio)는
**`startTime`을 무시하고 최근 500행(약 41시간)만** 돌려준다. 페이지네이션 코드가 있어도 두 번째
호출이 같은 구간을 반환해 루프가 끝난다. 그 결과 `dropna(DERIV)` 후 **FeatureEngineer 입력이
4,319봉 → 499봉**으로 잘렸다.

짧은 이력이 피쳐를 실제로 바꾸는지 측정했다(같은 봉을 긴/짧은 이력으로 계산해 대조):

| 꼬리 | 일 | 불일치/136 | 예측상관 | 중앙차 |
|---|---|---|---|---|
| 499 | 1.7 | **13** | 0.9909 | 0.70bp |
| 1,000 | 3.5 | 3 | 0.9933 | 0.57bp |
| 2,000 | 6.9 | 3 | 0.9947 | 0.26bp |
| **4,000** | **13.9** | **0** | **1.0000** | **0.00bp** |

⚠️`funding_pressure`는 **상관 −0.526으로 부호가 뒤집혔고** `ou_halflife`는 0.339였다.
HGB가 161개를 평균해 예측 상관 0.99를 유지하지만, 부호가 뒤집힌 입력을 그대로 둘 수는 없다.

**해결**: 일별 metrics 덤프(T+1 공개, `binance_data/metrics/`, 서버 973일치 보유)로 과거를 메우고
최근 41시간만 API로 덮는다. 없는 날은 공개 저장소에서 받아 캐시한다.
그리고 워밍업이 4,000봉 미만이면 **조용히 상수로 채점하지 않고 `insufficient_warmup`으로 실패**한다.

| | 수정 전 | 수정 후 |
|---|---|---|
| FeatureEngineer 입력 | 499봉 | **4,256봉** |
| median 대체 셀 | 105,808 | **0** |
| 예측 최대 | +48.5bp | **+67.7bp** |
| τ 통과 | 1.5% | **7.18%** (11.2개/일) |
| 예상 체결 | 0.3건/일 | **1.34건/일** (연구 HOLDOUT 0.76건/일) |

⚠️⚠️**배포된 레짐 스코어러(`live_regime_gbm3_signal_20260826.py`)도 같은 경로를 쓴다.**
그쪽은 마지막 봉만 채점하므로 출력에 미치는 영향은 별도 측정이 필요하지만, **같은 500행 제약
아래 있다**는 사실 자체는 확인됐다. 미측정 항목으로 남긴다.

### 체결 판정 두 가지를 나란히 기록 (사용자 결정)

  · `fill_5m` -- 백테스트 그대로(5분봉 저가≤지정가). **포지션은 이 기준으로 연다** -- 동결 수치와
    직접 비교 가능해야 하므로.
  · `fill_1m` -- 1분봉 해상도로 `through`(뚫음=확실 체결) / `touch_only`(스침=큐 의존) / `none` 분류.

⚠️**maker 워커의 큐 규칙은 이식 불가**였다. 그건 터치(L1) 주문용이고 우리 지정가는 3 ATR(약 0.8%)
떨어져 `/fapi/v1/depth?limit=1000`(약 $10 범위) **바깥**이라 제출 시점에 앞선 큐를 관측할 수 없다.
관측 가능한 축(뚫음 vs 스침)으로 대체했고, `touch_only` 비율이 높으면 그때 스트림 기반 큐 추정을
설계한다.

### 사전등록 — 모델 전환 규칙 (착수 전 고정)

> 섀도우 체결 **200건 이상** 누적 **그리고** 같은 후보 봉 기준 페어드 부트스트랩(TabPFN − HGB)
> 95% CI **하한 > 0** 이면 TabPFN 전환. 미달이면 HGB 유지.

체결 1.34건/일이면 200건까지 약 150일이다. 그 사이 비용 실측(peg-maker)과 `touch_only` 비율이
먼저 나온다.

### 기타 확인

  · `sig_id`는 예측 분산 0.02bp로 **거의 쓰이지 않는다**(0은 아니다).
  · 봉 단위 피쳐가 후보 간 동일하므로 같은 봉의 예측값은 arm으로만 갈린다.
  · `_fetch_klines`가 저장소에 둘 있고 **형성 봉 처리가 다르다** -- v_rebound 쪽은 버리고
    레짐 쪽은 버리지 않는다. 후자를 쓰므로 직접 버린다.
---

## 9. B16 — TabPFN 임베딩 / 차원축소 (2026-09-03)

B15가 남긴 유일한 미시도 지렛대(`TabPFNEmbedding`)를 닫았다.
`scripts/research_eth_entry_b16_tabpfn_embedding_20260903.py`

### 설치 — 공유 env를 건드리지 않았다

`tabpfn_extensions`를 `~/tabpfn_ext_libs`에 `--target`으로 깔고 `sys.path`에 선행 주입했다
(`quant_ai`는 라이브 공유 env라 site-packages를 변경하지 않는다 — TabFM 때와 같은 이유).

⚠️**텔레메트리**: `tabpfn_extensions`는 import 시점에 PostHog 애널리틱스를 초기화하고
**비-CI에서 기본 ON**이다(`default_disable = "1" if ci else "0"`, `telemetry/core/service.py`).
자체 트레이딩 리서치를 다루는 저장소이므로 opt-out 플래그를 믿는 대신 **전송 경로 자체를
제거**했다: ①`posthog`를 무동작 스텁으로 대체 ②뉴스레터/신원 프롬프트 모듈
(`telemetry.interactive`)을 `sys.modules`에 무동작 주입 ③`TABPFN_DISABLE_TELEMETRY=1`.
벤더 파일은 고치지 않았다.

### 결과 — 임베딩도 PCA도 전부 기준선 아래

| 팔 | VAL | OOS | HOLDOUT |
|---|---|---|---|
| A HGB-full 161 (동결) | +60.24 | +58.72 | +59.66 |
| **B TabPFN 직접(분류)** | **+63.11** | **+62.09** | **+68.56** |
| C 임베딩(512d) → 로지스틱 | +56.34 | +57.45 | +59.56 |
| D 임베딩(512d) → HGB | +55.19 | +50.99 | +62.66 |
| E PCA16 → HGB | +55.73 | +51.24 | +44.77 |
| E PCA32 → HGB | +48.96 | +54.85 | +42.28 |
| E PCA64 → HGB | +52.92 | +54.36 | +49.85 |

- **임베딩 기각**: 문서 권장 조합(C, OOF 임베딩 + 저용량 헤드)조차 VAL −3.9 / OOS −1.3bp.
  ⭐임베딩은 512차원으로 원본 161보다 **오히려 크다** — 축소가 아닌데도 정보를 잃었다.
  트랜스포머 내부 표현이 **자기 in-context 예측에 최적화**된 것이지 범용 피쳐 기저가 아니다.
- **PCA 기각**: k=16/32/64 전부 기준선 아래이고 HOLDOUT이 특히 나쁘다(+42~50 vs +59.66).

⭐**차원축소 세 방식이 모두 같은 방향으로 실패했다**: 피쳐 선별(B8, k=15~161 평평),
TabPFN 문서식 선별(B15, 161→30에서 −3.4/−10.4bp), 그리고 이번 PCA·임베딩.
**독립적인 세 증거가 "이 161피쳐 집합에는 압축할 잉여가 없다"로 수렴한다.**

### 사전등록 판정: **HGB 회귀 동결 유지**

최선 후보는 여전히 **B TabPFN 직접**으로 3창 전부 A를 앞선다(+2.9 / +3.4 / +8.9bp).
①양 창 승리 ✅ ②순열검정 **VAL p=0.3092 · OOS p=0.2843 ❌**.

⚠️이 p값은 **B15의 p값(0.139/0.052)과 직접 비교할 수 없다.** B15는 시드별 페어드 검정
(5시드 각각의 정책 성과를 섞음)이고, B16은 **봉 단위 예측값 스왑** 검정이다 — 후자가 더
보수적인 귀무를 쓴다. 게다가 B16은 멤버 3개(B15는 5개)다. 두 검정이 모두 미달이라는 사실만
공통이다.
