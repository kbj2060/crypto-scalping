# ⚠️진입 모델 라벨 결함 — 체결 봉의 유리한 폭을 크레딧한다 (2026-09-03)

사용자 질문 *"이게 진짜인지 전수조사해줘"* 에서 시작해 **첫 항목에서 걸렸다.**
재현: `scripts/audit_eth_entry_intrabar_fill_bar_credit_20260903.py`

## 결함

`trail_out(side, e, a, h[f:f+H], l[f:f+H], c[f:f+H])`가 **체결 봉 `f` 자체부터** 평가한다
(`research_eth_entry_b6_expand_20260903.py:114`). 지정가는 **직전 종가보다 3 ATR 아래**에
걸리므로, 그 봉이 우리를 체결시키려면 가격이 3 ATR 떨어져야 하고 **그 봉의 고가는 그 하락이
시작된 지점 — 즉 체결 이전**이다. 그걸 "진입 후 유리한 움직임"으로 크레딧하면 포지션이 없던
시점의 가격을 쓰는 것이다.

```python
peak = hi[0]                       # ≈ e*(1+3a)  ← 체결 봉의 고가 (대개 체결 이전 시점)
(peak-e)/e = 3a ≥ ARM*a            # → 그 봉에서 즉시 무장
stop = peak*(1-TRAIL*a) ≈ e*(1+2.9a)   # → 진입가보다 2.9 ATR 위에 스톱이 잠긴다
```

**실측: 체결 봉의 유리한 폭이 중앙 1.76 ATR · 평균 1.94 ATR이고, 82.3%가 ARM(1.0)을 넘는다.**
5분봉 안에서 진입 후에만 그만큼 움직였다고 볼 수 없는 크기다.

## 크기 — 성과의 대부분이다

전체 후보 팔 14,864개(모델·선택과 무관한 **라벨 자체**):

| 라벨 | 평균 | 중앙 | 승률 | **PF** |
|---|---|---|---|---|
| **L0 현행** (체결봉 포함) | **+27.64** | +28.05 | 86.3% | **3.66** |
| **L1 정직** (f+1부터) | **+1.71** | +15.73 | 69.5% | **1.07** |
| L2 비관 (f, 유리갱신 금지) | −0.83 | +15.20 | 68.0% | 0.97 |

v2가 실제로 고른 트레이드:

| 창 | L0 현행 | **L1 정직** | 격차 | L0 승률 → L1 |
|---|---|---|---|---|
| VAL | +68.44 | **+15.82** | −52.62 | 91.7% → 72.6% |
| OOS | +66.43 | **+37.12** | −29.30 | 92.2% → 81.2% |
| **HOLDOUT** | +78.16 | **−2.99** | **−81.15** | 90.2% → 70.6% |

⭐**정직한 라벨에서 전체 후보 PF가 1.07로 사실상 무엣지가 되고, HOLDOUT은 부호가 뒤집힌다.**

## 영향 범위 — 진입 모델 계보 전체

`y`가 오염됐으므로 그 위에 세운 것이 전부 영향받는다:
- 동결 v1(HGB) +62.29/+63.18/+59.66 · 동결 v2(TabPFN) +68.44/+66.43/+78.16
- 대조군 7종(모멘텀뒤집기·무작위필터·순환이동·시드·부트스트랩·DSR/PBO)
- 아키텍처 스윕 B9~B16, 피쳐 선별 B2/B8, 레짐 게이트 B13
**대조군이 통과한 것은 "L0 라벨 안에서 신호가 무작위보다 낫다"는 뜻이지, 엣지가 실재한다는
뜻이 아니다** -- 대조군도 같은 오염된 라벨로 채점됐다.

## ⭐섀도우 러너는 이미 정직했다 (그리고 그 불일치를 몰랐다)

`live_eth_entry_limit_fade_shadow_runner_20260903.py::manage()`가
`b["timestamp_utc"] > p["entry_bar_utc"]`로 **체결 봉을 제외**한다 -- L1 컨벤션이다.
즉 섀도우는 백테스트(+68bp)가 아니라 **+15bp 수준을 보고하게 돼 있었다.**
이 불일치를 오늘까지 인지하지 못했다. 섀도우가 낮게 나왔다면 "라이브가 나쁘다"가 아니라
"백테스트가 부풀었다"가 정답이었을 것이다.

## 미해결 — 1분봉 해상이 필요하다

L0(체결봉 전부 크레딧)와 L1(체결봉 전부 배제)은 **양 극단**이다. 실제로는 체결 후 그 5분봉
안에 남은 시간의 움직임은 정당하게 우리 것이다. 정확한 답은 **1분봉으로 체결 시점을 특정**하고
그 이후 구간만 크레딧하는 것이다. 그 전까지 L1을 보수적 기준선으로 쓴다.

## 교훈

⚠️이 저장소에 이미 `feedback_intrabar_ordering_optimistic_pessimistic_bracket_20260830`이
있었다. **알고 있던 함정에 다시 빠졌다.** 체결가가 봉의 극단(저가 근처)인 진입에서는
그 봉의 반대쪽 극단(고가)이 거의 확실히 체결 이전이라는 점이 특수하게 위험한데,
그 특수성을 새 코드에서 다시 점검하지 않았다.

---

# 1분봉 해상 확정 + 정직 라벨 재평가 (2026-09-03)

## L3 = 1분봉으로 체결 시점을 특정한 라벨 — **L1과 거의 같다**

체결 5분봉을 1분봉 5개로 쪼개 **지정가에 처음 닿은 분**을 찾고, 그 **다음 분부터**의
고가/저가만 체결 봉의 사후 폭으로 쓴다. 이후 봉은 5분봉 그대로다.
(`scripts/audit_eth_entry_1m_resolved_labels_20260903.py`)

| 라벨 | n | 평균 | 중앙 | 승률 | **PF** |
|---|---|---|---|---|---|
| L0 현행 (체결봉 전부) | 14,864 | +27.64 | +28.05 | 86.3% | **3.66** |
| **L3 1분해상 (정답)** | 14,491 | **−0.20** | +15.02 | 70.4% | **0.99** |
| L1 정직 (f+1) | 14,864 | +1.71 | +15.73 | 69.5% | 1.07 |

⭐**L3가 L1과 거의 같다** = 체결 후 그 5분봉에 남은 시간의 기여가 거의 없다는 뜻이고,
**L0가 크레딧하던 1.76 ATR이 사실상 전부 체결 이전**이었음을 1분 데이터로 확정한다.
⚠️1분봉이 2026-07-31까지라 HOLDOUT 후보가 2,585 → 2,212로 줄었다.

## 정직 라벨에서 다시 물었다 (`audit_eth_entry_honest_label_reeval_20260903.py`)

| | VAL | OOS | **HOLDOUT** |
|---|---|---|---|
| **① 무필터 (기계 자체)** | +1.48 (1240) | +1.95 (890) | **−3.06** (1202) |
| ② 모델 필터 (HGB) | **+12.62** (150) | **+21.62** (111) | **−5.67** (67) |
| ④ 피쳐 한 봉 밀기 | +5.81 (154) | +6.14 (123) | **+11.14** (75) |
| ③ 무작위 필터 대조 p | **0.010** ✅ | **0.000** ✅ | **0.847** ❌ |

⭐**"양팔 페이드 + 슬롯" 기계 자체에 엣지가 없다.** 이전에 *"무작위 봉도 +16~21bp,
트리거 기여는 30%뿐"* 이라고 쓴 것은 전부 L0 위의 숫자였다.
⭐**모델은 VAL/OOS에서 진짜로 판별한다**(무작위 대비 p=0.010/0.000). 그러나 **HOLDOUT에서
무작위보다 나쁘다**(−5.67 vs −2.13, p=0.847).
⚠️**피쳐를 한 봉 밀어도 나빠지지 않고 HOLDOUT에선 오히려 낫다**(+11.14 vs −5.67).
3창 전부 양수인 쪽은 stale이다. 신선함의 이득이 VAL/OOS의 노이즈일 가능성을 시사한다.

## TabPFN 재학습에서 드러난 별개 결함 — TRAIN 유도 임계값이 성립하지 않는다

depth3/wait6로 좁히면 TRAIN이 8,450행뿐이라 `min(SUB=18000, 8450)`으로 **컨텍스트가 TRAIN
전체**가 된다. 그러면 TRAIN 행 예측은 사실상 자기 자신을 보는 것이라 확률이 극단적으로
확신에 차고, **그 분위수로 만든 임계값이 표본외 분포보다 훨씬 위**에 놓인다:

  유지율 0.05 / 0.10 → 3창 모두 **0건 통과**
  유지율 0.2037     → VAL 1,148/1,240 = **93% 통과** (사실상 무필터)

따라서 이 설정에서 TabPFN이 받은 "통과"(OOS p=0.017, HOLDOUT p=0.043)는 **판별력의 증거가
아니다** -- 거의 아무것도 거르지 않은 결과다.
⚠️**동결 v2는 다르다**: 거기서는 TRAIN 81,168행에서 18,000행을 뽑았으므로 컨텍스트가 TRAIN의
22%였다. 이 결함은 이번 재학습 설정에서만 발생한 것이고, 대신 **v2는 L0 오염 라벨로 학습됐다.**

## ⚠️내 설계 결함 — 학습 모집단을 줄였다

원 설계는 **모든 깊이·대기 81,168행으로 한 번 학습**하고 depth/wait을 피쳐로 넣는다.
이번 재학습은 depth3/wait6로 먼저 걸러 **8,450행으로 학습**했으므로 원본과 비교가 성립하지
않는다. 전체 모집단 L3 라벨로 다시 돌리는 중이다.

## 현재 판정

**진입 모델은 자동매매 근거가 없다.** 앞서 보고한 예상 수익(+26.9%)·MDD(−0.89%)·Calmar(30)은
전부 L0 위의 값이라 성립하지 않는다. 남은 것은 **VAL/OOS의 판별력**이고, 그것이 HOLDOUT에서
유지되지 않는다.

---

# L0 기반 가정 전수 재시험 (2026-09-03)

사용자 요청 *"L0 위에 올려진 가정들은 모두 재시험해줘"*.
`scripts/audit_eth_entry_l3_assumption_retest_20260903.py`

## ⭐먼저: 오염 크기가 **깊이에 정확히 비례**한다 (메커니즘 확증)

| depth | L0 평균 | L3 평균 | **차이** |
|---|---|---|---|
| 2.0 | +19.79 | −2.21 | **22.0** |
| 2.5 | +23.71 | −1.40 | **25.1** |
| 3.0 | +28.08 | −0.20 | **28.3** |
| 3.5 | +31.34 | +0.03 | **31.3** |

체결 봉 고가가 진입가보다 `depth × ATR` 위에 있다는 메커니즘 그대로다. **L0는 깊은 지정가를
체계적으로 편애했고**, 그래서 깊이 선택 자체가 편향된 비교였다. L3에서는 전 깊이·대기 셀이
0 근처이고 **HOLDOUT은 전부 음수**다 -- 깊이를 바꿔도 해결되지 않는다.

## ⭐⭐핵심 반전 -- **"양팔"이 틀렸다**

| 구성 | VAL | OOS | HOLDOUT |
|---|---|---|---|
| **신호방향만 (arm1)** | **+4.01** (822) | **+6.02** (635) | **−0.99** (844) |
| 역방향만 (arm0) | −1.21 (703) | **−11.33** (428) | −5.27 (613) |
| 양팔 *(배포)* | +1.48 (1240) | +1.95 (890) | −3.06 (1202) |

세션 초반의 핵심 발견 -- *"두 팔 다 excursion 페이드라 양쪽 다 걸기가 방향 오라클을 이긴다"* --
가 무너진다. **신호방향만이 3창 전부 양팔보다 낫고 역방향은 명확한 손실**이다.

⭐**역방향 팔이 아티팩트의 최대 수혜자였다.** top 신호에서 위에 매도를 걸면 그 봉의 **저가**가
체결가보다 한참 아래라 즉시 무장됐다. L0에서 역방향이 +로 보였던 이유이고, 그래서 "양쪽 다"가
우월해 보였다.

## ② 슬롯 수 -- 무관

| 슬롯 | 1 | 2 | 3 | **4** | 6 | 무제한 |
|---|---|---|---|---|---|---|
| VAL | +2.43 | +0.53 | +1.90 | **+1.48** | +1.89 | +2.83 |
| OOS | +2.95 | +2.23 | +2.56 | **+1.95** | +1.78 | −2.46 |
| HOLDOUT | −2.32 | −3.17 | −3.71 | **−3.06** | −4.01 | −1.87 |

**어떤 슬롯 수로도 HOLDOUT이 음수**다. 4가 특별하지 않다.

## ④ 청산 파라미터 -- 트레일링은 기여하나 부호를 못 바꾼다

| SL / ARM / Trail | VAL | OOS | HOLDOUT |
|---|---|---|---|
| 3.0 / 1.0 / 0.1 *(배포)* | +1.48 | +1.95 | −3.06 |
| 3.0 / **0.5** / 0.1 | +1.80 | +3.42 | −2.97 |
| 3.0 / 1.5 / 0.1 | +0.04 | +0.24 | −4.30 |
| 3.0 / 1.0 / 0.3 | −1.11 | −0.66 | −4.80 |
| 3.0 / 1.0 / 0.5 | −2.59 | −2.76 | −5.77 |
| 2.0 / 1.0 / 0.1 | −2.42 | −0.41 | −2.78 |
| **4.0** / 1.0 / 0.1 | +0.61 | +4.41 | **−1.27** |
| **시간청산만** | −1.82 | −3.00 | **−14.00** |

⭐**트레일링 자체는 확실히 기여한다**(HOLDOUT −3.06 vs 시간청산만 −14.00). 좁은 트레일(0.1)이
넓은 것(0.3/0.5)보다 낫고, 낮은 무장(0.5)이 높은 것(1.5/2.0)보다 낫다.
그러나 **어떤 조합도 HOLDOUT을 양수로 만들지 못한다.**
⚠️L0에서는 무장이 사실상 공짜였으므로(82.3% 즉시 무장) ARM 값의 비교가 무의미했다.
L3에서 비로소 이 축이 의미를 갖는다.

## ⑤ 신호별 -- 3창 전부 양수인 신호가 **하나도 없다**

| 신호 | VAL | OOS | HOLDOUT |
|---|---|---|---|
| kalman_deviation_meanrev | +1.85 (417) | +3.74 (284) | −0.52 (370) |
| orthogonal_combo | −2.70 (113) | +5.20 (77) | +4.79 (89) |
| demarker_extreme | +9.91 (191) | −5.82 (116) | −2.31 (242) |
| smt_divergence | +2.43 (374) | −3.56 (188) | −3.37 (307) |
| liquidity_sweep | +1.49 (392) | −2.29 (256) | −2.34 (380) |
| short_term_return_z | +0.96 (264) | −7.60 (195) | −1.68 (255) |
| taker_delta_z_climax | −0.64 (398) | −1.97 (295) | −3.73 (382) |
| fib_extension_exhaustion | −5.00 (93) | −9.89 (63) | −6.39 (108) |

무작위 봉 대조군이 실패한 이유가 여기 있다 -- **개별 신호도 일관되게 기여하지 않는다.**

## 종합

**구조 파라미터를 어떻게 바꿔도 기계 엣지가 없다**(깊이·대기·슬롯·청산 전부).
실질적 발견은 둘뿐이다:
1. ⭐**양팔 → 신호방향만**이 낫다 (L0 결론 반전)
2. 트레일링은 손실을 줄이는 데 실제로 기여한다

⚠️여기서 나온 "최선" 조합을 채택 근거로 쓰면 안 된다. VAL/OOS/HOLDOUT은 이미 여러 번
소진됐고, 이건 *"L0에서 고른 것이 L3에서도 최선인가"* 를 묻는 진단이다.

---

# 모델 수준 가정 재시험 + 최종 종합 (2026-09-03)

`scripts/audit_eth_entry_l3_model_assumption_retest_20260903.py`

## ⚠️가장 중요한 발견 -- **같은 설정이 세 번 다른 값을 낸다**

`HGB squared_error · 양팔 · 유지율 0.2037` (동일 구성):

| 실행 | VAL | n | 차이점 |
|---|---|---|---|
| `l3full` | **+2.16** | 200 | float64 DataFrame으로 학습 |
| `l3mdl` ⓐ | **−4.55** | 204 | float32 배열로 학습 |
| `l3mdl` ⓑ (k=161) | **−7.52** | 205 | float32 + 컬럼 순서가 중요도순 |

**dtype 하나 바꾸면 VAL 부호가 뒤집힌다.** 어떤 개별 숫자보다 이게 정보량이 크다 --
이 표본에서 **VAL 추정치는 0과 구별되지 않는다.**

## ⓐ 아키텍처 -- 유일하게 견고한 패턴은 arm1만 > 양팔

| | VAL | OOS | HOLDOUT |
|---|---|---|---|
| HGB squared · 양팔 | −4.55 | +23.75 | +13.89 |
| HGB squared · arm1만 | −12.11 | +27.29 | +24.83 |
| HGB absolute · arm1만 | −2.33 | +29.91 | +31.71 |
| HGB quantile0.6 · arm1만 | +7.62 | +33.89 | +33.66 |
| HGB 분류 · arm1만 | +6.59 | +25.61 | +17.66 |
| TabPFN 분류 · 양팔 | +9.65 | +19.22 | +23.63 |
| **TabPFN 분류 · arm1만** | **+10.58** | **+37.06** | **+32.75** |

**8/10 조합에서 arm1만이 양팔보다 낫다** -- 구조 재시험과 일치한다. 그 외에는 아키텍처별
승자가 없다(VAL이 −12.11 ~ +13.15로 흩어진다).

## ⓑ 피쳐 선별 -- 신호 없음

k = {15,30,50,100,161}에 일관된 방향이 없다. HOLDOUT 최선 k=50(+33.85), OOS 최선 k=100(+33.44),
VAL은 전부 노이즈. **"161 전부"라는 L0 결론은 지지되지도 반박되지도 않는다.**
L3 기준 상위 피쳐: `depth · vwap_dist_288 · atr_pct · bb_width · arm · btc_corr_60 …`

## ⓒ 유지율 -- 낮출수록 n이 붕괴한다

`TabPFN·arm1만·0.10 → HOLDOUT +76.30 (n=11)` · `HGB·arm1만·0.05 → HOLDOUT +85.37 (n=6)`.
**n=6의 +85bp는 의미가 없다.** 유지율을 낮추면 숫자가 커지는 건 표본이 줄어서다.

---

# 🔴 최종 종합 -- 자동매매 근거는 없다

## 사라진 것

동결 v1(+62.29/+63.18/+59.66) · 동결 v2(+68.44/+66.43/+78.16) ·
예상수익 +26.9%/MDD −0.89%/Calmar 30 · 대조군 7종 통과 · DSR 1.0000/PBO 0.0437 ·
**"양팔이 방향 오라클을 이긴다"(세션 핵심 발견, 반전됨)** · 아키텍처 스윕 B9~B16 ·
피쳐 선별 B2/B8 · 레짐 게이트 B13 판정. **전부 L0 오염 라벨 위에 있었다.**

## 견고하게 남은 것 -- 셋, 그리고 어느 것도 "수익"이 아니다

1. **페이드 방향성 실재** -- 추격으로 뒤집으면 3창 전부 손실(−65.69/−6.12/−56.65)
2. **arm1만 > 양팔** -- 구조·모델 재시험 양쪽에서 일관(8/10 조합)
3. **모델 선별력이 시드에 안정** -- 새 시드 5개 × 3창 = **15/15** 무필터 초과

⚠️셋을 다 합쳐도 **HOLDOUT 무필터가 −0.99bp**(arm1만)다.

## 확립을 막는 두 벽

- **트리거가 무작위 봉보다 못하다**(VAL +1.48 vs +2.87 · OOS +1.95 vs +5.45).
  8종 증거신호가 "어디서 페이드할지"를 짚어주지 못한다.
- **독립 일수가 42~45일뿐**이라 일 단위 군집 부트스트랩 CI 하한이 VAL/OOS에서 음수다.
  무작위 필터 대조군이 p=0.003으로 통과한 건 **행 단위 재추출이라 같은 날 트레이드를
  독립으로 셌기 때문**이다.

## 조치

- **섀도우 러너 정지**(2026-09-03). 채점 규약은 정직(L1)한데 모델이 L0로 학습돼 정합이
  깨져 있었다 -- 그대로 두면 쌓이는 데이터가 무의미하다.
- 동결 v1·v2 artifact는 **재현용으로만** 남긴다. 승격 후보가 아니다.
- 되살릴 유일한 길은 **전진 섀도우**다. VAL/OOS/HOLDOUT은 하루에만 수십 번 재사용돼
  더 이상 판정 근거가 못 된다.

## 교훈 -- 이미 갖고 있던 두 경고를 지나쳤다

1. `feedback_intrabar_ordering_optimistic_pessimistic_bracket_20260830` -- **진입가가 봉의
   극단에 있는 구조에서는 반대쪽 극단이 거의 확실히 진입 이전**이라는 특수 위험을 새 구조에서
   다시 점검하지 않았다.
2. `xrp_btc_delayed_anchor_control_invalidates_economics_20260903` -- **"대조군이 너무 완전하게
   통과할 때가 적신호"**. 대조군 7종이 전부 통과했을 때 의심했어야 했다. 대조군도 같은 오염된
   라벨로 채점되므로 오염을 잡지 못한다.
