# 앵커 재정의 — 중첩(개수) vs 조합(정체) 전수 스크린 (2026-09-07)

사용자: *"증거신호의 첫 발동은 정확도가 너무 낮아. 중첩으로 쌓이고 어떤 조합인지가 굉장히
중요해. 바닥 앵커와 천장 앵커에서의 최고의 조합이나 중첩을 찾아봐."*

스크립트 `scripts/research_eth_anchor_combination_screen_20260907.py` · 산출 `tmp/eth_anchor_combo_20260907/`

## 0. 한 줄 결론

**중첩이 앵커 정확도를 올린다는 직관은 맞다 — 단 천장 측면에서만이고, "어떤 조합인가"는
아무 정보도 주지 않는다.** 천장은 3종 이상 중첩이 현행 first-fire 대비 ATR 통제 후
**VAL +5.0pp · OOS +6.4pp**(any3/W12, 두 창 CI>0)로 개선된다. 바닥은 **0/7** — VAL에서만
오르고 OOS에서 사라지며 4종 중첩은 오히려 음수다. 조합 정체는 TRAIN→VAL 순위 이월이 없다
(ρ 중앙 **−0.065**, 12개 중 유의 1개이고 그마저 음의 방향). **그리고 페이드/지속 레짐은
어떤 앵커에서도 여전히 동전이다** — 188개 평가가능 후보 중 통과 1개(인접 셀 불안정 = 잡음).

## 1. 왜 지금 이게 정당한가

`eth_regime_outcome_label_20260906.md` §5가 방향축을 닫으면서 부활 조건을 스스로 명시했다:
*"되살릴 조건은 모집단 자체가 바뀌어 레짐 빈도가 0.5에서 유의하게 벗어나는 것이고, 그건
발동 조건을 바꾼다는 뜻이다."* 조합 앵커가 정확히 그 변경이다.

이미 닫힌 것과의 차이: `eth_confluence_vote_lift_revalidation_and_fade_guard_20260906.md`는
**동시발동 수(votes)** 를 페이드 진입 경제성으로 검정해 36셀 0통과로 닫았다. 여기서 새로 묻는
것은 **조합의 정체**이고, 라벨도 경제성이 아니라 **출구 없는 앵커 정확도**다(09-07 `sim_exit`
결함 무관).

## 2. 앵커 정의 (인과적, 뒤만 본다)

부분집합 S(|S|≤3), 창 W∈{1,3,12}봉에 대해 봉 t가 앵커 ⟺
(1) 모든 s∈S가 [t−W+1, t] 안에서 **raw** 발동했고, (2) 적어도 하나가 **정확히 t에** 발동하며
(t = 조합 완성 봉), (3) 같은 (side,S,W) 앵커가 직전 GAP=12봉 안에 없다.

`_active`/`_fill`(지속창)이 아니라 raw 발동을 쓴다 — 지속창 길이가 신호마다 달라
(taker 24봉·str_z 12봉) '동시성'을 오염시킨다(재검증 문서 §1: 천장 표 총합의 21.1%가 장기유지 2종).

**대조군 `any-k`**: 정체 무관 "아무 k종이 W봉 안에 겹침". 조합 정체의 직접 대조군.
**기준선 `first_fire_union`**: 현행 규약(신호별 GAP12 첫발동 합집합).

## 3. 라벨 (전부 출구·비용 없음)

- **L1 앵커 정확도** = P(K=12봉 안에 같은 종류 지그재그 피벗). `event_study` 계보 그대로.
- **L2 레짐 편향** = P(R1>0.5), R1 = MFE_cont/(MFE_cont+MFE_fade), H=12/48. 09-06 순수 레짐 라벨 원문.

두 라벨 모두 **ATR 십분위 매칭 기준선**을 뺀 초과분으로 판정한다(앵커별 excess_i = y_i − m_d(i),
평균이 정확히 관측−매칭귀무). 09-06 재검증이 보인 대로 raw lift 상승의 대부분은 변동성이다.

## 4. 사다리 — 개수는 실재한다

정확도 P(12봉 안 피벗), TRAIN / VAL / OOS:

| 측면 | 앵커 | TRAIN | VAL | OOS | n(T/V/O) |
|---|---|---|---|---|---|
| bottom | **first_fire_union (현행)** | 0.301 | 0.324 | 0.317 | 7954 / 1496 / 638 |
| bottom | any2 / W1 | 0.321 | 0.346 | 0.315 | 2550 / 486 / 197 |
| bottom | any3 / W1 | 0.370 | 0.372 | 0.333 | 1353 / 250 / 108 |
| bottom | any4 / W1 | **0.377** | **0.391** | 0.345 | 714 / 133 / 58 |
| top | **first_fire_union (현행)** | 0.280 | 0.270 | 0.274 | 8137 / 1590 / 599 |
| top | any3 / W1 | 0.344 | 0.329 | 0.391 | 1231 / 243 / 92 |
| top | any3 / W12 | 0.307 | 0.320 | 0.336 | 1739 / 338 / 122 |
| top | any4 / W3 | 0.344 | 0.331 | **0.459** | 949 / 175 / 61 |

ATR 십분위 매칭 초과분도 k에 대해 커진다(bottom first_fire +0.155/+0.178/+0.147 →
any4/W1 +0.212/+0.228/+0.166; top +0.141/+0.136/+0.131 → +0.233/+0.197/+0.207, 전부 CI>0).

## 5. ⭐현행 앵커 대비 — 천장만 통과 (ATR 통제 초과정확도의 차이, 일군집 CI95)

| 측면 | 앵커 | TRAIN Δ | VAL Δ [CI 하한] | OOS Δ [CI 하한] | 두 창 CI>0 |
|---|---|---|---|---|---|
| bottom | any2/W1 | +0.029 | +0.034 [+0.009] | +0.013 [−0.035] | ✗ |
| bottom | any3/W1 | +0.063 | +0.047 [+0.005] | +0.014 [−0.060] | ✗ |
| bottom | any4/W1 | +0.057 | +0.050 [−0.016] | +0.018 [−0.088] | ✗ |
| bottom | any3/W3 | +0.055 | +0.032 [−0.005] | +0.041 [−0.017] | ✗ |
| bottom | any4/W3 | +0.049 | +0.066 [+0.015] | **−0.026** [−0.115] | ✗ |
| bottom | any3/W12 | +0.050 | +0.035 [+0.000] | +0.032 [−0.018] | ✗ |
| bottom | any4/W12 | +0.054 | +0.065 [+0.008] | **−0.025** [−0.094] | ✗ |
| **top** | any2/W1 | +0.014 | +0.027 [+0.003] | +0.029 [+0.001] | **✓** |
| **top** | any3/W1 | +0.066 | +0.058 [+0.009] | **+0.115** [+0.047] | **✓** |
| top | any4/W1 | +0.091 | +0.061 [−0.022] | +0.076 [−0.035] | ✗ (n=41) |
| **top** | any3/W3 | +0.048 | +0.041 [+0.002] | +0.095 [+0.033] | **✓** |
| **top** | any4/W3 | +0.065 | +0.061 [+0.002] | **+0.177** [+0.085] | **✓** |
| **top** | any3/W12 | +0.032 | +0.050 [+0.018] | +0.064 [+0.008] | **✓** |
| **top** | any4/W12 | +0.049 | +0.062 [+0.013] | +0.123 [+0.050] | **✓** |

**천장 6/7 통과, 바닥 0/7.** 이 측면 비대칭은 09-06 재검증이 남긴 유일한 생존 조각
("천장 측면만 세 창 전부 ATR초과 양수이고 N에 대해 증가")과 **독립 재현**이다 — 앵커 구성
(raw 공동발동+GAP dedup vs votes≥N 전봉)·지표(정확도 vs lift)·귀무(십분위 가중 vs 재표집)가
전부 다른데 같은 결론이 나왔다. 측면 비대칭 자체는 같은-측면 기준선(그 창의 천장 피벗 발생률)
안에서 잰 것이라 `feedback_side_asymmetry_needs_same_side_null` 함정에는 걸리지 않는다.

## 6. ⭐조합 정체는 정보가 없다 (사용자 가설 기각)

**(A) TRAIN 최고 조합이 같은 k·W의 any-k를 못 이긴다** — 12개 셀 전부 차이 CI가 0을 포함.
bottom k3/W3 `taker+sweep+kal`은 TRAIN 0.397·VAL 0.437인데 OOS 0.281(any3 0.351보다 나쁨).

**(B) 순위가 이월되지 않는다** — 조합별 TRAIN 정확도 ↔ VAL/OOS 정확도 Spearman ρ:
ρ(TRAIN,VAL) 중앙 **−0.065**, ρ(TRAIN,OOS) 중앙 +0.317. 12개 셀 중 p<0.05는 각 1개이고
VAL 쪽 유일한 유의 결과는 **음수**(bottom k3/W12 ρ=−0.341). 다중검정 하 정확히 우연 수준이다.

⇒ **"어떤 조합인가"는 앵커 정확도를 예측하지 못한다. 예측하는 것은 "몇 개가 겹쳤는가"뿐이다.**
따라서 "최고의 조합"이라는 형태의 답은 없다 — 답은 "천장에서 3종 이상"이다.

## 7. 레짐(페이드/지속)은 여전히 동전

| 측면 | 앵커 | TRAIN P(지속) | VAL | OOS | OOS excess [CI] |
|---|---|---|---|---|---|
| bottom | first_fire | 0.504 | 0.508 | 0.508 | −0.020 [−0.082, +0.038] |
| bottom | any4/W1 | 0.513 | 0.526 | 0.414 | −0.113 [−0.246, +0.006] |
| top | first_fire | 0.501 | 0.484 | 0.463 | −0.012 [−0.082, +0.055] |
| top | any4/W3 | 0.479 | 0.463 | 0.550 | +0.078 [−0.056, +0.212] |

188개 평가가능 후보 중 L2 통과 **1개**(bottom any3/W12: VAL −0.054 [−0.104, **−0.002**] ·
OOS −0.090 [−0.175, −0.013]). VAL 상한이 −0.002로 간신히 걸쳤고, **인접 셀(any3/W1, any4/W12)이
VAL에서 부호가 뒤집힌다.** 사전 α 하 기대 오탐 ≈0.24건 — 1건은 잡음으로 읽는 것이 맞다.

⇒ 09-06 결론 유지: **앵커를 바꿔도 이후 페이드/지속은 예측할 것이 없다.** 다만 검정력은
크게 떨어진다(n 30~200, CI 반폭 ±0.06~0.12) — 10pp 이상의 큰 편향만 배제할 수 있고
09-06의 ±0.007 정밀도와 비교하면 안 된다.

## 8. 다중도 — 시간이동 플라시보

신호별 독립 원형이동(±3~30일, 발동률·자기상관 보존) R=20회: **L1 통과 0/20 · L2 통과 0/20**
(평가가능 평균 42.6). 실제 L1 통과 142는 플라시보 최대값 0을 압도한다 — 증거신호가 피벗을
실제로 탐지한다는 것은 견고하다. 다만 142/188이라는 통과율 자체는 **"너무 완전한 통과"**(게이트 T1)
이므로, 판정 기준을 ATR 매칭 귀무가 아니라 **현행 first_fire 대비**(§5)로 올린 것이 옳았다.

## 9. 한계

- OOS 창이 **48일**(2026-01-01~02-17, 캐노니컬 klines 커버리지)로 짧다. top any4/W3의
  OOS n=61은 33일에 흩어져 있어 소표본 클러스터 위험이 남는다(그래서 any3 계열을 헤드라인으로 둔다).
- 정확도(피벗 적중)는 **경제성이 아니다**. 09-07 `sim_exit` 결함으로 이 저장소의 트레일링
  기반 bp 수치가 전부 재계산 중이므로, 이 앵커의 경제성은 **아직 묻지 않았다**.
- ETH HOLDOUT은 소진 상태다(달력 대기). 이 결과는 VAL/OOS까지다.

## 10. 다음

1. **천장 any3 앵커를 현행 first-fire 대신 놓고** 기존 파이프라인(칩·대시보드 표시)에서 재평가.
2. 바닥 측면은 왜 OOS에서 사라지는지 — 창 길이인지 국면인지 분해.
3. 경제성은 수정된 `sim_exit`(`infeasible="exit"`) 위에서만 묻는다.

---

# 부록 A — 앵커 라벨 재정의: "+3봉 이내 지역적 극점" (2026-09-07, 같은 세션)

사용자: *"앵커의 정의부터 정해야할 것 같아. +3개봉 이내에 지역적 극점에 도달한다면 앵커가
맞다라고 하는건 어때?"*

## A0. 판정

**채택 권고. 그리고 이 라벨은 §5의 천장 결론을 뒤집는다** — 천장 우위는 지그재그 라벨의
**허용 시차 12봉**이 만든 것이었다. 시차를 3봉으로 조이면 **바닥이 이기고 천장이 진다.**

## A1. 제안이 남긴 두 가지 미지정 — 이렇게 고정한다

**(1) 중심창이 아니라 전방향(forward-only)으로 정의한다.** 봉 b가 극점 ⟺ `low[b] == min(low[b..b+W])`.
±W 중심창을 쓰면 안 된다: `liquidity_sweep`·`short_term_return_z` 계열은 발동 조건 자체가
"직전 구간 저점 돌파"라 **뒤쪽 절반이 기계적으로 충족**된다. 2026-09-01에 local_extreme만
중심창으로 재서 적중률 93.4%가 나왔다가 인과적으로 다시 재니 23%로 무너진 사고가 정확히 이
메커니즘이다(`v_rebound_local_extreme_lookahead_contamination_20260901.md` §정정).
전방향 정의는 실전 의미도 그대로다 — *"여기서 잡으면 W봉 동안 더 안 간다"*.

**(2) 유지 창 W = 48봉(4시간)을 헤드라인으로.** W가 정의를 지배한다. 기저율(D=3, 전체 봉):

| W | 6 | 12 | 24 | **48** | 96 |
|---|---|---|---|---|---|
| bottom | 0.545 | 0.397 | 0.287 | **0.202** | 0.139 |
| top | 0.528 | 0.379 | 0.268 | **0.188** | 0.132 |

W=48이 현행 지그재그 라벨(기저율 0.124)과 난이도 동급이다. W=6/12은 너무 쉽고 W=96은 표본이 얇다.
W=24/96은 민감도로 병기한다.

## A2. ⭐라벨을 바꾸니 측면이 뒤집힌다

현행 `first_fire_union` 대비 ATR 통제 초과정확도 차이(일군집 CI95), 두 창 통과 개수:

| 라벨 | 바닥 통과 | 천장 통과 |
|---|---|---|
| 지그재그 피벗 ≤12봉 (§5) | 0/7 | **6/7** |
| 전방향 극점 W48 / D3 | 0/7 | **0/7** (OOS 전부 **음수** −0.025~−0.098) |
| 전방향 극점 W24 / D3 | 1/7 (any3/W12) | 0/7 |
| 전방향 극점 W96 / D3 | 0/7 | 0/7 |

**천장 중첩은 3봉 안에 오지 않는다.** D를 12봉까지 늘려도 회복하지 않는다(OOS D=12: any3/W1
0.440 · any4/W3 0.400 vs 현행 0.418).

## A3. 원인 — 시차 구조 (앵커→W48 극점까지 봉 수, ATR 십분위 매칭 귀무 B=200)

| 창 | 측면 | 앵커 | n | 관측 중앙시차 | 귀무 중앙 | 귀무분포 백분위 | D≤3 관측 | D≤3 귀무 |
|---|---|---|---|---|---|---|---|---|
| VAL | bottom | first_fire | 1496 | 4.0 | 5.9 | 0.0 | 0.270 | 0.207 |
| VAL | bottom | any3/W12 | 332 | **2.5** | 5.9 | 0.0 | **0.361** | 0.206 |
| OOS | bottom | first_fire | 634 | 4.0 | 6.6 | 0.0 | 0.273 | 0.192 |
| OOS | bottom | any3/W1 | 107 | **2.0** | 7.0 | 0.0 | **0.430** | 0.191 |
| OOS | bottom | any4/W3 | 85 | **1.0** | 6.8 | 0.0 | **0.435** | 0.196 |
| VAL | top | first_fire | 1590 | 4.0 | 5.9 | 0.0 | 0.267 | 0.197 |
| VAL | top | any3/W1 | 243 | 3.0 | 5.9 | 0.0 | 0.350 | 0.195 |
| OOS | top | first_fire | 594 | 4.0 | 5.7 | 0.0 | 0.274 | 0.202 |
| OOS | top | any3/W1 | 91 | **7.0** | 5.8 | **72.5** | 0.253 | 0.210 |
| OOS | top | any4/W3 | 60 | **8.0** | 5.9 | **83.5** | 0.200 | 0.202 |

- **바닥 중첩 = 두 창 모두 시의성이 있다.** 중첩할수록 극점이 더 빨리 온다(4.0 → 1.0~2.5봉),
  백분위 0.0(귀무 200회 전부보다 빠름), D≤3 적중률이 귀무의 **2.2배**(0.435 vs 0.196).
- **천장 중첩 = OOS에서 귀무와 구분되지 않는다.** 중앙시차가 오히려 **길어지고**(4.0 → 7~8봉)
  백분위 50~84로 귀무 안이다. VAL만 좋다.

⇒ §5의 "천장 6/7 통과"는 **12봉 허용 시차가 '늦게라도 맞으면 정답'으로 채점**한 결과였다.
같은 데이터에 시차를 3봉으로 조이자 그 우위가 사라진다. 사용자 제안이 이 구분을 만들어냈다.

## A4. 남는 유보

- **엄격한 판정(현행 first_fire 대비, 두 창 CI>0)은 42셀 중 1셀 통과**로 우연 수준이다.
  바닥의 견고한 우위는 **ATR 매칭 귀무 대비**에서 나오고, 그 상당 부분을 현행 first_fire가
  이미 잡고 있다(귀무 0.19~0.21 → first_fire 0.27 → 중첩 0.33~0.44).
- TRAIN이 +91.7% 상승장, VAL −32.1% · OOS −34.4% 하락장이다. 측면 판정은 이 비대칭에
  노출돼 있다 — 바닥/천장 결론은 상승장 OOS가 생기기 전까지 잠정이다.
- §6(조합 정체는 정보 0)과 §7(레짐은 동전)은 이 라벨에서도 바뀌지 않는다.

---

# 부록 B — 바닥 앵커 W×D 격자, 최적 셀 확정 (2026-09-07)

사용자: *"W와 D를 격자로 훑어서 바닥 앵커 최적 셀 확정해줘"*
스크립트 `scripts/research_eth_bottom_anchor_wd_grid_20260907.py` · 산출 `tmp/eth_anchor_combo_20260907/wd_grid_*.{parquet,csv}`

## B0. 확정

**앵커 = `any3 / Wc3`** — 8종 증거신호 중 **3종 이상이 3봉(15분) 안에 바닥 발동**하고 마지막
하나가 그 봉에 발동하는 봉(GAP 12봉 중복제거). 하루 **2.7~2.8회**.
**라벨 = W48/D1 또는 W24/D2** — "이후 4시간(2시간)의 최저가가 앵커 봉 +1봉(+2봉) 안에 나온다".

| 라벨 | 창 | 앵커 정확도 | ATR 귀무 | 초과 [CI하한] | 현행 first_fire 대비 [CI하한] |
|---|---|---|---|---|---|
| **W48/D1** | VAL | **0.268** | 0.142 | +0.125 [+0.074] | **+0.063** [+0.020] |
| **W48/D1** | OOS | **0.269** | 0.131 | +0.139 [+0.067] | **+0.071** [+0.008] |
| **W24/D2** | VAL | **0.348** | 0.251 | +0.097 [+0.047] | **+0.051** [+0.008] |
| **W24/D2** | OOS | **0.454** | 0.238 | +0.216 [+0.119] | **+0.111** [+0.036] |
| (참고) 현행 first_fire W48/D1 | VAL / OOS | 0.205 / 0.198 | 0.142 / 0.131 | +0.062 / +0.067 | — |

두 라벨 셀 모두 **8시드 부트스트랩 전부에서 C1·C2 유지**. n: VAL 310 / OOS 130.

## B1. 격자 설계 — W·D 를 최적화하지 않은 이유

W(극점 유지 창)·D(도달 허용 봉수)는 **라벨을 바꾸는 축**이라 셀마다 기저율이 달라진다
(W12/D3 0.397 ↔ W192/D0 0.02x). 셀 간 정확도를 직접 비교해 argmax 를 고르면
`btc_eth_tuning_parity_no_replacement_20260903` §5-H("격자 축이 라벨을 바꾸면 AUC 비교 금지",
kalman 확장승자 히트율 0.509→0.058)에 그대로 걸린다. 그래서:

- **라벨 축 (W,D)** = 배경. 5×8=40셀에서 **앵커 순위가 유지되는지**만 본다.
- **앵커 축 (k, Wc)** = 선택 대상. 9종(k∈{2,3,4} × Wc∈{1,3,12}봉).
- 판정은 두 대조 **동시 충족**: C1 ATR 십분위 매칭 귀무 대비 · C2 현행 first_fire 대비.
  각각 VAL·OOS 두 창 일군집 CI 하한 > 0.

## B2. 통과 지도 — 연속 영역이고 경계가 뚜렷하다

**C1 통과 앵커 수 / 9** (행 W, 열 D):

| W \ D | 0 | 1 | 2 | 3 | 4 | 6 | 9 | 12 |
|---|---|---|---|---|---|---|---|---|
| 12 | 1 | 4 | 5 | 3 | 0 | 0 | 0 | 0 |
| 24 | 2 | 6 | **7** | 4 | 1 | 0 | 1 | 0 |
| 48 | 2 | **7** | **8** | 5 | 0 | 1 | 0 | 0 |
| 96 | 1 | 5 | 6 | 2 | 0 | 0 | 0 | 0 |
| 192 | 0 | 0 | 1 | 0 | 0 | 0 | 0 | 0 |

능선이 **D∈{1,2,3} × W∈{24,48,96}** 에 연속으로 뭉쳐 있고, **D≥4 와 W=192 에서 완전히 소멸**한다.
흩어진 단일 셀이 아니라 하나의 영역이라 사전 등록한 안정성 요건을 만족한다.
C1∧C2(=PASS)는 360셀 중 **9셀**, 전부 이 능선 안이다.

**다중도**: 시간이동 플라시보(신호별 원형이동 ±3~30일) R=10 → PASS 평균 **0.10**, 최대 1
(평가가능 160~240). 관측 9/360 은 그 45배다.

## B3. 앵커 순위 — k=3 이 라벨 격자 전체에서 이긴다

40 라벨셀 중 통과 수:

| 앵커 | C1 | C2 | PASS | 평균 OOS 현행대비 | OOS n |
|---|---|---|---|---|---|
| any3/Wc12 | **13** | 1 | 1 | +0.016 | 138 |
| **any3/Wc3** | 11 | **3** | **3** | +0.030 | 130 |
| any4/Wc12 | 10 | 2 | 2 | +0.042 | 104 |
| any3/Wc1 | 9 | **3** | **3** | +0.047 | 107 |
| any2/Wc1 | 9 | 0 | 0 | +0.012 | 195 |
| any2/Wc3 · any2/Wc12 | 4 · 6 | 0 | 0 | +0.009 · +0.011 | 200 · 196 |
| any4/Wc1 · any4/Wc3 | 2 · 8 | 0 | 0 | −0.002 · +0.005 | 58 · 85 |

- **k=2는 부족하고(C2 0/40) k=4는 표본이 얇다**(OOS n 58~104). **k=3이 답이다.**
- 8시드 안정성으로 갈리는 것은 Wc: **`any3/Wc3` 가 C1 8/8 라벨셀**로 유일하게 만점,
  `any3/Wc1` 은 6/8. C2는 둘 다 3/8. Wc3 이 표본도 크다(310/130 vs 250/107).
- `any3/Wc1` 은 **OOS 이득이 더 크지만(W48D1 +0.128 vs +0.071) VAL 안정성이 낮다**.
  더 공격적인 변형으로만 병기한다.

## B4. ⭐사용자 원제안 D=3 은 경계 밖이다

W48/D3(부록 A의 헤드라인 셀)에서 `any3/Wc3` 의 현행 대비는 VAL +0.027 [−0.008] ·
OOS +0.054 [−0.003] 로 **두 창 다 아슬하게 0을 포함**한다. D를 1~2로 조이면 통과한다.

이는 부록 A의 시차 측정과 정확히 일치한다 — 바닥 중첩 앵커의 극점 도달 중앙시차가
**OOS 1.0~2.5봉**이었다. **엣지는 "3봉 이내"가 아니라 "지금 또는 다음 봉"에 있다.**
D를 늘릴수록 라벨이 포화하면서(기저율 W48: D1 0.141 → D3 0.202 → D6 0.271) 초과가 희석된다.

## B5. 유보

- OOS n=130(48일)이다. W24/D2의 OOS 정확도 0.454는 인상적이지만 **VAL 0.348과 격차가 크다** —
  하락장 OOS의 국면 효과일 수 있다(TRAIN +91.7% 상승 / VAL −32.1% / OOS −34.4%).
- 정확도이지 경제성이 아니다. 진입가·비용·청산을 붙이면 다른 질문이 된다
  (수정된 `sim_exit(infeasible="exit")` 위에서만).
- ETH HOLDOUT 소진. 이 확정은 VAL/OOS까지의 잠정이다.

---

# 부록 C — 등락 폭 기준과 타점 (2026-09-07)

사용자: *"국소적 극점을 타점으로 잡고 있는거 맞아? 앵커의 기준을 몇 퍼센트 등락으로 잡았지?"*

## C0. 두 질문에 대한 사실

**(1) 등락 퍼센트 기준은 넣지 않았다.** 부록 A/B의 라벨은 순서만 본다 —
`low[b] == min(low[b..b+W])`, 즉 "이후 W봉 동안 더 낮은 저가가 없다". 반등 폭 조건이 없다.
**(2) 극점을 타점으로 잡고 있지 않다.** 앵커 봉 t가 트리거이고 라벨은 "극점이 t..t+D 안에
온다"이다. 진입가·진입 시점은 이 스크린에서 한 번도 모델링하지 않았다.

## C1. ⚠️정정 — 지그재그 라벨에는 크기 기준이 있었다

`tmp/zigzag_action_labels_extended_20260809/zigzag_action_label_audit.json`:
`zigzag_reversal_pct: 0.01` (1% 반전) · `min_wave_bars: 8` · `atr_multiplier: 1.0`.

부록 A에서 지그재그 라벨과 전방향 극점 라벨의 차이를 **허용 시차(12봉 vs D봉)로만** 설명한 것은
불완전했다. 두 번째 차이는 **크기 기준의 유무**다. 천장 측면이 뒤집힌 원인이 시차인지 크기인지는
아직 분해하지 않았다(부록 A의 시차 측정은 시차 쪽 기여를 직접 보인 것이라 무효는 아니다).

## C2. 크기 기준은 사실상 비구속적이었다

전방향 극점 봉의 **이후 W봉 최대 상승폭** 분포:

| 라벨 | n | p10 | 중앙 | p90 | <0.5% | <1.0% |
|---|---|---|---|---|---|---|
| W48 (4h) | 22,105 | 0.91% | **1.84%** | 3.86% | **1.0%** | 13.5% |
| W24 (2h) | 31,343 | — | **1.27%** | — | **4.7%** | 34.5% |

"4시간 동안 더 낮은 저가가 없다"는 조건 자체가 이미 강해서, 경제적으로 무의미한 미세 극점은
거의 안 들어온다. 앵커 적중분의 중앙 상승폭도 VAL 2.35% / OOS 1.62~2.11%로 전체와 비슷하다.

## C3. ⭐크기 게이트를 실제로 걸어도 확정 셀이 유지된다

라벨에 "극점 + 이후 W봉 최대상승 ≥ g%" 를 추가하고 `any3/Wc3` 를 재검정:

| 라벨 | 게이트 | 기저율 | VAL 정확도 (귀무) | VAL 현행대비 [CI하한] | OOS 정확도 | OOS 현행대비 [CI하한] | 판정 |
|---|---|---|---|---|---|---|---|
| W48/D1 | 0.0% | 0.141 | 0.268 (0.142) | +0.063 [+0.023] | 0.269 | +0.071 [+0.010] | 통과 |
| W48/D1 | 1.0% | 0.122 | 0.226 (0.125) | +0.054 [+0.014] | 0.231 | +0.069 [+0.009] | 통과 |
| W48/D1 | 1.5% | 0.090 | 0.194 (0.095) | +0.060 [+0.025] | 0.169 | +0.049 [−0.005] | – |
| W24/D2 | 0.0% | 0.247 | 0.348 (0.251) | +0.051 [+0.007] | 0.454 | +0.111 [+0.032] | 통과 |
| **W24/D2** | **1.0%** | 0.161 | 0.265 (0.176) | **+0.058 [+0.018]** | 0.346 | **+0.109 [+0.040]** | **통과** |
| W24/D2 | 1.5% | 0.095 | 0.190 (0.107) | +0.053 [+0.020] | 0.262 | +0.094 [+0.031] | 통과 |
| W24/D2 | 2.0% | 0.057 | 0.129 (0.065) | +0.040 [+0.011] | 0.200 | +0.081 [+0.038] | 통과 |

0~2% 전 구간에서 통과가 유지된다(W48/D1의 1.5%만 예외인데 2.0%가 통과하므로 잡음).
⇒ **부록 B의 확정 셀은 크기 기준 부재의 산물이 아니다.**
⭐**권고 갱신: `W24 / D2 / 게이트 1.0%`** — 기저율이 0.247→0.161로 내려가는데 현행 대비 CI 하한이
오히려 올라간다(+0.007→+0.018 · +0.032→+0.040). 지그재그의 1% 기준과도 정합적이다.

## C4. ⭐타점 — 여기가 미측정 영역이다

| 창 | 앵커 | n | 앵커 봉이 곧 최저인 비율 | 앵커 종가→이후 12봉 최저 역행 중앙 | p75 | p90 |
|---|---|---|---|---|---|---|
| VAL | first_fire | 1496 | 21.3% | 0.47% | 0.93% | 1.70% |
| VAL | any3/Wc3 | 310 | **26.1%** | 0.49% | 1.03% | 1.83% |
| OOS | first_fire | 636 | 21.1% | 0.53% | 1.09% | 1.96% |
| OOS | any3/Wc3 | 130 | 21.5% | 0.48% | 1.12% | 1.75% |

- **앵커 봉 자체가 최저인 경우는 21~26%뿐이다.** 앵커는 "극점이 근처"를 말하지 "여기가 바닥"을
  말하지 않는다.
- 앵커 봉 종가에 진입하면 중앙 **0.5%**, p90 **1.8~2.4%** 를 더 먹힌다. 중앙 상승폭 1.3~2.4%
  대비 비율은 나쁘지 않지만, **이 축은 스크린이 한 번도 채점하지 않았다.**
- ⇒ 다음 단계는 타점 규칙(앵커 봉 종가 진입 vs 지정가 −0.3~0.5% 대기 vs 극점 확인 후 진입)을
  라벨이 아니라 **체결 가능성과 함께** 재는 것이다.

---

# 부록 D — 화면 AUC(0.63~0.75)와 앵커 정확도(0.27~0.45)는 왜 다른가 (2026-09-07)

사용자: *"그럼 증거신호의 AUC는 어떻게 나온거지?"*

## D0. 두 숫자는 다른 라벨이다

| | 화면 AUC | 부록 A~C 앵커 정확도 |
|---|---|---|
| 라벨 | **MFE ≥ K×ATR within H** (터치) | 전방향 W봉 로컬극점이 t..t+D 안 |
| 묻는 것 | **"곧 K×ATR 만큼 움직이는가"**(크기) | **"여기가 전환점인가"**(구조) |
| 모집단 | 그 신호의 발동 봉 | 앵커 봉 vs 전체 봉(ATR 매칭) |
| 파라미터 | 신호마다 H 8~72봉 · K 0.70~4.20 | W 12~192 · D 0~12 공통 |

`live_evidence_signal_metalabel_20260829.py::METALABEL_SIGNALS` + `live_evidence_signal_dashboard_20260823.py::K_OVERRIDE`:
taker H24/K2.00 · strz H12/K1.75 · sweep H30/K4.00 · orth H24/K3.571 · smt H72/K4.20 ·
fib H20/K2.35(touch_and_mae) · demarker H8/K0.70 · kalman H12/K2.5.

## D1. ⭐그 라벨에는 방향 내용이 거의 없다 (실측)

발동 봉에서 **유리 방향** 터치율과 **불리 방향** 터치율을 같은 기준가·같은 K×ATR로 나란히 재면:

| 신호 | H | K | n | 바닥: 유리(상승) | 불리(하락) | 차이 | 천장 차이 |
|---|---|---|---|---|---|---|---|
| taker_delta_z_climax | 24 | 2.00 | 3096 | 0.532 | 0.536 | **−0.004** | +0.011 |
| short_term_return_z | 12 | 1.75 | 1521 | 0.473 | 0.474 | **−0.001** | +0.020 |
| liquidity_sweep | 30 | 4.00 | 2489 | 0.247 | 0.331 | **−0.083** | −0.022 |
| orthogonal_combo | 24 | 3.57 | 1030 | 0.257 | 0.316 | **−0.058** | −0.008 |
| smt_divergence | 72 | 4.20 | 2126 | 0.438 | 0.494 | **−0.056** | −0.002 |
| fib_extension_exhaustion | 20 | 2.35 | 735 | 0.463 | 0.472 | −0.010 | +0.053 |
| demarker_extreme | 8 | 0.70 | 1068 | 0.757 | 0.697 | +0.061 | +0.069 |
| kalman_deviation_meanrev | 12 | 2.50 | 2806 | 0.258 | 0.303 | **−0.045** | +0.014 |

**바닥 8종 중 6종이 음수** — 불리 방향이 더 자주 터치된다. 천장도 ±0.02 안이 대부분.
⇒ 터치 라벨은 **어느 쪽으로 가는가와 거의 무관한 크기(변동성) 질문**이다.
저장소의 반복 검출과 정확히 같다: "Tier0는 변동성만 예측·방향 IC≈0"(09-04 §5.22 계열),
"크기는 배워지고 방향은 안 배워진다"(09-06).

(재구성 주의: 기준가를 `close[i]`로 잡아 원 학습 스크립트의 절대 터치율과는 다를 수 있다.
**방향 차이는 두 방향이 같은 기준가를 쓰므로 이 선택에 견고하다.**)

## D2. 난이도가 신호마다 다르다 — AUC 자체를 신호 간 비교하면 안 된다

같은 재구성에서 터치율이 demarker 0.757 ↔ liquidity_sweep 0.247로 3배 넘게 벌어진다.
`feedback_cross_model_auc_comparison_requires_matched_label_difficulty_20260901` ·
`eth_label_k_increase_rejected_auc_vs_pnl_20260902`("라벨 다른 AUC 비교 금지")가 이 지점이다.
`atr_pct` 단독 AUC는 0.40~0.46 — 타깃이 `K×ATR[i]`로 스케일돼 **현재 변동성이 높을수록 오히려
터치가 어렵다**. 즉 이 라벨은 변동성 *수준*이 아니라 *지속/평균회귀*를 묻는다.

## D3. 저장소가 이미 이 잣대를 폐기했다

`docs/homer/README.md` §5.24·5.26 (2026-09-04):
- *"터치 확률은 큰 움직임이 올 봉을 맞히지만, 그 확률 상위 30%를 고르면 OOS 두 측면 차이가
  배포 8종 중 6종에서 음수"* → 칩 교체 관문 **0/8 통과**.
- **라벨 기본값 개정**: 새 신호는 L2 경제 방향 라벨로 학습. *"자기라벨 AUC는 학습 진단용이다."*
- 09-04 칩 정확도 정정: **화면 AUC는 앵커 모집단 수치이고 라이브 실측은 0.58~0.68**(확률 과신).
- README:108 — kept-only 헤드라인 AUC가 과대평가였음이 드러나 전체모집단 **0.665/0.680/0.667**로 정정.

## D4. 결론

화면 AUC는 조작된 숫자가 아니지만 **"이 봉이 바닥이다"를 맞힌 수치가 아니다.** 그것은
"발동 봉 이후 H봉 안에 K×ATR 만큼 (어느 쪽으로든) 움직이는가"의 분류 성능이고, 신호마다
난이도가 다르며, 라이브 실측으로는 0.58~0.68이다. 부록 A~C가 잰 앵커 정확도와 모순되지 않는다 —
**서로 다른 질문에 답한 서로 다른 숫자**다. 전환점 자체를 묻는 잣대는 부록 C의 확정 셀
(`any3/Wc3` · W24/D2 · 게이트 1.0%)이 처음이다.

---

# 부록 E — "방향 콜이 아니라 갈림길" 재구성 검정 (2026-09-07)

사용자: *"이 증거신호로 바닥에는 롱, 천장에는 숏을 치는게 말이 안돼. 단지 이 증거신호 8가지는
이 신호가 추세와 되돌림의 갈림길이 될 수 있다는 거야. 이 지점을 잡는게 중요해."*
스크립트 `scripts/research_eth_anchor_fork_resolution_20260907.py` · `tmp/eth_anchor_combo_20260907/fork_*.{parquet,csv}`

## E0. 판정 — 재구성의 전제는 옳고, "갈림길"의 뜻 셋 중 둘만 성립한다

| 읽기 | 지표 | 결과 |
|---|---|---|
| ① "여기서 **한쪽으로 결판**난다" | F1 결단력 · F2 브라켓 | ❌ **0/42 · 0/126 통과** |
| ② "여기서 **크게 움직인다**"(방향 무관) | F3 진폭/ATR | ✅ **16/42 통과** (바닥 +0.9~1.2 ATR) |
| ③ "**직전 움직임이 여기서 끝난다**"(측면별) | 저점극점 A | ✅ 두 창 CI>0, **고점극점은 안 오름** |

⇒ **"바닥→롱"이 말이 안 된다는 지적이 맞다.** 앵커는 하방이 막히는 자리를 알려주지(③)
위로 간다고 말하지 않는다(① 없음). 앵커의 정보는 **손절선 자리 + 크기**이지 **수익 방향**이 아니다.

## E1. F1/F2 — 결판은 없다 (사전등록대로 F3만 통과)

발동 봉 종가 c0 기준 이후 H봉: `up=(max high−c0)/c0`, `dn=(c0−min low)/c0`
- **F1 결단력** `|up−dn|/(up+dn)` — 크기로 나눠 변동성 축과 분리. 0.5≈양방향 흔들림, 1≈한쪽 결판.
- **F2 브라켓** `max(up,dn) ≥ K·atr ∧ min(up,dn) < m·atr`, (K,m)=(2,1)·(3,1)·(3,1.5).
- **F3 크기** `(up+dn)/atr`. 맥락(이미 부분적으로 닫힌 축).

| H | F1 VAL 초과 평균 (CI>0 개수) | F1 OOS 초과 평균 |
|---|---|---|
| 12 | **−0.031** (0/14) | −0.030 (0/14) |
| 24 | **−0.026** (0/14) | −0.028 (0/14) |
| 48 | −0.013 (0/14) | −0.020 (0/14) |

**전부 음수**이고 여러 셀은 CI 상한 < 0으로 **유의하게 나쁘다**(top:any3/Wc3 OOS −0.116 [−0.191,−0.036]).
게다가 F1 **분포 자체가 ATR매칭 무작위와 구분되지 않는다** — H24 bottom any3/Wc3 VAL:
평균 0.489/귀무 0.517 · sd 0.269/0.274 · p90 0.843/0.871 · P(F1>0.6) 0.410/0.437.
쌍봉도 아니고 꼬리도 같다. ⇒ 앵커가 바꾸는 것은 **크기뿐이고, 그 확장이 양쪽에 균등하게 배분된다.**
**F2가 0/126인 것이 실무 결론이다 — 양방향 브라켓(스트래들)은 먹히지 않는다.**
09-06 재검증의 "역행폭이 N에 대해 증가 → 포지션은 작아져야 한다"와 같은 말이다.

## E2. ⭐그러나 구조적 전환점은 **측면별로** 실재한다 (빠져 있던 대조)

부록 B/C는 바닥 앵커에서 저점 극점만 쟀다. 같은 앵커에서 **고점 극점**도 같이 오르는지가
빠진 대조였다(`feedback_control_group_must_cover_the_actual_code_path` 계열). W48/D1:

| 창 | 앵커 | n | 저점극점 초과 [CI하한] | 고점극점 초과 [CI하한] | **저−고 차이 [CI]** |
|---|---|---|---|---|---|
| VAL | bottom:first_fire | 1496 | +0.062 [+0.038] | +0.002 [−0.020] | +0.060 [+0.021, +0.102] |
| VAL | bottom:any3/Wc3 | 310 | **+0.125** [+0.072] | +0.004 [−0.033] | **+0.121** [+0.056, +0.185] |
| OOS | bottom:any3/Wc3 | 130 | **+0.139** [+0.070] | −0.003 [−0.066] | **+0.141** [+0.029, +0.256] |
| OOS | bottom:any3/Wc1 | 107 | **+0.195** [+0.098] | +0.036 [−0.037] | **+0.159** [+0.024, +0.316] |
| VAL | top:any3/Wc3 | 304 | +0.006 [−0.035] | **+0.066** [+0.025] | −0.060 [−0.127, +0.009] |

**두 창 모두 저−고 차이 CI가 0을 제외한다.** 바닥 앵커는 "극점 일반"이 아니라 **48봉 저점**을
특정한다. 천장 앵커는 대칭적으로 고점 쪽이나 OOS가 약하다.

## E3. 구조는 잡히는데 경로 우세로 전환되지 않는다

같은 앵커·같은 H=48에서 두 잣대를 나란히:

| 창 | 앵커 | A) P(48봉 저점 = t..t+1) 초과 [CI] | B) P(up>dn) 초과 [CI] |
|---|---|---|---|
| VAL | any3/Wc3 | **+0.125 [+0.075, +0.177]** | +0.025 [−0.036, +0.087] |
| OOS | any3/Wc3 | **+0.139 [+0.067, +0.216]** | +0.089 [−0.013, +0.194] |
| OOS | any3/Wc1 | **+0.195 [+0.101, +0.303]** | +0.077 [−0.039, +0.197] |
| OOS | any4/Wc12 | +0.178 [+0.095, +0.266] | +0.106 [**+0.011**, +0.210] |

"48봉 저점이 여기"는 **하방이 막힌다**는 뜻이지 **상방으로 간다**는 뜻이 아니다. 구조적 이득
+0.13이 경로 우세로는 +0.02~0.09밖에 전달되지 않고 대부분 CI가 0을 포함한다.

⚠️**§7의 "레짐은 동전"을 정밀화한다** — 그 진술은 **경로 우세 잣대(B)에서 참**이고
**구조 잣대(A)에서는 거짓**이다. §7이 잣대가 너무 뭉툭했다(A의 +0.13이 B에서는 +0.03으로 희석).
§7이 "1건 통과, 잡음"으로 처리한 bottom any3/W12(VAL −0.054 · OOS −0.090, 둘 다 페이드 우세 방향)는
같은 실체의 약한 그림자였을 가능성이 높다.

## E4. 그래서 앵커가 실제로 주는 것

1. **크기** — 이후 24봉 진폭 5.5 → 6.6~6.9 ATR (두 창 CI>0)
2. **하방 차단(측면별 구조)** — 48봉 저점일 확률 0.13 → 0.27 (두 창 CI>0, 고점은 불변)
3. **경로 우세·결판** — 없음

⇒ 운용 함의: 앵커는 **손절선을 어디 두는가**와 **포지션을 얼마나 줄이는가**의 입력이지
**어느 쪽으로 진입하는가**의 입력이 아니다. 수익 방향은 별도 근거가 필요하다.
양방향 브라켓은 F2 0/126으로 배제된다.

---

# 부록 F — 학습 라벨 데이터셋 산출 (2026-09-07)

사용자: *"앵커 이후 지속일지 되돌림일지는 다음 딥러닝 모델을 만들어서 진행할거야. 우선 그 전에
잘만든 앵커를 기준으로 학습라벨 데이터를 만들려고 지금 이 작업을 하고 있는거야."*

빌더 `scripts/build_eth_anchor_label_dataset_20260907.py` · 산출 `tmp/eth_anchor_label_dataset_20260907/`

## F1. 산출물

| 파일 | 내용 |
|---|---|
| `anchors_labels.parquet` | **45,231행 × 84열** — 앵커 5종 × 양측면, 라벨 74열 |
| `fills.csv` | 게이트 L4용(진입 = `open[t+1]`, `btf=1`) |
| `bar_features.parquet` | 봉 인덱스 프레임(한 봉 밀기 트립와이어용, 현재 최소본) |
| `gate_config.json` · `layer_gates.json` | 게이트 계약과 결과 |

데이터 상한 **2026-07-31**(펀딩 커버리지). 이전 부록들이 쓰던 `data/eth_5m_1year.csv`(2026-02-17)
대신 카노니컬 `binance_data/klines/ETHUSDT/ETHUSDT-5m-api.csv`를 쓴다 — 겹치는 224,353행이
OHLC 전부 일치함을 확인했고 **OOS가 48일 → 90일**로 늘어난다.

| split | 기간 | bottom | top |
|---|---|---|---|
| TRAIN | 2024-01-01~2025-08-31 | 14,800 | 14,901 |
| VAL | 2025-09-01~2025-12-31 | 2,777 | 2,905 |
| OOS | 2026-01-01~2026-03-31 | 2,105 | 2,062 |
| HOLDOUT_SPENT | 2026-04-01~2026-07-31 | 2,867 | 2,808 |

⚠️`HOLDOUT_SPENT`는 **이미 소진된 구간**이다(다른 실험에 노출됨). 신선한 검정으로 쓰지 말 것.

앵커별(전 split 합): `any3/Wc3` **4,918** · `any2/Wc3` 8,246 · `any3/Wc1` 3,958 ·
`any4/Wc12` 3,591 · `first_fire` 24,518. 확정 셀은 `any3/Wc3`, 나머지는 ablation용.

## F2. 라벨 패밀리 (74열)

방향축이 이 저장소에서 10형태 기각됐으므로(부록 A) **한 라벨에 걸지 않는다.**
모델이 무엇을 배우는지 분해할 수 있도록 다섯 축을 모두 담았다. 전부 앵커 측면 기준
(bottom: 페이드=롱/지속=숏, top: 페이드=숏/지속=롱), H ∈ {12,24,48,96}.

| 축 | 컬럼 | 부록 근거 |
|---|---|---|
| 경로(**1차 목표**) | `mfe_fade_H*` `mfe_cont_H*` `r1_H*` **`y_cont_H*`** | A §3 순수 레짐 라벨 |
| 결단력(**음성 대조**) | `f1_H*` = \|up−dn\|/(up+dn) | E §E1 — 앵커가 못 움직이는 축 |
| 크기(**보조 과제**) | `size_H*` = (up+dn)/atr | E §E1 — 앵커가 움직이는 축 |
| 구조(**확정 라벨**) | `y_ext_W24_D2_g1` `y_ext_W48_D1_g0/g1` `y_ext_W48_D3_g1` `lead_bars_W48` | B·C 확정 셀 |
| 경제(**참고**) | `net_fade_bp` `net_cont_bp` `margin_bp` `y_econ_cont` | 수정 트레일링 |

- 각 라벨은 `_c0`(close[t] 기준, 부록 A~E 대조용)와 `_e1`(open[t+1] 기준, **라이브 실행 규약**)
  두 벌. 실측상 두 기준가 차이는 **중앙 $0.01(≈0.04bp)** 이라(무기한 선물 5분봉은 갭이 없다)
  라벨 값이 사실상 같지만, 계약상 `_e1`을 쓴다.
- 경제 라벨은 **수정된 트레일링**(`infeasible='exit'`) 규약이다. 게이트 `trail(anchor='entry')`와
  무작위 2,000경로 파리티 **|Δ|max = 2.6e-16**(비트 일치). 셀 5.0/1.5/0.1 ATR·200봉·왕복 10bp.
  ⚠️출구 구조 의존이므로 **1차 목적함수로 쓰지 말 것**.

## F3. ⭐게이트 — L4 PASS · L1 PASS

```
L4  PASS  fill_idx↔timestamp 정합 · known_ts assumption · n_entry_not_after_known=0
L1  PASS  표본 120발동·절단 354회 · 자기 known_ts에서 사라진 발동 0 · 유령 발동 0
```

**L1 첫 실행은 FAIL이었다**(사라진 발동 118/120 · 유령 11,376). 원인은 신호가 아니라 **계약 위반**:
게이트는 `kl.iloc[cut−warm:cut].reset_index(drop=True)` 로 창을 잘라 `build_fires` 를 부르고
`known_ts` 를 뺀 **모든 컬럼**을 발동 식별키로 쓰는데, 내가 `bar_idx`(절단마다 값이 바뀜)를
반환하고 있었다. 제거 후 PASS. **`build_fires` 는 절단 불변 컬럼만 반환해야 한다**
(bar_idx 는 빌더가 timestamp 로 조인해 붙인다). warmup 은 8,000봉.

별도로 raw 발동 자체의 인과성도 직접 확인했다 — 500봉 앞에서 잘라 재계산해도 8종 전부 **불일치 0**.

L2(라벨 1분 재구성)·L2P(채점 파리티)·L3(피쳐 누수)는 **피쳐셋과 라이브 채점기가 생긴 뒤** 돌린다.
현 시점 계약: `fresh_forward_bar_by_bar=true` · `trade_ledgers_used_as_input=false` ·
`saved_parent_exit_timestamps_used=false` · `future_rows_used_for_entry=false`.

## F4. DL 모델을 붙이기 전에 알아야 할 것

1. **1차 목표(`y_cont_H*`)는 이 저장소에서 10형태가 기각된 축이다**(부록 A). 기저율이
   TRAIN/VAL/OOS 전부 0.45~0.53으로 동전에 가깝다. 새 모델이 여기서 AUC를 낸다면
   **먼저 누수를 의심**할 것 — 부록 D의 터치 라벨처럼 크기 축을 대신 배우고 있는지
   (`size_H*`를 단독 피쳐로 넣은 대조 모델과 비교) 확인해야 한다.
2. **음성 대조가 데이터셋에 들어 있다**: `f1_H*`는 앵커가 못 움직이는 축이므로, 모델이
   `y_cont`와 `f1`을 비슷하게 맞힌다면 그건 학습이 아니라 아티팩트다.
3. **피쳐는 아직 없다.** 앵커·라벨만 동결했다. 피쳐를 붙이는 순간 L3(피쳐 누수·조인 시점)와
   T2(수치 취약성)를 돌려야 한다 — 특히 **조인 시점**(결정 시각 행이 마감봉인가)은
   2026-09-01 exit 스모크가 AUC +0.10 부풀었던 자리다.

---

# 부록 G — ⚠️구조 라벨 off-by-D 버그와 허용범위 재스윕 (2026-09-07)

## G0. 버그

`ext_label` / `ext_labels` 가 `out[:n-D] = a[D:]` 로 라벨을 **D만큼 밀어** `[t, t+D]` 가 아니라
**`[t+D, t+2D]`** 를 보고 있었다. 단위 테스트(`e=[0,0,1,0,0]`, D=1 → 정답 `[0,1,1,0]` vs 실제 `[1,1,0,0]`).
`a[b] = max(e[b..b+D])` 이므로 그대로 쓰는 것이 정답이고 꼬리 D봉만 미정으로 둬야 한다.

**무효**: 부록 B의 W×D 격자와 확정 셀 "D=1~2" · 부록 C의 게이트 표 · 부록 E §E2의 저점/고점 대조 ·
데이터셋 `y_ext_*` 컬럼 · 2026-09-07 전송 차트의 `y_ext` 표기.
**영향 없음**: `lead_bars_*`(별도 루프) · 경로 라벨 `r1`/`y_cont` · 부록 D · 부록 E의 F1/F2/F3 ·
게이트 L4/L1 · 부록 F의 데이터셋 구조·split·경제 라벨.
징후는 이미 차트에 있었다 — `극점 +2봉`인데 `y_ext=1(D=1)` 인 패널이 그것이었다.

## G1. 재스윕 (사용자 지정 격자)

`scripts/research_eth_anchor_label_tolerance_sweep_20260907.py` · `tmp/eth_anchor_tolerance_sweep_20260907/`
E,D ∈ {0,1,2,3} · P ∈ {0, 0.05, 0.10}% · W ∈ {12, 24} · g = 0.5% · 앵커 `any3/Wc3`.

라벨: 봉 b가 극점 후보 ⟺ `low[b] ≤ min(low[b..b+W])·(1+P/100)` ∧ `반등 ≥ g`.
`y=1` ⟺ 후보가 **[t−E, t+D]** 안에 존재. (E = 앵커가 극점보다 늦어도 인정 · D = 일러도 인정)

**바닥 PASS 9/96 · 연속영역 8 · 플라시보 R=10 전부 0.**

| W \ P | 통과 위치 |
|---|---|
| **12** | **전부 탈락** (기저율 0.24~0.50 — 라벨이 너무 쉬워 앵커 우위가 희석) |
| **24** | **D=3 열만** 통과. P=0/0.05 는 E=0~2, P=0.10 은 E=0~1 |

**D=0,1,2 는 전 조합 탈락 — D=3이 필요하다.** E는 커질수록 CI 하한이 줄어 E=0이 최강이다.

| W | P% | E | D | 기저율 | VAL pos | VAL 초과 [하한] | VAL 대현행 [하한] | OOS pos | OOS 초과 [하한] | OOS 대현행 [하한] |
|---|---|---|---|---|---|---|---|---|---|---|
| **24** | **0.00** | **0** | **3** | 0.268 | 0.426 | +0.144 [+0.080] | **+0.077 [+0.030]** | 0.413 | +0.146 [+0.085] | **+0.063 [+0.008]** |
| 24 | 0.05 | 0 | 3 | 0.309 | 0.458 | +0.138 [+0.080] | +0.077 [+0.033] | 0.444 | +0.143 [+0.073] | +0.063 [+0.004] |
| 24 | 0.10 | 0 | 3 | 0.346 | 0.487 | +0.130 [+0.069] | +0.077 [+0.031] | 0.458 | +0.122 [+0.052] | +0.054 [+0.001] |

⇒ **새 확정 셀: `W=24 · D=3 · E=0 · P=0~0.10% · g=0.5%`.** P는 클래스 균형(기저율 0.27→0.35)을
원하면 0.10%까지 열어도 통과가 유지된다. **천장 측면은 0/96 통과.**

## G2. ⚠️측면 특이성이 사라진다 — 부록 E §E2 정정

같은 앵커에서 `저점 극점 초과 − 고점 극점 초과`:

| 셀 | VAL 측면차 [하한] | OOS 측면차 [하한] |
|---|---|---|
| W24/P0/E0/D3 | +0.056 [−0.031] | +0.045 [−0.064] |
| W24/P0/E1/D3 | −0.068 [−0.160] | −0.074 [−0.185] |
| W24/P0/E2/D3 | −0.165 [−0.260] | −0.165 [−0.274] |

**전 셀에서 CI가 0을 포함하거나 음수다.** 부록 E §E2의 "바닥 앵커는 저점 극점만 올린다"는
밀린 라벨 위에서 나온 결과였다. 바른 라벨에서는 **바닥 앵커가 '저점'이 아니라 '극점 아무거나'를 잡는다.**
⇒ 부록 E §E0의 읽기 ③("직전 움직임이 측면별로 여기서 끝난다")은 **철회**한다. 남는 것은
②크기와, 방향 없는 ③′"여기서 가격이 꺾인다"뿐이다. 이는 부록 E의 F1/F2 결과(결단력 없음)와도 정합적이다.

## G3. 진입 진단 — 먹을 것과 먹히는 것이 비슷하다

확정 셀(W24/P0/E0/D3) 히트 앵커 1,017건:

| 지표 | 값 |
|---|---|
| 진입가(`open[t+1]`) → 극점까지 역행 | 중앙 **0.21%** · p90 **0.96%** |
| 진입 후 W=24봉 안 최대 상승 | 중앙 **0.99%** |

**잔여 상승 중앙 0.99% ≈ 역행 p90 0.96%.** 구조 라벨이 맞아도 그 위에서 바로 돈이 되지는 않는다 —
경제성은 별도 질문이고 수정된 `sim_exit` 위에서만 물어야 한다.

---

# 부록 H — 방향 라벨(되돌림 vs 지속) 재설계 (2026-09-07)

사용자: *"증거신호는 앵커로서 역할을 잘 하고 있어. 이제 이걸 가지고 y_cont_H* 를 만들어서
예측 모델을 만들어보자."* → *"우선 이 방향의 라벨 생성 로직부터 다시 만들어야 해."*

`scripts/build_eth_anchor_direction_labels_20260907.py` · `tmp/eth_anchor_direction_labels_20260907/`

## H1. 기존 `y_cont_H*`(MFE 비교)의 결함 3가지

1. ⭐**순서를 안 본다.** `mfe_cont > mfe_fade` 는 "어느 쪽이 더 멀리 갔나"다. 위로 2% 갔다가
   아래로 2.1% 간 경로와 그 반대가 **같은 라벨**이 된다 — 실제 거래는 정반대인데.
2. **미결정을 억지로 이진화한다.** 양쪽 다 거의 안 움직인 봉도 0/1 중 하나로 밀어넣는다.
3. **5분봉 해상도로는 순서를 못 가린다** — 그런데 MFE 비교는 그 사실 자체를 숨긴다.

## H2. 새 설계 — 터치 시각을 원시로 저장하고 라벨은 유도

진입 `open[t+1]`(L4 계약). 진입가 기준 **대칭** 배리어 `entry × (1 ± K·atr_pct)`.
**1분봉으로** 두 배리어의 첫 터치 시각을 각각 찾아 원시 저장:

`hit_cont_min_K{K}` · `hit_fade_min_K{K}` · `ambig_K{K}`(같은 분봉 동시 터치)

```
y_order(K,H) = 1     hit_cont < hit_fade 이고 hit_cont ≤ H×5분
             = 0     hit_fade < hit_cont 이고 hit_fade ≤ H×5분
             = NaN   둘 다 미터치(미결정) 또는 ambig
```
K ∈ {0.5, 1, 1.5, 2, 3} · H ∈ {12, 24, 48, 96} 을 **학습 시점에 자유 선택**.
⭐**대칭 배리어가 기본**이다 — 09-06 `y_order`는 페이드 2.0ATR vs 지속 1.5ATR 비대칭이라
그 자체로 한쪽에 유리했다.

## H3. ⭐5분 해상도로는 못 가린다 (실측)

| K | 5분봉이 양쪽 동시 터치 | 5분 vs 1분 라벨 불일치 |
|---|---|---|
| 0.5 | **15.83%** | **7.93%** |
| 1.0 | 1.76% | 1.13% |
| 1.5 | 0.35% | 0.22% |
| 2.0 | 0.10% | 0.07% |

**K ≤ 1.0 에서 5분봉 라벨은 쓰면 안 된다.** K ≥ 1.5 부터는 무시할 만하다.

## H4. ⭐새 라벨은 기존 라벨과 4건 중 1건 이상 다르다

| K | H | 결정 n | 기존 `y_cont`와 불일치 |
|---|---|---|---|
| 1.0 | 48 | 45,094 | **35.4%** |
| 1.5 | 48 | 44,814 | **27.0%** |
| 1.5 | 24 | 43,633 | 18.3% |
| 2.0 | 48 | 43,672 | 19.8% |

순서를 무시한 대가가 이만큼이다. 이 차이가 그대로 라벨 잡음이었다.

## H5. 확정 앵커 `any3/Wc3` 기준선 (K=1.5 · H=48)

| split | side | n | 결정비율 | 지속비율 |
|---|---|---|---|---|
| TRAIN | bottom / top | 1670 / 1568 | 0.992 / 0.994 | 0.525 / 0.492 |
| VAL | bottom / top | 310 / 304 | 0.997 / 0.990 | 0.476 / 0.465 |
| OOS | bottom / top | 225 / 219 | 1.000 / 1.000 | 0.484 / 0.539 |

미결정 비율(K,H)별: K1.5 는 H12 11.9% → H48 0.9%, K3.0 은 H12 56.5% → H96 5.6%.

**지속비율은 여전히 0.47~0.54 — 동전이다.** 다만 이제 **깨끗한 동전**이다(순서 반영·미결정 분리·
1분 해상도). 모델이 여기서 무언가를 찾는다면 그것은 라벨 결함이 아니라 실제 신호다.

⚠️ 터치 **속도**(지속 배리어가 페이드보다 중앙 7분 빨리 닿음, ATR매칭 귀무 백분위 0~3)는
**해석 보류** — 두 중앙값이 서로 다른 부분집합(각자 터치한 건만)에서 계산돼 짝비교가 아니다.
깨끗한 통계량은 `P(지속 먼저 | 결정)` 이고 그건 위 표대로 동전이다.

---

# 부록 I — 학습셋 정제 (2026-09-07)

사용자: *"양쪽 다 거의 안 움직인 봉은 제거하고 라벨 데이터를 최대한 잘 깎아서 모델한테 잘 먹여야해."*
*"등락폭은 ±1%로 해줘."*

`scripts/build_eth_anchor_training_set_20260907.py` · `tmp/eth_anchor_training_set_20260907/`

## I1. ⭐정제의 제1원칙 — 세 가지를 구분한다

| 종류 | 예 | 적용 범위 |
|---|---|---|
| **(A) 라벨 미정의** | H봉 안에 어느 배리어도 안 닿음 | 제거는 **필연**(정답이 없다). 단 **제거분을 별도 회계** |
| **(B) 인과 필터** | 진입 시점 ATR·시간대 등 | **양쪽 창 모두** — 라이브에도 같이 적용 |
| **(C) 라벨 품질 필터** | 두 배리어 터치 시각 차(여백) | **TRAIN 전용**. VAL/OOS 에 쓰면 평가 모집단이 라이브와 달라진다 |

이 구분을 흐리면 **"미래를 보고 쉬운 것만 남기고 잘 맞혔다"** 가 된다. 실측으로 (C)의 편향은
작았지만(VAL 지속비율 0.470 → 여백≥30분 0.463) 원칙은 지킨다 — 플래그 `train_quality_ok` 로만 두고
적용은 학습 코드가 선택한다.

## I2. 라벨 (사용자 지정 ±1%)

진입 `open[t+1]` · **고정 ±1% 대칭** 배리어 · **1분봉**으로 먼저 닿는 쪽.
±1% = 100bp 로 왕복 비용 10bp 의 **10배** — `feedback_atr_normalized_label_low_volatility_bp_floor_check`
의 저변동성 bp 하한 함정을 구조적으로 회피한다.

| H | 결정비율 | 지속비율 | 5분vs1분 불일치 |
|---|---|---|---|
| 12 | 0.339 | 0.571 | 0.19% |
| 24 | 0.515 | 0.538 | 0.13% |
| **48** | **0.709** | 0.519 | 0.09% |
| 96 | 0.877 | 0.510 | 0.08% |

⚠️ **H가 짧을수록 지속비율이 높은 것은 선택 효과다** — H12 에서 결정된 34% 는 "빨리 1% 간" 건들이고
그건 강한 모멘텀 구간이다. 미결정을 버린 뒤의 비율을 예측력으로 읽으면 안 된다.

## I3. 최종 학습셋 (앵커 `any3/Wc3`, 양측면)

H=48: 미결정 제거 **1,352건(27.5%)** · 퍼징 0 · 엠바고 1 → 잔존 3,565
H=96: 미결정 제거 559건(11.4%) → 잔존 4,357

| H | split | n | 지속비율 | 유효표본 | 고유도 평균 | 서로 다른 날 | 중앙 보유(봉) |
|---|---|---|---|---|---|---|---|
| **48** | TRAIN | 2,397 | 0.528 | 2,267 | **0.946** | 580 | 11.6 |
| **48** | VAL | 450 | 0.511 | 428 | 0.951 | 111 | 10.4 |
| **48** | OOS | 340 | 0.529 | 329 | 0.968 | 84 | 9.0 |
| 96 | TRAIN | 2,921 | 0.514 | 2,617 | 0.896 | 590 | 16.4 |
| 96 | VAL | 536 | 0.500 | 487 | 0.908 | 117 | 13.8 |
| 96 | OOS | 399 | 0.526 | 363 | 0.909 | 87 | 12.2 |

## I4. ⭐고유도 0.95 — 09-06 대비 17배 개선

09-06 페이드/지속 데이터셋은 평균 고유도 **0.055**(유효표본이 명목의 1/18)였다. 여기는 **0.95**다.
이유: 앵커가 GAP12 dedup + 3종 중첩이라 희소하고(하루 2.7회), 라벨 창도 중앙 11.6봉으로 짧다.
**검정력 측면에서 결정적인 차이**이고, 09-06 의 "유효표본 700" 제약이 여기서는 없다.

## I5. 남은 계약 — 반드시 지킬 것

- **미결정 27.5% 는 라이브에서 시간청산이 된다.** 이 모델은 "결정된 건 중 방향"만 맞힌다.
  경제성을 말할 때 그 몫을 반드시 별도 회계할 것 — 안 하면 27.5% 를 공짜로 없앤 셈이 된다.
- **검정력 상한**: OOS n=340(균형) 이면 AUC 표준오차 ≈ 0.031. **AUC 0.56 미만은 못 가른다.**
  이보다 작은 개선을 주장하면 안 된다.
- 가중치 `w_uniq`(AFML 고유도)를 학습·평가 양쪽에 쓴다.
- `train_quality_ok` 는 **TRAIN 에서만** 켤 수 있다.

---

# 부록 J — 라벨 v2(3클래스+가중치) → 피쳐 → 기준선 → 대조군 (2026-09-07)

사용자 목표: *"2와 3을 같이 두고 피쳐 분석부터 tabpfn 모델 학습까지 모두 진행. 각 단계마다
버그가 있는지 자체 검증도."*

## J1. 라벨 v2 — 3클래스 + 품질 가중치

`scripts/build_eth_anchor_labels_v2_20260907.py` · `tmp/eth_anchor_labels_v2_20260907/`

v1(먼저 닿기)은 **닿자마자 진입가로 되돌아온 경로**도 지속 승으로 불렀다. v2 는 두 조건을 더한다:
`pre_adv < 0.5%`(닿기 전 반대 이탈) ∧ `end_dir >= 1.0%`(끝점이 그 방향 유지).

```
y3 = 2 지속 승(clean) · 0 되돌림 승(clean) · 1 혼재(지저분 + 미결정)
y2 = 먼저 닿는 쪽 (표본 유지, 미결정 NaN)
w_quality = 1/(1+pre_adv/0.5) x clip(end_dir/1.0, 0, 1)   ·   w_train = w_uniq x w_quality
```

⭐**끝점은 마지막 6봉(30분) 평균 종가**다. 단일 종가는 1봉 노이즈로 라벨이 뒤집힌다 —
자체검증 V4 가 `12-08 23:55` 에서 잡았다(last1 +1.14% → 지속승 / last6 +0.57% → 혼재).

**자체 검증 12/12 PASS.** V4 는 사용자가 지목한 10건을 그대로 재현한다
(인정 11-21·01-29·01-20 / 제외 나머지 7).

| split | 되돌림승 | 혼재 | 지속승 | clean 비율 | clean 중 지속 |
|---|---|---|---|---|---|
| TRAIN | 402 | 2,394 | 441 | 0.260 | 0.523 |
| VAL | 85 | 448 | 79 | 0.268 | 0.482 |
| OOS | 48 | 331 | 65 | 0.255 | 0.575 |

## J2. 피쳐 — 09-06 계보 36종 + votes/atr_pct/side

`scripts/build_eth_anchor_features_20260907.py` · 3,711행 × 39피쳐.
`build_context`/`window_features`(09-06 `research_eth_fire_ordering_model_20260906.py`) 축자 재사용.
창 [발동−2, 발동] = −10분~0(인과), 방향 피쳐는 페이드 방향 정렬.
데이터 완전 구간 **2024-04-20 ~ 2026-03-31**(bookdepth 시작 ~ spot/btc/metrics 끝) — VAL·OOS 완전 포함.

**자체 검증 8/8 PASS.** 발견·수정한 것 둘:
1. **±inf 6개** (2024-07 OI 결측 봉에서 `oi1/oi0`) → NaN 처리. 남겨두면 StandardScaler 가
   오버플로로 NaN 을 만들어 로짓이 죽는다. 검사 `F2b` 신설.
2. **F5 측면 대칭 FAIL → 오탐이었다.** `w_oi_x_ret` KS 0.81 의 원인은 코드가 아니라
   **`d_oi` 자체의 부호 뒤집힘**(바닥 발동 OI 중앙 −0.71% 청산·디레버리징 vs 천장 +0.47% 신규 롱).
   실제 시장 비대칭이다. 검사를 **정렬 피쳐에만** 적용하도록 좁히고(7/7 KS ≤ 0.16, 부호 일치),
   `side_is_bottom` 을 명시 피쳐로 추가했다.
- F1 앵커 절단 재계산 40건 불일치 0 · F3 최고 단일피쳐 AUC 0.535 · F4 조인 시점 마감봉 +0.922 vs 다음봉 −0.025.

## J3. 기준선 (로컬)

| 라벨 | 팔 | VAL AUC [일군집 CI] | OOS AUC [일군집 CI] |
|---|---|---|---|
| y2 | 전체피쳐 **로짓** | 0.5308 [0.481, 0.581] | **0.5889 [0.526, 0.654]** |
| y2 | 전체피쳐 HGB | 0.5441 [0.491, 0.597] | 0.5099 [0.450, 0.574] |
| y2 | C1 크기단독(atr_pct) | 0.5129 | 0.4803 |
| y3_clean | 로짓 | 0.5083 | 0.5606 (n=113) |

HGB < 로짓 — 이 저장소가 반복 관측한 "HGB 과적합" 서명과 일치.

## J4. ⭐⭐대조군 — 셔플 귀무가 0.5가 아니다

| 대조군 | VAL | OOS |
|---|---|---|
| 기준선(로짓) | 0.5308 | 0.5889 |
| **C4 라벨 셔플 귀무(일 안)** | 평균 **0.5212** · p95 **0.5487** | 평균 **0.5415** · p95 **0.5942** |
| **⇒ 관측 백분위** | **70** ✗ | **92** ✗ |
| C2 음성대조(결단력 f1 라벨) | 0.5158 | 0.5145 |
| C3 방향 뒤집기 | 0.4692 (= 1−base ✓) | 0.4111 (= 1−base ✓) |
| C5 TRAIN 일 부트스트랩 B=40 | 0.5272±0.0125 | 0.5657±0.0293 |
| C6 수치 취약성(float32/64×순서3) | 폭 0.0000 | 폭 0.0000 |

🔴 **판정: 기준선 미통과.** 일군집 CI 는 OOS 하한 0.526 으로 통과처럼 보였지만,
**같은 파이프라인에 라벨만 일 안에서 셔플해도 VAL 0.52 / OOS 0.54 가 나온다.**
일별 라벨 구성이 보존되고 피쳐가 그날 특성을 담고 있어 "그날 어느 쪽이 우세했나"를 부분적으로
맞히기 때문이다. ⇒ **이 문제에서 AUC 0.5 를 귀무로 삼으면 안 된다. 셔플 귀무가 옳은 기준선이다.**

⚠️**C5 정정**: 처음에 시드 대조(8시드)를 넣었는데 HGB/로짓은 결정적이라 **분산이 정확히 0**이었다
(무의미한 대조). TRAIN 일 단위 부트스트랩으로 교체했다.

## J5. TabPFN (서버 GPU) — 그리고 최종 판정

`scripts/research_eth_anchor_direction_tabpfn_20260907.py` · `tmp/eth_anchor_tabpfn_20260907/`
피쳐·라벨·분할 동일, 5시드, **셔플 귀무를 같은 모델로 병기**(B=20).

| 모델 | 라벨 | VAL 관측 (귀무 p95) → 백분위 | OOS 관측 (귀무 p95) → 백분위 |
|---|---|---|---|
| 로짓 | y2 | 0.5308 (0.5487) → **70** | 0.5889 (0.5942) → **92** |
| HGB | y2 | 0.5441 | 0.5099 |
| **TabPFN** | y2 | 0.5614±0.0092 (0.5739) → **90** | 0.5415±0.0031 (0.5576) → **80** |
| **TabPFN** | y3_clean | 0.4668±0.0123 (0.5367) → 25 | 0.5366±0.0128 (0.5420) → **90** |

### 🔴 판정: 8팔(4구성 × 2창) 중 **통과 0**

가장 가까운 것이 로짓 OOS 백분위 92, TabPFN VAL 90 · y3 OOS 90 — 전부 95 미달이고,
**두 창을 같이 통과한 팔은 하나도 없다.** 모델 클래스를 바꿔도(로짓 → HGB → TabPFN),
라벨을 깎아도(y2 → y3_clean), 가중치를 붙여도 결과가 바뀌지 않는다.

### ⭐이번에 확립된 것 — 방법론 쪽

1. **이 문제의 귀무는 AUC 0.5 가 아니다.** 라벨을 일(day) 안에서만 셔플해도 VAL 0.52~0.53 ·
   OOS 0.49~0.54 가 나온다. 일별 라벨 구성이 보존되고 피쳐가 그날 특성을 담아 "그날 어느 쪽이
   우세했나"를 부분적으로 맞히기 때문이다.
   ⇒ **일군집 부트스트랩 CI 는 이 효과를 못 잡는다.** 로짓 OOS 는 CI 하한 0.526 으로 통과처럼
   보였지만 셔플 귀무로는 백분위 92 였다. **앞으로 이 모집단에서는 셔플 귀무를 반드시 병기한다.**
2. **시드 대조는 모델에 따라 무의미하다.** HGB·로짓은 결정적이라 `random_state` 만 바꾸면
   분산이 **정확히 0** 이다(실측). TabPFN 은 시드 분산이 실재한다(±0.003~0.013).
   결정적 모델에는 **TRAIN 일 단위 부트스트랩**을 써야 한다.
3. **자체 검증이 실제로 버그를 잡았다** — 라벨 V4(끝점 1봉 노이즈), 피쳐 F2b(±inf 6개가
   StandardScaler 오버플로로 로짓을 죽임), 그리고 F5 의 **오탐 1건**(측면 비대칭이 코드가 아니라
   `d_oi` 부호 뒤집힘이라는 시장 사실). 검사 자체가 틀릴 수 있다는 것도 같이 기록한다.

### 남은 자산 (재사용 가능)

`tmp/eth_anchor_labels_v2_20260907/` (3클래스+가중치 라벨 4,912행) ·
`tmp/eth_anchor_features_20260907/` (3,711×39, 인과 검증 완료) ·
`tmp/eth_anchor_controls_20260907/` (셔플 귀무 분포) — 새 재료가 생기면 라벨·피쳐를 다시 만들 필요 없이
같은 대조군 위에 바로 얹을 수 있다.


---

# 부록 J — 방향 라벨 정정: 「먼저 닿기」만으로는 틀렸다 (2026-09-07)

사용자: *"25년 11월 21일 1:00 바닥 앵커 지속 승 같은 데이터만 지속 승으로 가야해. 나머지는
되돌림과 지속이 섞여있어. 라벨 로직이 지금은 틀린거야."* → *"2와 3을 같이 두고"*

## J1. 무엇이 틀렸나

부록 H/I 의 라벨은 **"어느 배리어를 먼저 닿았나"만** 본다. 그래서 **1% 닿자마자 진입가로
되돌아온 건도 지속 승**으로 부른다. 사용자가 지목한 10건을 실측하니 정확히 갈렸다:

| 인정(깨끗) | 끝점 | 제외(섞임) | 끝점 |
|---|---|---|---|
| 11-21 01:00 | −3.68% | 10-09 17:15 | −0.05% |
| 01-29 23:25 | −4.47% | 12-14 20:20 | −0.57% |
| 01-20 15:15 | −1.03% | 01-09 17:05 | −0.59% |
| | | 12-08 23:55 | −0.85% |
| | | 03-19 21:50 | −0.87% |

**끝점이 정확히 1% 에서 갈린다.** 추가로 10-20(닿기 전 역행 +0.90%)·12-16(+0.71%)은 경로가
반대로 먼저 갔다 온 건이라 역행 조건으로 걸린다.

## J2. 새 라벨 — 3클래스 + 품질 가중치를 **같이** 둔다

```
깨끗(clean) = 닿기 전 반대 역행 < 0.5%   ∧   판정창 끝점이 그 방향으로 ≥ 1.0%
y3 = 2  지속승(깨끗) · 0  되돌림승(깨끗) · 1  혼재(결정됐으나 안 깨끗 + 미결정)
y_bin   = 「먼저 닿기」 이진 라벨(미결정 NaN)        <- 옵션③용
q_label = clip(1−역행/0.5,0,1) × clip(끝점/1.0,0,1)  in [0,1]
w_final = w_uniq(AFML 고유도) × q_label
```
⇒ 학습 시점에 **세 방식 중 무엇이든** 고를 수 있다:
① `y3∈{0,2}` 만 골라 이진(하드 필터) · ② `y3` 3클래스 · ③ `y_bin` + `w_final` 가중.

## J3. 최종 학습셋 (H=48, 앵커 `any3/Wc3`, 양측면)

| split | n | 지속승 | 혼재 | 되돌림승 | 깨끗 n | 이진 n | q 평균 | 고유도 | 다른 날 |
|---|---|---|---|---|---|---|---|---|---|
| TRAIN | 3,237 | 0.137 | 0.735 | 0.128 | 857 | 2,397 | 0.224 | 0.905 | 608 |
| VAL | 612 | 0.124 | 0.732 | 0.144 | 164 | 450 | 0.231 | 0.917 | 119 |
| OOS | 444 | 0.149 | 0.734 | 0.117 | 118 | 340 | 0.207 | 0.907 | 90 |

H=96 변형도 같이 저장(깨끗 989/187/125).

## J4. 🔴옵션별 검정력 — 먼저 못박아 둔다

| 옵션 | OOS n | AUC 표준오차 | 가릴 수 있는 최소 AUC |
|---|---|---|---|
| ③ 가중 이진 | 340 | 0.031 | **0.56** |
| ② 3클래스 | 444 | — | 클래스별로 더 낮음 |
| ① 하드 필터 | **118** | **0.053** | **0.61** |

**①은 라벨이 가장 깨끗하지만 AUC 0.61 미만을 못 가린다.** 세 옵션을 같이 둔 이유가 이것이다 —
①에서 뭔가 보이면 ③으로 표본을 늘려 재확인할 수 있다.
그리고 **혼재 73.4% 는 라이브에서도 발생한다** — 어느 옵션이든 그 몫의 회계가 별도로 필요하다.


## J5. 3차 정정 — **경로 효율**이 빠져 있었다 (2026-09-07)

사용자: *"2열 1행, 2행 같은건 혼재로 가는게 맞는데 그렇게 하려면 어떤 로직이 개선되어야 하지?"*
(10-11 05:50 · 12-06 01:05)

J2 의 깨끗 판정(닿기 전 역행 + 끝점)은 **닿은 뒤 경로를 안 본다**. 두 건 모두 배리어를 닿은 뒤
크게 왕복했다. 10건에 여섯 지표를 대보니 **단 하나가 완전 분리**했다:

**경로 효율 = |끝점 진행| / 창내 전체 진폭** (Kaufman efficiency ratio 형태)

| | 효율 |
|---|---|
| 지목 2건(혼재로 가야 함) | 10-11 **0.14** · 12-06 **0.38** |
| 나머지 8건(지속승 유지) | 0.54 ~ 0.89 |

10-11 은 창 안에서 **14.80%** 나 움직였는데 순진행은 그 **14%** 뿐이다. 다른 다섯 지표
(닿은 뒤 진입가 복귀·배리어 위 복귀·배리어 아래 체류비율·재교차 횟수·전체 진폭)는 전부 겹쳤다.

⇒ 깨끗 판정에 `path_eff >= 0.40` 추가, `q_label` 에도 `clip(eff/0.40,0,1)` 을 곱한다.
재판정 결과 **지목 2건 → 혼재(y3=1), 나머지 8건 → 지속승(y3=2) 유지**로 의도대로 갈렸다.
깨끗 표본 1,284 → **1,229** (OOS 118 → 111).


---

# 부록 K — 기준선 + 대조군 (2026-09-07)

사용자: *"끝점 30분 평균으로 바꾸고 기준선+대조군 돌려줘"*
`scripts/research_eth_anchor_direction_baseline_eval_20260907.py` · `tmp/eth_anchor_baseline_eval_20260907/`

## K1. 입력

`tmp/eth_anchor_modeling_table_20260907/modeling_table.parquet` **(3,711 × 64)**
= 피쳐 41종(동시 세션 `build_eth_anchor_features_20260907`, 09-06 `build_context`/`window_features`
축자 재사용 · 창 [t−2,t] 인과 · 페이드 방향 정렬 · 단일 피쳐 최대 AUC 0.5354)
+ 확정 라벨(±1% 대칭 · 1분봉 첫 터치 · **끝점 = 마지막 6봉 30분 평균 종가**).

끝점을 30분 평균으로 바꾼 효과: 깨끗 표본 TRAIN 675→663 · VAL 154→153 · OOS 111→107.

## K2. 결과

| 팔 | n(tr/va/oo) | VAL AUC [일군집 CI] | OOS AUC [일군집 CI] |
|---|---|---|---|
| A1 HGB 41피쳐 (가중이진) | 1982/450/340 | 0.5343 [0.486, 0.581] | 0.5719 [0.506, 0.638] |
| A2 하드필터 `y3∈{0,2}` | 663/153/107 | 0.5108 [0.419, 0.600] | 0.5036 [0.391, 0.627] |
| A1 선형(로지스틱) | 1982/450/340 | 0.5210 [0.472, 0.567] | 0.5699 [0.511, 0.626] |
| **P1 양성대조 — 라벨을 크기로** | 1982/450/340 | **0.6523 [0.599, 0.706]** | **0.6290 [0.564, 0.691]** |
| C1 `atr_pct` 단독 | 1982/450/340 | 0.5408 [0.489, 0.590] | 0.5148 [0.461, 0.574] |
| C3 TRAIN만 뒤집기 | 1982/450/340 | 0.4657 (기대 0.4657) | 0.4281 (기대 0.4281) |

- **C4 라벨셔플 귀무**(일 내, B=30): VAL 평균 0.4998·p95 0.5415 / **OOS 평균 0.5189·p95 0.5607·최대 0.5761**
- **C5 시드**: HGB 8시드 표준편차 0.000(이 설정에서 결정적)
- **C6 수치취약성**: float32/64 × 열순서 3변형 OOS AUC [0.5642, 0.5719] 폭 0.0078, 부호반전 없음
- **C3 대칭 편차 0.0000** — 파이프라인 정상

## K3. 판정 — **통과 실패**

사전 등록 통과 조건 = VAL·OOS 둘 다 CI 하한 > 0.5 ∧ C4 귀무 p95 초과 ∧ C1 대비 우위.
- A1 **VAL CI 하한 0.486 < 0.5** → 실패
- A1 OOS 0.5719 는 C4 p95(0.5607)를 넘지만 **초과분이 +0.053** 으로 사전 등록 검정력 한계(0.56)와 같은 크기
- 두 창이 엇갈린다(VAL 0.534 / OOS 0.572)

## K4. ⭐양성대조가 결정적이다 — 코드가 아니라 라벨의 성질

**같은 피쳐·같은 파이프라인으로 라벨만 '크기'로 바꾸면 AUC 0.652 / 0.629 가 나온다.**
방향은 0.534 / 0.572 다. 파이프라인·피쳐·앵커는 정상이고 **방향 성분만 없다.**
저장소가 10형태에서 반복 검출한 *"크기는 배워지고 방향은 안 배워진다"* 를
**새 앵커 · 새 라벨(±1%·1분봉·3클래스·품질가중) · 새 피쳐에서 11번째로 재확인**했다.

## K5. ⭐라벨 잡음 가설도 기각된다

A2(하드필터, 가장 깨끗한 라벨)가 **0.511 / 0.504 로 오히려 더 나쁘다.** "라벨이 지저분해서
안 배워졌다"면 깨끗하게 걸렀을 때 올라야 하는데 내려간다(표본 감소 효과가 라벨 정제 효과보다 크다).
⇒ **라벨을 더 깎는 방향은 닫힌다.**

## K6. 발견한 대조군 설계 결함 (기록)

초판 C3 는 **학습·평가 라벨을 둘 다** 뒤집어 AUC 가 수학적으로 불변이었다(원본과 소수점까지 동일).
아무것도 검정하지 못한다. **TRAIN 라벨만 뒤집고 평가는 원본**으로 고쳐야 1−AUC 대칭이 확인된다.
새 방향뒤집기 대조군을 쓸 때마다 이 형태인지 확인할 것.

## K7. 남은 것

- DL 은 이 결과 위에서는 근거가 약하다. 09-06 Tier 0(파라미터 59개)이 **첫 에폭부터 과적합**했고
  여기 유효표본은 그때보다 크지만(고유도 0.95) OOS n=340 이라 용량을 늘릴 여지가 없다.
- 살아 있는 축은 **크기**다 — P1 이 0.63 으로 예측 가능함을 보였다. 방향을 포기하고
  크기 예측을 사이징·손절폭에 쓰는 쪽이 데이터가 지지하는 방향이다(부록 E §E4 와 같은 결론).


## K8. ⚠️정정 — 크기 라벨의 0.63 은 **모델이 아니라 ATR 지속성**이고, 모델은 그보다 나쁘다

크기 라벨 정의: `range_pct = (max high[t+1..t+48] − min low[t+1..t+48]) / open[t+1] × 100`,
`size_y = range_pct > 2.00%`(TRAIN 중앙값). 방향 무관 이진.
양성률 TRAIN 0.670 / VAL 0.696 / OOS 0.741 (임계값이 TRAIN 고정이라 뒤 창이 고변동성이면 치우친다).

K4 에서 빠뜨린 대조군을 채우니:

| 학습 입력 | VAL AUC [CI] | OOS AUC [CI] |
|---|---|---|
| 41피쳐 전체 | 0.6523 [0.598, 0.704] | 0.6290 [0.564, 0.692] |
| **`atr_pct` 단독** | 0.6485 [0.589, 0.708] | **0.6817 [0.618, 0.742]** |
| `atr_pct` 제외 40피쳐 | 0.6555 [0.603, 0.707] | 0.6260 [0.556, 0.695] |
| **학습 없이 raw `atr_pct`** | — | **0.7076** |

**모델(0.629)이 단일 피쳐 `atr_pct`(0.682), 심지어 학습 없는 raw 값(0.708)보다 나쁘다.**
40피쳐를 더하면 희석될 뿐이다. 09-06 페이드/지속에서 *"`cont_pnl>+50bp` AUC 0.82 인데
`atr_pct` 하나가 0.8216 이고 나머지 57피쳐 기여 +0.001"* 이었던 것과 같은 구조다.

⇒ **양성대조로서의 역할은 유효**하다(파이프라인은 학습 가능한 신호를 실제로 배운다).
그러나 K7 의 *"살아 있는 축은 크기다"* 는 **약화된다** — 크기를 알기 위해 모델이 필요 없고
`atr_pct` 하나면 되며, 배포 사이징은 이미 `notional = 0.004/(5·atr_pct)` 로 그것을 쓰고 있다
(09-06: 그 공식이 ATR 우위의 85% 를 상쇄). **크기 축도 이미 상당 부분 소진돼 있다.**


---

# 부록 L — 🔴피쳐 누수 적발: `w_quality`/`w_train` (2026-09-07)

## L1. 무엇이 새었나

모델링 테이블을 만들 때 동시 세션 피쳐 파일에서 `w_` 접두어로 피쳐를 골랐는데,
그 파일에는 **그 세션의 라벨 품질 가중치** `w_quality`(= 역행 감쇠 × 끝점 포화)와
`w_train`(= w_uniq × w_quality)이 같은 접두어로 들어 있었다. 둘 다 **라벨 파생값**이다.

내 `feature_cols()` 드롭 목록에는 **내 이름**(`q_label`·`w_uniq`·`w_final`)만 있었고
**피어 이름**이 없었다. 그래서 41개 "피쳐" 중 2개가 라벨이었다.

| 피쳐 | 혼재 라벨 단일 AUC(방향무시) |
|---|---|
| `w_quality` | VAL **0.9710** · OOS **0.9648** |
| `w_train` | VAL 0.9465 · OOS 0.9447 |

**게이트 L3 기준(단일 피쳐 AUC ≥ 0.95) FAIL.** 정화 후 39피쳐의 단일 최대는
혼재 0.6129(`atr_pct`) · 방향 0.5863(`w_lsr_retail_chg`) 로 정상이다.

## L2. 교훈 — **이름이 아니라 출처로 거른다**

`w_` 는 이 파이프라인에서 "window feature"(w_ret_atr 등)와 "weight"(w_quality, w_uniq)
**두 뜻으로 쓰이고 있었다.** 접두어 기반 피쳐 선택은 그 충돌을 못 본다.
⇒ 피쳐는 **화이트리스트**(빌더가 명시한 목록)로 가져오고, 외부 파일에서 열을 긁어올 때는
드롭 목록이 아니라 **허용 목록**을 쓴다.

## L3. 무효화 범위

부록 K(기준선·대조군)와 옵션② 3클래스 1차 결과는 **전부 이 41피쳐 위에서 나왔다 → 재계산 대상.**
방향 AUC(0.53~0.57)는 `w_quality` 가 방향 정보를 갖지 않으므로 크게 부풀지 않았을 것으로
보이나, **검증 전에는 인용하지 않는다.**

## L4. 같이 발견한 두 번째 결함 — 귀무의 셔플 단위

옵션② 3클래스 1차에서 **일 내 셔플 귀무가 혼재 AUC 0.74** 로 나왔다. 라벨이 **날 단위로
뭉쳐 있어**(저변동성 날은 하루 종일 혼재) 일 내 셔플이 라벨을 거의 안 바꾼 것이다.
**날 블록 셔플**로 바꾸니 귀무가 0.4979/0.4929(p95 0.569/0.574)로 정상화됐다.
⇒ **귀무는 라벨의 군집 구조를 깨는 단위로 섞어야 한다.** 일 내 셔플을 기본으로 쓰면 안 된다.


---

# 부록 M — 3팔 × 튜닝 × 피쳐선택 (정화 후) (2026-09-07)

사용자: *"위 3가지를 모두 병렬적으로 진행해보자. 하이퍼파라미터 튜닝하고 피쳐선택까지."*
`scripts/research_eth_anchor_direction_tuned_20260907.py` · `tmp/eth_anchor_direction_tuned_20260907/`

## M1. 설계

- 피쳐 **39개**(부록 L 누수 2개 제거) · HP 12셀 · 피쳐셋 5종(all39/nocorr/perm10/perm20/atr)
- **튜닝·피쳐선택은 TRAIN 내부 purged K-fold(4겹, 엠바고 48봉)에서만.** VAL/OOS 는 최종 1회.
- 귀무는 **날 블록 셔플**(부록 L4)

## M2. 결과 — **3팔 전부 통과 실패**

| 팔 | 최적 피쳐셋 | CV | VAL AUC [CI] | OOS AUC [CI] | 귀무 p95 (V/O) |
|---|---|---|---|---|---|
| ① hard (깨끗 이진) | **atr (1개)** | 0.5441 | 0.6053 [0.519, 0.693] | **0.4401** [0.332, 0.559] | 0.595 / 0.602 |
| ② three — 방향 | perm20 | 0.5425 | 0.4693 [0.381, 0.553] | 0.4688 [0.347, 0.584] | 0.568 / 0.617 |
| ② three — 혼재 | perm20 | — | 0.5164 [0.462, —] | 0.5363 [0.471, —] | — |
| ③ wbin (가중 이진) | perm20 | 0.5372 | 0.5125 [0.456, 0.568] | 0.5697 [0.509, 0.632] | 0.539 / 0.551 |
| **P1 양성대조(크기)** | 39피쳐 | — | **0.6504 [0.592, 0.704]** | **0.6451 [0.579, 0.709]** | — |

- ① VAL 0.605 는 귀무를 겨우 넘지만 **OOS 0.440 으로 0.5 아래** — 두 창이 반대다
- ② 방향은 VAL·OOS 둘 다 0.47 — 0.5 아래
- ③ VAL 0.513 이 귀무 p95(0.539) 미달, OOS 0.570 은 귀무(0.551) 겨우 초과 — 엇갈림

## M3. ⭐혼재 0.95 는 전부 누수였다

부록 L 이전 41피쳐에서 혼재 AUC 0.962/0.954 였던 것이 정화 후 **0.516/0.536**,
CI 하한 0.462/0.471 로 **0.5 를 포함**한다. `w_quality` 하나가 만든 값이었다.
⇒ *"방향은 못 맞혀도 혼재는 걸러낼 수 있다"* 는 옵션② 의 기대는 **성립하지 않는다.**

## M4. ⭐이월성이 음수 — 튜닝할 것이 없다

TRAIN CV 상위 8셀의 CV 점수와 OOS AUC 의 Spearman ρ:

| 팔 | ρ(CV, OOS) | OOS 범위 |
|---|---|---|
| ① hard | **−0.937** | [0.434, 0.529] |
| ② three | +0.500 | [0.444, 0.485] |
| ③ wbin | −0.214 | [0.512, 0.570] |

**①은 거의 완전한 역상관이다** — TRAIN CV 에서 좋을수록 OOS 에서 나쁘다.
그리고 ①이 고른 최적 피쳐셋은 **`atr` 1개**였다. TRAIN CV 조차 나머지 38피쳐를 쓰지 못한다.
⇒ **"튜닝을 안 해서 안 나온 것"이라는 가능성이 배제된다.**

## M5. 양성대조는 살아 있다 — 파이프라인은 정상

| 크기 라벨 학습 입력 | VAL AUC | OOS AUC |
|---|---|---|
| 39피쳐 전체 | 0.6504 | 0.6451 |
| `atr_pct` 단독 | 0.6485 | **0.6817** |
| `atr_pct` 제외 38피쳐 | 0.6484 | 0.6263 |
| 학습 없이 raw `atr_pct` | — | **0.7076** |

누수 제거 후에도 크기는 0.65 로 배워진다(오염본 0.629 와 거의 같다 — `w_quality` 는 크기 라벨을
부풀리지 않았다). **파이프라인·피쳐·앵커는 정상이고 방향 성분만 없다.**
그리고 여전히 **모델이 단일 피쳐 `atr_pct` 보다 못하다**(0.645 vs 0.682, raw 0.708).

## M6. 판정

**방향 예측 12번째 기각.** 이번에는 3팔 전부 · HP 12셀 · 피쳐셋 5종 · TRAIN 내부 purged CV ·
날 블록 귀무 · 누수 정화까지 마친 뒤의 결과다. 남은 변명이 없다:
- 라벨이 지저분해서? → ①(가장 깨끗)이 가장 나쁘다(OOS 0.440)
- 튜닝을 안 해서? → 이월성 ρ −0.937, 최적 피쳐셋이 atr 1개
- 파이프라인이 고장? → 양성대조 0.65
- 혼재를 안 나눠서? → ② 방향 0.47, 혼재도 0.52


---

# 부록 N — 피쳐 확장 3종 (2026-09-07)

사용자: *"피쳐를 늘려서 테스트해줘"* → *"150여개 피쳐가 있자나"* → *"카테고리별로 나눠서 스윕하는건가?"*

## N1. 세 가지 피쳐셋을 같은 절차로

| 세트 | 개수 | 출처 | TRAIN/VAL/OOS |
|---|---|---|---|
| ① 자체제작 확장 | 199 | 창 W∈{2,6,12,24,48}봉 × 36 + 신호원핫 + 시간 + 앵커간격 | 2,655 / 612 / 444 |
| ② **DC 엔지니어링** | **150** | `ilias_eth_154feature_*` 중 **감사 통과분** | **3,237** / 612 / 444 |
| ③ 카테고리 분해 | 10~44씩 8축 | ②를 이름 규칙으로 분할 | 동일 |

⭐②의 룩어헤드 감사(`audit_eth_154feature_lookahead_20260901.py`)는 **작성만 되고 실행된 적이 없었다.**
이번에 처음 돌렸다 → `data/research/eth_154feature_audit_20260901/report.json`:
**leak_likely 0 · suspect 0 · suspect_future_peak 1 · exclude_model_output 3 · pass 150.**
제외 4종은 `regime3_current_sensitive_wide24_{bear,bull,confidence}`(모델 출력, 순환성) ·
`whale_retail_ratio`(교차상관 피크가 미래). L3 재검사도 통과(방향 0.573 · 혼재 0.604).

## N2. ⭐피쳐를 늘릴수록 TRAIN CV 가 나빠진다 (①)

| 팔 | atr(1) | perm20 | perm40 | perm80 | all199 |
|---|---|---|---|---|---|
| hard | **0.5441** | 0.5381 | 0.5322 | 0.5238 | 0.5046 |
| three | **0.5398** | 0.5397 | 0.5183 | 0.5042 | 0.5039 |
| wbin | 0.5366 | **0.5499** | 0.5440 | 0.5331 | 0.5133 |

VAL/OOS 가 아니라 **학습 창 안에서** 단조 감소한다. *"피쳐가 부족해서 안 됐다"* 의 반대 증거다.

## N3. 세 세트 최종 — 전부 실패

| 세트 | 팔 | 선택 | VAL AUC [CI] | OOS AUC [CI] | 귀무 p95 |
|---|---|---|---|---|---|
| ① 199 | hard | atr(1) | 0.6053 [0.519, 0.693] | **0.4401** [0.332, 0.559] | 0.598/0.580 |
| ① 199 | three | atr(1) | 0.5505 | **0.4452** | 0.547/0.543 |
| ① 199 | wbin | perm20 | 0.4959 | 0.5389 | 0.546/0.538 |
| ② 150 | hard | perm20 | **0.4457** | 0.4749 | 0.549/0.599 |
| ② 150 | three | perm40 | 0.5261 | 0.5104 | 0.583/0.597 |
| ② 150 | wbin | all150 | 0.5247 | 0.5254 | 0.517/0.547 |
| ③ 카테고리 | 3팔 × 10셀 | — | — | — | **통과 0/30** |

②에서 hard 의 TRAIN CV 는 0.5922(전 실험 최고)인데 VAL 0.446 / OOS 0.475 다 — **CV 와 밖의 괴리**가 크다.

## N4. ⭐카테고리 축 순위가 창 사이에 이월되지 않는다

| 팔 | VAL 상위 3축 | OOS 상위 3축 | 겹침 | ρ(VAL, OOS) |
|---|---|---|---|---|
| hard | 변동성 · 교차자산 · 구조 | 변동성 · 구조 · 분수차분 | 2/3 | +0.310 |
| three | 변동성 · 교차자산 · 기타 | **추세/모멘텀 · 기타 · 포지셔닝** | 1/3 | **−0.333** |
| wbin | 변동성 · 교차자산 · 포지셔닝 | 변동성 · 구조 · 스프레드 | 1/3 | +0.143 |

가장 자주 상위인 것은 **변동성/레인지**(hard VAL 0.619 / OOS 0.564 · wbin 0.574 / 0.561)인데
귀무(0.572/0.565 · 0.544/0.532)를 못 넘거나 겨우 걸친다. 그리고 그건 **이미 아는 크기 축**이다.

⇒ **어느 축에도 방향 신호가 없다.** 전체를 넣어 "없다"만 알던 것을 축별로 분해해도 결론이 같다.


---

# 부록 O — TabICLv2 (2026-09-07)

사용자: *"tabiclv2 먼저 진행해줘"* → 설계 승인 후 실행.
`scripts/research_eth_anchor_direction_tabicl_20260907.py` · `tmp/eth_anchor_tabicl_20260907/`

## O1. 왜 TabICLv2 · 실행 조건

표본 300~48K · 컬럼 2~100 에 사전학습된 tabular foundation model(arXiv 2602.11139).
우리 TRAIN 3,237 이 정확히 그 구간이다. **HP 튜닝 불필요**를 주장하므로 그리드가 사라져
다중검정이 줄고, 부록 M 의 이월성 문제(ρ −0.937)를 우회한다.
체크포인트 `tabicl-classifier-v2-20260212.ckpt` 는 **토큰 없이** HF 에서 받아진다
(TabPFN 8.5.0 은 라이선스 토큰이 없어 로컬에서 못 돌린다 — 이 차이가 결정적이었다).
환경: `quant_ai`(torch 2.11, **CPU 전용**). fit+predict n_estimators=2 약 19초 · 8 약 53초.
⚠️`fit(X, y)` 에 **sample_weight 가 없다** → wbin 의 `w_final` 가중을 못 썼다(그 사실을 명시).

## O2. ⭐양성대조 — TabICLv2 가 처음으로 `atr_pct` 를 넘었다

크기 라벨(`range_pct > 2.00%`), dirtop40, VAL:

| 모델 | VAL AUC |
|---|---|
| HGB 39/150피쳐 | 0.650 |
| raw `atr_pct` (학습 없음) | 0.708 |
| **TabICLv2** (n_est 2 / 8) | **0.7581 / 0.7600** |

**모델 선택 자체는 실제 개선이다.** 지금까지 "모델이 단일 피쳐보다 못하다"였는데 처음 뒤집혔다.

## O3. 27셀 — HGB 를 21/27 에서 넘지만

| 팔 | 최고 셀 | VAL AUC [CI] | OOS AUC [CI] | HGB 기준선 |
|---|---|---|---|---|
| hard | perm40 | 0.6043 [0.522, 0.682] | 0.5455 [0.418, 0.657] | 0.446 / 0.475 |
| three | perm80 | 0.6189 [0.533, 0.705] | 0.5287 [0.413, 0.632] | 0.526 / 0.510 |
| **wbin** | **dirtop80** | **0.5697 [0.513, 0.623]** | **0.5628 [0.505, 0.620]** | 0.525 / 0.525 |

**CI 조건(두 창 하한 > 0.5)을 통과한 것은 `wbin/dirtop80` 하나**뿐이다 — 이 세션 처음이다.

## O4. 🔴귀무 — 21셀 후보 전부 미달, 최종 통과 0

날 블록 셔플 B=15. **작은 표본에서 귀무가 얼마나 높은지가 드러난다**:

| 팔 | 귀무 p95 범위 (VAL / OOS) |
|---|---|
| hard (OOS n=107) | 0.525~0.589 / 0.550~**0.611** |
| three (OOS n=444, 깨끗 107) | 0.532~0.587 / 0.522~**0.613** |
| wbin (OOS n=340) | 0.524~0.557 / 0.544~0.578 |

**라벨을 완전히 셔플해도 hard/three 에서 AUC 0.61 이 나온다.** 사전 등록한 검정력 한계
(hard 는 0.61 미만 못 가름)가 실측으로 확인된 셈이다.

`wbin/dirtop80`: 관측 OOS 0.5628 vs 귀무 p95 **0.5659** — **0.003 차이로 미달**.
⚠️B=15 의 p95 는 사실상 최댓값이라 귀무가 과대추정된다 → **B=60 으로 재추정**(§O5).


## O5. 귀무 재추정(B=60) — 팔별 최고 셀

B=15 의 p95 는 사실상 최댓값이라 과대추정된다 → 사전 규칙 `min(VAL,OOS)` 최대 셀에 B=60:

| 팔 | 셀 | VAL 관측 vs 귀무p95 (경험적 p) | OOS 관측 vs 귀무p95 (p) | 판정 |
|---|---|---|---|---|
| hard | perm20 | 0.5838 vs 0.5815 (0.050) | 0.5706 vs **0.6113** (0.067) | 미달 |
| **three** | **dirtop20** | **0.5995 vs 0.5844 (0.017)** | **0.5713 vs 0.5601 (0.017)** | **통과** |
| wbin | dirtop80 | 0.5697 vs 0.5395 (**0.000**) | 0.5628 vs 0.5773 (0.100) | 미달 |

⭐귀무 평균은 0.489~0.501 로 정상인데 **p95 가 0.54~0.61** 이다 — 작은 표본(깨끗 OOS n≈107)에서
셔플만으로도 그만큼 튄다. 사전 등록한 검정력 한계(hard 0.61)가 실측으로 확인됐다.

## O6. 🔴통과한 셀을 **세 번째 창**에서 확인 — 소멸

`three/dirtop20` 은 27셀 중 최고를 고른 것이므로 다중검정 보정이 필요하다:
`1−(1−0.017)^27 ≈ 0.37` — 그 자체로 유의하지 않다. 그리고 관측 0.5713 은 **사전 등록한 검정력
한계(0.61) 아래**다. 그래서 **이 셀이 한 번도 평가된 적 없는 세 번째 창**에서 확인했다:

| HOLDOUT_SPENT (2026-04-01~07-31, 깨끗 n=104) | |
|---|---|
| 관측 AUC | **0.4745** [0.366, 0.582] |
| 귀무 평균 / p95 | 0.4927 / 0.6025 |
| 경험적 p | **0.575** (40회 중 23회) |

**완전히 우연 수준이다.** VAL·OOS 의 p=0.017 은 재현되지 않는다.

⇒ **TabICLv2 최종: 27셀 → 귀무 통과 1 → 세 번째 창에서 소멸 → 순통과 0.**
**방향 예측 14번째 기각.** 단 §O2 의 양성대조(TabICLv2 가 크기 라벨에서 HGB·raw atr_pct 를 넘음)는
유효하다 — **모델 선택은 개선됐고, 그럼에도 방향은 없다**는 것이 이번 실험의 내용이다.


---

# 부록 P — TabM 3헤드 다중과제 (2026-09-07)

사용자: *"tabm 3헤드 진행해보자"* · `scripts/research_eth_anchor_direction_tabm3head_20260907.py`

저장소 `ThreeHeadTabM` 백본 축자 차용 + 헤드 교체. **h32·L2·k32 → 파라미터 6,344**(TRAIN 3,237 의
2.0배; 저장소 기본 h192·L3·k8 은 34.4배라 못 쓴다). 헤드 A `y3` 3클래스 · B 크기 3분위 ·
C 시차 2클래스, `L = L_A + 0.4·L_B + 0.3·L_C`. 8시드 · TRAIN 내부 홀드아웃 조기종료 ·
**세 창 동시 평가**(부록 O 교훈).

| 구성 | VAL [CI] | OOS [CI] | 3번째 창 [CI] |
|---|---|---|---|
| dirtop40 **3head** | 0.5282 [0.434, 0.620] | 0.5419 [0.440, 0.642] | 0.4944 [0.375, 0.611] |
| dirtop40 1head | 0.5210 | 0.5201 | 0.4963 |
| all150 3head | 0.4935 | 0.5011 | 0.4472 |
| all150 1head | 0.4844 | 0.5100 | 0.4430 |

3head 가 1head 보다 약간 낫지만 **CI 가 전부 0.5 를 포함**하고 귀무 p=0.150~0.600 으로 미달.
에폭 곡선: best_ep 중앙 **2**(3head)/6(1head), 총 에폭 15/18 — **매우 이른 조기종료**.

---

# 부록 Q — TabM **헤드별 다른 피쳐 경로** (2026-09-07)

사용자: *"각 헤드는 각자 다른 라벨데이터에 잘 맞는 피쳐만 넣어서 진행해줘"*
`scripts/research_eth_anchor_direction_tabm_perhead_20260907.py`

## Q1. 구조
```
x_A(방향 40) → in_proj_A ─┐
x_B(크기 40) → in_proj_B ─┼→ **공유 blocks** → head_A / head_B / head_C
x_C(시차 40) → in_proj_C ─┘
```
입력 투영만 과제별, **백본 몸통은 공유**. 각 손실이 자기 경로로 흐르고 역전파가 백본에서 만난다.

## Q2. ⭐헤드별 피쳐가 말해주는 것

TRAIN 시간순 2등분해 두 반쪽 부호일치 요구, 타깃별 상위 40:

| 헤드 | 최강 피쳐 (전반/후반) | 안정 피쳐 수 |
|---|---|---|
| A 방향 | `bb_width` 0.461/0.441 | 낮다 |
| B 크기 | **`parkinson_vol` 0.733/0.724** | 118/150 |
| C 시차 | **`parkinson_vol` 0.756/0.759** | 124/150 |

**겹침: A∩B 13/40(32%) · A∩C 14/40(35%) · B∩C 35/40(88%)**
⇒ 보조 두 과제는 **사실상 같은 축(변동성)** 이고, 주 과제와는 1/3만 겹친다.
다중과제가 도우려면 표현이 공유돼야 하는데, **공유할 것이 애초에 적다.**

## Q3. 결과 — 세 창 모두 미달

| 구성 | VAL [CI] | OOS [CI] | 3번째 창 [CI] |
|---|---|---|---|
| 3head(헤드별 피쳐) | 0.5259 [0.443, 0.618] | **0.4437** [0.342, 0.556] | **0.4483** [0.341, 0.550] |
| 1head(A 피쳐만) | 0.5026 | 0.4437 | 0.4472 |

귀무(날 블록 B=20): VAL p=0.600 · OOS p=0.850 · 3rd p=0.650 — **전부 우연 아래**.

## Q4. 🔴가장 선명한 과적합 서명

| | TRAIN 내부 홀드아웃 | VAL | OOS | 3번째 창 |
|---|---|---|---|---|
| 3head | **0.6181 ± 0.0320** | 0.5259 | 0.4437 | 0.4483 |
| 1head | **0.6379 ± 0.0313** | 0.5026 | 0.4437 | 0.4472 |

**TRAIN 내부에서는 0.62~0.64 가 나오는데 밖으로 나가면 0.44~0.53 이다.** 조기종료를 TRAIN 내부
홀드아웃으로 걸었는데도 그렇다 — 그 홀드아웃마저 TRAIN 분포 안이기 때문이다.
이 격차(0.19)가 이 세션 전체에서 가장 큰 안팎 괴리이고, **방향 성분이 없는 데이터에 용량을 주면
무엇이 일어나는지**를 보여준다.

⇒ **방향 예측 16번째 기각.** 다중과제도, 헤드별 피쳐도 답이 아니다.

---

# 부록 R — TabICLv2 심화: 방향 27셀 × 세 창 + 크기 축 정식 검정 (2026-09-07)

사용자: *"TabICLv2는 이 정도면 써도 되지 않아? 좀 더 TabICLv2를 파보자"*

부록 O 는 VAL/OOS 로 고른 **1셀만** 세 번째 창에서 봤다. 고르는 절차 자체가 우연을
만들 수 있으니, 여기서는 **27셀 전부를 세 창에서** 본다. 그리고 지금까지 양성대조로만
쓰던 **크기 축을 정식 검정**한다.
스크립트: `scripts/research_eth_anchor_tabicl_deep_20260907.py`

## R1. 방향 — 27셀 × 세 창: **0/27 통과**

세 창 = VAL(2025-09~12, n=612) · OOS(2026-01~03, n=444) · HOLDOUT_SPENT(2026-04~06, n=462).

| 팔 | 최고 셀 | VAL [CI하한] | OOS [CI하한] | 3번째 창 [CI하한] | min3 |
|---|---|---|---|---|---|
| hard | dirtop20 | 0.5479 [0.464] | 0.5538 [0.438] | 0.5337 [0.433] | **0.5337** |
| three | perm20 | 0.6092 [0.520] | 0.5663 [0.460] | 0.5105 [0.394] | 0.5105 |
| wbin | perm20 | 0.5442 [0.488] | 0.5501 [0.488] | 0.4818 [0.422] | 0.4818 |

**세 창 모두 CI 하한 > 0.5 인 셀: 0개.** min3 최고가 0.5337 이고 그 CI 하한은 0.433 이다.

**창 간 순위 상관** ρ(VAL,OOS) = +0.293 · ρ(OOS,3rd) = +0.600 · ρ(VAL,3rd) = +0.347.
부록 M 의 TRAIN CV → OOS 이월성 ρ = **−0.937** 보다는 낫지만, 어느 창에서 고른 셀도
다른 창에서 상위를 지키지 못한다. **피쳐셋 평균 min3 순위**: dirtop20 0.492 > volrange 0.484
> perm20 0.472 > … > all150 0.421 — **피쳐가 적을수록 낫다**는 부록 N 의 패턴이 그대로 반복된다.

⇒ **방향 예측 17번째 기각.**

## R2. 크기 축 — 세 창 전부 통과 (⭐이 세션에서 통과한 유일한 축)

라벨: `range_pct > TRAIN 중앙값`(±1% 배리어 창 안의 고저 폭). 방향이 아니라 **크기**다.

| 피쳐셋 | VAL [CI하한] | OOS [CI하한] | 3번째 창 [CI하한] |
|---|---|---|---|
| dirtop40 | 0.7598 [0.708] | 0.7925 [0.736] | 0.7273 [0.667] |
| perm40 | 0.7623 [0.713] | 0.8017 [0.751] | 0.7456 [0.693] |
| volrange | 0.7601 [0.712] | 0.7924 [0.737] | 0.7345 [0.672] |
| **all150** | **0.7740 [0.724]** | **0.8155 [0.768]** | **0.7548 [0.696]** |

날 블록 셔플 귀무(B=15, all150): 세 창 모두 **p = 0.000** (관측 0.774/0.816/0.755 vs 귀무 p95 0.619/0.679/0.642).

## R3. ⚠️정정 — "atr 단독 대비 +0.26" 은 부풀려진 값이다

스크립트가 찍은 `atr 단독` 행(VAL 0.5136 · OOS 0.5410 · 3rd 0.5369)은 **TabICLv2 에
1컬럼만 넣은** 값이다. TabICLv2 의 사전학습 범위는 **2~100 컬럼**이므로 1컬럼 입력은
그 하한 아래이고, 모델이 제 성능을 못 낸다. 이 값을 기준선으로 쓰면 증분이 과대평가된다.

올바른 기준선은 **학습이 필요 없는 raw `atr_pct` 랭킹**이다:

| 창 | all150 (TabICLv2) | raw `atr_pct` | 실제 증분 |
|---|---|---|---|
| VAL | 0.7740 | 0.7576 | **+0.0164** |
| OOS | 0.8155 | 0.7899 | **+0.0256** |
| 3번째 창 | 0.7548 | 0.7430 | **+0.0118** |

**스크립트 출력의 +0.2604/+0.2744/+0.2179 는 무효다. 실제 증분은 +0.012~+0.026.**

### 이 증분이 실전에서 의미가 있는가 — 아니다

배포 사이징은 **이미 `atr_pct` 를 쓴다**: `notional = margin_th / (5 · atr_pct)`, `margin_th = 0.004`.
즉 raw `atr_pct` 가 현행 배포의 기준선이고, 150피쳐 TabICLv2 를 얹어 얻는 것은
**AUC +0.012~0.026** 이다. 부록 K 의 규율(자본구조는 AUC 가 아니라 **로그성장 + MDD** 로,
그것도 **크기 매칭 짝비교**로 판정)에 따르면 이 폭은 사이징 규칙 교체를 정당화하지 못한다.

⇒ 크기 축은 **통계적으로는 살아 있고**(세 창 CI + 귀무 통과) **실전 증분은 미미하다**.
「앵커 이후 큰 변동성이 온다」는 예측은 되지만, 그 대부분을 이미 `atr_pct` 한 줄이 하고 있다.

## R4. 남는 것

| 산출물 | 상태 |
|---|---|
| 방향(지속/되돌림) 예측 | ❌ 17가지 기각 |
| 크기 예측 (세 창 통과) | ⚠️ 통과하나 `atr_pct` 대비 증분 +0.012~0.026 |
| 앵커 자체의 진폭 기여 | ✅ ATR 매칭 무작위봉 대비 **+17~22%** |
| 앵커 자체의 극점 기여 | ✅ P(48봉 저점이 여기) 0.13 → **0.27** |

⇒ **앵커는 실재한다. 방향은 없다. 크기는 이미 `atr_pct` 가 갖고 있다.**

---

# 부록 S — 월 1회 재학습 walk-forward: 「낡아서인가 정보가 없어서인가」 (2026-09-07)

사용자: *"학습할 때 모든 라벨 데이터를 가지고 진행하는거야? 아니면 실제 데이터 24년 1월부터
5분봉 하나하나 지나가면서 진행하는거야?"* → *"제안해준대로 진행해줘"*

지금까지 방향 축 17가지 기각은 전부 **고정분할**이었다: TRAIN(~2025-08)으로 한 번 학습한
모델이 세 창을 본다. 3번째 창으로 갈수록 나빠지는 패턴이 **(a) 모델이 낡아서**인지
**(b) 정보가 없어서**인지를 가른다.
스크립트: `scripts/research_eth_anchor_direction_walkforward_20260907.py`

## S1. 설계
- 2025-09 ~ 2026-06 **10개월**, 각 월 시작 시점에 그때까지의 앵커로 **재학습** 후 그 달만 예측
- 엠바고 4시간 (라벨 지평 H=48봉이 평가 월로 새지 않도록 `ts < M_start - 4h`)
- 다중검정 통제: 27셀을 다시 훑지 않고 **팔별 min3 최고 1셀씩만** 사전 지정
- P1 크기축(all150)을 **같은 walk-forward** 로 함께 돌려 구현 정상성을 확인

## S2. 결과 — 월간 재학습이 아무것도 바꾸지 않는다

| 팔 | VAL WF (고정, Δ) | OOS WF (고정, Δ) | 3번째 창 WF (고정, Δ) |
|---|---|---|---|
| hard/dirtop20 | 0.5538 (0.5479, +0.0059) | 0.5419 (0.5538, −0.0119) | 0.5397 (0.5337, +0.0060) |
| three/perm20 | 0.6120 (0.6092, +0.0028) | 0.5477 (0.5663, −0.0186) | 0.4970 (0.5105, −0.0135) |
| wbin/perm20 | 0.5381 (0.5442, −0.0061) | 0.5606 (0.5501, +0.0105) | 0.4840 (0.4818, +0.0022) |

**창별 Δ 평균: VAL +0.0008 · OOS −0.0067 · 3rd −0.0018.**
9개 창-셀 중 날 블록 귀무 p95 를 넘은 건 1개(wbin OOS 0.5606 vs 0.5552) — 우연 기대치 그대로.
**세 창 CI+귀무 통과 0/3.**

## S3. ⭐월별 곡선이 가장 선명하다

```
hard/dirtop20   0.57 0.52 0.51 0.57 0.67 0.63 0.37 0.55 0.46 0.49
three/perm20    0.58 0.50 0.58 0.77 0.55 0.70 0.51 0.49 0.42 0.51
wbin/perm20     0.55 0.49 0.49 0.65 0.64 0.52 0.52 0.48 0.48 0.49
size/all150     0.67 0.76 0.77 0.81 0.83 0.81 0.76 0.66 0.69 0.83
```
방향은 0.37~0.77 을 **무작위로 튄다** -- 낡아가는 단조 하락이 아니라 잡음이다.
크기는 열 달 내내 0.66~0.83 으로 안정적이다.

## S4. P1 크기축이 같은 구현으로 통과 → 방향 실패는 walk-forward 버그가 아니다

| 창 | 크기 WF | 고정분할 | raw `atr_pct` | 귀무 p95 |
|---|---|---|---|---|
| VAL | 0.7745 [0.726] | 0.7740 | 0.7576 | 0.5717 |
| OOS | 0.8199 [0.771] | 0.8155 | 0.7899 | 0.5775 |
| 3번째 창 | 0.7604 [0.704] | 0.7548 | 0.7430 | 0.5749 |

`atr_pct` 대비 증분 +0.017/+0.030/+0.017 -- 부록 R 의 고정분할 값(+0.016/+0.026/+0.012)과 같은 크기.

⇒ **「모델이 낡아서」는 확정 기각. 방향 예측 18번째 기각.**

---

# 부록 T — 추세 축(DeMarker 연속값 + 레짐 공식) · 8종 오실레이터 코어 (2026-09-07)

사용자: *"DeMarker 지표와 레짐을 피쳐로 넣는건 어때? 이게 추세를 읽기 좋아"*
       *"DeMarker 증거신호 말고 순수 DeMarker 지표 자체를 말한거야"*
       *"복합 오실레이터 신호에서도 뽑아낼 수 있는 순수 지표가 있나?"*

## T1. 왜 이게 진짜 빈틈이었나
`demarker_extreme` 과 `kalman_deviation_meanrev` 는 **앵커를 이루는 8종 안에** 있다.
지금까지 쓴 건 "DeMarker 가 0.90/0.10 을 쳤다"는 **이진 발동**뿐이고 그건 앵커 정의에
이미 흡수돼 있다. **오실레이터 연속값은 한 번도 피쳐가 된 적이 없다.**
features154 확인: `demark` 0개 · `kalman` 0개 · `delta_z`/`ret3_z`/`p_fast`/`p_slow` 0개.

## T2. 🔴배포 레짐 분류기는 쓸 수 없다
`tmp/eth_regime_s12k3_20260902/model.joblib` 의 `train_range` 가
**2024-01-01 ~ 2026-06-30** 으로 이 데이터셋 전 구간(~2026-06-29)을 덮는다.
그 모델의 `predict_proba` 를 세 창에 얹으면 학습 구간을 다시 채점하는 꼴이다.
⇒ 대신 **라벨 공식을 직접 계산**했다(er12/er24/net24/slope12 + K=3 확정, 임계값은 원래
TRAIN 에서만 보정된 값). 학습 없음, 누수 0, 3클래스로 뭉개기 전 연속값도 함께 낸다.

## T3. 두 블록 (31피쳐) — 검증 통과
| 블록 | 내용 | 검증 |
|---|---|---|
| 추세 16 | dem14/28/56 연속값·기울기, er12/er24, net24, slope12, 확정레짐, 레짐지속봉 | 인과성 재구성 PASS (오차 0.00e+00) · 기존 DC 추세 피쳐·atr_pct 와 \|r\|>0.5 **없음** |
| 오실 15 | p_fast/p_slow(0~1), delta_z, ret3_z, kalman_dev_z, funding_z, **fib 확장배수**, 스윕 깊이/되찾기 | **파리티 PASS** (8종 12갈래 재구성 불일치 0) · 인과성 PASS (0.00e+00) |

⭐fib 확장배수는 라이브가 계산해 놓고 0.272~0.618 구간으로 **이진화하며 버리던 값**이다.
⭐파리티 검사 설계: 내 연속 코어에서 8종 불리언을 되만들어 라이브 `compute_signals()` 출력과
비트 단위 대조. 전사 오류를 잡는다.

## T4. 원시 단일피쳐 리프트 — 세 창 일관 상승 0/31
세 창 모두 >0.5 인 피쳐 없음. 반대쪽으로 `slope12_al`·`net24_al`·`er24` 가 **네 창 모두
0.5 아래**로 일관(뒤집으면 0.545~0.586) -- "지속 방향으로 이미 추세가 났으면 지속 확률이
낮다"는 소진 해석과 맞지만, 31피쳐 스크린에서 서로 상관된 2~3개가 부호 일관인 건
**우연 기대치 수준**이다.

## T5. 모델 검정 — 세 창 CI 통과 0/9 · 귀무 통과 0셀, 그리고 **일부는 유의하게 해롭다**

`dirtop20` 대비 같은 날 표집 짝비교 증분:

| 팔/셋 | VAL Δ | OOS Δ | 3번째 창 Δ |
|---|---|---|---|
| three/new32 | **−0.1046 [−0.183, −0.025]** 🔴 | −0.0086 [−0.145, +0.139] | +0.0120 [−0.097, +0.124] |
| three/top20+new32 | **−0.0555 [−0.113, −0.003]** 🔴 | +0.0065 [−0.083, +0.100] | +0.0367 [−0.039, +0.115] |
| wbin/new32 | +0.0041 [−0.053, +0.064] | +0.0125 [−0.060, +0.082] | +0.0325 [−0.043, +0.111] |
| hard/top20+new32 | −0.0414 [−0.105, +0.022] | +0.0452 [−0.050, +0.146] | +0.0259 [−0.068, +0.112] |

양수 증분은 **전부 CI 가 0을 포함**하고, 음수 둘은 CI 상한이 0 아래다.

⇒ **방향 예측 19번째 기각.** 이 축은 "아직 안 넣어봐서 모르는 것"이 아니라
**넣어보니 없는 것**이었다. 리스크 격리 설계 덕에 `features154.parquet` 는 무손상이다
(새 피쳐는 별도 parquet 2개, 합치는 건 평가 스크립트 런타임에만).

---

# 부록 U — 경로(시퀀스) 가설: 지연 블록 → MASHT (2026-09-07)

사용자: *"지금 현재 학습 과정이 라벨 데이터의 입력피쳐가 앵커지점 딱 1개 5분봉 입력
       피쳐들로 학습하는건가?"* → *"시퀀스로 먹일 순 없나?"*
       → *"TabICL 모델에서 시퀀스 모델을 잘 먹이는 방법에 대해 외부 문헌 조사해줘"*
       → *"MASHT를 적용해야하는데"* → *"서버로 gpu 로 돌려줘"*

## U1. 현재 표현이 무엇이었나
학습 행렬 `(4755 앵커, 150 피쳐)` -- **앵커당 1행**, 인접 앵커 간격 중앙 3시간 30분
(5분 간격 인접쌍 0.17%). 과거는 롤링 요약으로만 들어간다: 1~12봉 13개 · 48봉 10개 ·
96봉 4개 · 288봉 14개. **접근 경로의 모양**은 어떤 피쳐도 담지 않았다.

## U2. 1단계 지연 블록 -- 경로 기여 **0/3 전 팔 전 셋**
`build_eth_anchor_lag_features_20260907.py` / `research_eth_anchor_lag_eval_20260907.py`
TRAIN-only 순위 상위 8피쳐 × `t,t-1,t-3,t-6,t-12,t-24` → 레벨 48열 + 차분 40열.

`t0_only`(같은 8피쳐를 t 에서만) 대비 짝비교 증분:

| 팔 | lag_level | lag_delta | lag_all |
|---|---|---|---|
| hard | VAL −0.0823 · OOS −0.0269 · 3rd +0.0079 | −0.0168 · −0.0297 · +0.0450 | −0.0397 · −0.0283 · +0.0229 |
| three | −0.0385 · −0.0427 · −0.0450 | −0.0173 · −0.0323 · −0.0135 | −0.0366 · −0.0237 · −0.0326 |
| wbin | −0.0085 · −0.0276 · +0.0138 | −0.0031 · −0.0186 · +0.0010 | −0.0021 · −0.0315 · +0.0104 |

**15개 창-셀 중 CI 하한 > 0 인 것 0개.** 세 창 CI 통과 0/12.
사전 규칙(`두 창 이상 CI>0 이어야 2단계`)에 따라 `build_sequence_model = False`.

## U3. 문헌 조사 — CNN 보다 나은 길이 있다
- **TabPFN-TS** (arXiv 2501.02945): 시퀀스를 안 먹이고 **경량 시간 피쳐**(달력·sin/cos·
  러닝인덱스)만 만들어 표로 넣는다. 11M 파라미터로 65배 큰 Chronos-Large 를 따라잡음.
  단 이건 *예측* 문제이고 "언제인가"를 담는다 -- 우리의 "경로 모양"과는 다르다.
- **MASHT** (arXiv 2607.19234): **MultiRocket + Hydra → TabPFN in-context**.
  UTF-112 평균정확도 0.892 로 HIVE-COTE 2.0(0.891) 동률, MR-Hydra 대비 65/112 승.
  PCA·피쳐선택 없이 표본수 기반 예산만 쓴다(<1,000 → 10,000피쳐, <100,000 → 2,000).
  같은 벤치마크 상위 4위가 전부 랜덤 합성곱 계열, catch22 는 7.2위 --
  **이 규모에서는 학습된 CNN 보다 고정 랜덤 커널이 낫다.**
- 🔴 그대로 못 옮기는 이유: **TabICLv2 는 최대 100피쳐로 사전학습**됐다(TabPFN 은 2,000).
  2,784피쳐를 TabICLv2 에 넣는 건 부록 R 에서 잡은 "1컬럼 하한 위반"의 반대편 오류다.
  ⇒ 사용자 승인으로 **TabPFN 8.5.0** 사용.

## U4. MASHT 적용 -- 설계
창 `(4755, 8채널, 48봉)`, `[t-47, t]` 로 **앵커 봉 포함해 끝남**.
채널: logret · path_atr(ATR정규화 경로) · dem14 · p_fast · delta_z · ret3_z ·
kalman_dev_z · hl_range. 앞 7개 측면정렬(`cont_sign = +1 top / -1 bottom`), 마지막은 크기.
**미래참조 트립와이어**: `path_atr[t]` 는 정의상 0 이어야 한다 → 표본 200개 전부 0, PASS.
결측봉 있는 인스턴스 제외(4,743/4,755 유효).

변환: MultiRocket `n_kernels=252` → 2,016열 + Hydra `n_groups=16` → 768열 = **2,784열**.
⚠️논문 예산은 2,000(각 1,000)인데 aeon 입도상 불가(`n_kernels<252` 는 내부 `n_kernels//84`
구조로 ZeroDivisionError). 2,784 가 가장 가까운 실현 가능점이고 무작위 부분표집으로
억지로 맞추지 않았다.
⚠️TabPFN 8.5.0 은 200열 초과 시 추정기마다 **상위150 중요피쳐 + 무작위 채움** 앙상블을
자동 적용한다(`FEATURE_SUBSAMPLING_IMPORTANCE_TOP_K_COUNT="auto"`) -- 논문의 "축소 없음"과 다름.

## U5. 결과 — 세 창 CI 통과 **0/9** · 증분 두 창 이상 CI>0 **0개**

| 팔 | masht2784 | dirtop20 | masht+top20 |
|---|---|---|---|
| hard | 0.4959 | 0.4928 | 0.4636 |
| three | 0.4914 | 0.4843 | 0.4839 |
| wbin | **0.5325** | 0.4699 | 0.4994 |

(세 창 중 최악 min3)

증분(`dirtop20` 대비 같은 날 표집 짝비교) 18개 창-셀 중 CI 하한>0 은 **0개**.
유일하게 0을 포함하지 않는 건 음수쪽이다: `hard/masht+top20` 3번째 창 **Δ−0.1402 [−0.269, −0.013]**.

## U6. ⭐P1 양성대조 통과 — 이게 결론을 결정적으로 만든다

라벨만 크기(`range_pct > TRAIN 중앙값`)로 바꿔 **같은 MASHT 피쳐로** 돌린 결과:

| 창 | MASHT | raw `atr_pct` |
|---|---|---|
| VAL | 0.7597 [0.713] | 0.7576 |
| OOS | 0.7811 [0.718] | 0.7899 |
| 3번째 창 | 0.7315 [0.670] | 0.7430 |

**48봉 창에서 MultiRocket+Hydra 가 크기를 `atr_pct` 와 같은 수준으로 복원한다.**
⇒ 파이프라인은 정상이고 경로 창에 실제 정보가 있다. **없는 건 방향뿐이다.**

귀무(날 블록 B=8, min3 최고 `wbin/masht2784`): VAL p=0.000 · OOS p=0.000 이나
3번째 창 p=0.250 → 최종 ❌.

⇒ **방향 예측 20번째 기각.** 표현(representation)이 문제가 아니었다.
단일 봉 → 지연 → 랜덤 합성곱 경로 인코딩까지 갔는데, **크기는 매번 나오고 방향은 매번 안 나온다.**

---

# 부록 V — 현행 모델 MASHT 교체 · V자 라벨 분해 · 크기 축 재검정 (2026-09-07)

사용자: *"기존에 쓰던 증거신호와 v자 급등락과 v자반등자동매매 모델들을 TabPFN/MASHT로 바꿔서 테스트"*
       → *"V자 급등락이 곧 되돌림 아니야?"* → *"MASHT도 크기를 잘 맞추지 않았어?"*

## V1. MASHT 교체 — 8과제 전부 **현행 우세 또는 동등**, 우세 0/8

현행 8종은 **이미 TabPFN 을 쓴다**. 바뀌는 것은 피쳐 표현이다.
  A 현행 = 발동 봉의 배포 피쳐 22~24개 · B MASHT = 48봉×8채널 → 2,784열 · C = A+B
같은 모집단·라벨·분할, 판정은 **A 대비 같은 날 표집 짝비교 CI**.

| 과제 | A mean3 | B mean3 | ΔB−A (VAL/OOS/3rd) | 판정 |
|---|---|---|---|---|
| taker_delta_climax | 0.6486 | 0.6268 | −0.011 / −0.028 / −0.027 | 동등 |
| short_term_return_z | — | — | −0.028 / −0.011 / +0.011 | 동등 |
| liquidity_sweep | — | — | −0.042 / −0.009 / −0.040 | 동등 |
| orthogonal_combo | — | — | −0.044 / +0.055 / −0.018 | 동등 |
| smt_divergence | — | — | −0.007 / −0.018 / −0.034 | 동등 |
| fib_extension | — | — | −0.057 / +0.049 / −0.045 | 동등 |
| dalton_rule2 | 0.5996 | 0.5681 | −0.028 / −0.015 / −0.030 | 동등 |
| **v_rebound(V자급등락)** | **0.6706** | 0.5627 | **−0.083 / −0.125 / −0.110** | 🔴**열세** |

**두 창 이상 우세 0/8.** V자는 세 창 모두 CI 상한이 0 아래로 **유의하게 나쁘다**(n=13,815, TRAIN 9,158 —
검정력 부족이 아니다). 합침(C)도 회복하지 못한다.
⇒ **손으로 만든 22~24개가 랜덤 합성곱 2,784열을 이긴다.**

### ⚠️하네스가 세 번 틀렸다 (기록)
1. `move_atr_mult`(=라벨 정의 그 자체) 누수로 A팔 AUC **1.0000**(3과제). 이 저장소 재발 함정이다.
2. 🔴**가드 순서 버그** -- `Xa` 를 가드 **앞에서** 만들어 "제외했다"고 **출력하면서 그대로 넣었다**.
   헤더 피쳐수 23 vs 열수 A 24 불일치가 유일한 단서였다. **로그만 보면 정상으로 보인다.**
3. 임계(0.95)를 통과하는 미래파생이 남는다 -- `mae_atr_mult`(실현 최대역행폭) 0.8502.
⇒ 최종 해법은 임계가 아니라 **A팔을 배포 피쳐 목록으로 고정**하는 것이었다. 그 목록 23개에는
   미래파생이 하나도 없음을 교차확인했다. 부수 결함 2건: 파일명 불일치(2과제 누락),
   **tz-aware 타임스탬프로 V자 창 0개**(가장 중요한 과제가 조용히 빠져 있었다).

### ✅배포 증거신호는 무효가 아니다
동결 컨텍스트 CSV 에는 `move_atr_mult` 가 실려 있지만 **k 임계값 검증용 진단 컬럼**이고,
라이브는 `row[feature_cols]` 로 **명시 목록만** 뽑는다. 8종 전부 그 목록에 미래파생 없음(확인 완료).
누수는 "숫자 컬럼 전부"를 넣은 **내 하네스의 결함**이었다.

## V2. V자 급등락은 되돌림인가 — **방향은 맞지만 맞히는 건 크기다**

라벨 원문: `label = int(move >= 1.5*atr and confirmed)`, `move` 는 되돌림 방향.
`label 0` 은 *"continuation, slow/partial reaction, chop"* 을 **한 덩어리로** 묶는다.

같은 모집단(스윕 14,259)·같은 배포 피쳐 22개, 라벨만 교체:

| 라벨 | 무엇을 묻나 | mean3 |
|---|---|---|
| L0 원본 | 되돌림이 1.5ATR 이상 왔나 | 0.6699 |
| **S 크기** | 양방향 중 **뭐라도** 1.5ATR 움직였나 | **0.7708** |
| D 방향(극점) | 크기 통제 후 되돌림 쪽? | 0.7191 |
| **D2 방향(종가)** | 같은 것, **대칭 기준점** | **0.5277** |

**파리티 PASS** -- 재계산 `reb` 가 저장값과 2.84e-14 일치, 원본 라벨 재현 일치율 **1.0000**.
⚠️D(극점) 0.7191 은 신호가 아니라 **측정 비대칭**이다. 스윕 정의가 "저가가 레벨 아래로 갔다가
**종가는 위에서 마감**" 이라 사건에 이미 되돌림이 들어 있고, 극점에서 재면 reb 가 구조적으로 크다
(양성률 **83.5%**). 중립 기준점(종가)으로 대칭으로 재면 양성률 42.9%, AUC **0.5277**.

⇒ 크기(0.771) > 원본(0.670) > 방향(0.528). **크기 라벨이 원본보다 더 잘 맞는다.**
   이 저장소의 V자반등 최종 판정("매매 엣지 없음 / 재량 지표로만 유효")과 앞뒤가 맞는다.

### ⭐앵커 축과 숫자가 겹친다
| | 크기 | 방향 |
|---|---|---|
| 앵커(4,755) | 0.77 | 0.54 |
| V자 스윕(14,259) | 0.77 | 0.53 |
모집단·지평·라벨이 전부 다른데 같은 값이 나온다 -- 앵커 방향 실패가 모집단/지평 탓이 아니라는 방증.

## V3. 🔴크기 축 재검정 — 대부분이 ATR 이었다

앵커 크기 라벨은 `range_pct > 중앙값` 으로 **ATR 정규화가 없다**. 정규화 여부로 갈라 보면:

| 라벨 | atr 단독 | 전체 150피쳐+TabPFN | 모델 증분 |
|---|---|---|---|
| `S_raw` (range_pct) | **0.7635** | 0.7959 | **+0.032** |
| `S_norm` (range/atr) | 0.3352 → 뒤집으면 0.6648 | 0.7305 | +0.066 |
| V자 `S` (move ≥1.5·atr) | 0.37~0.42 → 뒤집으면 0.59~0.63 | 0.7708 | **+0.15~0.18** |

⚠️AUC 0.335/0.41 은 "정보 없음"이 아니라 **부호가 반대**일 뿐이다(ATR 이 분모라 당연).
"atr 단독이 0.5 아래니 변동성 지속성이 아니다"는 **잘못된 읽기이고, 이 세션에서 한 번 그 실수를 했다**.

⇒ **세션 내내 "살아남은 유일한 축"이라고 부른 앵커 크기는 대부분 변동성 지속성이었다.**
   배포 사이징이 이미 `atr_pct` 를 쓰므로 새로 얻을 것이 거의 없다는 판단이 오히려 강해진다.
⇒ 반대로 **ATR 정규화된 단기 크기**(V자 S 형태, 15분 안 1.5×ATR)가 이번 세션에서 본 것 중
   **모델 증분이 가장 큰 타깃**이다(+0.15~0.18). 방향은 여전히 모르므로 단독 매매 규칙은 못 되고,
   진입 타이밍/필터로 방향 규칙과 곱해져야 의미가 생긴다.

---

# 부록 W — 🔴단기 지평 "발견" 철회: `local_extreme` 모집단 선택 룩어헤드 (2026-09-08)

사용자: *"V자 급등락이 곧 되돌림 아니야?"* → *"52%면 쓸만하지 않아?"* → *"1~4번 돌려줘"*
       → *"앵커 방향의 짧은 horizon은 결과가 어떻게 된거지?"*

## W1. 무엇을 발견했다고 생각했나
멀티트리거 매봉 모집단(66,382)에서 지평을 바꾸니:
    종가진입·30분 0.7381 · 다음봉시가·30분 0.7368 · **다음봉시가·4시간 0.5590**
앵커 축은 전부 H=48(4시간)이었고 0.53~0.56이었다. "지평이 전부였다 -- 짧은 지평은 한 번도
안 봤다"로 읽었다. first-touch(거래 가능 형태)로 바꿔도 **0.7366**(세 창 CI 하한 >0.5)이었고
비용 후 상위30% **+13.6/+16.8/+8.5bp**('항상 되돌림' −0.5/+1.9/−2.3bp)였다.

**대조군 3종이 전부 통과했다**: 뒤집기 합 1.0021(정확한 대칭) · 날블록 귀무 p95 0.54~0.57 ·
**무작위 direction 할당 0.5107/0.5048/0.4952**(동전으로 붕괴).

## W2. 감사 4종 -- 3번이 뒤집었다
| # | 검정 | 결과 |
|---|---|---|
| 1 | `atr` 인과성 | ✅ PASS. 봉 i 까지 인과 재계산과 최대상대오차 **2.5e-16**, ±1봉 밀면 0.13% 일치 |
| 2 | 배포 라벨과 겹침 | 예측 상관 +0.21 · L0 모델은 같은 라벨에서 0.55~0.58 (증분 +0.16~0.19) |
| 3 | **배리어 폭 가설** | 🔴**기각**. 앵커를 ±1.5×ATR 로 바꿔도 30분 **0.5134** · 4시간 **0.5126** |
| 4 | 청산 구조 | 트레일링이 브래킷보다 좋게 나왔으나 오염된 예측 기준이라 무효 |

3번이 결정적이었다 -- **똑같은 라벨 형태를 앵커 모집단에 걸면 0.51**이다. 배리어도 지평도 아니었다.

## W3. 🔴원인 -- `local_extreme` 모집단 선택 룩어헤드

모집단을 좁혀가며(라벨·피쳐 고정, first-touch ±1.5ATR H=6):
| 모집단 | n | 되돌림 양성률 | mean AUC |
|---|---|---|---|
| 전체 멀티트리거 | 43,129 | 0.633 | 0.7339 |
| n_triggers≥3 | 8,183 | 0.623 | 0.7034 |
| **앵커(any3/Wc3) 타임스탬프만** | 3,637 | **0.482** | **0.5251** |
| 전체 · TRAIN 2,500 축소 | 18,132 | 0.641 | 0.7324 |
표본 크기가 아니다(TRAIN 2,500 으로 줄여도 0.7324).

GAP-12 dedup 을 걸면 **올라간다**(0.734 → 0.8298) -- 중복 희석 가설도 틀렸다. 그리고:
| 팔 | n | 되돌림 양성률 | mean AUC |
|---|---|---|---|
| A 전체 | 18,566 | 0.731 | 0.8298 |
| **B `local_extreme` 포함 행만** | 15,923 | **0.983** | **0.9656** |
| C `local_extreme` 제외(8종만) | 8,542 | 0.358 | 0.6943 |

**B 에서 되돌림이 98.3% 이긴다. 예측이 아니라 정의다.**
`local_extreme_flags()` 는 `for i in range(W, n-W)`, **W=6=30분** -- **전방 6봉**을 봐서 국소 극점을
표시한다. 그리고 내 라벨 창이 정확히 그 6봉이다. 국소 저점이면 정의상 다음 30분에 새 저점을
못 만드니 반대쪽 배리어가 먼저 닿는다. **완전한 순환 논리다.**
멀티트리거 행의 **45.6%가 `local_extreme` 포함, 29.8%가 단독**이다.

⚠️이 저장소가 이미 알고 있던 미해결 이슈다 --
`docs/homer/v_rebound_open_issues_20260901.md` / 메모리 `project_v_rebound_local_extreme_lookahead_unresolved_20260901`:
*"local_extreme 후보는 구조적으로 fast_mult 를 올린다 -- 정의상 다음 30분에 새 저점/고점을 만들 수 없기 때문"*

## W4. ⭐왜 대조군 3종이 못 잡았나 -- 다섯 번째 메커니즘
룩어헤드가 **라벨이 아니라 모집단 선택**에 있었다. `local_extreme` 은 어느 봉을 데이터셋에
넣을지를 미래로 골랐다. 라벨을 날 블록으로 섞어도, 방향을 뒤집어도, **direction 을 무작위로
할당해도** 그 선택은 그대로 남는다. 무작위 direction 대조군(0.495~0.511)이 통과한 것이
그 증거다 -- 그 대조군은 "방향이 진짜인가"만 묻지 "이 행들이 왜 여기 있나"는 묻지 않는다.

⇒ 호메로스 §5.21 의 A~D 분류에 **E: 모집단 선택 룩어헤드**를 추가한다.
   층별 재구성 게이트(L1 발동인과성·L2 라벨재구성)도 **모집단 구성은 안 본다**.
   체크리스트 추가: **"이 행들이 데이터셋에 들어온 기준이 미래를 보는가"** 를 라벨·피쳐와
   별도로 물을 것. 트리거 목록을 나열하고 각각의 전방 참조 여부를 확인한다.

## W5. 앵커 방향 지평×배리어 격자 -- **25셀 전부 실패**
`research_eth_anchor_direction_horizon_barrier_grid_20260907.py`
H ∈ {3,6,12,24,48}봉 × K ∈ {0.75,1.0,1.5,2.0}×ATR + ±1% 대조. 라벨 = first-touch, 지속=1.

| H | 0.75×ATR | 1.0×ATR | 1.5×ATR | 2.0×ATR | ±1%(대조) |
|---|---|---|---|---|---|
| 3 (15분) | 0.5326 | 0.5413 | 0.5335 | 0.5847 | 0.6627 |
| 6 (30분) | 0.5381 | 0.5393 | 0.5134 | 0.5353 | 0.6362 |
| 12 (1h) | 0.5335 | 0.5397 | 0.5075 | 0.5345 | 0.6103 |
| 24 (2h) | 0.5327 | 0.5387 | 0.5088 | 0.4890 | 0.5623 |
| 48 (4h) | 0.5334 | 0.5403 | 0.5126 | 0.4951 | 0.5119 |

**세 창 모두 CI 하한 > 0.5 인 셀: 0/25.**

⚠️±1% 열이 높아 보이는 것도 같은 함정이다 -- H=3 에서 4,755 중 **622개만** 해소되고
지속률이 **0.690** 으로 치우친다(H=48 은 3,474개·0.528). 모집단을 극단 사건으로 좁히면
기저율이 치우치고 AUC 가 오른다. 그리고 **세 창 중 3번째 CI 하한이 전부 0.5 아래**다
(0.436/0.426/0.445/0.453/0.390) -- VAL·OOS 만 높고 홀드아웃에서 무너진다.

## W6. 배포된 앵커 방향과의 비교
배포(MASHT·±1%·H=48·워크포워드 풀링) **0.5453**(귀무 p=0.017) vs 격자 중앙 ~0.535.
**같은 대역이고, 배포본은 87셀 중 Top-1 이라 승자의 저주가 얹혀 있다.**
"배포된 게 더 낫다"고 말할 근거가 없다 -- 차이가 선택 편향 폭 안이다.

⇒ **앵커 방향은 (DC 150 피쳐·5분봉 판정 기준으로) 지평·배리어 어느 조합에서도 동전이다.**
   오늘의 "0.74 발견"은 철회한다.

## W7. ⚠️무엇을 철회했고 무엇은 아닌가 (사용자 지적으로 명확화)
"근거가 사라졌다"를 뭉뚱그려 써서 **섀도우까지 접는 것처럼 읽혔다**. 그런 뜻이 아니다.

**격자는 배포 구성을 검정하지 않았다.**
| | 배포 섀도우 | 이 격자 |
|---|---|---|
| 피쳐 | MASHT 2,784 (MultiRocket+Hydra) | DC 150 |
| 배리어 판정 | **1분봉** first-touch | 5분봉 first-touch |
| 프로토콜 | 월간 재학습 워크포워드 | 고정분할 |
| 결과 | 0.5453 (날블록 귀무 p=0.017) | 0.49~0.66 |
격자가 말하는 것은 "DC 피쳐로는 어느 지평·배리어에서도 안 된다"이지
"MASHT/±1%/H=48 도 안 된다"가 아니다 -- **그 셀은 재보지 않았다.**
`local_extreme` 룩어헤드는 **멀티트리거 데이터셋**의 문제이고 앵커 파이프라인(`any3/Wc3`)은
그 트리거를 쓰지 않는다(8종 증거신호만).

| 항목 | 상태 |
|---|---|
| MASHT 앵커 방향 섀도우 | ✅ **유지** -- 배포 시점 근거 불변 |
| 대시보드 앵커 방향 칩 | ✅ **유지** |
| "단기 지평 방향 0.74 발견" | ❌ 철회 (local_extreme 모집단 선택 룩어헤드) |
| 그걸 전제로 한 방향/크기 병기 개편 | ❌ 보류 |
| V자 크기 모델 아티팩트 | ⚠️ 동결만 함, **미배선** -- 재검증 후 결정 |

배포 시점에 명시한 조건은 그대로다: 87셀 Top-1 이라 **기대는 56~57% 로 할인** ·
Bonferroni 0.0167 에 p=0.017 로 간발 미달 · CI 하한 53.94% 가 손익분기 53.90% 에 간신히 걸침.
격자가 더한 것은 "이 셀이 대안 대비 특별히 좋지는 않다"는 맥락이고, 이미 말한 승자의 저주
할인을 **뒷받침**할 뿐 새로운 반증이 아니다.
⭐섀도우는 **주문을 내지 않는 가상 원장**이다. 불확실한 것에 전방 증거를 쌓는 것이 존재 이유이고,
불확실하다고 끄면 영원히 판정할 수 없다. 라이브 자본을 거는 결정과 기준이 다르다.

---

# 부록 X — 🔴지평 재단과 **해소 선택 편향**: 세 지평 전부 라이브 기준 음수 (2026-09-08)

사용자: *"배포(MASHT, ±1%, H=48)에서 h를 60분으로 해서 라벨을 다시 만들어서 재학습하면?"*
       → *"이전처럼 룩어헤드 문제가 있는건 아니지?"*
       → *"왜 자꾸 H가 짧은게 좋았다가 버그가 있다가 왜 이렇게 왔다갔다 하는거야?"*

## X1. 재단 방법 (재계산 불필요)
`build_eth_anchor_direction_labels_20260907.py` 가 **1분봉 first-touch 시각(분)** 을 저장해 뒀다
(`hit_cont_min_P1`/`hit_fade_min_P1`). H 변경은 그 시각을 자르는 것뿐이고 배리어(±1%)·해상도·
진입(`open[t+1]`)은 배포와 동일하다 -- **H 만 다른 깨끗한 격리**다.
⚠️1차 시도가 `timestamp` 만으로 dedup 해 4,743 중 2,634(55.5%)만 조인됐다(사용자 지적).
   앵커의 키는 **(timestamp, side)** 다 -- 같은 봉에 바닥·천장이 둘 다 있을 수 있다. 고쳐서 100.0%.

## X2. 해소분 잣대 -- H 가 짧을수록 단조로 좋다
| H | 라벨 | 평가 | 지속률 | AUC | 상위30% 적중 | CI 하한 | 건당(해소분) | 해소율 |
|---|---|---|---|---|---|---|---|---|
| 12 (60분) | 1,809 | 580 | 0.589 | 0.6074 | 69.54% | 62.03% | +31.3bp | 38.1% |
| 24 (2h) | 2,596 | 815 | 0.550 | 0.5704 | 63.11% | 56.74% | +18.4bp | 54.7% |
| 48 (4h·배포) | 3,473 | 1,083 | 0.527 | 0.5494 | 58.64% | **53.16%** | +9.5bp | 73.2% |
손익분기 53.90%. 이 표만 보면 H=12 가 압도적이고 배포(H=48)는 유일하게 미달이다.

## X3. 🔴그러나 이 잣대는 짧은 H 에 기울어 있다 -- **해소 선택 편향**
H 를 줄이면 라벨이 붙는 행이 준다(38.1% / 54.7% / 73.2%). 그리고 **어느 앵커에 라벨이 붙는지는
미래가 결정한다**(그 창 안에 배리어를 쳤는가). 모집단(앵커)은 그대로이므로 `local_extreme` 같은
모집단 룩어헤드는 **아니다** -- 그러나 **평가 대상이 결과로 걸러진다**.

라이브는 상위30%를 **전건 진입**하고 시간청산을 받는다. 학습은 해소된 행으로 하되(배포자가 할 수
있는 최선) **예측·채점을 전 앵커에** 대고 미해소를 **시간청산 실현손익**(H봉 뒤 종가)으로 세면:

| H | 해소율 전체 / **상위30%** | 해소분 건당 | **라이브 건당** | 갭 | 시간청산 평균 |
|---|---|---|---|---|---|
| 12 | 38.2% / **17.1%** | +46.0bp | **−3.56bp** [−8.46, +0.81] | −49.6bp | −6.0bp |
| 24 | 53.7% / **29.0%** | +19.5bp | **−7.32bp** [−13.40, −1.10] | −26.8bp | −10.5bp |
| 48 | 71.3% / **54.1%** | +10.1bp | **−1.46bp** [−8.21, +5.74] | −11.5bp | −7.3bp |

⭐**모델이 해소되지 않는 앵커를 골라낸다.** 세 지평 전부 상위30%의 해소율이 전체보다 **낮다**
(17.1 vs 38.2 · 29.0 vs 53.7 · 54.1 vs 71.3). 확신이 높다고 고른 건일수록 배리어에 안 닿고
시간청산되며, 시간청산은 손실이다. 해소분만 보면 이 편향이 **전혀 안 보인다**.

⇒ **세 지평 전부 라이브 기준 음수. H 를 고를 수 없다.**
⇒ 역설적으로 **배포(H=48)가 라이브 기준 최선**이다(−1.46bp, CI 0 포함) -- 해소율이 가장 높아
   시간청산 노출이 가장 적기 때문. 해소분 잣대 최하위가 라이브 최상위가 된다.

## X4. ⭐하루에 세 번 반복한 **하나의** 오류
| 사례 | 라벨이 붙는 조건 | 증상 |
|---|---|---|
| `local_extreme`(부록 W) | 전방 6봉이 국소 극점을 확정 | 되돌림 98.3%, AUC 0.9656 |
| 25셀 격자 ±1%/H=3(부록 W5) | 15분 안에 ±1% 도달 | 4,755→622, 지속률 0.690, 3rd창 CI 0.436 |
| H 재단(여기) | H 안에 배리어 도달 | H=12 에서 62% 가 평가에서 빠짐 |
셋 다 **"빠른 사건만 남기면 기저율이 치우치고 AUC 가 오른다"** 는 같은 메커니즘이다.
매번 새 현상처럼 취급하고, 숫자를 먼저 보고한 뒤에 커버리지를 확인했다(H=12 는
+31.3 → +7.1 → −3.56bp 로 세 번 내려갔다).

### 규칙 (앞으로 모든 수치에 선행)
보고 **전에** 두 질문을 답한다:
1. 이 수치가 **거래 가능한 모집단의 몇 %** 를 덮는가
2. 그 % 가 **결과로 정해졌는가**
100% 가 아니고 결과로 정해졌으면 **라이브 수치가 아니다**. 라이브 형태로 다시 재기 전엔 보고 금지.

⚠️흔들린 것은 전부 **결과로 걸러진 부분집합**의 값이다. 전 모집단·H=48 의 방향 AUC 0.53~0.55 와
25셀 격자 0/25 는 하루 종일 움직이지 않았다.

## X5. 배포 섀도우에 대한 함의
사전등록 수치 **상위30% 59.88% / 건당 +12.0bp 는 해소분(커버리지 71.3%) 기준**이었다.
라이브 기준으로 다시 재면 **−1.46bp [−8.21, +5.74]** -- 기대치가 0 근처이고 CI 가 0을 포함한다.
"손익분기를 간신히 넘는다"던 판단은 그 커버리지 위에서였다.

**섀도우는 계속 돌린다** -- 주문을 내지 않고 원장이 `outcome: timeout` 까지 남기므로
이 질문에 전방 데이터로 답할 수 있는 유일한 장치다. 다만 판정 잣대를 고쳐야 한다:
| | 기존 | 고쳐야 할 것 |
|---|---|---|
| 판정 지표 | 상위30% 진입 **정확도** | **라이브 건당 bp**(시간청산 포함) |
| 임계 | CI 하한 > 53.90% | CI 하한 > 0bp |
| 기대치 | 56~57%(승자의 저주 할인) | **−1.46bp** |
추가 수집 없이 기존 원장으로 계산된다(사용자 결정 대기).

---

# 부록 Y — 지평×배리어 동시 조정: **해소율↔적중률 반비례**가 이 축의 값을 드러낸다 (2026-09-08)

사용자: *"horizon이 줄어들수록 배리어 간격을 줄여야 하는거 아니야?"*

## Y1. 맞는 지적이었다
부록 X 는 배리어를 ±1% 로 **고정한 채** H 만 줄여 해소율을 무너뜨렸다(H=12 는 38.1%).
가격 이탈폭이 대략 √H 로 커지므로 H 를 1/4 로 줄이면 배리어도 1/2 로 줄여야 한다.
저장된 1분봉 터치 시각에 `P0.5`/`P0.75`/`P1` 이 있어 재계산 없이 9셀 격자를 짤 수 있다.
⚠️단 공짜가 아니다 -- **손익분기가 오른다**: ±1.0% 53.90% · ±0.75% 55.20% · ±0.5% **57.80%**.

## Y2. 격자 (학습=해소행 · 채점=전 앵커 · 미해소=시간청산 실현손익 · 월간재학습)
| H | 배리어 | BE | 해소율 전체/상위30% | 해소분 적중 | 해소분bp | **라이브 건당** |
|---|---|---|---|---|---|---|
| 12 | ±0.5% | 57.80% | 73.8% / 47.5% | 57.87% | +0.1 | −7.16 [−10.41, −3.55] |
| 12 | ±0.75% | 55.20% | 53.9% / 26.8% | 65.57% | +15.6 | −5.89 [−9.42, −1.44] |
| 12 | ±1.0% | 53.90% | 38.2% / 17.1% | 76.92% | +46.0 | −3.56 [−8.10, +0.82] |
| 24 | ±0.5% | 57.80% | 85.5% / 71.4% | 52.62% | −5.2 | −7.89 [−12.09, −4.21] |
| 24 | ±0.75% | 55.20% | 69.2% / 50.3% | 58.95% | +5.6 | −5.37 [−10.70, −0.28] |
| 24 | ±1.0% | 53.90% | 53.7% / 29.0% | 63.64% | +19.5 | −7.32 [−13.28, −0.81] |
| 48 | ±0.5% | 57.80% | 94.1% / 89.5% | 50.12% | −7.7 | −7.92 [−12.04, −3.16] |
| 48 | ±0.75% | 55.20% | 83.9% / 73.4% | 52.69% | −3.8 | −6.53 [−12.24, +0.05] |
| **48** | **±1.0%** | 53.90% | 71.3% / 54.1% | 58.94% | +10.1 | **−1.46** [−7.53, +5.93] |

**라이브 건당 CI 하한 > 0 인 셀: 0/9.** 최선은 배포 구성(H=48/±1%) −1.46bp 이고 CI 가 0을 포함한다.

## Y3. ⭐이 격자가 드러낸 것 -- 해소율↔적중률이 반비례한다
| 해소율 | 해소분 적중 |
|---|---|
| 94.1% | **50.12%** |
| 85.5% | 52.62% |
| 83.9% | 52.69% |
| 73.8% | 57.87% |
| 53.9% | 65.57% |
| 38.2% | **76.92%** |

**해소되기 어려운 조건일수록 해소된 것의 적중률이 높다.** 예측력이 아니라 **선택**이다 --
빨리 한쪽으로 크게 움직인 사건만 남으면 그 방향이 맞는 것은 당연하다.
⇒ 해소율이 100% 에 가까워져 **선택이 사라지면 적중률이 50.12%(동전)로 수렴한다.**
   **그것이 이 축의 진짜 값이다.** 오늘 본 69.54% · 76.92% 는 전부 그 선택이 만든 값이다.

배리어를 줄이면 해소율은 **정확히 고쳐진다**(H=48/±0.5% 는 94.1%, 상위30% 도 89.5%로
시간청산 노출이 거의 사라진다). 그런데 그 순간 적중이 손익분기 아래로 떨어져 거래 자체가
적자가 된다(해소분 −7.7bp). **시간청산을 없앤 대가로 엣지가 사라진다.**

⇒ **지평·배리어 어느 조합으로도 양수가 없다. 선택할 것이 없다.**
   신호 빈도 확장(진입 폭·앵커 정의)은 이 위에 쌓는 것이라 착수 의미가 없어졌다.

---

## 부록 Z — 배리어/지평 확장: 앵커에는 어떤 규모에서도 방향 정보가 없다 (2026-09-08)

사용자: *"난 자러 갈테니 테스트하고 승격할만한 결과가 나올 때까지 계속 연구하고 테스트를 반복해"*

### 질문
방향 **모델**은 ~120셀 기각됐다. 그런데 모델 없이 **전건 지속 진입**의 총수익(비용 이전)은
배리어 크기에 따라 달랐다: H=12/±0.5% → +0.66bp · H=48/±1.0% → **+6.34bp**.
비용은 건당 고정 7.8bp 인데 총수익은 배리어에 비례해 커진다. ⇒ **총수익이 7.8bp 를 넘는 규모가 있는가?**

### 설계 (모델 없음, 순수 산술)
`any3/Wc3` 앵커 · 진입 `open[t+1]` · 1분봉 첫터치 · **커버리지 100%**(미터치는 H봉 뒤 종가) ·
동시터치는 보수적 손실 · 트레일링 없음(09-07 체결가정 결함 회피) ·
H ∈ {48, 96, 144, 288}봉(4/8/12/24h) × 배리어 ∈ {±1.0, 1.5, 2.0, 3.0}%.
**귀무 = 같은-측면 무작위 봉**(B=60, 앵커별 짝지음, 같은 창) — VAL/OOS 하락장 베타 제거용.
스크립트 `scripts/research_eth_anchor_barrier_horizon_extension_20260908.py` ·
산출 `tmp/eth_anchor_barrier_ext_20260908/grid.csv`.

### 결과 — **두 창(VAL·OOS) 초과 CI 하한 > 0 인 셀 0/16**

| 배리어 | VAL 초과 (H=48/96/144/288) | OOS 초과 | HOLDOUT 초과 |
|---|---|---|---|
| ±1.0% | +1.40 / +0.95 / +0.89 / +1.74 | **−3.65 / −4.58 / −3.00 / −4.66** | −4.80 / −4.82 / −5.82 / −5.76 |
| ±1.5% | **+8.73 / +10.09 / +9.63 / +11.35** | +0.22 / +0.44 / +0.30 / −3.37 | −0.97 / +2.19 / +0.67 / +3.67 |
| ±2.0% | +3.29 / +6.17 / +7.59 / +11.20 | +1.14 / +0.32 / −1.83 / −10.85 | −3.24 / −1.63 / −1.11 / −2.36 |
| ±3.0% | +0.66 / −1.53 / +1.94 / +6.90 | −5.71 / −9.02 / −10.94 / −23.38 | −3.59 / −2.58 / +2.25 / +7.40 |

- 순bp(=총−7.8) > 0 인 셀 5/48 — 전부 VAL 이거나 HOLDOUT 단일 창.
- ⭐**±1.5% 열은 VAL 에서 네 지평 모두 +8.7~11.4 로 일관**되지만 OOS 는 +0.2~−3.4. 창 내부 일관성이
  창 간 재현으로 이어지지 않는 전형적 형태다.
- ⭐**±1.0% 는 OOS·HOLDOUT 두 창 모두 초과가 음수**(−3.0~−5.8). 배포 팔이 쓰는 배리어에서
  앵커의 지속 방향은 같은-측면 무작위보다 **못하다**. (뒤집어 페이드로 읽어도 VAL 이 −1.4 라 두 창 불통과.)
- 귀무 검증: `null_short(OOS, H48, ±1%) = +5.03bp` — [[feedback_side_asymmetry_needs_same_side_null_20260905]]
  의 "하락장이라 무작위 숏도 +5bp" 가 그대로 재현됐다. 같은-측면 귀무가 없었으면 이 표는 전부 통과했을 것이다.

### 결론
**앵커 방향의 실패는 모델의 실패가 아니라 모집단의 실패다.** 배리어·지평을 4×4 로 넓혀도
총수익이 비용을 넘는 (VAL·OOS 동시) 조합이 없다. 09-06 [[eth_fade_vs_continuation_direction_closed_8forms_20260906]]
의 "방향 AUC 0.49~0.54" 는 모델이 못 배운 게 아니라 **배울 것이 없었다**는 뜻으로 확정된다.

---

## 부록 AA — 사건군 전체 종결: TRAIN(선택 밖·최대검정력)에서 45/45 셀 음수 (2026-09-08)

### 두 단계
**1) 사건 모집단 스크린 (342셀).** 앵커 5종(first_fire·any2/Wc3·any3/Wc3·any3/Wc1·any4/Wc12) ×
측면 3 × H{48,144} × 배리어{±1.0,1.5,2.0}% × 창 3. 모델 없음, 커버리지 100%.
결과: 비용을 배제한 승격 후보 **0/114셀**, 비용을 무시하고 두 창 CI 부호만 봐도 **0/114**.
스크립트 `scripts/research_eth_event_population_gross_edge_screen_20260908.py`.

⭐그런데 **바닥측만 보면** OOS 초과가 +15~23bp 로 컸다. 천장측을 같이 놓자 −7~−21 로 **거울상**.
corr(top,bottom) = −0.36(OOS 는 사실상 −1), `|top+bottom|` 6.01 < `|top−bottom|` 8.96.
⇒ 앵커 정보가 아니라 **발동 시점의 잔존 베타**다. 창 단위 무작위 봉 귀무는 사건의 시점 군집을
맞추지 못한다. 별도 기록: `feedback_side_mirror_excess_is_residual_beta_not_signal_20260908`.

**2) 양측 합산 + TRAIN 확인 (45셀).** 양측 합산이 베타 없는 통계다. 앵커 5종 × H{48,144,288} ×
배리어{±1.5,2.0,3.0}%. **TRAIN 은 셀 선택에 쓰인 적이 없다**(순수 산술 규칙이므로 진짜 확인창이고,
n 이 2,353~16,091 로 가장 강력하다).
스크립트 `scripts/research_eth_anchor_bothside_train_confirm_20260908.py`.

| 앵커 | TRAIN 순bp 범위 (n) | VAL | OOS | HOLDOUT |
|---|---|---|---|---|
| first_fire | **−7.7 ~ −10.5** (16,091) | −4.9 ~ −11.7 | −4.5 ~ −12.4 | −2.3 ~ −5.9 |
| any2/Wc3 | −7.2 ~ −9.9 (5,435) | −0.4 ~ −8.4 | −0.9 ~ −9.7 | −2.5 ~ −11.2 |
| any3/Wc3 | −7.4 ~ −11.2 (3,238) | +3.2 ~ −8.0 | −6.8 ~ −32.0 | +1.2 ~ −10.3 |
| any3/Wc1 | −8.1 ~ −12.7 (2,584) | +9.4 ~ −7.6 | −2.9 ~ −34.3 | +6.1 ~ −12.1 |
| any4/Wc12 | −9.0 ~ −14.0 (2,353) | +5.4 ~ −18.3 | +0.5 ~ −34.1 | +2.1 ~ −7.1 |

- ⭐**네 창 모두 CI 하한 > 0: 0/45. 네 창 모두 점추정만 양수여도 0/45**(우연 기대 2.8건).
- ⭐**TRAIN 은 45/45 전부 음수이고 44/45 는 CI 상한도 0 미만.** 순bp −7.2 ~ −14.0
  (비용 7.8 이므로 총수익은 **0 ~ −6bp**).
- VAL 에서 양수로 보인 셀(any3/Wc1 H288 ±2.0% +9.38 등)은 TRAIN −12.71 · OOS −16.12 로 뒤집힌다.

### 결론 (프로그램 수준)
**증거신호 발동 사건에는 어떤 앵커 정의·지평·배리어에서도 방향 정보가 없다.**
2026-09-06 의 "방향 AUC 0.49~0.54" 는 모델이 못 배운 것이 아니라 **배울 것이 없었다**는 뜻으로 확정.
단일자산 사건-방향 축은 여기서 닫힌다. 다음 축은 구조적으로 다른 것이어야 한다
(→ 부록 AB: 59종 무기한 **횡단면 롱숏**, 베타가 설계상 제거되는 첫 시도).

---


---

## 부록 AB~AK 는 **다른 문서로 이동**했다 (2026-09-08)

이 문서의 부록 Z·AA 로 ETH 단일자산 사건-방향 축이 모집단 수준에서 닫힌 뒤, 같은 날 밤
**바이낸스 무기한 60종 횡단면 롱숏**이라는 별개 프로그램을 열었다. 대상·질문·보유기간·
비용 지위·산출물이 전부 달라 같은 문서에 둘 이유가 없다.

➡️ **[xsec_perp_crowding_fade_20260908.md](xsec_perp_crowding_fade_20260908.md)**
   (부록 AB~AK: 횡단면 되돌림 · 극단이탈 · 펀딩 캐리 · 포지셔닝 메트릭 · **쏠림 페이드** 후보)

여기 남는 것은 앵커 연구의 마지막 조각뿐이다.

## 부록 AI — 앵커 손익 회귀 9/9 완주 (서버 GPU, ~4h · 2026-09-08)

이 부록의 나머지 절(쏠림 페이드 절대 유동성·수용량)은 횡단면 문서로 이동했다.

`tmp/eth_anchor_pnl_regression_20260908/regression.csv`. 커버리지 전 셀 100%.

| H | 배리어 | 해소율 | 전건 | 상위30% [일군집 CI] | ρ | 분류(참조) |
|---|---|---|---|---|---|---|
| 12 | ±0.5% | 76.8% | −7.14 | −7.04 [−10.81, −3.18] | +0.039 | −7.16 |
| 12 | ±0.75% | 55.2% | −5.68 | −1.41 [−6.66, +2.85] | +0.050 | −5.89 |
| 12 | ±1.0% | 38.1% | −5.53 | −5.57 [−11.05, −1.58] | +0.041 | −3.56 |
| 24 | ±0.5% | 88.4% | −8.08 | −9.43 [−13.22, −5.55] | +0.016 | −7.89 |
| 24 | ±0.75% | 71.5% | −6.52 | −5.34 [−11.15, +0.64] | +0.038 | −5.37 |
| 24 | ±1.0% | 54.7% | −6.65 | −0.75 [−8.11, +5.99] | +0.073 | −7.32 |
| 48 | ±0.5% | 96.0% | −7.73 | −6.83 [−11.67, −2.25] | +0.006 | −7.92 |
| 48 | ±0.75% | 85.7% | −6.52 | −4.54 [−10.60, +1.45] | +0.029 | −6.53 |
| **48** | **±1.0%** | 73.2% | −5.81 | **+5.35 [−2.98, +12.91]** | +0.056 | −1.46 |

- **CI 하한 > 0 인 셀 0/9.** 전건 진입은 9/9 음수(−5.53~−8.08).
- **ρ 가 0.006~0.073** — 회귀 예측에 순위 정보가 사실상 없다.
- 양수 점추정은 배포 셀(H=48·±1.0%)의 +5.35 하나뿐이고 CI 가 0 을 포함한다.
⇒ **회귀는 분류를 구제하지 못한다.** 부록 AA 의 모집단 결론과 일치.
(⚠️스크립트가 요약절에서 `null_p95` 를 계산 전에 참조해 예외로 끝났다 — 격자는 완주·저장됐다. 수정함.)


---

## 부록 AL — ⭐**되돌림이냐 돌파냐: 답은 되돌림이다** (관측 기반, 2026-09-08)

사용자: *"나는 지금 가진 증거신호를 이용하려면 무조건 되돌림인지 돌파인지를 알아야해"*

### 이게 부록 Z·AA 와 다른 질문이다
Z·AA 가 닫은 것은 **"앵커 봉에서 방향을 예측할 수 있는가"**(시각 t 정보만으로). TRAIN 45/45 음수.
이 부록은 **예측하지 않는다.** 앵커 후 W봉 동안 기다렸다가 가격이 먼저 어느 쪽으로 ±T 움직이는지
**관측한 뒤** 그 방향(돌파) 또는 반대(되돌림)로 진입한다. 방향이 관측값이므로 예측 스킬이 필요 없다.
남는 질문 하나: **이미 T 만큼 움직인 뒤 남은 움직임이 늦은 진입 + 비용을 덮는가.**

설계: 기준가 `open[앵커봉+1]` · 트리거 T ∈ {0.25,0.5,0.75,1.0}×atr_pct 및 고정 {0.15,0.25,0.40}% ·
탐색창 W ∈ {3,6,12,24}봉 · 진입 = 트리거 레벨 + **슬리피지 2bp 불리** · 청산 ±P(0.5/1.0%) 1분봉
첫터치, 미터치는 H=48봉 종가(**커버리지 100%**) · 비용 **12bp**(테이커 왕복 10 + 슬리피지 2) ·
트레일링 없음 · 앵커 3종 × 336셀 · TRAIN 은 셀 선택에 쓰지 않음.
`scripts/research_eth_anchor_breakout_vs_reversal_20260908.py` · `tmp/eth_anchor_breakout_20260908/`

### ⭐답: 되돌림. 네 창에서 일관되게.
발동률 0.47~1.00 (커버리지 문제 없음 — 결과로 걸러진 부분집합 아님).

**되돌림 − 돌파 격차** (총수익 격차의 2배):

| | TRAIN | VAL | OOS | HOLDOUT |
|---|---|---|---|---|
| 평균 | **+2.51** | −0.69 | **+3.95** | **+2.70** |
| 중앙 | +3.13 | +0.16 | +3.85 | +2.34 |

**네 창 모두 되돌림 우위인 셀 47/168** (우연 기대 10). 되돌림 팔 총수익도 같은 47셀이 네 창 양수.

⭐**트리거가 클수록 되돌림 우위가 커진다** (TRAIN, `any3/Wc3`, 되돌림 총수익 bp):

| 트리거 | W=3 | W=6 | W=12 | W=24 |
|---|---|---|---|---|
| 0.25×ATR | **−1.80** | −1.79 | −1.79 | −1.79 |
| 0.5×ATR | +1.38 | +1.29 | +1.25 | +1.25 |
| **0.75×ATR** | **+2.97** | +2.41 | +2.38 | +2.41 |
| **1.0×ATR** | **+2.88** | +2.59 | +2.55 | +2.55 |
| 고정 0.40% | +2.86 | +2.31 | +2.48 | +2.14 |

작은 움직임(0.25×ATR)은 오히려 **돌파** 쪽이다(−1.8). 0.5×ATR 부터 부호가 바뀌어 되돌림이 이긴다.
탐색창 W 는 거의 무관(대부분 3봉 안에 발동).

### 🔴 그러나 거래로는 못 바꾼다
- 최선 셀 총수익 **+2.0~+4.4bp** · 네 창 전부 양수. 그런데 **비용 12bp**.
- **네 창 순bp CI 하한 > 0: 0/336. 네 창 점추정 양수도 0/336.**
- 되돌림 팔 순bp 최선이 −9.2bp. 즉 **답을 알아도 그 답으로 돈을 벌 수는 없다.**
- 크기 정합: 09-08 횡단면 단기 되돌림(+1.24bp)과 같은 자릿수다. 단기 되돌림은 실재하되
  마켓메이커의 엣지라는 결론이 시계열에서도 재현됐다.

### 그래서 증거신호를 어떻게 쓰나
1. ✅ **정보 표시로는 쓸 수 있다** — "발현 후 되돌림 편향"은 네 창에서 재현되는 **관측 기반** 진술이다.
   현행 MASHT 섀도우의 "앵커 봉에서 예측한 지속/되돌림 확률"(AUC 0.50, 재현 안 됨)보다 정직하다.
   표시 규약: 트리거 ≥0.5×ATR 이 발현되면 **되돌림 편향**, <0.5×ATR 이면 편향 없음(또는 약한 돌파).
2. ❌ **진입 신호로는 못 쓴다** — 총수익 2~3bp < 비용 12bp. 이건 실행비용 문제이지 신호 문제가 아니다.
3. ⇒ **은퇴 결정의 성격이 바뀐다**: "어떤 알파를 잃는가"가 아니라 **"대시보드에 무엇을 표시할 것인가"**다.

---

## 부록 AM — 돌파/되돌림 **정확도** 과제: 1분 미래참조 발견·수정 후 +2pp (2026-09-08)

사용자: *"이 앵커 방향으로 돈을 버는게 아니야. 돌파하냐 되돌리냐만 정확도를 크게 올려줘."*
→ *"이것도 분명 룩어헤드나 버그가 있을 수 있어. 자세히 검증해줘."*

### 0) 먼저 정정 — 베이스라인은 52~53% 가 아니라 **50.0%**
부록 AL 에서 bp 를 정확도로 환산해 "무조건 되돌림 52.9%"라고 적었는데 **틀렸다**.
그 bp 우위(+2.6bp)의 대부분은 **슬리피지 부호 실수**였다: `entry = level*(1+sgn*2bp)` 로 놓으면
돌파팔은 2bp 불리해지지만 **되돌림팔은 2bp 유리해진다**(같은 가격이 숏에게는 좋은 진입).
라벨을 직접 세면 돌파율 **0.4931~0.5020** — 동전이다. 정확도를 지표로 쓰면 체결가정과 무관해져
이 함정 자체가 사라진다.

### 1) 과제·데이터
발현 방향이 관측된 뒤 그 방향으로 +P(0.5%) 가 먼저인가(돌파 y=1) 반대가 먼저인가(되돌림 y=0).
시간청산은 종가 부호 → **커버리지 100%**, 배리어 해소율 94.5~94.9%. 사건 98,587건.
빌더 `build_eth_breakout_reversal_dataset_20260908.py` (v1 53피쳐) +
`build_eth_breakout_reversal_features_v2_20260908.py` (v2 발현경로·레벨맥락·교차자산 19피쳐).

### 2) 🔴 **1분 미래참조** — v2 초판이 걸렸다
라벨은 `first_touch(hi1, lo1, s1, ...)` 로 **트리거 분 `s1` 부터** 배리어를 탐색한다.
그런데 경로 피쳐의 창을 `mask = span <= tmin` 으로 잡아 **`s1` 을 포함**시켰다.
⇒ **피쳐와 라벨이 1분봉 하나를 공유.** 그 분에 큰 움직임이 있으면 (a) 경로 피쳐가 커지고
(b) 같은 움직임이 배리어를 때려 라벨을 정한다.

| | 수정 전 | **수정 후(`tmin-1` 까지)** |
|---|---|---|
| VAL | 0.5465 | **0.5075** [0.4878, 0.5275] |
| OOS | 0.5744 | **0.5354** [0.5103, 0.5595] |
| HOLDOUT | 0.5536 | **0.5275** [0.5100, 0.5455] |
| `v2_mv_maxbar_atr` Q5−Q1 | **+0.098** | **+0.012** |

⭐**증상은 결과 안에 이미 있었다**: 순열 중요도 1~5위가 전부 경로 피쳐인데
**단변량 분위 효과는 평평**했고(직진성 Q5−Q1 −0.016), **모델 없는 2피쳐 규칙은 50.8~51.3%** 였다.
*모델만 5~7pp 앞서고 그 피쳐들의 단변량 효과가 없으면 상호작용이거나 누수다* — 이 격차를 먼저 본다.
부수 버그: `v2_mv_maxbar_atr` 가 가격차를 `ref` 로 안 나눠 **가격 수준**을 인코딩하고 있었다.

### 3) 정밀 감사 (`verify_eth_breakout_reversal_leak_audit_20260908.py`)
| 검사 | 결과 |
|---|---|
| **라벨 셔플 대조군** ×2 | VAL 0.503/0.505 · OOS 0.502/0.509 · HOLD 0.498/0.499 — ✅**깨끗** |
| 엠바고 4h→12h→24h | VAL 0.508/0.514/0.501 · OOS 0.535/0.521/0.532 — 엠바고는 원인 아님 |
| 절제 v1만(38) | 0.5064 / 0.5167 / 0.5110 |
| 절제 전체(57) | 0.5075 / **0.5354** / 0.5275 |
| 폴드(월) 안정성 | 표본외 11개월 중 **8개월** 50% 초과 · 평균 0.5219 · 0.484~0.555 |
| ⭐**세 창 모두 CI 하한 > 50%** | **아니오** (VAL 하한 0.4878) |

### 4) 정직한 현재 위치
- **셔플 대조군이 깨끗하다** ⇒ 남은 것은 구조적 누수가 아니라 **작지만 실재하는 신호**로 보인다.
- 크기는 **기저 50.1% → 52~53%, 즉 +2pp**. OOS·HOLDOUT 은 CI 하한이 50% 위지만 **VAL 은 동전**이다.
- 방향성 단서(수정 후에도 유지되는 것): **발현이 빠를수록 되돌림**(속도 Q5−Q1 −0.053),
  **진행방향 1분봉 비율이 높을수록 돌파**(+0.039). 직진성·역행폭은 수정 후 거의 평평.
- ⇒ *"정확도를 크게 올린다"* 는 아직 달성 못 했다. **+2pp 가 현재 상한**이고 세 창 일관도 아니다.

### 5) 이 사고에서 남길 규칙
⭐**사건 트리거로 시작하는 라벨은, 그 트리거 봉을 피쳐가 만지면 안 된다.**
라벨 탐색 시작 인덱스(`s1`)와 피쳐 창의 끝 인덱스가 **같으면 그 자체로 미래참조**다.
피쳐 창은 `s1-1` 에서 끝내거나 라벨을 `s1+1` 부터 시작해야 한다.
탐지 신호: **모델 정확도 − 최고 단변량 효과**가 크게 벌어지고, 그 피쳐군을 빼면 이득이 사라진다.

---

## 부록 AN — 세 갈래 동시 진행: 관찰창 · OI/자금흐름 · 완화 라벨 (2026-09-08)

사용자 지시 3건을 동시에 돌렸다. 빌더 `build_eth_breakout_reversal_v3_20260908.py`(145,125행) ·
모델 `research_eth_breakout_reversal_v3_model_20260908.py` ·
OI 절제 `research_eth_breakout_reversal_oi_ablation_20260908.py`.

### 🔴 설계 단계에서 먼저 잡은 것 — **OBS=0 은 무효**
관찰창 0분(트리거 즉시 결정)은 돌파율이 **0.7321 / 0.6779 / 0.6327**(P=0.25/0.35/0.5%)로 튄다.
OBS≥5 에서는 0.49~0.51 이다. 원인: 트리거는 분 `s1` **안에서** 발생하는데 기준가는 `cl1[s1-1]`
(그 이전 종가)라 **트리거를 만든 움직임 자체가 배리어를 때린다**. 트리거를 아는 시점은 `s1` 종료
후이므로 최소 지연은 1분이다. 이건 부록 AM 과 같은 계열이고, 이번엔 **모델을 돌리기 전에** 잡혔다.

### 1) 관찰창 5·10·15·30분 — **0/24 셀, 전부 우연**
| | VAL | OOS | HOLDOUT |
|---|---|---|---|
| 정확도 범위 | 0.463~0.515 | 0.477~0.519 | 0.478~0.538 |
| AUC 범위 | 0.452~0.527 | 0.481~0.517 | 0.477~0.539 |
| 세 창 CI 하한 > 기저 | **0/24** | | |
최고 셀조차 세 창 최소 초과정확도가 **−0.005**(기저 미만)이고 셔플 대조군과 구분되지 않는다.

⭐**왜 무효인지가 분명하다**: 관찰창을 넣으면 기준가가 `close[s2-1]`(현재가)로 옮겨가고,
질문이 *"지금 가격에서 어느 쪽이 먼저인가"* 가 된다. 그건 **부록 Z·AA 가 이미 닫은 대칭 방향
문제**다. 남아 있던 +2pp 는 **트리거 직후의 즉시성** 자체였고, 기다리면 그게 사라진다.

### 2) 미결제약정(OI) — **기여 없음 또는 음수**
⚠️한계: 바이낸스 공개 **청산 원장은 2024~2026 구간에 없다**(`liq_magnet_collector.py` 는 2026-08
가동, 나머지는 집계 JSON). 그래서 5분봉 메트릭 패널의 OI 와 테이커 불균형으로 대체했다.

수정판 v2 구성(결정=트리거 시점, 기준=트리거 레벨) 위에서 잰 순수 기여:

| 구성 | VAL | OOS | HOLDOUT |
|---|---|---|---|
| 기준 (57피쳐) `T=1.0` | 0.5026 | 0.5434 | 0.5369 |
| **+OI** (60) | 0.5066 | 0.5397 | **0.5163** |
| **+자금흐름** (69) | **0.5233** [0.5029] | **0.5464** [0.5208] | **0.5382** [0.5172] |
| +OI+자금흐름 (72) | 0.4996 | 0.5306 | 0.5330 |
OI 3피쳐는 넣으면 HOLDOUT 이 53.7→51.6 으로 **내려간다**. 기각.

### 3) 완화 라벨 P=0.25 / 0.35% — **효과 없음**
해소율은 올랐지만(0.945→0.994) 정확도는 그대로다. v3 격자 24셀에 포함되어 전부 우연 수준.

### ⭐뜻밖의 발견 — **포지셔닝/자금흐름 피쳐만 유일하게 도움**
`x_m_retail_* · x_m_ttc_* · x_m_ttp_* · x_m_tkv_*`(개미·상위트레이더 롱숏비, 테이커 매수매도비)
12개를 넣으면 `first_fire T=1.0` 이 **52.33 / 54.64 / 53.82%**(기저 50.4/50.8/50.2)가 된다.
세 창 CI 하한은 **50.29 / 52.08 / 51.72** — VAL 이 기저를 **0.001 차이로 못 넘는다**.
⇒ 통과는 아니지만 오늘 밤 유일하게 세 창 모두 기저 위에 붙은 구성이다.
같은 데이터 계열(메트릭 패널)이 횡단면 쏠림 페이드에서도 유일한 리드였던 것과 일관된다.

### 종합 판정
**세 갈래 모두 정확도를 올리지 못했다.** 현재 상한은 여전히 **기저 50% → 52~54%(+2~4pp)** 이고,
그 기여는 (a) 트리거 직후의 즉시성 (b) 포지셔닝/자금흐름 피쳐 두 가지에서만 나온다.
관찰창·OI·완화라벨은 셋 다 기각.

---

## 부록 AN — 사용자 3축(CVD/흡수 · OI · 호가/스푸핑) 전면 구현: **+2pp 상한 못 넘음** (2026-09-08)

사용자가 세 가지 실시간 데이터로 되돌림/지속 모델을 만들자고 제안했다:
*"(1) CVD 급등인데 못 뚫으면 흡수 → 되돌림 (2) 돌파+OI 급감 = 숏스퀴즈 연료 → 되돌림
(3) 앞쪽 벽이 취소되며 뚫리면 지속, 뚫은 직후 반대 벽이 서면 유동성 사냥 → 되돌림"*

부록 AM 의 과제(발현 방향 관측 후 ±0.5% 어느 쪽이 먼저인가, 기저 50.0%)에 세 축을 그대로 붙였다.

### 1) 데이터 — 세 축 모두 실재하나 해상도가 갈린다

| 축 | 소스 | 해상도 | 기간 | 판정 |
|---|---|---|---|---|
| CVD/흡수 | `data/research/eth_tape_1m_20260906.parquet` (aggTrades 29컬럼) | **1분** | 2024-04-20~2026-09-05, 결측 34분 | ✅완비 |
| OI | `binance_data/metrics/` | **5분** | 2024-01~ | ⚠️트리거가 5분봉 *안*에서 나므로 그 봉의 OI 는 원리적으로 못 봄 |
| 호가/OBI | `binance_data/bookDepth/ETHUSDT` 869일 | **30초** ±0.2/1/2/3/4/5% 누적 | 2024-04-20~2026-09-05 | ✅OBI 가능 / ❌스푸핑 불가 |

새로 패널화: `scripts/build_eth_bookdepth_panel_20260908.py` → `data/research/eth_bookdepth_30s_20260908.parquet` (2,448,323행).
⚠️**30초 스냅샷은 원리적으로 스푸핑을 못 본다** — 주문 제출→취소는 초 단위다. 잡을 수 있는 건 "밴드 깊이가 급감했다"는 사후 흔적뿐이다.

### 2) 🔴호가 데이터 결함 발견 (이 부록의 가장 실무적인 산출물)
한쪽 밴드가 상수로 굳거나 근접 밴드가 붕괴하는 구간이 있다. `bd_ok` 로 스냅샷 단위 플래그를 저장했다.

| 구간 | 증상 | 영향 |
|---|---|---|
| 2025-10-11~15, **2025-10-30~12-01** (38일) | 매수 밴드 전체가 상수(±1~5% 동일값), −0.2% 밴드 결측 | **VAL 창의 31%** (bd_ok VAL **0.700**) |
| **2026-09-04~05** | 매도 근접밴드(+0.2/+1%) 붕괴 | ⚠️**현재/라이브** — 지금 OBI 를 라이브에 붙이면 쓰레기를 읽는다 |

TRAIN 0.9987 · OOS 1.0000 · HOLDOUT 1.0000. 2025-11 은 밴드가 통째로 상수라 **복구 불가**.

### 3) ⭐단변량을 모델보다 먼저 (부록 AM 규칙)
s1 시점 24피쳐 중 최대 |Q5−Q1| = **0.044** (앞쪽 벽 비중 0.2%), 2SE = ±0.048 → **어느 것도 2SE 를 못 넘었다**.

| 사용자 가설 | 피쳐 | Q5−Q1 | 판정 |
|---|---|---|---|
| ①흡수 → 되돌림 | 흡수계수 CVD/이동폭 | **−0.014** (0.517→0.503, **단조**) | 방향은 가설대로, 크기 1.4pp |
| ②OI 급감 → 되돌림 | ΔOI 5/15/30/60분 | +0.005 ~ +0.009 | **평평** |
| ③벽 취소 → 지속 | 앞쪽 벽 취소율 | **−0.027** | 🔴**부호가 가설과 반대** |
| ③얇은 벽 → 지속 | 앞쪽 벽 비중 0.2% | +0.044 (Q1 0.482 / Q5 0.526) | 🔴**반대** — 앞이 얇으면 되돌림 |

### 4) 서술된 규칙 자체를 모델 없이 검정 — **CI 가 0.5 를 배제한 규칙 0/9**
`scripts/research_eth_breakout_reversal_stated_rules_20260908.py` (표본외 3창 n=7,374).
최고는 "OI 급감 & 발현 빠름" 정확도 0.5416 인데 **커버리지 8.6%** · CI [0.411, 0.506] 이 0.5 를 포함한다.
3축 합의("흡수↑ & OI↓ & 앞벽 두껍")는 커버리지 3.6% · 0.4695 로 **오히려 반대**.

### 5) 모델 절제 (월 walk-forward · 엠바고 4h · HGB · 일군집 CI)
`scripts/build_eth_breakout_reversal_features_v3_20260908.py` (30피쳐) + `..._v3_20260908.py`.
앵커·트리거 3셀 × 4개 추가구성 = 12조합, 기저 대비 증분(pp):

| 셀 | +CVD/흡수 | +OI | +호가 | +전체v3 |
|---|---|---|---|---|
| first_fire T=0.75 | −0.44 / **+0.71** / **+1.50** | −0.07/+0.61/+0.19 | −0.59/+0.91/**−1.53** | −0.66/+0.91/+0.97 |
| first_fire T=1.0 | +0.83/**−0.97**/+0.18 | +0.92/−1.03/+0.75 | **+1.58**/−0.18/+0.44 | +0.31/−1.03/+0.09 |
| any2/Wc3 T=0.75 | +1.16/−2.28/+2.35 | **+2.32/+0.91/+3.81** | +0.21/+0.15/+2.35 | +1.37/−1.06/+1.46 |

*(VAL / OOS / HOLDOUT 순)*

- **세 창 모두 개선: 2/12** — 우연 기대 **1.5**. 정확히 우연 수준이다.
- **세 창 모두 CI 하한 > 0.50: 0/15** (기저 포함 전 구성).
- ⭐**최고 구성이 셀마다 다르고 부호가 뒤집힌다**: T=0.75 는 CVD, T=1.0 은 호가, any2 는 OI.
- 라벨 셔플 대조군 ×2: VAL 0.5009/0.4903 · OOS 0.4813/0.4899 · HOLD 0.4927/0.4976 — ✅**깨끗**(구조적 누수 없음).

### 6) ⭐지연관측 팔 — 사용자 가설 ②③은 원래 '돌파 직후' 관측이다
OI 급감도 유동성 사냥도 트리거 시점엔 원리적으로 없다. 그래서 판단을 D분 늦추고 라벨도 `sd+1` 부터
다시 잡았다(피쳐 창 `sd` 까지, **한 봉도 공유하지 않음**). 커버리지 100%, 기저 돌파율 0.509~0.512.
`scripts/build_eth_breakout_reversal_delayed_20260908.py` · `..._delayed_20260908.py`.

**🔴결정적 결과: 기저 86피쳐조차 지연 팔에서 0.49~0.51 로 붕괴한다.**

| D | 기저(s1시점 86) | +사후 전체 | +사후 OI만 | +사후 호가만 | 사후만(19) |
|---|---|---|---|---|---|
| 5분 | .5050/.5025/.4938 | .4950/.5071/.5024 | .4983/.5121/.4837 | .4987/.5056/.4871 | .4932/.5207/.4927 |
| 10분 | .4917/.5116/.4994 | .4976/.4980/.5103 | .4954/.4879/.5069 | .5090/.5076/.5129 | .5112/.4894/.5024 |

⇒ **부록 AM 의 +2pp 는 트리거 순간에 국한된 효과이고 5분 안에 소멸한다.** 20구성 전부 0.48~0.52.
지연 팔 단변량에서 t≥2.3 인 게 둘 나왔지만(앞쪽 벽 비중 사후 −0.052 t=2.6 · 반대 벽 형성 +0.044 t=2.3)
22검정 중 |t|>2 가 2개는 **우연 기대 1.1** 이고, 전자는 s1 팔의 같은 계열과 **부호가 반대**,
후자는 **사용자 가설과 반대 부호**(반대 벽이 서면 돌파율 53.7%)에 비단조다.

### 7) 결론
세 축을 전부 구현해 붙였으나 **부록 AM 의 +2pp(OOS 0.5354 / HOLD 0.5275, VAL 은 동전) 상한을 못 넘었다.**
셔플 대조군이 깨끗하므로 누수는 아니고, 단변량·규칙·모델·지연 어느 경로로도 신호가 2SE 를 못 넘는다.

**남은 정직한 길은 데이터이지 모델이 아니다.**
1. ⭐**진짜 L2(주문단위)** — 30초 스냅샷으로는 스푸핑이 원리적으로 안 보인다. 자체 수집
   `data/live/l2_anomaly_snapshots.duckdb` 는 2026-08~ 뿐이라 VAL/OOS 와 안 겹친다. **달력 대기**가 유일.
2. **OI 해상도** — Binance metrics 5분이 상한. 1분 이하는 자체 수집해야 한다.
3. **bookDepth 2025-11 은 복구 불가** — 밴드가 통째로 상수다.

### 8) 재사용 자산
- `data/research/eth_bookdepth_30s_20260908.parquet` (2.45M행, `bd_ok` 플래그 포함)
- `tmp/eth_breakout_reversal_20260908/dataset_v3.parquet` (98,587×116) · `dataset_delayed.parquet` (295,761)
- `v3_ablation.csv` · `delayed_ablation.csv`
