# 기계적 매매 최적해 연구 종합 (2026-07-26)

**질문**: 세계에 존재하는 모든 지표를 찾거나 만들어서 공식화하면 최고의 기계적 매매가 되는가?

**답**: 아니다. 그리고 그것을 반증하는 데이터가 이 저장소에 이미 축적돼 있다. 병목은 지표
개수가 아니라 **탐색 과정 자체가 가짜 승자를 생산하는데 그것을 탐지할 계측기가 없었다**는
점이다. 이 문서는 (1) 그 계측기를 만들어 측정한 결과, (2) 저장소 전체 증거가 가리키는
실제 구조, (3) 남은 유일한 증거기반 후보의 사전등록 설계를 담는다.

---

## 1. 지표를 더 찾는 것이 왜 답이 아닌가

이 저장소는 이미 그 실험을 여러 번 했다.

| 근거 | 내용 |
|---|---|
| 지표 보유량 | `data/training_features_5m.csv` 기준 **141개** 피처가 이미 존재 |
| Sigma8 | 피처 추가로 Sigma6를 넘으려는 시도 → 실패 |
| Omega4.6.1 exit-logic | 10개 라운드 전부 VAL/OOS 실패 |
| Omega4.7-RL | end-to-end RL로 지표 조합 학습 → OOS -88~-99% |
| 1m 스캘핑 | 사상 최강 결과로 보였으나 lookahead 버그로 전면 무효화 |
| microstructure | 신호는 실재(t=-7.6)하나 0.3~2bps로 비용 4~9bps에 미달 |
| F3-A 펀딩 캐리 | 24/24 변형 kill. 펀딩 컴포넌트 자체가 전부 음수 |
| F3-B 크로스섹션 | 총수익은 양수이나 일일 리밸런싱 비용이 압도 |
| BTC | 3단계 전부 소진, 구조적 효율성 천장으로 결론 |
| VPVR 공식 (오늘) | walk-forward 5개 fold 중 양수 **0개** |

지표 축은 이미 포화 상태이고, 실패는 지표 부족 때문이 아니었다.

---

## 2. 새 계측기: `core/selection_stats.py`

모든 실패가 같은 형태였다 — 최적화 → 화려한 IS 숫자 → OOS 붕괴. 빠져 있던 것은
**"이 숫자가 같은 규모의 탐색이 순수 노이즈에서 뽑아내는 숫자보다 큰가"** 를 묻는 도구다.

| 함수 | 내용 | 출처 |
|---|---|---|
| `expected_max_sharpe` | N회 독립 시행의 기대 최대 Sharpe = 노이즈 바닥 | Bailey & López de Prado 2014 |
| `probabilistic_sharpe_ratio` | 왜도/첨도 보정 P(true SR > 기준) | Bailey & López de Prado 2012 |
| `deflated_sharpe_ratio` | 기준을 탐색 자신의 기대 최대치로 놓은 PSR | Bailey & López de Prado 2014 |
| `pbo_cscv` | 조합대칭 교차검증 기반 과최적화 확률 | Bailey/Borwein/LdP/Zhu 2017 |

**검증** (수식이 틀리면 아래 전부가 무의미하므로 선행 실시):

- `expected_max_sharpe`: N=200에서 공식 2.7655 vs 몬테카를로 실측 2.7201 (오차 1.7%)
- `PSR`: 기준=관측치일 때 정확히 0.5, 음의 왜도가 값을 낮춤 (0.9785 < 0.9871)
- `pbo_cscv`: 순수 노이즈 100열에서 **0.476** (이론 0.5), 한 열에만 진짜 우위를 심으면
  **0.000** — 판별력 확인

---

## 3. VPVR 공식에 적용한 판정

200회 **독립 랜덤** 탐색(TPE는 독립 가정을 위반하므로 의도적으로 랜덤 사용), 학습구간
651일의 일별 수익률 기준.

```
노이즈 바닥 E[max daily SR]                0.3598
──────────────────────────────────────────────────────
랜덤 탐색 최고                             0.0309   (바닥의 0.09배)
optuna sharpe 우승자   (train  +79.6%)     0.0654   (바닥의 0.18배)
optuna pnl_mdd 우승자  (train +345.9%)     0.0769   (바닥의 0.21배)
optuna pnl 우승자      (train +1715.0%)    0.0869   (바닥의 0.24배)
```

**모든 우승자가 노이즈 바닥보다 한참 아래다.** DSR은 전부 1e-22 미만. 즉 이 전략 공간은
"우위가 없는" 정도가 아니라, 200회 탐색한 무작위 전략보다도 못한 성적을 낸다.

### 3.1 최적화를 세게 할수록 붕괴가 커진다

| 탐색 | train PnL | holdout PnL |
|---|---|---|
| 랜덤 200회 | +29.0% | **-4.4%** |
| Optuna 200회 | +1715.0% | **-87.8%** |

TPE가 더 큰 IS 숫자를 찾아낸 것은 더 나은 전략을 찾았기 때문이 아니라 **노이즈를 더 깊이
파냈기 때문**이다. IS 숫자의 크기는 신뢰도의 반대 지표로 작동했다.

### 3.2 PBO 0.115는 좋은 신호가 아니다 (주의)

PBO가 0.115로 낮게 나오지만 긍정적으로 해석하면 안 된다. 샘플된 설정의 **98.5%가 완전
파산**(시행 PnL 중앙값 -100%)이라, "파산을 피한다"는 안정적이고 일반화되는 순위 기준이
존재할 뿐이다. 이는 알파와 무관하다. PBO는 수익성이 아니라 상대 순위를 재는 지표이므로
파산이 지배하는 공간에서는 낮게 나온다.

---

## 4. 통과한 것들의 공통 구조 — 실증적 답

이 저장소에서 val+OOS를 통과했거나 최강 후보로 남은 것들(Omega6 v2, Sigma6, Omega4.6.1)의
공통점은 지표가 아니라 **구조**다.

1. **저빈도** — 1h 봉. "작동하는 레버는 아키텍처가 아니라 낮은 빈도"라는 결론이 반복 확인됨.
   비용이 4~9bps인데 5m은 하루 288봉, 1m은 1440봉이다. 빈도가 곧 비용이다.
2. **추세추종** — 평균회귀가 아니라 방향 지속에 베팅.
3. **레짐 필터** — chop 구간에서 아예 거래하지 않음. 이 저장소에서 가장 반복적으로 검증된
   단일 개선.
4. **이익 확대 허용(트레일링)** — 낮은 승률을 큰 우승 거래로 상쇄.
5. **적은 거래수** — 4개월 19~26건 수준.

"최고의 기계적 매매 방법"에 대한 이 저장소의 실증적 답은 새 공식이 아니라 **이 다섯 가지
구조를 지키는 것**이다. 학계에서 가장 널리 복제된 이상현상(time-series momentum,
Moskowitz-Ooi-Pedersen 2012)과도 일치한다.

---

## 5. 남은 유일한 증거기반 후보 — F3-B 저빈도 리밸런싱

F3-B(크로스섹션 모멘텀)는 kill됐지만 **kill 사유가 다른 모든 실패와 다르다**. 신호가
없어서가 아니라 **비용에만 밀렸다**.

- 비용 전 총수익이 k∈{7,14,30}일 **전부 양수**(1.5~8.2bps/일), k가 클수록 커짐 —
  방향성 있는 진짜 신호의 특징
- 일일 양다리 리밸런싱 비용 20bps/일이 이를 압도
- 실행 문서가 후속 아이디어를 명시적으로 열어둠: 주 1회 리밸런싱 시 비용 약 7배 감소

산술적으로 k=30 기준 총수익 8.2bps/일, 주 1회 리밸런싱 시 비용 2.86bps/일 → 이론상
+5.3bps/일. 다만 **포지션을 일주일 방치하면 총수익도 함께 열화**되므로 실측이 필요하다.
이것이 이 실험의 전부이자 핵심 질문이다.

### 5.1 사전등록 설계 (실행 전 확정, 사후 변경 금지)

기존 anti-fishing 규칙에 따라 별도 사전등록이 필요하므로 즉흥 실행하지 않고 설계만 남긴다.

| 항목 | 값 |
|---|---|
| 가설 | 리밸런스 주기를 늘리면 총수익 열화보다 비용 절감이 커서 순수익이 양수가 된다 |
| 격자 | lookback k ∈ {14, 30, 60}일 × 리밸런스 주기 ∈ {3일, 7일, 14일} = 9개 (고정) |
| 유니버스 | ETH/BTC/SOL 3자산 (확장 금지 — 확장은 별도 실험) |
| 비용 | cost1(10bps 왕복) 주 판정, cost3(30bps) 강건성 |
| 탐색구간 | 2024-01 ~ 2025-08 |
| kill 기준 | 탐색구간 net_cost1 > 0 **그리고** day-block bootstrap t > 3. 미달 시 val/OOS 진행 금지 |
| val / OOS | 2025-09~12 / 2026-01~03 (fresh-forward 규칙 준수, 저장 원장 입력 금지) |
| 노이즈 바닥 | 9개 격자에 대해 `expected_max_sharpe(9, trial_sr_std)` 를 **실행 전** 계산해 기록 |
| 승격 기준 | OOS DSR ≥ 0.95 **그리고** PBO ≤ 0.25 |

3자산 유니버스로 크로스섹션 모멘텀을 검증하는 것은 원래 약한 검정이다(문헌은 20종 이상을
전제). 통과해도 근거의 강도는 제한적임을 미리 명시한다.

### 5.1.1 실행 결과 (2026-07-26, 사전등록 직후 실행) — **KILLED, 단 사유가 중요**

스크립트 [`scripts/research_f3b_lowfreq_rebalance_20260726.py`](../scripts/research_f3b_lowfreq_rebalance_20260726.py) /
결과 [`data/ensemble/metrics/f3b_lowfreq_rebalance_20260726.json`](../data/ensemble/metrics/f3b_lowfreq_rebalance_20260726.json)

**구현 검증 선행**: (1) 미래 가격을 전부 오염시켜도 절단일 이전 PnL·비용이 불변임을 확인해
lookahead 부재 입증, (2) rebal=1로 원본 F3-B 총수익을 정확 재현 (k=7 → 1.53, k=14 → 3.03,
k=30 → 8.17 bps/일; 원본 보고 "1.5~8.2, k에 비례"와 일치).

탐색구간 2024-06-01~2025-08-31 (SOL 데이터가 2024-06 시작이라 사전등록의 2024-01보다 짧음 —
원본 F3-B와 동일 제약). 단위 bps/일:

| k | 리밸런스 | netFLAT | t(FLAT) | t(TURN) | t=3 도달 필요 연수 |
|---|---|---|---|---|---|
| 14 | 3 | -0.56 | -0.08 | 0.56 | — |
| 14 | 7 | -3.53 | -0.50 | -0.30 | — |
| 14 | 14 | **+9.03** | 1.31 | 1.38 | **6.3년** |
| 30 | 3 | +6.59 | 0.89 | 1.63 | 13.3년 |
| 30 | 7 | +7.58 | 1.10 | 1.38 | 9.2년 |
| 30 | 14 | +4.17 | 0.59 | 0.71 | 31.5년 |
| 60 | 3 | -5.41 | -0.75 | 0.06 | — |
| 60 | 7 | -1.05 | -0.14 | 0.18 | — |
| 60 | 14 | -1.51 | -0.21 | -0.07 | — |

**0/9 게이트 통과. val/OOS 미진행 (사전등록 준수).**

가설의 전반부는 맞았다 — 리밸런스 주기를 늘리자 비용이 실제로 6.70 → 1.45bps/일로 줄었고
4개 변형에서 순수익이 양수로 돌아섰다(최대 +9.03bps/일 ≈ 연 33%). 그러나 **어느 것도 0과
통계적으로 구분되지 않는다** (최대 t=1.31). 노이즈 바닥 대비로도 최고 변형의 일간 Sharpe는
0.0625 vs 바닥 0.0542로 1.15배에 불과하다.

**핵심 진단 — "효과 없음"이 아니라 "확인 불가"다.** 3자산 크로스섹션의 일간 변동성은
약 145bps/일인데 효과 크기는 7~9bps/일이다. 신호대잡음비가 0.06 수준이라 t>3에 도달하려면
**6.3~13.3년치가 필요**한데 1.2년뿐이다. 사전등록한 게이트는 이 유니버스 크기에서
**구조적으로 통과 불가능**했다. (원본 F3-B 문서도 "3자산 유니버스 한계"를 이미 명시했다.)

**부수 발견 — 원본 F3-B의 비용 모델은 약 9배 과대계상이었다.** 원본은 순위가 안 바뀌어도
매일 왕복비용 전액(20bps/일)을 부과했으나 실제 회전율 기준은 2.25bps/일이다 (k=30 일간
리밸런스 기준 — 30일 모멘텀 순위는 평균 9일에 한 번 바뀐다). 다만 이 관대한 회전율 모델로
재계산해도 **최대 t=1.63으로 여전히 미달**이므로 원본의 kill 판정 자체는 결과적으로 옳았다.
비용 모델이 아니라 표본이 부족한 것이다.

**갈림길**: 3자산으로는 이 축을 영원히 확인할 수 없다. 선택지는 둘뿐이다.
(a) 문헌 표준대로 **20종 이상 유니버스로 새 사전등록 실험**을 설계한다 — 종목 수가 늘면
분산으로 크로스섹션 잡음이 줄어 검정력이 올라간다. (b) 축을 영구 종결한다.
**중간은 없다** — 3자산에서 t를 올리려 파라미터를 더 만지는 것은 정의상 노이즈 채굴이다.

### 5.1.2 유니버스 확장 재설계 — 25종 사전등록 (2026-07-26)

**유니버스 선정 절차 (성과와 무관하게, 실행 전 확정)**: 모멘텀 결과를 본 뒤 종목을 고르면
그 자체가 낚시질이므로, 유동성 스냅샷만으로 기계적으로 결정한다.

1. Binance USDT-M perpetual 중 `status=TRADING` 전체 (공개 API, 계정 정보 불필요)
2. `onboardDate ≤ 2024-01-01` — 전 종목이 2년 이상 거래이력을 갖도록 강제, 25종 전체가
   "늦게 합류" 로직 없이 하나의 고정 시작일을 공유할 수 있게 함
3. 위 조건을 만족하는 종목을 조회 시점(2026-07-26) 24시간 거래대금 순으로 정렬해 **상위 25개
   그대로** 채택 (성과 지표 아님, 순수 유동성 지표)

스크립트 [`scripts/select_f3b_universe_25_20260726.py`](../scripts/select_f3b_universe_25_20260726.py) /
결과 [`data/ensemble/metrics/f3b_universe25_selection_20260726.json`](../data/ensemble/metrics/f3b_universe25_selection_20260726.json)

**확정 유니버스 (25종, onboard 전부 2024-01-01 이전)**:
`BTCUSDT, ETHUSDT, SOLUSDT, 1000SHIBUSDT, DOGEUSDT, ZECUSDT, 1000PEPEUSDT, XRPUSDT, AVAXUSDT,
WLDUSDT, RIFUSDT, BNBUSDT, ADAUSDT, SUIUSDT, LINKUSDT, NEARUSDT, ACEUSDT, AAVEUSDT, INJUSDT,
UNIUSDT, 1000BONKUSDT, LTCUSDT, TRXUSDT, DOTUSDT, FILUSDT`

가장 늦은 onboard는 ACEUSDT(2023-12-18)로, 60일 lookback 워밍업을 두어도 2024-01-01부터
25종 전체가 참여 가능하다.

**격자/판정 (F3-B-LF와 동일 관례, 저빈도 리밸런싱 교훈 이미 반영)**:

| 항목 | 값 |
|---|---|
| 포지션 구성 | k일 모멘텀 순위 상위 20%(5종) 롱 / 하위 20%(5종) 숏, 각 레그 동일가중, 달러중립(gross 50%/50%) |
| 격자 | k ∈ {14, 30, 60}일 × 리밸런스 ∈ {3, 7, 14}일 = 9개 (F3-B-LF와 동일 격자) |
| 데이터 | 일봉 종가, Binance futures 공개 REST (`fapi.binance.com/fapi/v1/klines`, interval=1d) |
| 비용 | cost1 FLAT(왕복 10bps, 매 리밸런스 gross 100% 전액 과금) 주 판정, TURNOVER(실제 바뀐 종목만) 참고, cost3 강건성 |
| 탐색구간 | 2024-01-01~2025-08-31 |
| kill 기준 | 탐색구간 net_cost1_flat > 0 **그리고** day-block bootstrap t > 3 |
| 검정력 진단 | F3-B-LF에서 배운 대로, 변형별 `days_needed_for_t3`를 **결과 해석 전에** 반드시 같이 본다 |
| 노이즈 바닥 | 9개 격자에 대해 `expected_max_sharpe(9, trial_sr_std)`을 실행 후 결과 해석 전에 기록 |
| val / OOS | 2025-09~12 / 2026-01~03 (fresh-forward 규칙 준수) |
| 승격 기준 | OOS DSR ≥ 0.95 **그리고** PBO ≤ 0.25 |

원 F3-B/F3-B-LF와의 유일한 차이는 유니버스 크기(3→25)와 레그 구성(1종 대 1종 → 5종 바스켓
대 5종 바스켓)이다. 나머지(비용모델, kill 기준, 창구, 승격 기준)는 전부 동일하게 유지해
결과가 "유니버스 확장 자체의 효과"인지 다른 것에서 온 것인지 헷갈리지 않게 한다.

### 5.2 그 외 미검증 축 (우선순위 순)

1. **변동성 타게팅 사이징** — 신호가 아니라 **사이징 규칙**. 실현변동성에 반비례해 포지션을
   조절하는 것은 거의 모든 자산군에서 Sharpe를 개선하는 것으로 알려져 있는데, 현재는 고정
   레버리지(3x/10x)다. Sigma6에 즉시 얹어 검증 가능하며 비용이 거의 들지 않는다.
   **지표를 늘리지 않고 개선을 노리는 유일한 저비용 축.**

   #### 5.2.1 사전등록 설계 (실행 전 확정)

   Sigma6 frozen 프로덕션 설정(`research_f4b_sigma6_dated_ledger_20260719.py`의
   `sigma6_lev3`/`sigma6_lev4`, contract 문서와 일치 확인됨) 위에 사이징만 교체하고 나머지는
   전부 동결한다.

   | 항목 | 값 |
   |---|---|
   | 실현변동성 프록시 | `atr_pct` (이미 causal, 이미 stop 거리 계산에 쓰임 — 신규 lookahead 없음) |
   | 사이징 규칙 | `notional_t = base_notional * clip(target_atr / atr_pct[entry], lo, hi)` |
   | `target_atr` | VAL 구간(2025-07~12) `atr_pct` 중앙값으로 1회 보정, 이후 고정 상수로 취급 (VAL/OOS 전체에 동일 적용 — sl_atr 등 기존 동결 상수와 같은 취급) |
   | 배율 상하한 (lo, hi) | (0.4, 2.0) — 저변동 구간에서 레버리지가 무한정 커지는 것을 방지 |
   | 적용 대상 | `sigma6_lev3`, `sigma6_lev4` 각각, 다른 파라미터(진입/청산/레짐필터) 전부 동결 |
   | 1차 판정 구간 | VAL 2025-07-01~12-31 (frozen 설정과 동일 구간) |
   | 승격 기준 (VAL) | MDD 개선 **그리고** Sharpe(또는 PnL/MDD) 비열화 — F4-B에서 쓴 AND 기준과 동일 |
   | OOS | 2026-03-02~06-30. **이 구간은 Sigma6 자체 + Sigma8-11 + F4-B에서 이미 반복 재사용됨 — 원 계약서가
   스스로 "Nth use → 증거가치 저하"를 명시한 구간이다. 여기 재사용도 그 한계를 그대로 승계하며 새로운
   확인으로 포장하지 않는다.** |
   | 확장 구간 (참고용, 승격 근거 아님) | 2026-07-01~07-20 — 이 구간은 2026-07-20 세션에서 sigma3-1h
   앙상블을 리트레인하면 frozen baseline이 재현 안 되는 fragility가 이미 발견된 바 있어, 리트레인
   없이 causal 추론만으로 확장된 tape을 그대로 쓰고 결과는 참고 문맥으로만 보고한다 |

   #### 5.2.2 실행 결과 (2026-07-26) — **VAL 게이트 0/2 실패, OOS 미진행. 원인 특정됨**

   스크립트 [`scripts/research_sigma6_vol_targeting_20260726.py`](../scripts/research_sigma6_vol_targeting_20260726.py) /
   결과 [`data/ensemble/metrics/sigma6_vol_targeting_20260726.json`](../data/ensemble/metrics/sigma6_vol_targeting_20260726.json)

   **실행 전 발견 — tape 오염**: 프로덕션 tape(`tape_ensemble.parquet`)이 2026-07-20에 재생성돼
   frozen 계약서 수치(lev4 +45.9%, lev3 +16.6%)를 더 이상 재현하지 못한다(현재 tape로는 lev4
   -22.0%, lev3 -9.7%). 이는 이미 문서화된 sigma3-1h 앙상블 리트레인 fragility와 정확히 일치한다
   (별도 세션에서 발견, 미해결). 이번 실험은 이 fragility를 고치는 것이 범위 밖이므로, **동일
   tape 위에서 고정-명목가 baseline과 변동성 타게팅을 나란히 돌리는 쌍대비교**로 사이징 효과만
   분리했다 — 절대 수익 수준이 아니라 baseline 대비 상대 변화만 근거로 삼는다.

   VAL 2025-07~12, target_atr=VAL 중앙값(0.00952), 배율 (0.4, 2.0):

   | 설정 | notional | PnL | MDD | Sharpe유사 |
   |---|---|---|---|---|
   | lev4 baseline | 1.20 (고정) | +71.1% | -15.9% | 5.34 |
   | lev4 vol-targeted | 0.48~2.40 | +42.1% | **-18.1%** | **3.87** |
   | lev3 baseline | 0.90 (고정) | +34.3% | -14.2% | 3.91 |
   | lev3 vol-targeted | 0.40~1.80 | +15.9% | **-18.4%** | **2.16** |

   **두 설정 모두 게이트 실패** — MDD 개선은커녕 악화(lev4: -15.9%→-18.1%, lev3: -14.2%→-18.4%),
   Sharpe유사도 27~45% 하락. 사전등록 기준에 따라 OOS는 진행하지 않았다.

   **원인 진단 (같은 스크립트에 내장, 사이징 미적용 상태로 별도 측정)**: 진입 시점 ATR과 거래당
   수익(명목가 대비) 사이 상관관계를 계산했다.

   | | lev4 | lev3 |
   |---|---|---|
   | corr(entry_atr, return) | **+0.234** | **+0.359** |
   | 저ATR tercile 평균수익 | -0.005% | -0.71% |
   | 중ATR tercile 평균수익 | +1.83% | +1.65% |
   | 고ATR tercile 평균수익 | +2.69% | +2.93% |

   **양쪽 다 뚜렷한 양의 상관, 단조 증가.** Sigma6는 "이익 확대 허용(트레일링)" 구조의 추세추종
   전략이라, 트레일링 청산이 정확히 진입 후 변동성이 확대되는 큰 추세 구간에서 큰 승리를 잡아낸다
   — 즉 **고ATR 진입이 바로 이 전략의 엣지가 실현되는 지점**이다. 변동성 타게팅은 이런 거래의
   비중을 줄이고 저ATR(손실 경향) 거래의 비중을 늘리므로, 사이징 규칙이 전략의 엣지 구조와
   정면으로 반대 방향이다. 일반 자산군에서 변동성 타게팅이 Sharpe를 개선한다는 문헌상 통념은
   ATR과 수익이 무관하거나 음의 관계인 전략을 전제하는데, Sigma6는 그 전제 자체가 성립하지 않는다.

   **결론**: 이 형태의 (ATR 역비례) 변동성 타게팅은 Sigma6에 적용 불가. 재시도 가치가 있다면
   방향을 반대로 뒤집은 **ATR 비례 사이징**(고변동 진입에 더 큰 사이즈)일 텐데, 이는 이미 트레일링
   구조가 암묵적으로 하고 있는 일(청산 폭을 ATR 배수로 잡아 고변동 구간에서 자연히 더 넓게
   태움)과 방향이 겹쳐 순수 사이징 개선이라기보다 레버리지 증가에 가까워 별도 사전등록 없이
   진행하지 않는다. 이 축은 여기서 종결한다.

2. **포트폴리오 결합 확장** — F4-B에서 균등가중 결합 Sharpe 2.58이 구성요소 둘(2.21, 1.58)을
   모두 상회하는 교과서적 다각화 신호가 나왔다. 다만 상관 추정이 거래 희소로 불안정
   (CI가 0을 포함). 표본이 쌓인 뒤 재측정 대상.
3. **다중 horizon TSMOM 앙상블** — Sigma6는 단일 horizon. 문헌은 복수 lookback 앙상블을
   권고. 구조가 이미 검증된 축의 확장이라 리스크가 낮다.

---

## 6. 연구 프로토콜 제안

지금까지의 실패는 개별 아이디어의 문제가 아니라 절차의 문제였다. 다음을 기본값으로 삼기를
권한다.

1. **노이즈 바닥을 먼저 계산한다.** 탐색 규모 N이 정해지면 `expected_max_sharpe(N, σ)`를
   실행 **전에** 기록한다. 이 값을 못 넘으면 결과를 보지 않고 폐기한다.
2. **DSR을 승격 게이트에 넣는다.** `promotion_pass` 조건에 `DSR ≥ 0.95`를 추가.
   기존 Omega artifact integrity gate와 같은 층위로 다룬다.
3. **IS 숫자의 크기를 성과로 보지 않는다.** 오늘 측정에서 IS 수익률과 OOS 붕괴폭은
   양의 상관을 보였다. `+1715%` 같은 숫자는 경보로 읽어야 한다.
4. **탐색은 랜덤으로, 최적화는 마지막에.** TPE/CMA-ES는 독립 가정을 깨서 DSR을 무력화한다.
   후보 선별까지는 독립 랜덤 탐색, 확정된 단일 가설의 미세조정에만 guided search를 쓴다.
5. **경제적 근거를 먼저 적고 그 다음에 테스트한다.** 지표 조합에서 출발하면 반드시
   데이터 스누핑이 된다. F3-A/F3-B는 근거를 먼저 적었기에 kill 판정이 깔끔했다.

---

## 부록: 산출물

| 경로 | 내용 |
|---|---|
| `core/selection_stats.py` | DSR/PSR/PBO/노이즈 바닥 계측기 (검증 완료) |
| `scripts/research_selection_stats_vpvr_20260726.py` | VPVR 공식 선택편향 판정 실행기 |
| `data/ensemble/metrics/vpvr_selection_stats_20260726.json` | 위 판정 결과 |
| `data/ensemble/metrics/vpvr_poc_rsi_vwma_ema_formula_walkforward_result.json` | walk-forward 5 fold 결과 |
