# V자급등락(V_REBOUND) 미해결 이슈 모음 (2026-09-01)

호메로스 프로젝트의 서브 문서. 2026-09-01 하루 동안 BTC 확장 작업과 다른 세션의 ETH 감사에서
발견된 V자급등락 관련 미해결 문제를 한곳에 모았다. 전부 **기록만 됐고 아직 하나도 고쳐지지
않았다** — 한 번에 몰아서 처리할 때 이 문서를 체크리스트로 쓸 것.

## 1. local_extreme "지연확인" 구조 → 신호가 갑자기 과거기록과 함께 나타나는 UX 문제

**증상** (사용자가 이전 다른 세션에서 이미 신고): V자반등 신호가 "없음" 상태였다가 갑자기 이미
지난 과거 시점을 포함한 상태로 신호가 나타난다.

**원인**: `local_extreme_flags()`(양쪽 코드베이스 동일 — `scripts/build_btc_5m_evidence_signal_
candidates_tier0_20260901.py`와 ETH 라이브 `scripts/live_eth_sweep_v_rebound_signal_20260829.py::
_multitrigger_rows()`)가 `for i in range(W, n-W)`(W=6=30분)로 정의돼 있어, **"지금 이 봉"은
구조상 절대로 로컬극값 판정을 받을 수 없다** — 판정에 미래 30분(W봉)이 반드시 필요하기 때문.
그래서 매 계산 사이클마다 30분 전에는 후보가 아니었던 봉이 뒤늦게 "확정"되면서, 이벤트의 시작
시각(`t0`)이 이미 30분 전 과거로 찍힌 채 화면에 새로 나타난다 — 이게 "없었다가 갑자기 과거
기록까지 합쳐져서 나온다"로 보이는 정체.

**영향 범위**: BTC·ETH 라이브 서빙 코드 둘 다(동일 패턴), local_extreme이 관여하는 모든 V자급등락
표시(현재 콜/히스토리 스파크라인 전부).

**미해결 — 검토할 수 있는 방향** (결정 안 됨):
- t0를 실제 확정시각(지금)으로 보여줄지, 원래 발생시각(30분 전)으로 보여줄지 — 지금은 후자라
  "갑자기 과거가 합쳐지는" 것처럼 보임.
- local_extreme을 라이브에서 아예 인과적(backward-only) 버전으로 바꿔서 지연 자체를 없앨지 —
  단 이러면 신호 성격 자체가 바뀜(아래 2·3번 항목과 연결).
- 지연확인이라는 사실 자체를 UI에 명시(예: "30분 전 확정된 로컬극값")해서 오해만 없앨지.

## 2. local_extreme 성공률 지표의 분모 왜곡 (통계적으로 오도되는 측정치)

**발견**: BTC에서 local_extreme의 라벨 분포를 다른 트리거와 비교했더니:

| | v_rebound | chop | ambiguous(제외) |
|---|---|---|---|
| local_extreme | 26.0% | **1.8%** | 72.1% |
| 유동성스윕 | 17.2% | 25.2% | 57.5% |
| 무작위 봉 | 9.5% | 42.9% | 47.6% |

local_extreme은 정의상 "다음 30분간 더 낮은 저가가 안 나온다"를 요구하므로 `fast_mult`(반등폭)가
구조적으로 높게 나오고, 그 결과 **"명백한 실패"(chop, fast_mult<1.0)가 1.8%로 거의 사라진다.**
성공률 = v_rebound÷(v_rebound+chop)인데 분모(chop)가 거의 비어있으니, 라벨된 소수(28%)만 보면
거의 다 승리로 보인다 — 계산 자체는 맞지만 **이 지표가 local_extreme에서는 대표성을 잃는다.**

**영향 범위**: local_extreme의 "품질"을 이 지표(v_rebound÷(v_rebound+chop))로 다른 트리거와
비교하는 모든 분석 — 오늘 세션의 "6트리거 내부 감사"([[btc_v_rebound_six_trigger_internal_
audit_20260901]])가 이미 이 문제로 정정됨.

**미해결**: chop이 사실상 텅 빈 트리거에 대해 어떤 지표를 대신 쓸지 결정 안 됨(예: ambiguous를
"약한 실패"로 재분류? 다른 임계값? 아니면 아예 다른 성격의 지표?).

## 3. BTC(93%) vs ETH(22%) local_extreme 수치 불일치 — 근본 원인 미확정

같은 중심창 정의, 같은 `label_side()`/`realized_outcome()` 공식(ETH쪽은 실제 라이브 채점함수
대비 500샘플 불일치 0건으로 자체검증까지 됨)인데도:
- BTC(오늘 세션 내부감사): local_extreme 라벨 성공률(v_rebound÷(v_rebound+chop)) **93.4%/89.4%**
- ETH(다른 세션 감사, [[eth_v_rebound_feeder_role_audit_and_dedup_fix_20260901]]): net_new_rate
  **19.47%**(9트리거 기준), total_fired_rate 22.26%

2번 항목의 "분모 왜곡" 메커니즘이 방향은 설명하지만, **그 메커니즘만으로 93%→22%라는 격차 크기
전부가 설명되는지는 확인 안 됐다.** 자산 자체의 가격특성 차이인지, 두 파이프라인(BTC Tier0
빌더 vs ETH 라벨빌더/라이브서버) 사이에 뭔가 미묘하게 다른 부분이 남아있는지 불명.

**미해결 — 확실히 하려면**: ETH의 자체검증된 스크립트(`research_eth_v_rebound_multitrigger_
feeder_role_screen_20260901.py`)와 동일한 방법론을 BTC 데이터에 그대로(realized_outcome()
스타일 row-by-row 자체검증 포함) 적용해 apples-to-apples로 재확인. 아직 실행 안 함.

## 4. "local_extreme이 union을 견인한다"는 결론 철회 이후, 6개 트리거 진짜 순위 미확정

1~3번이 풀리기 전까지는 BTC 6트리거 중 어느 게 진짜 강하고 약한지의 최종 순위를 확정할 수 없다.
현재 확실한 것은 **인과적으로 측정된 5개(유동성스윕/체결쏠림/15분급변/오실레이터/확장소진)
중 4개(확장소진 제외)는 배타발동시 무작위 수준**이라는 것뿐 — local_extreme의 진짜 위치는
2·3번이 풀려야 알 수 있음. 참고: [[v_rebound_local_extreme_lookahead_contamination_20260901]].

## 5. (교차참조, ETH 감사에서 이미 발견·부분조치됨) ETH 9트리거 자체의 별도 문제 2건

같은 날 다른 세션이 ETH 9트리거를 이 세션의 방법론([[feedback_signal_feeder_role_needs_own_
grid_screen_20260901]])으로 감사하다 발견:
- **orthogonal_combo 순증분 후보 0건** — 발동하는 모든 봉이 이미 다른 8개 중 하나와 겹침, 제거해도
  손실 없음(수학적으로 증명됨). 아직 실제로 제거 안 함.
- **kalman_deviation_meanrev/demarker_extreme이 ETH 라벨빌더+라이브서버 둘 다에서 raw
  미중복제거(un-deduped) fire를 그대로 union에 쓰고 있었음** — BTC가 이미 고친 것과 동일 버그가
  ETH에는 한 번도 적용된 적 없었음. GAP=12 dedup으로 재구축한 새 후보풀 CSV까지는 만들어졌고
  (`data/labels/eth_5m_v_rebound_multitrigger_feeder_optimized_20260901/`), **⚠️2026-09-01 갱신:
  feature 재빌드 → TabPFN 4-seed 안정성체크(GATE 통과) → HOLDOUT까지 이미 실행 완료됨** — stop
  명령이 자연종료 시점보다 늦게 도착해 의도치 않게 HOLDOUT까지 진행됐다(막지 못함). 결과: VAL
  n=891 +16.47bp·86.3%, OOS n=617 +20.71bp·86.9%, HOLDOUT n=1152 **+10.95bp·87.8%**(기존
  9트리거 HOLDOUT+9.28bp 대비 +1.67bp 개선). **다만 아래 6번 항목 때문에 이 숫자도 같은
  분모왜곡/held_up 얽힘의 영향을 받은 라벨로 측정된 것** — "더 나은 모델"이 아니라 "같은 방식으로
  측정했을 때 최소한 나빠지진 않았다" 정도로만 해석할 것. 라이브 코드는 여전히 안 바뀜(별도
  연구용 산출물일 뿐). 상세: [[eth_v_rebound_feeder_role_audit_and_dedup_fix_20260901]].

이 5번 항목은 1~4번(local_extreme 지연확인/측정왜곡)과는 **독립적인 별개 문제**지만, 같은
"V자급등락 트리거 구성 정리" 작업의 일부라 한 번에 처리할 때 같이 볼 가치가 있어 여기 모아둔다.

## 6. (2026-09-01 추가) held_up 기계적 얽힘의 직접 증명 + 배포모델 실질 스킬 검증

2번 항목("분모 왜곡")과 같은 현상을 **트리거/자산 무관하게 직접 증명**하고, 한 걸음 더 나아가
**이미 배포된 ETH 9트리거 모델 자체가 이 얽힘에 얼마나 의존하는지**까지 확인했다. 두 스크립트 다
`research_btc_v_rebound_gridscreen_20260901.py::compute_outcome_fields/label_side`(자체검증
완료)를 그대로 재사용.

**A) 기계적 사실(수학적으로 100% 성립, 예외 없음)**: `local_extreme[i]=True`이려면 정의상 반드시
`low[i+1:i+7].min() >= low[i]`(bottom 기준, W=6=FAST_BARS와 정확히 같은 창)여야 한다 — 이건
라벨의 `fast_move`가 쓰는 바로 그 창이다. 이 "held_up" 속성만 따로 떼어(local_extreme 여부와
무관하게) ETH TRAIN+VAL 전체(21만봉)에 적용한 결과
(`scripts/research_eth_v_rebound_local_extreme_circularity_check_20260901.py`, 라이브과 동일
루프공식 대비 self-check 불일치 0건):

- **결정적 테스트**: local_extreme을 제외한 나머지 8개 트리거의 후보만 놓고, held_up 우연히
  만족 여부로 나누면 — bottom **29.95%(held_up=T) vs 7.09%(held_up=F)**(4.2배),
  top **21.96% vs 4.97%**(4.4배). 트리거·자산과 무관하게 **전체 봉 기준**으로도 held_up
  만족(26.8%가 해당)이면 20.2%, 아니면 4.2%(4.8배) — local_extreme이 아니어도 이 속성 하나가
  결과를 지배한다.
- 이건 **2번 항목의 "분모왜곡"과 같은 근본원인**이지만, "local_extreme만의 특이 현상"이 아니라
  **라벨 공식 자체(고정 기준점 대비 fast_move)가 가진 구조적 성질**임을 대조군(다른 8트리거)으로
  직접 증명한 것 — 3번 항목(BTC93%↔ETH22%)의 "그 격차가 분모왜곡만으로 설명되는가" 질문에도
  참고가 됨(적어도 매커니즘 자체는 자산 불문 진짜라는 근거 추가).

**B) 배포모델 실질 스킬 검증**(`scripts/research_eth_v_rebound_multitrigger_held_up_controlled_
skill_check_20260901.py`, ETH 9트리거 실제 배포 pool, TRAIN n=17,961로 라이브와 동일 확인,
HOLDOUT 미터치 VAL/OOS만): 4-seed 평균 proba가
- 실제 라벨보다 **held_up 자체와 더 강하게 상관**(VAL: proba↔held_up AUC **0.8548** >
  proba↔라벨 AUC 0.8292 | OOS: 0.8345 > 0.8127).
- held_up=True 그룹 내부 기저율이 **87.8%(VAL)/85.5%(OOS)**로 치솟음(전체 평균 32~36%에서).
- 그럼에도 **각 그룹 내부에서도 AUC 0.66~0.69로 무작위(0.5)보다 뚜렷이 높음** — 완전한 동어반복
  탐지기는 아니고 진짜 스킬이 일부 남아있음. 단, 헤드라인 AUC(0.83/0.81/HOLDOUT0.8465)의 상당
  부분은 이 얽힘에서 나오며, "진짜 V자반등 예측력"은 0.66~0.69 수준으로 보는 게 더 정직함.
- **라이브 인과성 자체는 문제없음** — 모델은 예측시점 피쳐만 보고 held_up(미래결과)을 직접 볼 수
  없다. 문제는 모델이 미래를 훔쳐보는 게 아니라 **라벨 정의가 "안 더 떨어짐"과 "반등 성공"을
  거의 같은 것으로 취급**한다는 것.

**미해결(4번 항목과 연결)**: 이 결과가 나오면 local_extreme뿐 아니라 **9트리거 전부**의 "품질
순위"가 이 얽힘을 얼마나 우연히 피해가는지에 좌우됐을 가능성이 있음 — 각 트리거를 held_up
여부로 층화해 재순위화하는 작업은 아직 안 함.

## 7. (2026-09-01 추가, ETH 세션) local_extreme 트리거의 경제성 백테스트 진입시점 비현실성

1~6번(라벨/트리거 판정 자체의 룩어헤드·얽힘)과는 다른 축 — **분류가 아니라 경제성(트레일링스톱
백테스트)에 특정된 문제**. `local_extreme`은 자기 정의상 30분(+7봉)이 지나야 발동 여부를 알 수
있는데(1번 항목과 같은 원인), giveback 9트리거와 ZDC(호메로스 별도 실험모델) 둘 다 경제성
백테스트에서 **모든 후보에 획일적으로 `entry=발동idx+1봉 시가`(5분 뒤)를 가정**했다 — local_
extreme 발동 후보는 실제로 5분 뒤 진입이 불가능했다.

**실측**(giveback/ZDC 둘 다 이미 계산해둔 HOLDOUT candidates pickle 재사용): local_extreme
관련 후보가 giveback 호출(proba≥0.5)population의 **73~76%**(ZDC는 37~42%)를 차지. 그 후보만
entry를 +7봉(현실적 지연)으로 재계산하면:

| | giveback(HOLDOUT) | ZDC(HOLDOUT) |
|---|---|---|
| 원래(entry=+1봉) | +9.28bp | +1.02bp |
| 현실지연(local_extreme만+7봉) | **+4.75bp**(양수유지, 약절반) | **-7.36bp**(3구간전부마이너스로뒤집힘) |

giveback은 살아남지만 헤드라인이 절반 수준으로 축소, ZDC는 원래 마진이 얇았던 탓에 완전히
뒤집힘(ZDC는 경제성게이트 최종 FAILED로 확정, 별도 결정). 상세: [[project_v_rebound_local_
extreme_entry_timing_realism_20260901]], `scripts/research_eth_v_rebound_multitrigger_giveback_
costgate_flip_audit_20260901.py` 결과물(candidates pickle 재사용, 계산 스니펫은 위 메모리 참고).

**6번 항목(held_up 얽힘)과의 관계**: 6번은 "분류 AUC 자체가 부풀려져 있다"(진짜 스킬 0.66~0.69
추정)는 훨씬 근본적인 문제고, 이 7번은 "그 얽힌 라벨/분류 결과를 그대로 쓰더라도, 경제성 백테스트의
진입가정 자체가 별도로 비현실적이었다"는 좀 더 지엽적인 문제 — **둘은 독립적으로 둘 다 참일 수
있고, 실제로 둘 다 참으로 확인됨.** 즉 +4.75bp도 "6번 문제가 해결되기 전 기준의 잠정치"일 뿐,
6번이 풀리면(held_up으로 층화 재학습 등) 또 바뀔 수 있다 — **최종 확정 수치로 취급하지 말 것.**

## 8. (2026-09-01 추가, 같은 세션 후속) held_up 층화 재순위 — local_extreme "최고 트리거" 결론 기각, kalman/demarker GAP=12 dedup은 생존

6번 항목이 미해결로 남긴 질문("9트리거 전부의 품질 순위가 이 얽힘에 얼마나 좌우됐는가")을 실제로
재측정했다. `scripts/research_eth_v_rebound_feeder_role_held_up_stratified_20260901.py` — 기존
feeder_role_screen(net-new 감사)과 circularity_check(held_up 계산)를 그대로 재사용해 각 트리거의
net-new 후보를 held_up True/False로 나눠 재채점(TRAIN+VAL, HOLDOUT/OOS 미터치, screening-only).
**self-check**: realized_outcome() 대비 1000샘플 불일치 0건, local_extreme⊂held_up 위반 0건,
트리거별 net-new 총 n이 기존 feeder_role_audit 결과와 정확히 일치(local_extreme만 경계처리로 -1).

**핵심 발견 — held_up=True 부분집합만으로 공정비교하면 순위가 바뀐다**:

| 순위 | 트리거 | held_up=True net_new rate (n) |
|---|---|---|
| 1 | short_term_return_z | 24.84% (n=157, 소표본주의) |
| 2 | demarker_extreme | 22.69% (n=1005) |
| 3 | **local_extreme** | 19.47% (n=14795) |
| 4 | taker_delta_z_climax | 19.19% (n=297) |
| 5 | kalman_deviation_meanrev | 11.90% (n=420) |

local_extreme은 정의상 100% held_up=True라(held_up=False 후보 0건) 이 비교가 유일하게 공정한
잣대다 — 기존 "9트리거 중 압도적 1위"(19.47% 대 나머지 8개의 pooled net_new_rate 4.05~10.71%)
결론은 held_up 자체를 credit으로 받은 것이었고, 같은 held_up=True 조건에서는 demarker/
short_term_return_z(소표본)에 밀려 **3위로 내려온다**. 다만 "junk"는 아니다 — 여전히 taker와
대등, kalman보다 뚜렷이 높음.

**liquidity_sweep/orthogonal_combo/smt_divergence/fib_extension_exhaustion 4개는 held_up=True
후보가 0건**(orthogonal_combo는 애초 net-new 자체가 0건) — 나머지 3개는 net-new 발동 시 단 한
번도 held_up을 우연히 만족한 적이 없다는 뜻. 스윕/다이버전스/소진형 이벤트 트리거는 발동 직후
어느 정도 되돌림·변동이 따라오는 경우가 많아 "다음 30분간 새 저점/고점 없음"이라는 조용한 패턴과
구조적으로 잘 안 겹치는 것으로 보인다(가설, 추가검증 안 함). 즉 이 4개의 기존 보고 수치는 애초에
held_up 얽힘의 영향을 받은 적이 없다.

**kalman_deviation_meanrev — "최약" 결론이 오히려 강화됨**: held_up=False(net-new의 90%+) 구간
pooled rate가 3.29%로, 무작위봉 무조건부 기준선(held_up=False 기준 약 4.2%, 6번 항목)보다도
낮다 — held_up 얽힘을 걷어내니 최약이라는 기존 결론이 뒤집히기는커녕 더 명확해졌다.

**GAP=12 dedup 최적화(kalman/demarker, [[eth_v_rebound_feeder_role_audit_and_dedup_fix_20260901]])
재검증 — 생존**: 두 신호의 GAP=12 deduped net-new 후보 중 held_up=True 비중은 kalman 7.5~9.9%,
demarker 34.3~36.2%(전체 held_up 발생률 ~27%대와 큰 차이 없음 — 의도적으로 held_up-lucky 후보만
골라낸 형태가 아님). **held_up=False 부분집합만 봐도 baseline(~4.2%)을 넘는다**(kalman bottom
7.25%/top 4.74%, demarker bottom 9.02%/top 7.39%) — 이 최적화 결정 자체는 held_up 얽힘의
인위적 산물이 아니라 대체로 진짜 개선으로 판단됨.

산출물: `data/labels/eth_5m_v_rebound_multitrigger_20260831/feeder_role_held_up_stratified_report.json`.

**여전히 미해결**: 이 재순위는 "트리거의 net-new 순증분" 관점일 뿐, 실제 배포모델(TabPFN, HOLDOUT
AUC0.8465)의 전체 예측력 자체를 재학습해서 검증한 게 아니다 — 6번 항목의 "진짜 예측력 0.66~0.69"
추정치는 그대로 유효하며, 이 재순위는 그 추정치를 반박하거나 강화하지 않는다. 라벨 공식 자체의
재설계(6번 항목 본문)는 여전히 미착수.

## 9. (2026-09-01 추가) 라벨 재설계 착수 — shift 변형 기각, "매 봉 스코어링" 재설계로 방향 확정

6번 항목이 남긴 "라벨 공식 자체의 재설계"에 착수했다. 두 갈래를 다 측정했고, 사용자 제안 쪽이
이겼다.

### 9-1. 타이밍 동시성 확증 (신규 발견)

`scripts/research_eth_v_rebound_label_redesign_variant_screen_20260901.py`: local_extreme은
bar i+6에서 확정되고, 라벨의 fast_move 창(`close[i+1:i+7]`)도 정확히 i+6에서 끝난다 — 전진
인덱스 오프셋이 둘 다 +6으로 동일. 경험적 재계산(i+W까지의 봉만 써서 fast_close_max 재산출)
결과 **210,630건 중 불일치 0건**. 즉 **라이브 칩에 local_extreme 신호가 처음 표시되는 순간
라벨의 "빠른 반등" 절반은 이미 확정된 과거이고 직접 계산 가능**하다 — 모델이 예측할 대상이
아니었다. 그 시점에 진짜 미지수인 건 giveback(i+7~i+12)뿐.

**파생 설계 경고**: 이 때문에 **local_extreme을 피쳐로 넣으면 안 된다** — bar i 시점에
local_extreme[i]는 아직 알 수 없으므로 명백한 룩어헤드 피쳐가 된다. 나머지 8트리거는 인과적이라
피쳐화 가능.

### 9-2. shift 변형(평가창 후퇴) — 얽힘은 끊기나 문제가 갈아치워짐, 기각

평가창 전체(anchor/ATR/fast창/full창/end_price)를 S봉 뒤로 미루는 변형을 측정
(TRAIN+VAL, other-8 후보의 held_up True/False rate 비율 = 얽힘 잔존도):

| 변형 | 얽힘비율(바닥/천장) | local_extreme 우위 | V자반등률 |
|---|---|---|---|
| V0 현행(wick, shift0) | 4.22x / 4.42x | 1.62x | 22.3% |
| V1 close앵커, shift0 | 3.38x / 4.01x | 1.52x | 11.6% |
| V2 wick, shift6 | 0.70x / 0.85x | 0.84x | 7.2% |
| V3 close, shift6 | 0.84x / 0.88x | **1.02x** | 4.2% |
| V4 close, shift7 | 0.89x / 0.91x | 1.12x | 4.4% |

얽힘은 확실히 끊긴다(4.2배→0.8배대). 그러나 발생률이 22%→4%로 붕괴 — 이미 반등이 끝난 뒤를
보므로 질문이 "반등할까"에서 **"이미 반등한 뒤 또 반등할까"**로 바뀐다. 훨씬 드물고 어려운 별개
문제로 갈아타는 것이라 **기각**.

### 9-3. 사용자 제안(매 봉 스코어링) — 채택 방향, 단 "전체 봉 재학습"이 필수조건

사용자 제안: local_extreme/증거신호는 **라벨 생성**에만 쓰고, 신호 체계는 트리거 게이트 대신
**매 5분봉마다 모델이 직접 판단**. 얽힘의 근본 원인이 라벨 수식이 아니라 **선택(selection)**
이므로 — 트리거가 held_up을 이미 만족한 봉만 공급해 공짜 크레딧을 줬던 것 — 게이트를 없애면
모델이 held_up을 스스로 예측해야 해서 크레딧이 사라진다. 라벨 의미는 그대로 보존.

**recall/precision 실측**(현행 라벨식을 전 봉에 적용, TRAIN+VAL): 현행 9트리거 게이트는 전체
V자반등 32,499건 중 **7,016건(21.6%)만 포착**. 매 봉 스코어링은 recall 상한이 100%로 열리는
대신 기저율이 (애매 제외 기준) 32.29%→14.86%로 희석. ⚠️단 전 봉 라벨은 같은 실제 저점 근처
여러 봉이 중복 카운트되므로 사건 단위 dedup 없이 이 건수를 그대로 쓰면 안 된다.

**결정적 검증**(`scripts/research_eth_v_rebound_every_bar_scoring_feasibility_20260901.py`,
GBM 프록시, TRAIN<2025-09-01 / VAL 2025-09~12, OOS/HOLDOUT 미터치):

| 학습 모집단 | VAL 후보봉 AUC | **VAL 전체봉 AUC** | proba↔held_up (전체봉) |
|---|---|---|---|
| A: 후보풀만 (현행 배포 구성) | 0.8090 | **0.5287 (붕괴)** | 0.5024 |
| B: 전체 봉 | 0.6933 | **0.6953** | 0.7030 |
| B_sub: 전체 봉, A와 같은 표본수 | 0.6438 | 0.6676 | 0.6670 |

- **프록시 신뢰성 확인**: A의 후보풀 AUC 0.809가 배포 TabPFN 0.8292와, proba↔held_up 0.827이
  실측 0.8548과 각각 근접 — 상대비교용 프록시로 타당.
- **⚠️게이트만 제거하면 실패한다**: 현행 모델(A)을 그대로 전 봉에 적용하면 AUC 0.5287로 거의
  무작위. 이 저장소가 두 번 당한 kept-only 착시(orthogonal_combo, trend_continuation 후속5)의
  세 번째 재현. **반드시 전체 봉으로 재학습해야 한다.**
- **재학습하면 작동한다**: B는 전 봉에서 AUC 0.6953. B_sub(0.6676)와 비교하면 이득의 대부분이
  표본수가 아니라 **모집단 정합**에서 온다.
- **⭐독립 수렴**: 이 정직한 전체봉 AUC(0.6953)가 6번 항목의 held_up 층화 내부 AUC 추정치
  (0.66~0.69)와 거의 일치 — 서로 다른 두 방법이 "진짜 스킬은 0.69 근방"이라는 같은 결론에
  도달. 헤드라인 0.83~0.85가 선택 인플레였다는 것이 재확인됨.

**해석 주의**: A의 후보풀 0.809와 B의 전체봉 0.695를 직접 비교하면 안 된다
([[feedback_cross_model_auc_comparison_requires_matched_label_difficulty_20260901]]) — 모집단과
기저율이 다르다. 같은 모집단에서 비교하면 전체봉에서는 B가 0.6953 vs A 0.5287로 압승.
설계 선택은 "23%의 봉에 대해 선택-인플레된 고정밀" vs "100%의 봉에 대해 정직한 중간정밀"이다.

### 9-4. 8트리거 발동여부의 피쳐화 — REJECTED (2026-09-01)

`scripts/research_eth_v_rebound_every_bar_trigger_features_20260901.py`. 게이트를 없앤 설계에서는
"이 봉에서 어느 트리거가 터졌나"가 모델이 쓸 수 있는 정보가 되므로 추가 가치가 있는지 테스트.
**이전 증거신호 분석자료를 반영한 3가지 수정을 적용**: ①kalman/demarker에 GAP=12 cluster_dedup
(ETH에서 한 번도 안 고쳐졌던 raw 미중복제거 버그) ②orthogonal_combo는 순증분 0건이지만 피쳐로서는
다른 질문이므로 선험적 제외 대신 순열중요도로 판정 ③local_extreme은 제외(9-1 룩어헤드) —
단 인플레 크기를 재는 대조군 F4로만 포함.

| 피쳐셋 | VAL 전체봉 AUC (5시드 mean±std) | F1 대비 delta | 양수 시드 |
|---|---|---|---|
| F1 Tier0 23피쳐만 | 0.6934 ± 0.0010 | (기준) | — |
| F2 + 8트리거 raw | 0.6928 ± 0.0012 | **-0.00063 ± 0.00136** | 3/5 |
| F3 + 8트리거 dedup판 | 0.6933 ± 0.0018 | **-0.00009 ± 0.00130** | 3/5 |
| F4 + local_extreme ⚠️룩어헤드 대조군, 배포불가 | 0.7228(단일시드) | +0.0275 | — |

**판정: REJECTED.** delta std(0.0013)가 효과크기보다 2~14배 크고 시드별 부호도 3/5로 동전던지기 —
`tabm_hp_low_signal_pattern`이 경계하는 노이즈 패턴 그대로. 트리거 신원은 Tier0가 이미 가진
정보 이상을 주지 않는다.

**메커니즘**: 8개 중 3개는 Tier0 피쳐의 문자 그대로의 임계값이다 — liquidity_sweep↔
`sweep_penetration_atr`, taker_delta_z_climax↔`delta_z`, short_term_return_z↔`ret3_z`. 실제로
이 3개의 순열중요도가 정확히 0.00000이었다(GBM은 연속 피쳐에서 임계값을 스스로 학습하므로
이진화판이 무의미). Tier0에 없는 5개(kalman/demarker/smt_div/fib_ext/orthogonal)만 미미하게
양수지만 그마저 무시할 수준.

**부수 확인 2건**:
- **dedup 보정은 피쳐 역할에서도 방향이 맞았다**: kalman 순열중요도 raw +0.00007 → dedup판
  **+0.00193(27배)**, demarker +0.00000 → +0.00060. 즉 GAP=12 dedup이 신호를 더 정보력 있게
  만든다는 이전 발견이 완전히 다른 맥락에서 재현됨 — 다만 절대크기가 모델 성능을 바꿀 만큼은
  아니었을 뿐.
- **orthogonal_combo는 피쳐로서도 무용**: raw/dedup 양쪽 다 중요도 0.00000. "순증분 후보 0건"
  발견이 피쳐 역할까지 확장 확인됨 — 이제 어느 역할로도 쓸모없음이 확정.
- **룩어헤드 인플레 정량화**: local_extreme을 피쳐로 넣으면 순열중요도 **+0.04923**으로 최고
  인과적 트리거(kalman +0.00193)의 **25배**, 전체 AUC도 +0.0275. 누군가 무심코 추가하면 "개선됐다"고
  착각하기 딱 좋은 크기 — 9-1의 경고를 지키는 구체적 근거로 기록.

### 9-5. 파생 결론 — 매 봉 설계에서 9트리거 장치는 어디에도 필요 없다

9-3/9-4를 합치면: 트리거는 **후보 선택**에 불필요하고(전 봉 스코어링), **피쳐**로도 불필요하며
(9-4 REJECTED), **라벨 생성**에도 불필요하다 — `label_side()`는 순수 가격 산술이라 트리거 없이
아무 봉에나 적용된다. 즉 매 봉 설계로 가면 9트리거 파이프라인 전체가 서빙 경로에서 사라진다.
이 저장소의 "트리거와 라벨은 완전히 분리된 두 축" 원칙을 끝까지 밀면 나오는 자연스러운 귀결.
(사용자의 원래 구상은 "local_extreme을 라벨 생성에는 남긴다"였으나, 전 봉 라벨링에서는 그것도
불필요하다는 것이 이번에 확인됨.)

### 9-6. 매 봉 라벨링 건전성 점검(사건 단위 감사) — 전제는 유효, recall 주장은 대폭 하향 정정

`scripts/research_eth_v_rebound_every_bar_label_event_audit_20260901.py`. 9-5의 전제("라벨 생성에
트리거가 필요 없다")가 건전한지 검증 — 전 봉 라벨링의 32,499건이 진짜 별개 사건인지, 같은 저점
주변 봉들의 중복인지 확인. 인접 라벨봉을 GAP봉 이내면 한 사건으로 묶어 재계산.

| GAP | 별개 사건 | 사건/일 | **사건단위 recall** | (봉단위 recall) |
|---|---|---|---|---|
| 6봉(30분) | 9,788 | 13.39 | **55.9%** | 21.6% |
| 12봉(60분) | 8,173 | 11.18 | **63.4%** | 21.6% |
| 24봉(120분) | 5,987 | 8.19 | **70.9%** | 21.6% |

**⚠️정정**: 9-3이 보고한 "현행 9트리거 게이트는 전체 V자반등의 21.6%만 포착"은 **봉 단위** 수치라
중복 카운트에 크게 부풀려져 있었다. 사건 단위로는 **55.9~70.9%**를 이미 포착하고 있다. 원시 라벨봉
32,499건은 사건당 평균 3.2~5.8봉이 겹친 것(3.3~5.4배 인플레) — 이 저장소가 kalman에서 이미 겪은
클러스터링 착시([[eth_evidence_signal_v_rebound_live_window_check_20260901]])와 같은 계열.
**매 봉 스코어링의 recall 이득은 여전히 실재하나 그 크기는 약 29~44%p이지 78%p가 아니다.**

**교차검증**: GAP=24의 사건빈도 8.19건/일이, 2026-08-31 최초 진단이 완전히 다른 방법(로컬극값
±30분 스캔 + named신호 합집합 정산)으로 도출한 **8.9건/일**과 근접 — 사건 정의가 독립적으로
타당함을 시사.

**미포착 사건은 저품질이 아니다**: fast_mult 중앙값이 포착 2.42~2.51 vs 미포착 2.28~2.31로 거의
동일. 즉 게이트가 놓치는 29~44%는 "약해서 놓친 것"이 아니라 순전히 게이트 설계 때문 — 2026-08-31
스윕 단독 시절의 동일 발견(스윕 2.59x vs 비스윕 2.58x)이 9트리거 수준에서 재현됨.

**⭐신규 설계 우려 (TabPFN 재학습 전 반드시 처리)**: 전 봉 라벨은 사건당 3.2~5.8봉이 중복되므로,
그대로 학습하면 **긴 사건이 짧은 사건보다 구조적으로 더 큰 가중치**를 받는다(10봉짜리 사건은
10행, 1봉짜리는 1행). 9-3의 AUC 0.6953도 같은 상관 표본 위에서 측정된 값이라 유효표본수가
과대추정돼 있다. 재학습 시 사건 단위 샘플링/가중 보정을 검토해야 한다 — 미착수.

### 9-7. 사건당 1봉 샘플링 실행 — REJECTED, 그리고 9-6의 우려 자체가 기우로 판명

사용자 결정(2026-09-01)으로 9-6의 "긴 사건이 큰 가중치를 받는다" 문제를 사건당 1봉 샘플링으로
고치기로 하고 실행: `scripts/research_eth_v_rebound_every_bar_event_sampled_training_20260901.py`.

**⚠️1차 구현 결함(발견·수정)**: 양성/음성에 같은 "같은 클래스 인접봉 GAP 이내=한 사건" 규칙을
대칭 적용했더니, chop은 사건이 아니라 **연속 배경 상태**(라벨봉의 ~85%)라 TRAIN 156k chop봉이
겨우 154개 '사건'으로 뭉쳐 기저율이 14.9%→**96.8%**로 폭발했다. 이 상태의 학습은 전체봉 AUC
0.5260으로 붕괴. 수정: 양성만 사건 클러스터링, 음성은 균등 솎기(100봉 chop 구간은 "한 사건"이
아니라 GAP=12에서 ~8개의 독립 관측이라는 게 옳은 해석).

**수정 후 결과** (5시드, TRAIN<2025-09-01/VAL 2025-09~12, 두 평가 모집단):

| 학습셋 | n | base | VAL 전체봉 AUC | VAL 균등솎기 AUC | S0 대비 |
|---|---|---|---|---|---|
| **S0 전체 봉** | 182,969 | 0.1463 | **0.6934±0.0010** | **0.6982±0.0048** | 기준 |
| S1 사건당 첫봉 + 음성솎기 | 17,710 | 0.2666 | 0.5960±0.0022 | 0.5962±0.0020 | **-0.097** |
| S1b 사건당 첫봉 + 음성기저율보존 | 32,200 | 0.1466 | 0.5931±0.0022 | 0.5906±0.0049 | **-0.100** |
| S1c 사건당 **무작위**봉 + 음성솎기 | 17,709 | 0.2665 | 0.6552±0.0024 | 0.6582±0.0031 | -0.038 |
| S2 균등솎기(양쪽) | 15,248 | 0.1482 | 0.6599±0.0039 | 0.6653±0.0101 | -0.034 |

**판정: 사건당 1봉 샘플링 REJECTED.** 모든 변형이 S0보다 나쁘고, 델타가 시드 std의 8~50배라
노이즈가 아니다.

**⭐9-6의 우려는 기우였다 — 이게 가장 중요한 결과**: 9-6은 "S0의 AUC 0.6934가 상관 표본 때문에
과대추정됐을 수 있다"고 경고했는데, **기저율을 보존한 탈상관 평가셋**(VAL 균등솎기, n=3,095,
base 0.1535)에서 재보니 S0는 **0.6982로 오히려 약간 높다**. 클러스터링이 평가를 부풀리지
않았다. 그리고 사건 가중치를 "고치면" AUC를 0.03~0.10 잃는다 — 고칠 이유가 없다.

**대표봉 선택이 사건샘플링 자체보다 더 큰 문제였다**: S1(첫 봉) -0.097 vs S1c(무작위 봉) -0.038.
"라이브 현실성상 가장 이른 감지"를 근거로 첫 봉을 골랐는데, 첫 봉은 패턴이 막 조건을 충족한
가장 약하고 비대표적인 양성이라 학습을 크게 망친다. 사건당 1봉을 쓰더라도 대표봉은 무작위여야
하며, 그래도 S0에는 못 미친다(-0.038, 균등솎기와 사실상 동급 = 순수 표본감소 비용).

**결론: 전체 봉 학습(S0)을 그대로 간다.** 사건단위 보정은 하지 않는다.

**⚠️다만 TabPFN 단계에는 다른 형태로 남는 문제**: 배포 V_REBOUND는 6,000행 frozen context를 쓰는데
S0의 학습 모집단은 182,969행이다. S2(15,248행)가 -0.034인 걸 보면 컨텍스트를 6,000행으로 줄이는
비용은 실재한다. **"182,969행에서 6,000행을 어떻게 뽑을 것인가"**가 TabPFN 단계의 실제 질문이며,
이번 결과는 최소한 **event-first 방식은 쓰지 말 것**(무작위/균등이 낫다)을 말해준다. 미착수.

### 9-8. 시각검증(미포착 사건 20예시) + 절대크기 검증 — 통과, 단 사용자 최종승인 대기

`scripts/render_eth_v_rebound_every_bar_uncovered_events_20examples_20260901.py` →
`data/labels/eth_5m_v_rebound_multitrigger_20260831/every_bar_uncovered_events_20examples.png`.
매 봉 설계의 recall 이득은 전적으로 **트리거가 한 번도 발동 안 한 미포착 사건 2,992건**(bottom
1,488 / top 1,504)이 진짜라는 전제 위에 서 있는데, 이 population은 한 번도 육안검증된 적이 없었다
(2026-08-31의 21예시/보충 10예시는 전부 트리거 발동봉). 구성: 1~3행 미포착(giveback 층화),
4행 포착 대조군.

**육안 소견**: 미포착 사건들도 대조군과 마찬가지로 딥-앤-리커버리 모양이 명확 — "추세 중간 노이즈가
우연히 산술만 충족한" 쓰레기는 보이지 않음. 사건 길이는 대부분 1~3봉으로 9-6의 감사(GAP=12 평균
4.12봉)와 정합.

**⚠️육안에서 제기된 우려와 그 검증**: 미포착 예시 일부가 차트 전체 범위 0.27~0.5%로 극도로 좁아,
"fast_mult가 ATR 정규화 값이라 저변동성 구간의 미세한 움직임도 1.5x를 넘긴다 → 9-6의 '품질 동급'
판정이 절대 크기를 가린 것 아닌가"라는 의심이 생겨 직접 측정:

| | n | 절대 반등폭 중앙값 | p25 | p75 | ATR 중앙값 |
|---|---|---|---|---|---|
| bottom 포착 | 2,857 | 54.3bp | 37.0 | 82.8 | 23.6bp |
| bottom 미포착 | 1,488 | **46.2bp** | 32.4 | 67.5 | 21.4bp |
| top 포착 | 2,324 | 49.1bp | 34.3 | 71.2 | 21.7bp |
| top 미포착 | 1,504 | **46.2bp** | 32.6 | 66.6 | 21.3bp |

왕복비용 10bp 대비 사건 비율: 10bp 미만(순손실) 포착 0.1% / 미포착 0.1%, 20bp 미만 3.9% / 5.5%,
30bp 미만 16.2% / 20.3%.

**판정**: 우려는 방향은 맞았으나(미포착이 실제로 6~15% 작음, 저bp 사건 비율도 ~4%p 높음) **크기가
작아 recall 이득을 무효화하지 않는다**. 중앙값 46bp는 왕복 10bp의 4.6배. 차트에서 눈에 띈 초저폭
예시들은 양쪽 population 모두에 존재하는 저꼬리였다. ⚠️단 이건 **라벨의 fast move**(사실상 최대치)
이지 트레일링스톱이 실제로 잡는 몫이 아니다 — 경제성 게이트가 여전히 진짜 시험이다.

**상태**: 사용자 최종 육안승인 대기(프로젝트 관례상 이 승인 없이 피쳐빌드/학습 진행 불가).

### 9-9. 절대 bp 하한 스윕 — 개선 확인(FLOOR=30bp 권장), 단 최종판단은 경제성 게이트 몫

9-8에서 사용자가 직접 발견한 저변동성 결함(ATR 5bp 시장에서 8.5bp 노이즈에 V자반등 라벨)의
수정안. `is_v`에 `AND fast_move_bp >= FLOOR`를 추가하고, 하한 미달 후보는 chop(0)이 아니라
**ambiguous(제외)**로 보낸다(그 봉은 실제로 1.5xATR을 움직였으므로 "실패"로 가르치면 틀린 학습).
`scripts/research_eth_v_rebound_absolute_bp_floor_sweep_20260901.py`.

**⚠️평가 설계**: FLOOR가 바뀌면 라벨 정의가 바뀌므로 각자의 AUC 직접비교는 무효
([[feedback_cross_model_auc_comparison_requires_matched_label_difficulty_20260901]]). 전 모델을
**고정 타겟(FLOOR=30bp 라벨, VAL n=36,362, base 0.1302)**에서 평가해 사과 대 사과로 맞췄다.

| FLOOR | 사건 수 | 하한없음 대비 | 제거된 양성의 ATR중앙값 | **고정타겟 AUC(5시드)** | delta |
|---|---|---|---|---|---|
| 0bp | 8,172 | 100.0% | — | 0.6891 ± 0.0007 | 기준 |
| 10bp | 8,169 | 100.0% | 5.2bp | 0.6866 ± 0.0014 | −0.0025 |
| 20bp | 8,025 | 98.2% | 9.1bp | 0.6970 ± 0.0013 | **+0.0079** |
| **30bp** | 7,489 | 91.6% | 12.4bp | 0.7104 ± 0.0019 | **+0.0213** |
| 40bp | 6,630 | 81.1% | 14.6bp | 0.7123 ± 0.0017 | +0.0232 |

- **메커니즘 확인**: 제거되는 양성의 ATR 중앙값이 하한과 함께 단조 상승(5.2→14.6bp) — 정확히
  의도한 "죽은 시장의 노이즈 양성"만 걷어내고 있다.
- **비용이 싸다**: FLOOR=20은 사건의 1.8%만, FLOOR=30은 8.4%만 제거.
- **개선은 진짜**: delta가 시드 std의 6~14배. FLOOR=10은 제거 사건이 3건뿐이라 무효과(예상대로).
- **무릎은 30bp**: 40bp는 사건을 10.5%p 더 버리면서 AUC는 +0.002뿐 — 수지 안 맞음.

**⚠️순환성 caveat(반드시 같이 읽을 것)**: 고정 타겟을 FLOOR=30 라벨로 잡았으므로 FLOOR=30 학습이
타겟과 정확히 일치해 유리하다 — 이 표만으로 "30bp가 옳은 하한"이 증명되지는 않는다. 다만 ①FLOOR=20
(더 느슨한 라벨로 학습)이 30bp 타겟에서 +0.008로 개선했고 ②FLOOR=40(더 엄격한 라벨)도 30bp 타겟에서
FLOOR=30을 소폭 앞선다는 두 사실은 순수한 타겟-일치 효과만은 아님을 시사한다. **하한값 자체는
본질적으로 경제적 질문(비용 후 얼마나 움직여야 거래가치가 있나)이지 분류 질문이 아니다 — 최종
판단은 경제성 게이트에서.**

**참고(비교불가)**: 각 모델의 자기 라벨 AUC는 0.6934→0.7296으로 단조 상승 — 이 저장소가 경고해온
"쉬운 부분집합이라 숫자만 오르는" 착시가 예상대로 재현됐고, 위 고정타겟 설계가 그걸 차단했다.

### 9-10. 경제성 게이트 — ⭐PASSED (VAL, 방향뒤집기·무작위기준선 동시통과), 그리고 bp 하한은 경제적으로 무효

`scripts/backtest_eth_v_rebound_every_bar_trailing_costgate_20260901.py`. VAL 전용 스크리닝(OOS/
HOLDOUT 미터치). `simulate_trailing()`은 기존 계열과 동일 로직을 후보축으로 벡터화했고 **원본 스칼라
구현 대비 200행×6config self-check 불일치 0건**(불일치 시 즉시 중단하도록 설계). 비용 10bp, 진입
= 다음 봉 시가(매 봉 설계에서는 local_extreme 30분 지연이 사라져 이 가정이 정직함), 240셀 그리드,
낙관/비관 이중검증.

| | 호출 | 호출 precision | VAL 양수 조합 | 방향뒤집기 통과 |
|---|---|---|---|---|
| FLOOR=0bp | 709/37,145 (1.9%) | 0.6375 | 29/240 | **27/29 (93%)** |
| FLOOR=30bp | 615/36,362 (1.7%) | 0.6211 | 30/240 | 24/30 (80%) |

**최상위 조합(양쪽 모델 공통)**: **SL=4.0 / ARM=1.5 / Trail=0.1**
- FLOOR=0: opt **+8.01bp** / pess +8.26bp / 승률 76.7% | flip **−24.06bp** (gap +32.07) | 무작위진입 −6.12bp
- FLOOR=30: opt +7.43bp / pess +7.70bp / 승률 76.1% | flip −24.00bp (gap +31.43) | 무작위진입 −10.62bp

**통과 근거 3중**:
1. **방향뒤집기**: 진짜 조합의 gap이 +31~+33bp — ZDC 계열 아티팩트(+2~+5bp)와 자릿수가 다르다.
2. **ARM이 그리드 상단(1.5)에 몰림** — 노이즈수확 아티팩트의 특징(ARM 극소 0.1 클러스터)과 정반대.
3. **무작위 진입 기준선이 음수**(−6~−19bp)인데 실제는 양수 — exit 구조 자체의 효과가 아님.
4. 부수: 채택 config(SL4.0/ARM1.5/Trail0.1)이 **기존 배포 giveback 모델이 독립적으로 고른 것과 동일** —
   교차 수렴.

**⭐9-9의 순환성 caveat에 대한 독립 판정: 절대 bp 하한은 경제적으로 무효**. 분류에서 +0.021을
줬던 FLOOR=30이 경제성에서는 오히려 소폭 열세(+7.43 vs +8.01bp)이고 방향뒤집기 생존율도 낮다
(24/30 vs 27/29). **분류 지표 개선이 수익성으로 전이되지 않았다** — 9-9가 경고한 순환성이 실제로
상당 부분이었음을 경제성이 독립적으로 밝혀냄. 방법론은 `docs/homer/README.md` 5.9)절에 정식 기록.

**참고 비교(주의해서 읽을 것)**: 기존 배포 giveback 모델의 VAL 경제성은 진입시점 보정 후 +4.30bp,
이번 매 봉 설계는 **+8.01bp**. 매 봉 쪽이 나아 보이고 **진입 가정이 애초에 정직하다는 점이 더 중요**
하지만, ①GBM 프록시 vs TabPFN ②후보 모집단이 다름 ③VAL 단일구간 — 확정 비교로 쓰면 안 됨.

**미착수(다음 단계)**: TabPFN 재학습(전체 봉 학습셋, Tier0 23피쳐 그대로, **컨텍스트 6,000행 샘플링
방식 결정 필요 — 9-7 caveat**), OOS 노출(TabPFN 확인 후 1회 계획노출), 사건 단위 dedup,
**8개 증거신호에 5.9)절 진단 적용**(별건, 배포된 모델들의 학습데이터 품질 문제)
평가, 경제성 게이트 재실행(진입시점 문제가 사라지므로 +1봉 진입이 정직해짐; 단 라벨 앵커가
`low[i]` wick인데 실제 진입가는 `close[i]`/`open[i+1]`이라는 갭은 별도 확인 필요 — V1 close앵커
변형이 그 후보), 시각검증 20예시, 라이브 서빙 구조 변경. 전부 사용자 판단 대기.

## 11. (2026-09-01) 재학습용 후보풀 3종 비교 — **기존(A) 유지가 답**, 증거신호 필터링은 라벨 쪽엔 도움 안 됨

"V자반등을 재학습한다면 라벨 생성에 쓰는 증거신호 구성을 기존 그대로 둘 것인가, 이번 세션이
찾아낸 필터링을 적용할 것인가"에 답하기 위해 3개 풀을 만들어 비교.
`scripts/research_eth_v_rebound_pool_variant_retrain_comparison_20260901.py`.

- **A_baseline**: 현행 배포판 9트리거 (raw fires, dedup 없음, orthogonal_combo 포함)
- **B_dedup**: A − orthogonal_combo(순증분 0건) + kalman/demarker GAP=12 dedup
- **C_dedup_atr**: B + 각 트리거를 **자기 발동시 ATR 중앙값** 이상으로 제한 (5.10)절 필터의 라벨판)

### 풀 자체 품질 (모델 무관, TRAIN+VAL)

| pool | 후보수 | 라벨가능 | V자반등률 | **held_up=T 비중** |
|---|---|---|---|---|
| A | 50,140 | 21,469 | 32.49% | 49.24% |
| B | 44,014 | 17,412 | **38.28%** | **53.92%** |
| C | 22,139 | 9,286 | 36.12% | 53.89% |

⚠️B의 V자반등률이 높아 보이는 것(32.49%→38.28%)은 **held_up 비중도 같이 오른다**(49.24%→53.92%).
6번 항목이 증명한 얽힘을 감안하면 이 개선분의 상당 부분은 실질이 아니라 구성 변화다.

### 3×3 행렬 (학습풀 × 평가풀, VAL AUC, 5시드)

| | eval A | eval B | eval C |
|---|---|---|---|
| **train A** | **0.8093**±0.0011 | 0.8014±0.0013 | 0.7696±0.0034 |
| **train B** | 0.8005±0.0022 | **0.8130**±0.0018 | **0.7890**±0.0036 |
| **train C** | 0.7907±0.0031 | 0.8033±0.0029 | 0.7884±0.0019 |

⚠️**대각선끼리(0.8093/0.8130/0.7884) 직접 비교 금지** — 서로 다른 모집단이다
([[feedback_cross_model_auc_comparison_requires_matched_label_difficulty_20260901]]). 같은
**열 안에서만** 공정 비교가 된다.

### 판정

1. **C(ATR 필터)는 기각.** 공정 비교가 되는 모든 열에서 최하위이거나 B에 밀린다(eval C에서조차
   B 0.7890 > C 0.7884). 학습셋이 7,796행으로 너무 줄어드는 게 주된 원인으로 보인다.
   **5.10)절의 ATR 필터는 경제성(거래 선택)에는 유효했지만 라벨 생성에는 도움이 안 된다** —
   두 축이 다르다는 것이 여기서도 재확인됨.
2. **"서빙하는 것으로 학습하라"** — 열마다 최고가 그 열의 풀로 학습한 모델이다(A@A, B@B).
   교차 학습은 항상 손해(A모델이 B에서 −0.0116, B모델이 A에서 −0.0088).
3. **A vs B는 사실상 무승부이고, 판정은 A 쪽으로 기운다.** B가 자기 turf에서 0.8130으로
   A의 0.8093보다 높지만 ①모집단이 달라 직접비교 불가 ②B의 held_up 비중이 4.7%p 더 높아
   그 우위가 얽힘 증가분과 구분되지 않는다 ③후보가 18% 줄어든다.
   **→ 재학습은 기존 풀(A)로 하는 것을 권고.** 바꾸려면 AUC가 아니라 경제성 게이트로 판단해야 한다.

**부수 확인**: `orthogonal_combo` 제거와 kalman/demarker dedup은 5번 항목이 "손실 없음"으로
기록했는데, **모델 관점에서는 손실이 있다**(A모델을 A에서 쓰는 것 대비 B로 갈아타면 공통기준
−0.0088). "후보풀 구성상 중복"과 "학습에 기여 없음"은 다른 얘기였다.

## 12. (2026-09-01) ⭐**매 봉 스코어링 라이브 배포 완료** — 1·2·7번 항목 해결, 4·8번 무효화

사용자 지시("모두 해결하고 진행해줘" → 칩 표시는 "①현재 봉 확률을 항상 표시")로 9-3--9-10에서
확정된 매 봉 설계를 **실제 라이브에 배포**했다. 커밋 `ba25c5c`, main 머지 `8a2a496`.

### 무엇이 바뀌었나

| | 이전(게이트) | 지금(매 봉) |
|---|---|---|
| 채점 대상 | 9트리거 발동봉만(전체의 23.45%) | **전체 봉 x 양방향** |
| 학습 컨텍스트 | 후보풀 TRAIN 17,969행(라벨률 32.5%) | 전체 봉 TRAIN 182,969행 중 무작위 **18,000행**(14.6%) |
| 이벤트/활성창 | t0부터 60분, 조기확정 로직 | **없음** — 봉마다 자기 점수 |
| `triggers` 필드 | 채점 대상 게이트 | **표시 전용 참고정보**로 격하 |

**코드**: `_multitrigger_rows()`/`_early_confirm_pos()` 및 `ACTIVE_WINDOW_MINUTES`/`FAST_BARS_
OUTCOME` 등 상수 삭제(게이트 제거로 호출자 소멸), `_every_bar_rows()` 신설. **응답 스키마는 그대로**
유지해 `dashboard/server.py`/`app.js` 배선을 건드리지 않았다 — 다만 `minutes_ago`는 항상 0,
`early_confirmed`는 항상 None으로 의미가 바뀌었다(라이브 docstring에 명시).

**프론트**: 게이트/6,000행 컨텍스트/0.847 AUC를 설명하던 문구가 전부 사실과 어긋나 재작성.
도달 불가능해진 "진행중(미확정) 바닥/천장/양쪽" UI 분기와 `vForming*` 변수 제거.

### 배포판 실측 (동결 컨텍스트 + TabPFN, 즉 **라이브와 문자 그대로 같은 fit**)

| | n | base | AUC |
|---|---|---|---|
| VAL | 37,145 | 0.1486 | **0.6992** |
| OOS | 27,273 | 0.1306 | **0.7051** |

**OOS가 VAL보다 높다 — 붕괴 없음.** 9-3의 3시드 추정(0.6942)과도 일치한다.
⚠️HOLDOUT 미터치(2026-04-01 경계에서 잘라 확인).

### 임계값 스윕 — 0.5는 합리적 운영점으로 확인됨

| thr | VAL 호출률 | VAL precision | VAL lift | OOS 호출률 | OOS precision | OOS lift |
|---|---|---|---|---|---|---|
| 0.40 | 4.45% | 0.5194 | 3.50x | 4.26% | 0.5056 | 3.87x |
| **0.50** | **2.52%** | **0.6058** | **4.08x** | **2.45%** | **0.5958** | **4.56x** |
| 0.60 | 1.31% | 0.7131 | 4.80x | 1.41% | 0.6831 | 5.23x |
| 0.70 | 0.69% | 0.8242 | 5.55x | 0.69% | 0.7302 | 5.59x |

라벨률이 32.5%→14.6%로 바뀌어 0.5 재사용 여부가 미결이었는데(9-3), **0.5에서 precision 0.60,
lift 4.1~4.6x, 호출률 2.5%**로 화면 신호로서 적정하다. VAL/OOS 값이 거의 겹쳐 안정적이다.
더 높은 임계값은 precision을 사지만(0.70에서 0.82) 호출이 0.7%로 줄고 **OOS precision이 VAL보다
크게 떨어져(0.8242 vs 0.7302) 과적합 신호**가 보인다 — 0.5 유지가 맞다.

### 이 항목이 해결/무효화하는 것

- **1번(지연확인 UX) 해결.** 이벤트/활성창 개념이 사라져 과거 이벤트가 이후 봉을 덮어쓰는 구조
  자체가 없다. "신호가 갑자기 과거기록과 함께 나타남"이 재현될 경로가 없다.
- **2번(분모 왜곡) 해결.** 트리거별 성공률이라는 지표 자체를 안 쓴다 — 모집단이 전체 봉 하나다.
- **7번(진입시점 비현실성) 해결.** 현재 봉에 점수가 나오므로 다음 봉 진입이 정직한 가정이다.
  게이트 시절 헤드라인 +9.28bp를 +4.75bp로 깎았던 30분 지연이 없다.
- **4번·8번(트리거 순위) 무효화.** 트리거가 더 이상 채점 대상을 정하지 않으므로 "어느 트리거가
  최고인가"는 이 신호에 대해 의미 없는 질문이 됐다. 다른 신호의 피더로 쓸 때는 여전히 유효.
- **3번(BTC 93% vs ETH 22%) 미해결로 남음** — BTC는 아직 게이트 구조다.

### ⚠️경제성 게이트 (실제 서빙 확률) — **thr>=0.55에서만 통과, 배포중인 0.50은 FAILED**

9-10의 +8.01bp는 GBM 프록시 확률로 뽑은 호출 population 기준이었다. 실제 서빙 설정(TabPFN +
동결 18,000행 컨텍스트, 라이브와 문자 그대로 같은 fit)으로 재실행한 결과 **결론이 달라졌다**.

방향뒤집기 대조군을 **그리드 전체(240셀 = SL 8 x ARM 6 x Trail 5)**에 적용했고
(feedback_trailing_stop_low_arm_noise_harvest_artifact — 단일 config만 뒤집으면 오판),
아티팩트가 사는 저ARM을 걷어내기 위해 **ARM=1.5 행(40셀)만** 따로 봤다:

| thr | VAL 정방향/뒤집기 | OOS 정방향/뒤집기 | 선택셀 VAL / OOS |
|---|---|---|---|
| 0.40 | 0 / 12 | 0 / 20 | −3.54bp / −1.26bp |
| 0.45 | 0 / 9 | 5 / 14 | −2.64bp / −4.06bp |
| **0.50**(배포중) | **4 / 6** | **10 / 13** | **+0.90bp / +6.81bp** |
| **0.55** | **29 / 3** | **19 / 10** | **+9.28bp / +11.93bp** |
| 0.60 | 35 / 0 | 22 / 9 | +16.05bp / +11.30bp |
| 0.65 | 35 / 0 | 24 / 9 | +14.37bp / +12.65bp |

**판정**: 0.50 이하에서는 **고ARM 구간에서도 뒤집기가 이긴다**(VAL 4대6, OOS 10대13) — 저ARM
노이즈수확 아티팩트로 설명되지 않는 진짜 경고다. 선택셀 하나가 +0.90/+6.81bp로 양수인 것은
그 이웃이 받쳐주지 않는 고립된 값이다. **thr>=0.55부터 부호가 명확히 역전**되고(VAL 29대3,
OOS 19대10) 선택셀도 두 구간 모두 두 자릿수 bp로 통과한다.

저ARM(0.1/0.25)에서 뒤집기가 임계값과 무관하게 항상 수익(37~40/40셀)인 것은 **알려진 노이즈수확
아티팩트 그대로**다 — 신호가 아니라 기계적 현상이라는 걸 임계값 무의존성이 확인해준다.

**⇒ 임계값 0.50 → 0.60 상향, 사용자 승인 후 배포 완료(2026-09-01).** 0.60이 경제성이 가장
깨끗하고(VAL 정35/뒤0, OOS 정22/뒤9) precision도 통과 선택지 중 최고다(0.713/0.683).
전체 그리드: `data/research/eth_v_rebound_every_bar_tabpfn_costgate_20260901/report.json`.

**빈도 손실 없음 확인** (사용자 질문 "0.6이면 신호가 너무 없나?"에 답하기 위해 실측).
⚠️위 호출률(0.60에서 1.31%/1.41%)은 **라벨 붙은 행** 기준이라 화면 체감과 다르다 — 라이브는
라벨 유무와 무관하게 모든 봉을 채점하고 봉당 max(bottom,top) 하나만 표시한다. 같은 집계로 다시
재면(`research_eth_v_rebound_every_bar_threshold_signal_frequency_20260901.py`):

| thr | 발동봉 VAL/OOS | 하루 사건 | 간격 중앙값 | 4h 스트립 | 신호없는 날 |
|---|---|---|---|---|---|
| 0.50 | 8.03% / 8.06% | 17.9 / 17.4 | 0.5h | 3.9/48 | 0% |
| 0.55 | 6.12% / 6.27% | 14.1 / 14.1 | 0.8h | 2.9~3.0/48 | 0% |
| **0.60** | **4.60% / 4.98%** | **11.0 / 11.6** | **1.1h / 0.9h** | **2.2~2.4/48** | **0%** |
| 0.70 | 2.44% / 2.80% | 6.2 / 7.0 | 2.1h / 1.8h | 1.2~1.3/48 | 0.8% / 0% |

0.60에서도 하루 11~12건, 약 1시간에 한 번, 신호 없는 날 0%다. 오히려 **0.50은 30분에 한 번꼴로
과했다**. 화면 발동률(4.6~5.0%)이 라벨 기준 호출률(1.31%)보다 3배 이상 높은 이유는 ①라벨 있는
행이 52.9%뿐이고 ②봉당 2행이 max로 1개 표시가 되기 때문이다.

⚠️**파생 발견**: 역산하면 라벨 없는(애매한) 봉의 발동률이 라벨 있는 봉보다 높다. 즉 화면 신호의
상당수가 excluded-middle 봉에 앉으므로, **인용하는 precision은 "화면 신호 전체의 적중률"이 아니라
"그중 평가 가능한 부분의 적중률"**이다. 라벨 설계에서 오는 구조적 한계이고 모든 임계값에 동일
적용된다 — 이 상향으로 생긴 문제가 아니다.

### 배지 지속성 (2026-09-01, 매 봉 전환 직후 사용자 보고로 추가)

매 봉 전환 후 사용자가 화면에서 **"V자 급등락 신호가 유지가 안 된다"**고 보고. 원인은 설계
그대로였다 -- 배지가 현재 봉만 반영하니 **사건당 평균 1.2봉**(0.60 기준 하루 13.25 발동봉 /
11.01 사건), 즉 대부분 5분만 떴다 사라진다. 게이트 시절엔 `ACTIVE_WINDOW_MINUTES=60`으로
최대 60분 유지됐으므로 체감이 크게 달라진 것.

**조치: 배지에만 지속성 부여**(`_badge_pos()`). 증거신호 8종 칩의 `_fill_until_tp_or_horizon`과
**같은 계약** -- 마지막 콜을 "익절 도달 또는 호라이즌 경과" 중 먼저 오는 쪽까지 유지.
상수는 이 신호 자신의 라벨에서 가져왔다(`BADGE_HORIZON_BARS=12`/60분 = FULL_BARS,
`BADGE_ATR_MULT=1.5` = ATR_MULT).

- **익절 판정은 종가 기준.** 이 신호의 라벨이 `fast_close_max - extreme`(종가)로 정의돼 있기
  때문이다. 증거신호는 intrabar 고가/저가 터치를 쓰지만 **그건 그쪽 라벨이 그렇게 정의돼서**이고,
  컨벤션을 신호 간에 옮기면 안 된다(CLAUDE.md 배리어 컨벤션 항목).
- **히스토리 스트립도 같은 규칙으로 칠한다.** 처음엔 배지에만 적용하고 스트립은 봉별 기록으로
  두려 했으나(매 봉 설계의 track을 살리려고), 사용자가 **"막대 게이지에 5분 칸 하나만 들어와
  있다"**고 다시 보고 -- 48칸 중 한 칸은 사실상 안 보인다. 애초 질문이 "**막대 게이지에서** 신호가
  사라지는거지?"였으므로 스트립이 본래 요구사항이었다. 지속 정의는 `_call_end_pos()` **한 곳**에
  두고 배지·스트립이 함께 쓴다. 겹치면 나중 콜이 덮어쓴다. 스트립 창 직전 발동분도 잡으려고
  채점 범위를 `HISTORY_BARS + 1 + BADGE_HORIZON_BARS`로 넓혔다(예측 비용은 컨텍스트 크기가
  지배하므로 사실상 공짜).
- ⚠️**지연확인 문제 재발 아님.** 그건 local_extreme이 30분 뒤 확정되던 데서 왔고, 지금은 봉
  자체에서 즉시 점수가 난다. 여기서 늘리는 건 "이미 현재인 신호의 표시 시간"이다.
- **룩어헤드 없음**: `_fetch_klines()`가 형성중 봉을 이미 버리므로 익절 판정이 보는 close는 전부
  확정된 과거 봉. 단위테스트 8번 케이스로 검증(`last_pos` 이후 봉은 판정에 안 쓰임).
- `minutes_ago`가 배지 봉의 나이로 **원래 의미를 회복**(현재 봉이면 0). 스키마 키는 불변.
- `_badge_pos()` 단위검증 10케이스 통과: 유지/익절소멸/호라이즌경계(정확히 12봉)/양방향 대칭/
  현재봉 무조건유지/임계값미만 제외/최근콜 우선/룩어헤드 방지.

## 13. (2026-09-01) 라벨 파라미터 그리드 스크리닝 Stage1~3 + 경제성 주장 철회

이 신호의 라벨 상수 4개는 한 번도 격자로 훑린 적이 없었다(증거신호 8종은 horizon×gap×K
스크리닝이 표준 관문인데 V자반등만 계보가 달라 건너뜀). 사용자 요청으로 실행.

| 단계 | 현행 대비 | 결과 |
|---|---|---|
| Stage1 (GBM, 54변형, 축소격자) | +3.94bp | `FULL_BARS`가 지배 축 |
| Stage2 (TabPFN, 240셀 전수, 후보5) | **+0.79bp** | 5분의1로 축소, **S1 1위가 최하위로 추락** |
| Stage3 (OOS 1회, `현행+FULL24`) | +4.36bp | 기준 통과했으나 **선택셀 뒤집기 +4.21bp 양수** → 미달 |

**결론: 재배포 안 함.** 사전 등록한 기준(우위≥1bp AND 선택셀 뒤집기 음수)이 없었다면
"OOS +4.36bp 우위" 헤드라인만 보고 배포할 뻔했다.

⚠️**Stage2에서 선정 로직 결함을 잡았다**: 240셀 전체에서 pess_bp 최대를 고르면 **5개 후보
전부 ARM=0.1이 뽑히고** 그 셀에서 뒤집기도 +5.05~+6.55bp 수익이었다. 저ARM 노이즈수확
아티팩트를 선정에서 제외(ARM≥1.0)하지 않으면 반드시 틀린 답이 나온다.

⚠️**임계값을 변형 간에 고정하면 안 된다**: 라벨이 바뀌면 확률 분포가 통째로 이동해 고정
임계값에서 변형이 "성능 없음"이 아니라 "측정 불가"로 탈락한다(첫 실행에서 6셀 중 5셀이 그렇게
죽었다). **동일 호출 빈도로 맞춰 비교**한다. AUC를 라벨 간에 비교하면 안 되는 것과 같은 이유.

제안했던 라벨 정의 변경 2건은 **둘 다 기각**:
- `fail_neg`(fast_mult≥K인데 giveback>T인 "튀고 되반납"을 음성으로 편입): 0/18 통과,
  **뒤집기 12/18** — 신호가 망가짐. 성공과 되반납 실패가 Tier0 피쳐로 구분되지 않는데 억지로
  가르라 시키면서 원래 작동하던 경계까지 무너진 것으로 보인다.
- `anchor=body`(종가 앵커): AUC 0.697→0.601, 1/18 통과 — **꼬리 앵커가 실제로 정보였다.**

### ⚠️⚠️ 경제성 주장 철회 (모집단 문제)

**이 신호의 모든 경제성 수치는 라벨이 붙은 행(~53%) 기준**인데 라이브는 모든 봉을 채점한다.
전체 모집단에서 방향뒤집기를 ARM≥1.0 80셀 전수로 돌린 2×2:

| | 정방향/80 | 뒤집기/80 | 중앙 정/뒤 | ARM1.5 정/뒤 |
|---|---|---|---|---|
| VAL × 전체봉 | 28 | **0** | −0.81 / −4.75 | 17/0 |
| VAL × 라벨행만 | 62 | **0** | +4.71 / −18.91 | 35/0 |
| **OOS × 전체봉** | 21 | **31** ⚠️ | −1.86 / −0.60 | **4/16** |
| OOS × 라벨행만 | 28 | 23 | −2.65 / −6.02 | 22/9 |

경제적 엣지는 결과가 깨끗하게 분류되는 봉에만 있다. **VAL 전체봉도 중앙값 −0.81** — 전체
모집단 경제성은 애초에 강했던 적이 없고 VAL에선 방향만 맞았다. 분류는 무사(OOS AUC 0.7051),
무너진 건 매매 방향성이다. 임계값 0.60은 분류 운영점으로 유지하고, app.js "자세히"를
"매매 신호로 쓰지 마세요"+근거표로 교체했다.

## 14. (2026-09-01) ⭐미포착 사건 정량분석 — 재설계가 준 것은 recall이 아니라 정직한 측정

사용자가 육안검증 20예시를 보고 "미포착인데 신호와 맞는 게 꽤 많지 않냐"고 지적 → 정량 확인.

**(가) 미포착 사건은 진짜다 — 포착 사건과 구분되지 않는다** (VAL, 25/50/75):

| | 포착(n=841) | 미포착(n=515) |
|---|---|---|
| fast_mult | 1.74/2.15/2.78 | 1.73/2.01/2.60 |
| 이동폭 bp | 34.3/48.8/70.6 | 31.2/45.0/67.8 |
| ATR bp | 16.1/22.0/31.6 | 14.9/20.7/29.2 |

**(나) 중복도 아니다.** "같은 V를 옆 봉에서 라벨한 것 아닌가" 가설 기각 — 미포착에서 최근접
포착 사건까지 **중앙값 40봉(3.3시간)**, GAP(12봉) 이내 **0.0%**. 전부 별개 사건이다.

**(다) ⭐그런데 모델이 못 잡는다** (임계값 0.60, 사건 내 최대확률 기준):

| | 포착 사건 | 미포착 사건 |
|---|---|---|
| VAL 모델포착률 | 27.9% | **8.5%** |
| OOS 모델포착률 | 27.4% | **12.6%** |

미포착 사건 확률 중앙값이 0.24/0.21로 임계값에 한참 못 미친다(포착 사건은 0.42).

**해석**: 9트리거는 대부분 모델이 보는 것과 같은 피쳐 공간에서 나온다. 어떤 트리거도 발동
안 했다는 건 그 봉이 그 피쳐로는 극단으로 보이지 않는다는 뜻이고, 같은 피쳐를 쓰는 모델도
낮은 확률을 준다. 결과는 V자반등이 맞지만 **사전 관측 가능한 특징으로는 예고가 없던 사건**이다.

**⇒ 재설계 평가 정정**: 게이트 제거 자체는 옳았다(held_up 얽힘 실재, 지연확인·진입시점 해소).
그러나 동기였던 **recall 이득은 실현되지 않았다**. 게이트는 사실상 "모델의 피쳐가 정보를 갖는
봉"으로 좁혀주는 조잡한 필터였고, 없애니 분모만 커지고 분자는 거의 그대로였다. 이것이 13번의
경제성 실패(피쳐가 무력한 구간까지 점수를 내며 방향성 소실)와 같은 뿌리다.

임계값 0.60에서 **전체 사건 포착률은 VAL 20.5% / OOS 21.8%** — 이것이 정직한 recall이다.

## 15. (2026-09-01) 피쳐 축 탐색 — 154피쳐 확장도 사각지대를 못 연다

14번이 남긴 질문("미포착 사건을 보이게 하려면 무엇이 필요한가")에 대한 답. 대체 데이터원
(청산/L2/OI/GEX)은 **전부 2026년에 수집 시작**이라 TRAIN(2024-01~2025-09)과 겹치지 않는다
(tail_risk 2026-05-03, microstructure_1m 2026-05-03, oi_lsratio 2026-08-22). 전 구간을 덮는
유일한 확장 후보가 154피쳐 엔지니어링 세트(2024-01-01~2026-06-30)였다.

**감사**(`audit_eth_154feature_lookahead_20260901.py`): 150 pass / 3 모델출력 제외
(`regime3_*`, 순환성) / 1 경계 아티팩트. 정보량 상위 피쳐가 전부 **다음 봉보다 직전 봉과 강하게
관계**(`vwap_dist_24` 0.4706 vs 0.6408, `btc_impulse_x_eth_beta` 0.4779 vs 0.69) -- 인과적 서명.
⚠️단 이 감사는 **총체적 누출 탐지기**이지 인과성 증명이 아니다. 교차상관 피크 시차 검사는 다수가
±12(탐색 경계)에 걸려 그 피쳐들에 대해선 무정보다.

**결과** (동일 호출 빈도 VAL 1,234 / OOS 937로 맞춰 비교):

| 피쳐셋 | VAL AUC | VAL 포착/미포착 | OOS AUC | OOS 포착/미포착 |
|---|---|---|---|---|
| F0 Tier0 23 | 0.6983 | 23.4% / **6.2%** | 0.7027 | 22.4% / **7.1%** |
| F1 +미시구조 22 | 0.7035 | 24.9% / 6.6% | 0.7124 | 22.8% / 7.3% |
| F2 +통과 150 | **0.7074** | 22.5% / 6.6% | **0.7200** | 21.1% / 8.9% |

**AUC는 진짜로 오른다**(OOS +0.017). **그런데 미포착 포착률은 거의 안 움직이고**(VAL +0.4%p),
같은 예산에서 **포착 사건 쪽이 오히려 떨어진다**. 총 사건 포착 수는 제로섬:
F0 OOS 167건 vs F2 OOS 166건.

**판정**: 문헌상 유일하게 강증거인 미시구조 축(`kyle_lambda`/`vpin`/`amihud`/`corwin_schultz`/
`ofi_acceleration`)을 포함해 150개를 더 넣어도 사각지대는 안 열린다. 미포착 사건은 **결과로는
진짜 V자반등인데 사전 관측 가능한 klines 정보에 흔적을 남기지 않는다.** 남은 길은 진짜 대체
데이터(L2/청산)를 1년 이상 축적하는 것뿐이다.

⚠️**같은 방법론 오류를 이 세션에서 3번 밟았다**: 첫 실행에서 임계값을 0.60으로 고정했더니 F2가
호출 1.53배 → 미포착 포착률도 정확히 1.53배(순증분 0)로 나와 "개선"처럼 보였다. **피쳐셋/라벨이
바뀌면 확률 분포가 이동하므로 임계값 고정 비교는 무효**다. 동일 호출 빈도로 맞춰야 한다.

## 16. (2026-09-01) 종합 판정 — 매매 엣지 없음, 재량 지표로는 유효

사용자 질문("V자반등도 경제성이 없다고 판단하는가")에 대한 답.

**엣지가 있는 곳을 실시간에 고를 수 없다는 것이 핵심이다.** 방향성은 라벨 붙은 봉(~53%)에서는
OOS에서도 살아 있다(ARM1.5 정22/뒤9). 그런데 "라벨이 붙는다"는 건 **미래 결과로 정의되는
성질**이라 결정 시점에 알 수 없다. 그래서 "라벨행 기준 +11.30bp"는 **실현 불가능한 숫자**이고,
전체를 거래하면 나머지가 상쇄하고도 남는다(OOS 전체봉 정21/뒤31, ARM1.5 정4/뒤16).

**분류는 유효**(OOS AUC 0.7051, 154확장 시 0.7200). VAL에서는 방향도 깨끗했으므로(뒤집기 0/80)
애초에 무작위였던 게 아니라 OOS에서 무너진 것이다.

**⇒ 재량 참고 지표로 유효, 매매 신호로 무효.** 현재 배포 상태(표시 전용, trading_bot.py 미연결)와
2026-09-01 개정한 UI 문구가 정확히 이 상태다.

**정확한 표현은 "영구히 없다"가 아니라 "현재 데이터·현재 출구구조에서 입증되지 않았다"**다.
약한 지점: OOS는 90일 한 구간이고, 이번 라운드는 트레일링 스톱 구조 하나만 봤다(고정 TP/SL은
v7b 시절 이미 반증됨). 판정을 뒤집으려면 ①OOS 축적 후 방향 유지 ②반증되지 않은 다른 출구구조
통과 ③애매한 봉을 예측 가능하게 만드는 데이터(최소 1년 축적) 중 하나가 필요하다.

### 남은 미결

- 9-8의 시각검증 20예시에 대한 **사용자 최종승인은 여전히 형식상 미완**.
- BTC 쪽은 손대지 않았다(여전히 6트리거 게이트 구조).

⚠️**타임스탬프 함정(이 세션에서 실제로 밟음)**: `build_called()`가 쓰는 `ts_to_pos`는
`kl["timestamp"].to_numpy()`를 키로 하는데, **tz-aware Series의 `.to_numpy()`는 datetime64가 아니라
Timestamp 객체 배열**을 준다 — 조회 키인 naive `np.datetime64`와 하나도 안 맞아 호출이 조용히
0건이 된다. 기존 검증된 스크립트에는 `kl["timestamp"].dt.tz_localize(None)` 한 줄이 있었는데
복사할 때 빠뜨려 전 임계값이 "표본 부족"으로 스킵됐다. 새 경제성 스크립트를 만들 때 반드시 확인.

## 17. (2026-09-01~02) 8트리거 일치 구성 — 얽힘은 제거되나 방향성은 회복 안 됨

16번의 종합판정 이후, 사용자 지시로 **local_extreme만 빼고 나머지 8개 증거신호로 학습·서빙
모집단을 일치시킨 구성**을 처음으로 측정했다. 매 봉 구성에서 실패한 것들도 "이전 기록으로
뛰어넘지 말고" 이 새 모집단에서 다시 검증했다.

### 17-1. 얽힘 제거는 성공, AUC는 이 모집단 기준 최고

`scripts/research_eth_v_rebound_8trigger_matched_population_20260901.py`.
held_up 비율: 전체봉 0.2622 / **8트리거풀 0.2854(1.09배)** / 9트리거풀 0.4966(1.89배).
**8트리거풀은 얽힘이 사실상 없다** — local_extreme 하나가 얽힘 전부를 만들고 있었음이 확인됨.
정직한 AUC는 **VAL 0.7551 / OOS 0.7654**(OOS>VAL). ⚠️매 봉 구성의 0.6992/0.7051과 **직접 비교
금지** — 모집단이 다르면 문제 난이도가 다르다([[feedback_cross_model_auc_comparison_requires_
matched_label_difficulty_20260901]]).

### 17-2. ambiguous 처리 8구성 — 전부 방향성 실패 (미측정 축 2개 포함)

`scripts/research_eth_v_rebound_8trigger_ambiguous_handling_20260901.py`. 고정 타깃(현행 정의
라벨) + **호출빈도 일치**(VAL 600/OOS 423)로 사과 대 사과 비교.

| 구성 | amb% | 고정타깃 AUC | VAL 정/뒤 | OOS 정/뒤 |
|---|---|---|---|---|
| drop\|CHOP1.00 (현행) | 48.9% | 0.7551 | 11/33 | 19/29 |
| drop\|CHOP1.25 | 40.3% | 0.7540 | 4/42 | 16/28 |
| drop\|CHOP1.50 | 32.2% | 0.7578 | 12/25 | 10/33 |
| fail_neg\|CHOP1.00 | 16.7% | **0.7240** | 9/47 | 29/51 |
| fail_neg\|CHOP1.25 | 8.1% | 0.7329 | 6/48 | 36/37 |
| fail_neg\|CHOP1.50 = all_neg | 0.0% | 0.7384 | 8/48 | 28/43 |
| 3class | 48.9% | 0.7528 | 11/33 | 19/52 |

**8개 구성 전부 VAL에서 뒤집기 우세** — 정방향이 이긴 구성이 하나도 없다.
- `fail_neg`/`all_neg`: 매 봉 구성에서의 기각이 **다른 모집단에서도 재현**됨. AUC까지 손상
  (0.7551→0.7240). 사용자가 "이전 실패로 뛰어넘지 말라"고 지시한 재검증의 결과.
- `CHOP_MULT`(한 번도 안 스윕한 축): ambiguous를 48.9%→32.2%로 줄여도 AUC 제자리, 뒤집기 우세 유지.
- `3class`(미측정 축): drop과 동급, 방향성 개선 0.
- ⚠️`fail_neg`의 OOS pess가 +13~15bp로 높지만 같은 셀에서 뒤집기도 +5.5~13.3bp — 방향 무관 수익.

### 17-3. ⭐꼬리 구간(thr≥0.75) — **VAL에서는 진짜다**, 그러나 OOS로 안 넘어간다

`scripts/research_eth_v_rebound_8trigger_tail_probe_20260902.py`. 17-2까지의 실험은 전부 thr
0.70 이하에서 끝났는데(기존 THRESHOLDS가 0.40~0.70), **0.70 위는 한 번도 측정된 적이 없었다.**
세 축을 같이 쟀다.

**A) 꼬리 미세 스윕(VAL, ARM≥1.0 80셀)** — 임계값이 오를수록 단조 개선:

| thr | 호출 | 사건(GAP12) | 정방향 | 뒤집기 | 차 | 최고pess |
|---|---|---|---|---|---|---|
| 0.60 | 641 | 502 | 7 | 26 | −19 | +1.73bp |
| 0.70 | 411 | 350 | 25 | 22 | +3 | +3.46bp |
| 0.75 | 325 | 290 | 49 | **0** | **+49** | +7.45bp |
| 0.80 | 241 | 221 | 58 | **0** | **+58** | +9.69bp |
| 0.90 | 131 | 127 | 61 | **0** | **+61** | +10.75bp |

0.75부터 뒤집기가 완전히 죽는다. 클러스터링 인플레도 없다(131호출=127사건). 저ARM 아티팩트도
아니다(ARM=1.0 이상만 사용).

**B) 확률 십분위 원시 방향수익(격자·트레일링 미사용)** — A와 정반대:

| 최상위 분위 | 6봉 | 12봉 | 24봉 | 48봉 | 스피어만(24/48봉) |
|---|---|---|---|---|---|
| VAL (p≈0.77) | −6.69 | −8.47 | −6.56 | −9.38 | −0.127 / +0.176 |
| **OOS** (p≈0.80) | −8.99 | **−12.65** | **−14.32** | **−28.88** | **−0.406 / −0.685** |

OOS에서 **최상위 분위가 10개 분위 중 최악**이다. 확률이 높을수록 방향수익이 나빠진다.
⚠️단 B는 고정보유, A는 200봉 트레일링이라 측정 대상이 다르다 — 손절이 왼쪽 꼬리를 자르면 평균이
음수여도 트레일링은 양수일 수 있으므로 **B가 A를 자동으로 반증하지는 않는다.**

**C) ⭐랜덤 부분표집 귀무분포(B=200)** — 같은 풀·같은 n·같은 격자에서 **모델 선택만 제거**:

| thr | n | 관측(정−뒤) | 귀무평균 | 백분위 | 관측 최고pess | 백분위 |
|---|---|---|---|---|---|---|
| 0.60 | 641 | −19 | −15.21 | 41.0% ⚠️ | +1.73 | 75.0% |
| 0.65 | 515 | −32 | −12.91 | 19.5% ⚠️ | +2.33 | 76.0% |
| 0.70 | 411 | +3 | −13.52 | 78.5% | +3.46 | 80.5% |
| **0.75** | 325 | **+49** | −14.37 | **100.0%** ✅ | +7.45 | 93.5% |
| **0.80** | 241 | **+58** | −12.91 | **100.0%** ✅ | +9.69 | 91.5% |
| **0.90** | 131 | **+61** | −11.90 | **98.5%** ✅ | +10.75 | 87.0% |

**판정: 꼬리의 우세는 표본크기 부산물이 아니다.** 무작위 선택은 평균적으로 뒤집기에 11~15셀
지고, thr≥0.75의 관측치는 **200회 복제 중 단 한 번도 도달되지 않았다**. 반대로 thr 0.60/0.65는
백분위 41%/19.5%로 **무작위보다 못하고**, 0.70의 "+3 우세"도 78.5%로 유의하지 않다 — 이전 라운드가
유일한 희망으로 기록했던 지점은 사실 노이즈였다.

**D) VAL 선정(thr=0.90, SL/ARM/Trail=1.5/1.0/0.1) → OOS 1회**:
- 선정 셀: n=117(사건 108), opt/pess **+2.95bp**, 승률 59.0%, 뒤집기 −0.09bp(음수)
- **OOS 격자 전체: 정방향 17/80 vs 뒤집기 36/80 (차 −19)** ⚠️

**⇒ 최종 판정: 16번의 "매매 엣지 없음"은 유지되나, 이유가 더 정확해졌다.** VAL 꼬리는 노이즈가
아니었다(C가 결정적으로 증명). 문제는 **그 선택력이 OOS로 전이되지 않는다**는 것이다. 선정 셀
하나는 +2.95bp로 간신히 통과하지만 **주변 격자 전체가 뒤집기 우세**이고, 그 −19는 VAL에서 측정한
무작위 선택 귀무평균(−10~−15)과 사실상 같은 수준이다 — 즉 **OOS에서 모델의 꼬리 선택은 무작위
선택과 구별되지 않는다**(엄밀히는 귀무분포를 VAL에서만 계산했으므로 시사적 근거). B의 "OOS 최상위
분위가 최악"과도 정합한다. 셀 하나만 통과하고 이웃이 전부 실패하는 것은 과적합의 전형적 서명이므로
**승격하지 않는다.**

산출물: `data/research/eth_v_rebound_8trigger_tail_20260902/report.json`.

**남는 여지**: OOS는 90일 한 구간이고 꼬리의 OOS 호출은 117건뿐이다. OOS가 더 축적된 뒤
thr≥0.75 구간만 재확인하는 것은 의미가 있다. HOLDOUT은 재노출하지 않는다.

## 아직 하지 않은 것

BTC 라이브 스크립트 수정(여전히 6트리거 게이트), BTC 6트리거 순위 재확정, 3번 항목의 BTC/ETH
수치 불일치 원인 규명 — 전부 사용자 판단 대기. HOLDOUT 재노출은 하지 않는다(5번 항목에서 이미
의도치 않게 소모됐으니 그 풀 정의로는 다시 만지지 말 것).
