# Zeus Baseline v4 섀도우 — 사전 등록 (2026-09-18)

v3 판(`shadow_prereg_v3_20260918.md`)을 대체한다. **바뀐 것은 단 하나 — 빈도다.**
v3 판이 「0.8건/일로는 1.5년을 돌려도 0과 못 가른다」며 수익 판정을 아예 뺐는데,
v4 는 **2.17건/일**이라 그 판정이 다시 가능해진다.

## 0. 검정력 먼저
```
SD = √(p(1−p)) × (TP + SL) = √(0.325·0.675) × 220bp = 103bp
MDE ≈ 2.8 · SD / √n
```
| 빈도 | 90일 | 180일 | **1년** | 1.5년 |
|---|---|---|---|---|
| 0.78건/일 (v3) | 34 | 24 | 17 | 14 |
| **2.17건/일 (v4)** | **21** | **14** | **10.2** | 8.3 |

기대 엣지 **+6.95bp** ⇒ 🔴**1년에도 MDE 10.2 > 6.95.** 여전히 «1년 안에 유의»는 아니다.
**사전등록 판정 지평 = 체결 436건 = 200일**(v3 는 1.5년). 그 시점의 MDE 는 13.8 이므로
**판정의 성격은 「유의성」이 아니라 「사전등록 구간 안에 있는가」다**(아래 §2).

## 1. 섀도우가 «실제로» 판정하는 것 (전부 90~200일 안에 가능)
| # | 항목 | 필요 표본 | 합격선 | 불합격 시 |
|---|---|---|---|---|
| **P** | 라이브↔오프라인 **파리티** | 결정 ~500건(≈1주) | 불일치 **0건** | 즉시 중단 |
| **F** | 체결 **빈도** | 30일 | **1.5 ~ 3.0건/일** (폴드별 실측 범위 1.68~2.68) | 30일 연속 벗어나면 중단 |
| **C** | 집행 **비용** 실측 | 체결 50건 | 왕복 **≤ 2.8bp** ⭐기준선은 가정이 아니라 **실측**이다 — ETHUSDC 메이커 섀도우 732 leg 에서 peg 왕복 **1.41bp**(체결률 100%). 합격선은 그 2배 | 2.8 초과면 v3(조인 게이트)로 전환 검토 |
| **G** | 손절 **미끄러짐** | 손절 50건 | 손절선 초과 최대 **< 1.5%** | 초과 시 half-Kelly 명목 재검토 |
| **S** | 슬롯 **경합** | 체결 50건 | 거절률 **94±3%** · 「전체후보−체결」 격차 −3~−6bp | 벗어나면 게이트 재점검 |
| **R** | 롤링 임계값 **버퍼 복구** | 재시작 1회 | 재시작 후 q 산출값이 중단 전과 연속 | 실패 시 중단 |

## 2. 수익은 «누적»하되, 이번엔 사전등록 구간이 있다
- 매월 건당bp·날짜블록 CI 를 **기록**한다.
- **최초 판정: 체결 436건(≈200일).** 판정 기준을 지금 박아둔다:
  - 건당 **≥ +1.02bp**(현실 비용 손익분기) → **계속**
  - 건당 **< −3.0bp** 이고 CI 상한 < +1.02 → **중단**
  - 그 사이 → **436건 더**(총 872건·≈400일)까지 연장, 그때 MDE 9.8 로 재판정
- 🔴 그때까지 **사양을 한 글자도 바꾸지 않는다.**

## 3. 즉시 중단 사유 (수익과 무관)
- 파리티 불일치 1건 이상 · 롤링 임계값 버퍼 복구 실패
- 손절 미끄러짐 −1.5% 초과 · 체결 빈도 30일 연속 1.0건/일 미만
- 서버 자원 경보(2026-09-18 OOM 전례) · 대시보드/봇 크래시 루프

## 4. 크기
실거래 **고정 크기**. half-Kelly(명목 ~5.8배)는 원장에 가정 손익으로만 병행 기록.
전환은 436건 판정 이후에만 검토한다.

## 4.1 ⭐회전 귀무 (2026-09-18 추가)
확인창 F4·F5(선택 미사용)에서 **관측 +4.94 vs 귀무 중앙 +0.23 · 백분위 96.4% · p=0.0361**.
귀무 중앙이 ≈0 이므로 배리어 기하학이 아니라 **고르는 능력**이다.
🔴단 **날짜블록 CI 는 여전히 0을 포함**한다 — 존재는 확인, 크기는 부정확. 그래서 이 사전등록의
「436건 전까지 판정 없음」은 **그대로 유효하다**.

## 5. 🔴 이 사전등록이 인정하는 약점
- **통합 +6.95 는 개발창 쪽으로 위로 편향돼 있다**(개발 +7.79 / 확인 +3.86).
  진짜 기대값은 **+4 ~ +7bp** 구간으로 읽는 게 정직하다. 그러면 현실 비용 1.02 에서
  순 **6.5~13bp/일**, peg 5.52 에서 **−3 ~ +3bp/일**이다.
- ~~peg 스트레스 비용에서는 자격 미달(G2)~~ → **정정(2026-09-18)**: peg 5.52 는 **ETH/USDT**
  값이다. v4 가 쓰는 **ETH/USDC** 에서 같은 자로 재면 왕복 **1.41bp**(732 leg·체결 100%)이고
  그러면 **G2 는 통과**한다(CI하한 +2.70 > 1.41). 🔴단 36시간 한 구간·모사 leg 이므로
  **C 항목이 여전히 이 판의 생사**다.
- **미접촉 창이 더는 없다.** F4·F5 를 썼고 2026-07~09 는 v3 가 소진했다.

## 6. 미충족 계약
**Omega Artifact Integrity** — `qXXX` 고정 임계값 규약과 불일치(롤링 분위 q=0.85·창 1,000).
조문 개정 제안은 `docs/zeus/README.md §3.1`. **승격(실주문)은 개정 승인 후에만.**
섀도우(기록 전용)는 이 조문에 걸리지 않는다.

## 7. 아티팩트와 **재학습 주기** (사양의 일부 — 사전 등록)
```
data/live/zeus_v4_shadow_20260918/{model.pt, meta.json}
  state_dicts 6개 · input_dim 170 · TRAIN 2022-01-01~2026-05-31(purge 30일)
  파리티 최대편차 0.000e+00 (참조 stageP_probs_noreg_purge30_ftall.npz SHADOW|N1s*)
```
🔴**이 아티팩트는 이미 «3.5개월 낡았다». 그게 맞다** — 6폴드 검증이 정확히 그 구조였다:
각 폴드가 TEST 시작 30일 전까지 학습하고 **6개월짜리 TEST 창을 그대로 통과**했다.
그러므로 이 아티팩트의 유효 구간은 **2026-07-01 ~ 2026-12-31**이고, 그 뒤
**6개월마다 purge 30일로 재적합**한다. 재적합은 사양 변경이 아니다(§2 의 「한 글자도
바꾸지 않는다」에 걸리지 않는다). **그 외의 어떤 변경도 표본을 무효로 만든다.**

## 8. 라이브 통합 — 상태 (2026-09-18 갱신)
러너: `scripts/live_zeus_v4_shadow_runner_20260918.py` (**주문 없음** · 원장만 · 자체점검 4/4)

| # | 항목 | 상태 |
|---|---|---|
| 1 | **추론 어댑터** | ✅**불필요해졌다.** 러너가 `model.pt`(6시드)를 직접 읽는다 — 라우팅이 없으므로 `_load_tabm_models` 의 bull/bear/chop 계약을 탈 이유가 없다. **라이브 경로 코드 변경 0.** |
| 2 | **157열 계약** | ✅**증명됨.** 원천은 연구 프레임과 같은 경로(BV 패널 ETH·BTC + OI/LSR + 복구 펀딩 → `FeatureEngineer.process` → `_with_raw_state12` → balnobb 중앙값 채움). `--parity` 실측: 12,512봉 **표준화 평균차 0.0062 · 방향 일치율 100.00% · 점수 상관 1.0000**. 🔴봇의 `decision_feature_snapshot.jsonl` 은 **형성 중인 봉**이라 쓸 수 없다(실험 원문 §10) |
| 3 | **롤링 임계값 상태** | ✅ `state.json` 에 버퍼·포지션·마지막 봉을 영속. 재시작 시 이어붙는다 |
| 4 | **주문** | ✅ 없음. 원장 `ledger.jsonl` 에 `cand/entry/exit` 만 기록 |
| 5 | **실시간성** | ✅**해결(사용자 결정: (b)).** 세 원천을 같은 스키마로 잇는다 — ①BV 패널(과거) ②`data.binance.vision` 일별(패널 끝~어제, 패널 빌더의 `day()` 를 **그대로 임포트**) ③REST(오늘). `--verify-rest` 실측: vision 288봉·REST 121봉에서 high/low/close/volume/trades/taker_buy_base/sum_open_interest **전부 완전일치 100.00%**. 실측 지연 **1~6분**(2분 주기) |

### 연기 시험 (성과 측정 아님 · 인용 금지)
배리어 판정에는 라벨이 필요 없으므로 라벨이 끝나는 2026-08-19 «이후»도 채점된다.
| 구간 | 체결 | 체결/일 | 건당bp | CI95 |
|---|---|---|---|---|
| 07-08~08-19 (연구가 엿본 창) | 66 | 1.56 | +26.67 | [−1.72,+55.73] |
| **08-20~09-14 ⭐완전 미접촉** | 49 | **2.10** | +10.82 | [−25.27,+53.42] |
🔴n=49 · MDE ≈ 42bp. **아무것도 증명하지 않는다.** 「빈도가 신선한 데이터에서 실제로 나온다」만 말한다.

## 9. 실행
```bash
python scripts/live_zeus_v4_shadow_runner_20260918.py --parity   # 프레임 동등성 증명
python scripts/live_zeus_v4_shadow_runner_20260918.py --once     # 새 봉만 채점(cron)
python scripts/live_zeus_v4_shadow_runner_20260918.py --selfcheck # 배리어·게이트 자체점검
```

## 10. 가동 상태 — **서버** (2026-09-18 10:54 KST)
```
호스트   서버(실거래 기기). 사용자 지시로 dev 에서 이관했다 -- 대시보드가 서버의
         data/live/ 를 읽으므로 dev 에 쌓으면 구조적으로 안 보인다.
등록     crontab @reboot ×2 (supervisor_zeus_shadow.sh <아티팩트>)
감시     scripts/ops/_supervise.sh (저장소 표준) · OMP_NUM_THREADS=2 · MALLOC_ARENA_MAX=2
러너     scripts/live_zeus_shadow_runner_20260918.py --live --art=<v3|v4> --bars=20000 --sleep=120
         ⭐v3·v4 가 **같은 러너**다. 사양 차이는 전부 아티팩트(model.pt/meta.json)에 있다.
감시기   ops_watchdog SHADOW_RUNNERS 에 2줄 등재(원장 신선도 warn 15분·critical 30분)
대시보드 /api/zeus-shadow + 카드 1개. 🔴톤이 손익이 아니라 «건전성»이다(436건 전까지 판정 없음)
산출     data/live/zeus_v{3,4}_shadow_20260918/{ledger.jsonl,state.json,rest_tail.parquet}
```
🔴**주문을 내지 않는다.** 🔴`pkill -f` 로 죽이지 말 것 — 패턴이 감시기와 호출 셸까지 잡는다
(2026-09-18 이 세션에서 두 번 실제로 당했다). PID 로 죽이고, 감시기를 먼저 죽인 뒤 자식을 죽인다.

### 기동 직후 실측 (2026-09-18 10:56 KST)
| | 게이트 버퍼 | 백필(표본 제외) | 표본 n | 마지막 봉 | 포지션 |
|---|---|---|---|---|---|
| v4 | 2,000 | 112건 | **0** | 2026-09-18 01:45Z | 보유 |
| v3 | 2,000 | 51건 | **0** | 2026-09-18 01:45Z | 보유 |

⭐백필은 **게이트 버퍼(1,000 후보 ≈ 4일)를 채우기 위해 필요**하지만 원장에 `bf` 로 표시돼
표본에서 빠진다. 사전등록 표본은 «켠 시점부터»이고 지금 **n=0 에서 시작한다**.

### 🔴가동 중 알려진 한계
- **러너는 데이터 구멍에서 «거부»한다**(중앙값으로 조용히 채우지 않는다). REST 메트릭이
  41.7h 만 주므로 그 앞의 vision 일별 파일이 늦으면 잠시 죽고, 파일이 올라오면 감시기가
  재시작해 자가회복한다. `logs/supervisor/zeus_v*_shadow_*.log` 로 확인한다.
- **바이낸스 가중치는 트레이딩 봇과 공유**(한도 2400/분). 헤더를 읽고 1,500 초과면 건너뛴다.
- **코드/아티팩트를 바꾸면 사람이 먼저 파리티를 확인한다**(부팅마다 돌리면 연구 parquet
  480MB 를 실거래 기기에서 매번 읽는다):
  `--parity --art=<디렉터리>` 와 `--verify-rest`.
