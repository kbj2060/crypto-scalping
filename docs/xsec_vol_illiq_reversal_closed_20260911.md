# 보류 축(변동성·레인지) 재개 및 종결 — 그리고 반복되는 시간 패턴 (2026-09-11)

사용자 2026-09-07: *"변동성 레인지는 나중에 내가 다시 연구할 수 있도록 기록해두고 다음에 알려줘."*
→ 2026-09-11: *"계속 끝까지 진행해줘"*.
스크립트 `research_xsec_vol_illiq_reversal_20260911.py` · 산출 `tmp/xsec_volilliq_20260911/report.json`

## 판정 — **세 구간 전부 통과 0건. 축 종결.**

## 재개 조건 3개 처리
① **`atr_pct` 통제 후 증분** → 충족. 시험변수를 매 시각 `rv288`(추세 24시간 실현변동성)에
   **횡단면 순위 회귀한 잔차**로 대체했다. "이미 아는 크기 축"을 뺀 뒤 남는 것만 봤다.
② **OOS 표본 증가** → 충족. 179종 × 2021-12~2026-08 확장 패널. 기록된 OOS n=107~340 → **1,534**.
③ **타깃을 크기·손절폭으로** → 🔴**폐기.** 2026-09-11 산술로 닫혔다: `순익=(2a−1)b−비용` 에서
   a≈0.5 면 크기 타깃은 이익을 못 낸다([[eth_volforecast_directional_cost_closed_20260911]]).

## 검정한 것 — 기록된 가설 그대로
> *"변동성·비유동성↑ → 되돌림"* (되돌림 쪽 단일피쳐 상위가 전부 변동성·유동성·스프레드였다)

매 시각 종목을 시험변수 3분위로 나누고 각 분위에서 되돌림 포트폴리오(직전 패자 롱/승자 숏, k=3).
**사전등록 방향: 되돌림 수익이 고분위에서 더 크다.** 1차 기준 = 고−저 일군집 부트 CI 0 배제, 세 구간 전부.

| 시험변수 | 지평 | EXT | IN | OUT |
|---|---|---|---|---|
| rv비율(단기/장기) | 4h | **+15.7 ⭐** | +11.4 | **−15.4** |
| rv비율(단기/장기) | 24h | **+52.1 ⭐** | −3.0 | **−29.3** |
| amihud(비유동성) | 24h | −2.8 | **+34.9 ⭐** | +29.5 |
| amihud(비유동성) | 4h | **−11.2**(반대 부호로 유의) | +0.1 | −26.5 |
| bb_width(레인지) | 전 지평 | — | — | — |

**통과: EXT 2 · IN 1 · OUT 0 · 세 구간 전부 0.**
가장 강했던 `rv비율`은 확장 구간에서 예측대로 나오다가 **최근 창에서 부호가 뒤집힌다.**
`bb_width` 는 어느 창에서도 통과하지 못했다. `amihud` 는 4h 에서 **예측과 반대 방향으로** 유의했다.

## ⭐⭐메타 발견 — 두 프로그램이 같은 시간 곡선을 그린다
같은 날 종결한 [쏠림 페이드](xsec_crowding_extended_sample_decayed_20260910.md)와 **시간 패턴이 같다**:

| | 2022~2023(EXT) | 2024~2025(IN) | 2025~2026(OUT) |
|---|---|---|---|
| 쏠림 페이드 | **31/32 통과** | 0/32 | 0/32 |
| 변동성·비유동성 되돌림 | 2 통과 | 1 | **0** |

서로 다른 신호·다른 가설·다른 설계인데 **둘 다 2022~2023에 있고 2024 이후에 없다.**
독립적인 두 관찰이라 우연으로 보기 어렵다. 가능한 설명:
(a) 무기한 시장이 효율화됐다 (b) 거래대금이 소수 종목에 집중돼 횡단면이 얇아졌다
(실측: $200M 문턱 통과 종목 **21~25종 → 10종**) (c) 2022 국면(루나·FTX)이 특수했다.
⇒ **이 저장소가 앞으로 횡단면 축을 다시 열 때는 "2022~2023에서만 되는 것 아닌가"를 먼저 물어야 한다.**

## 한계
- OUT 은 n=229 로 세 구간 중 가장 작고 CI 가 넓다. "OUT 에서 없다"는 "OUT 에서 못 쟀다"와 완전히
  구분되지 않는다. 다만 **부호가 뒤집힌** 셀이 여럿이라 단순한 검정력 부족으로만 보긴 어렵다.
- 되돌림 정의는 직전 L시간 수익 순위 하나다. 다른 되돌림 정의는 안 봤다.
- `parkinson_vol` 은 패널에 고가/저가가 없어 대체했다(`rv비율`·`bb_width`). 기록된 원 피쳐 그대로는 아니다.

## 🔴작업 중 낸 실수 2건 (기록)
1. **잔차화를 498,694행 전부에 계산**했으나 실제로 쓰는 건 일별 격자 **1,700행**뿐 — 290배 낭비.
   `rows` 인자를 받아 필요한 시각만 계산하도록 고쳤다(8분 → 즉시).
2. **`pkill -f` 를 썼다.** [[feedback_supervised_process_kill_matches_supervisor_20260908]] 에
   자폭 위험으로 기록돼 있는데도 썼고, 직후 백그라운드 작업 2개가 종료코드 144로 죽었다.
   ⇒ 실행 중 작업은 `TaskStop` 으로 멈춘다.
