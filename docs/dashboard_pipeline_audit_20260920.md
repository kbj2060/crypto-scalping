# 대시보드 파이프라인 — 데이터 흐름 지도와 감사 (2026-09-20)

개편(2026-09-15~09-20: 풋프린트 · 1초 수급 · 가격축 프로파일 · 호가 히트맵 · OI 레인이
캔들 SVG 안으로 들어옴) 이후의 전체 경로를 **실측으로** 확인하고, 거기서 나온 결함을
고친 기록이다. 숫자는 전부 재현 방법을 같이 적었다 — 추정치는 «추정»이라고 쓴다.

계측 환경
- 서버 실측: 라이브 서버의 `127.0.0.1:8787` 에 읽기 전용 curl (3회 반복, warm/cold 구분).
- 브라우저 실측: 라이브 서버에서 뜬 **실제 페이로드 30종**을 픽스처로 떠서 dev 에
  목 서버로 되먹이고, headless Chromium(playwright) 으로 CDP `Performance.getMetrics`
  20초 소크 + `performance.now()` 마이크로벤치. 같은 픽스처라 before/after 가 같은 입력이다.

---

## 1. 층 지도

```
   [거래소]                    [수집/워커 프로세스]              [대시보드 서버]            [브라우저]
                                                                 dashboard/server.py       app.js
 Binance @trade  WS ────────────────────────────────────────▶ footprint_state["bars"]  ─┐
   (ETHUSDT)     │                                              (메모리 288봉 링)        │
                 │                                             footprint_state["sec"]   │
                 └──────────────────────────────────────────▶   (메모리 360초 링)       │
                                                                       │                 │
 Binance @trade  WS ──────────────────────────────────────────────────────────────────▶ │ 브라우저 직결
   (현재가/진행봉)                                                     │                 │  (서버 우회)
                                                                       │                 │
 /fapi/v1/openInterest  0.25초 폴링 ─────────────────────────▶ oi_1s{} (360초 링)       │
                                             └── 10초 flush ─▶ data/live/oi_1s.duckdb ──┤
                                                                                         │
 /fapi/v1/klines 5m/1h  ───────────────────────────────────▶ swr 캐시(프레임/청산맵)  ──┤
 /fapi/v1/ticker/price  1초 ────────────────────────────────▶ SSE 팬아웃 ───────────────┤
 /fapi/v2/*, userTrades ───────────────────────────────────▶ binance_account 캐시 ─────┤
                                                                                         │
 depth WS ──▶ orderflow_raster 수집기 ──▶ data/.../*.f32 (seek) ─▶ /api/flow/heatmap ───┤
 forceOrder ─▶ tail_risk_interceptor ──▶ tail_risk.duckdb (r/o) ─▶ 청산 신호·이력 ──────┤
 trading_bot.py ──────────────────────▶ data/live/dashboard_state.json ─▶ SSE ──────────┤
 각종 워커(레짐·극점·V자·게이트·사이징·GEX·거시) ─▶ data/live/*.json ─▶ worker_payload ─┘
```

**규약 셋** (이 저장소가 반복해서 배운 것):
1. **요청 경로에서 모델을 돌리지 않는다.** 무거운 계산은 전부 워커가 하고 서버는
   상태 파일만 읽는다(`worker_payload`). 2026-09-10 에 인라인 채점이 to_thread 풀을
   고갈시켜 전 엔드포인트가 멈춘 실장애가 근거다.
2. **비싼 것은 stale-while-revalidate.** `swr_cached(key, ttl, produce, max_stale=)`.
   `max_stale=0` 은 «낡은 걸 주지 말고 기다려라»(차트 프레임이 그렇다).
3. **화면 경로와 연구 아카이브를 가른다.** 체결은 메모리+json 스냅샷(화면) /
   `trade_tape.duckdb`(연구), 호가는 `.f32` seek(화면) / parquet(연구).
   `deribit_gex.duckdb` 는 아예 안 연다(단일 writer 충돌).

### duckdb 접촉면 (전부)

| 파일 | 대시보드의 역할 | 보호 |
|---|---|---|
| `data/live/oi_1s.duckdb` | **유일한 writer**(10초 flush) + reader(`read_only=True`) | `duckdb_path_lock` in-process 락 |
| `data/live/tail_risk*.duckdb` | reader only (`read_only=True`, `to_thread`) | 별 프로세스가 writer |
| `*_shadow.duckdb` | mtime/size 만(etag) + 섀도우 페이로드 | — |
| `deribit_gex.duckdb` | **열지 않는다** — 수집기 JSON 만 읽는다 | 단일 writer 회피 |
| `trade_tape.duckdb` | **열지 않는다** — 화면 복원은 json 스냅샷(0.8ms vs 250ms) | 단일 writer 회피 |

---

## 2. 실측 — 서버는 병목이 아니다

`/api/*` 3회 연속, gzip 협상 (라이브 서버, 2026-09-20):

| 엔드포인트 | cold | warm | gzip 크기 | 클라 폴링 |
|---|---|---|---|---|
| `/api/footprint?bars=12` | 1.7ms | **1.2ms** | 3.1KB | 400ms |
| `/api/footprint?bars=48` | 3.9ms | **3.7ms** | 12.5KB → **0.6KB** (§8 증분) | 400ms |
| `/api/supply-1s` | — | **1.3ms** | 2.5KB | 1s |
| `/api/supply-profile?bars=12` | — | **0.6ms** | 1.0KB | 5s |
| `/api/flow/heatmap?…&mode=rows` | 14.8ms | **0.6ms** | 5.8KB | 1s (서버 1초 SWR) |
| `/api/state` | — | **1.3ms** | 5.6KB | (SSE 전용) |
| `/api/oi-5m?bars=96` | 15.4ms | **12.1ms** ← 캐시 없음 | 1.5KB | 15s |
| `/api/market-history` | 133ms | **3.6ms** | 1.7KB | 300s |
| `/api/liquidation-map` | 54ms | **2.0ms** | 6.2KB | 60s |
| `/api/chart-markers` | 597ms | **0.7ms** | 0.8KB | 60s |
| `/api/model-indicator-history` | — | 4.2ms | **26.1KB** | 1회(로드 차단) |

**결론: 서버·duckdb 왕복은 전부 밀리초대다.** 체감 지연의 원천은 서버가 아니라
(a) 클라 폴링 주기 (b) 브라우저 렌더 비용 (c) 필요 없는 바이트다. (a)는 사용자가
의도적으로 고른 값이라 손대지 않았고, (b)(c)를 고쳤다.

---

## 3. 고친 것

### 3.1 🔴 톤 띠가 «4시간»이라면서 4시간을 안 보여주고 있었다 (정확성)

`toneHistory`(수급 흐름 · 리테일 수급 · 청산 캐스케이드 · 변동성 수준)는 48칸 × 5분 =
4시간이라고 화면에 적혀 있고, 서버는 그 4시간을 `model_indicator_history.json` 에
**디스크로 남긴다**(배포 재시작이 잦아서 2026-09-12 에 일부러 넣은 것이다).

두 곳이 그 계약을 깨고 있었다.

1. `seedModelIndicatorHistory()` 가 48개 서버 샘플을 밀어 넣으면서 시각을 전부
   `new Date()` 로 찍었다 → **48칸이 모두 같은 순간**. 축이 4시간을 0초로 압축했다.
2. `render()` 의 라이브 푸시가 **SSE 상태 푸시마다** 한 칸씩 밀었다. 서버 실측
   `dashboard_state.json` 변경은 30초에 3회(≈10초 주기)라, 48칸이 **8분**이면 다 차고
   그 과정에서 서버가 남겨 둔 4시간이 통째로 밀려난다.

고침: `pushToneHistory(key, tone, at)` — `at`(씨앗)은 그 시각을 그대로 적고 주기 제한을
안 받는다. 라이브 푸시는 `TONE_PUSH_MIN_MS = 300000`(= 서버 `MODEL_INDICATOR_SAMPLE_SECONDS`)
으로 묶는다.

검증(같은 픽스처, headless): 띠 폭 **0분 → 251분**. 축 라벨
`안정 · 05:50:00~05:50:00` → `안정 · 03:49:19~05:50:00`.
자체점검: `node test/tone_history_cadence_20260920.js` (옛 코드에서는 실패한다).

부수 효과: `render()` 마다 바뀌던 `renderModelIndicatorList` 의 html 이 이제 5분에 한 번만
바뀌어, 6행 innerHTML 재작성이 그만큼 사라진다.

### 3.2 캔들 SVG 가 수급 패널 둘을 초당 2.5번 헛되이 다시 그렸다 (속도)

개편으로 «1초 수급»과 «가격축 프로파일»이 캔들 SVG 안의 중첩 `<svg>` 가 됐다.
캔들 SVG 는 풋프린트 모드에서 **체결이 올 때마다**(400ms 게이트) `innerHTML=""` 후
통째로 다시 만든다 — 두 패널도 같이 따라 만들어졌다. 그런데 그 둘의 입력은
0.2Hz(프로파일) · 1Hz(1초 수급) 로만 바뀐다. 게다가 두 패널은 **자기 폴링으로도**
다시 그린다(히트맵 1초 · 수급 1초 · 프로파일 5초) — 프로파일은 초당 3.7번 그려지고 있었다.

마이크로벤치(1h 탭, 실제 페이로드): `renderSnapshotChart` 6.08ms 중
**프로파일 2.89ms + 1초 수급 0.99ms = 3.87ms (64%)**.

고침:
- 두 패널의 `<svg>` 노드를 **살려 두고 다시 붙인다**(`subPanelCache`). `innerHTML=""` 은
  DOM 에서 떼어낼 뿐 JS 참조가 쥔 서브트리는 살아 있다 — 재부착 실측 **0.01ms**.
- 다시 «그리는» 것은 판번호(`supplyProfileVer` / `flowHeatmapVer` / `supply1sVer`)와
  기하(w·h·진입가)가 바뀐 때뿐이다.
- 현재가는 판번호에 **넣지 않는다** — 그 한 줄만 `updateSupplyProfileNow()` 가 transform 으로
  옮기는 게 원래 설계다. 넣으면 틱마다 캐시가 깨져 최적화가 통째로 무효가 된다.

### 3.3 «box 가 오면 재지 않는다»가 실제로는 재고 있었다 (속도)

`renderSupplyProfileSvg` 는 `box` 를 받으면 그 값을 쓰지만, `svg.getBoundingClientRect()`
**호출 자체는 그대로** 하고 결과만 버렸다. 이 함수는 캔들 렌더 한가운데서 불리므로
그 한 줄이 방금 만든 수천 노드의 레이아웃을 강제로 확정시킨다(forced synchronous layout).
`renderSupply1s` 의 `parentElement.clientWidth` 도 같다. 둘 다 `box` 가 있으면 건너뛴다.

### 3.4 풋프린트 모드에서 청산밀도 이력을 만들어서 버리고 있었다 (속도)

`renderSnapshotChart` 가 `liquidationDensityHistory()`(9 스냅샷 × ~115빈 = 1,000여 개 객체)를
매 렌더 만들고, 바로 다음 줄에서 `footprint ? [] : densityHistory` 로 버렸다. 쓸 때만 만든다.

### 3.5 SSE 가 상태 전체(30KB)를 밀고 있었다 (대역)

`app.js` 의 `render()` 가 읽는 것은 `state.session` · `state.microstructure` ·
`state.tail_risk` 셋뿐이고, `compactState` 는 **인자로 넘어가기만 하고 본문에서 한 번도
안 읽힌다**. 그런데 SSE 는 `dashboard_state.json` 전체를 상태가 바뀔 때마다 그대로 밀었다.
SSE 는 gzip 도 안 걸린다(`json_compress_etag` 는 StreamResponse 를 건드리지 않는다).

실측 **30,109B → 3,498B (8.6배)**. 변경 주기 ≈10초라 클라당 ~3KB/s → ~0.35KB/s.
`/api/state` 는 계약이 다르므로 **그대로 전체**를 준다(테스트가 그쪽을 본다).
⭐지우는 키는 클라가 애초에 안 읽던 것이라 **옛 app.js 와도 호환**된다.
자체점검: `test/test_dashboard_sse_state_view_20260920.py` — 세 이름이 app.js 가 실제로
읽는 이름과 같은지, `compactState` 를 쓰기 시작하지 않았는지까지 본다.

### 3.6 페이지 로드를 막는 응답이 162KB 였다 (대역)

`app.js` 는 `await seedModelIndicatorHistory()` **다음에** SSE 를 연다. 그 응답이
`microstructure`/`tail_risk` 블록 전체 × 48샘플 = 161,915B(gzip 26KB)였는데,
클라이언트가 읽는 것은 `classifyIndicators()` 가 쓰는 **다섯 필드뿐**이다.
나갈 때만 추린다(버퍼·파일은 그대로 — 나중에 다른 필드를 쓸 수 있게):
**161,915B → 11,468B (14배), gzip 26.1KB → 2.5KB.**

### 3.7 코인 전환 경쟁 (정확성)

`refreshLiquidation5mSignal` / `refreshBasisLiquiditySignal` / `refreshLiquidationMap` 이
`?asset=${activeSnapshotAsset}` 로 요청을 걸고, 응답이 왔을 때 **화면이 이미 다른 코인**
이어도 그대로 덮어썼다. 2026-08-31 「ETH 레짐 리본이 BTC 캔들 위에」와 같은 부류다
(그쪽은 변수를 갈라 고쳤고 여기는 «시점»이 문제다). 요청 시점 코인을 들고 가서 다르면 버린다.
⚠️지금은 `DASHBOARD_ASSETS=eth` 하나라 잠복 상태지만, 전환기는 살아 있고 환경변수 하나로 켜진다.

### 3.8 `/api/oi-5m` — 이 저장소에서 매 요청 duckdb 를 여는 유일한 엔드포인트

warm 12~15ms 로 다른 엔드포인트(0.5~4ms)의 4~20배였고, 파일 락을 잡으므로 10초마다 도는
쓰기(`oi_1s_persist`)와 **탭 수만큼** 부딪힌다. TTL 5초 SWR — 클라 폴링(15초)의 1/3이라
단일 탭에서는 늘 미스(신선도 불변)이고, 탭이 늘어도 duckdb 열기는 5초에 한 번으로 묶인다.
값 자체가 5분봉이라 5초는 해상도 아래다.
(부수 관찰: 서버가 «링에서 바로 준다»며 넣은 `openInterest` 필드는 **화면이 안 읽는다**.)

### 3.9 죽은 배선 제거

`refreshLiquidationDirectionSignal()` 은 2026-09-11 에 본문이 비워졌는데 게이트 변수와
호출부 4곳이 남아 매 틱 돌고 있었다. 함수·상수·변수·호출 전부 제거(−25줄).

---

## 4. 전후 실측

### 4.1 브라우저 20초 소크 (CDP `Performance.getMetrics`, 같은 픽스처, 2회 평균)

| 창 | 지표 | before | after | |
|---|---|---|---|---|
| 1h | TaskDuration | 0.953s | **0.803s** | −16% |
| 1h | ScriptDuration | 0.251s | **0.160s** | −36% |
| 1h | LayoutCount | 635 | **446** | −30% |
| 4h | TaskDuration | 1.289s | **1.133s** | −12% |
| 4h | ScriptDuration | 0.385s | **0.300s** | −22% |
| 4h | LayoutCount | 648 | **415** | −36% |

### 4.2 `renderSnapshotChart` 1회 (마이크로벤치, 30회 평균)

| 구성 | before | after | 생성 노드 |
|---|---|---|---|
| 풋프린트 1h | 6.08ms | **4.49ms** | 744 → 551 |
| 풋프린트 4h | 7.56ms | **6.44ms** | 1385 → 1146 |
| 청산맵 1h | 6.22ms | **4.45ms** | 767 → 574 |

### 4.3 회귀 검증 — 픽셀이 아니라 **DOM 을 비교했다**

전체 페이지 스크린샷 픽셀 비교는 글립 안티앨리어싱만으로도 같은 빌드끼리 2% 넘게
흔들려(대조군으로 확인) 쓸 수 없었다. 대신 `#candleSvgSnapshot` · `#liquidationMapList` ·
`#snapModelIndicatorList` · `#snapSpecializedSignalList` 의 `outerHTML` 을 통째로 비교했다.

- **AFTER vs AFTER: 바이트 단위 완전 일치** (내 판이 결정적이다)
- **BEFORE vs AFTER: 다른 것은 톤 띠의 `data-t` 시각과 축 라벨뿐**
  (88 hunk / 182줄, 전부 §3.1 이 의도한 변경). 캔들 셀 · 프로파일 막대 · 호가 막대 ·
  OI/청산 레인 · 청산맵 목록은 **한 글자도 안 바뀌었다**.

---

## 5. 안 고친 것 (근거와 함께)

- ~~**풋프린트 400ms 폴링의 중복.**~~ → **2026-09-20 §8 에서 해결.** 여기 적었던 두 가지가
  틀렸다: ①«31KB/s»는 `12.5KB × 2.5/s` 로 계산한 **추정치**이고 실측은 **9.8KB/s** 다
  (조건부 요청 20회 중 11회가 이미 304 였다) ②답은 SSE 가 아니라 **증분**이다 — 이 서버의
  SSE 는 gzip 이 안 걸려 그대로 옮기면 오히려 5배 나빠진다.
- **`/api/footprint` · `/api/supply-profile` 의 서버 캐시 없음.** 탭 수만큼 곱해진다
  (탭당 3~9.5ms/s). 캐시를 걸면 그만큼 낡아지므로 «느리게 하지 말 것» 제약과 충돌한다.
  입력(체결)이 초당 수백 번 바뀌어 «안 바뀌면 재사용» 키도 성립하지 않는다.
- ~~**캔들 SVG 전체 재생성.**~~ → **2026-09-20 §9 에서 해결.** 전면 diff 렌더(«1,300줄이라
  위험»하다고 미룬 그것)가 아니라 **봉 하나를 `<g>` 로 묶어 캐시**하는 국소 변경으로 풀었다.
- *(2026-09-20 후속으로 전부 정리했다 — 아래 §7 참조.)*

---

## 6. 재현 방법

```bash
# 서버 응답 시간/크기 (읽기 전용)
ssh <server> 'for p in "/api/footprint?bars=48" "/api/oi-5m?bars=96"; do
  for i in 1 2 3; do curl -s -o /dev/null -H "Accept-Encoding: gzip" \
    -w "%{time_total}s gz=%{size_download}B  $p\n" "http://127.0.0.1:8787$p"; done; done'

# 대역 A/B -- 🔴반드시 **교대**로. 순차로 재면 장 활동 차이가 «절감»으로 둔갑한다(§8).
#   A = since 없이 전량(옛 동작) · B = 꼬리 두 봉(새 동작), 각자 ETag·커서를 따로 들고
#   같은 20초 창 안에서 한 번씩 번갈아 친다. gzip + If-None-Match 로 브라우저와 같은 조건.
#   curl 은 `-H "Accept-Encoding: gzip"` 만으로는 **자동 해제를 안 한다** -- size_download 는
#   선 위 바이트로 그대로 쓰고, 커서 파싱만 gzip.decompress 로 따로 푼다(여기서 한 번 틀렸다).

# 자체점검
node test/tone_history_cadence_20260920.js
node test/render_supply_1s_smoke_20260920.js
node test/render_supply_profile_smoke_20260919.js
python3 -m pytest test/test_dashboard_sse_state_view_20260920.py -q
```

브라우저 소크/DOM 대조 하네스(목 서버 + playwright)는 스크래치패드에만 두었다 —
라이브 페이로드 픽스처가 필요해서 커밋하지 않는다. 다시 만들려면 위 §2 방식대로
30개 엔드포인트를 떠서 정적으로 되먹이면 된다.

---

## 7. 후속 — 낡은 테스트 정리 (2026-09-20, 같은 날)

빨간 스위트는 다음 회귀를 가린다. 9건을 **한 건씩 진단해서** 정리했다 — «실패하니 지운다»가
아니라 **테스트가 틀렸는지 코드가 틀렸는지**를 먼저 갈랐다. 결과가 셋으로 나뉘었다.

### 테스트가 틀렸다 (5건)
| 대상 | 왜 썩었나 |
|---|---|
| `chip_ids::test_model_chip_ids_all_exist_in_html` | `masht_anchor` 단정만 제거. **나머지 가드는 그대로** |
| `chip_ids::test_directional_keys_are_a_subset_of_chip_keys` | 〃 |
| `chip_ids::test_evidence_chip_ids_all_exist_in_html` | `EVIDENCE_STRIP_CHIP_IDS` 상수 자체가 없다(09-16) → 삭제 |
| `chip_ids::test_specialized_list_items_all_have_chips` | 「특화 목록 = 칩 필수」가 더는 설계가 아니다. `vol_forecast` 는 `be1ad097` 로 칩을 **일부러 뺐고** `evr_gate` 는 애초에 카드 전용 → 삭제 |
| `deploy_watcher::test_imported_live_module_restarts` | 예시 모듈명을 하드코딩. 같은 계약을 **import 에서 직접 뽑는** 형제 시험이 이미 있다 → 삭제 |

덤으로 `SpecializedDetectorChipsExistTest` 의 중복 시험 하나(값 정규식이 주석 속 따옴표까지
집던 엉성한 판)도 접었다.

### 환경이 틀렸다 (1건)
`test_dashboard_server.py::StaticAssetCacheHeaderTest` — `make_app()` 의 `add_static` 이
`data/live` 없이 죽는데 그 디렉터리는 gitignore 라 서버 말고는 없다. **개발 체크아웃에서 늘
실패**했다. 같은 파일의 다른 시험들이 이미 쓰는 방식(임시 디렉터리 패치)을 적용. 아무 데서도
못 도는 시험은 회귀를 못 잡는다.

### 코드가 틀렸다 (3건) — 지우면 안 되는 것들이었다
- **실행 경보 배너**: 봇은 여전히 `execution_alert` 를 쓰고 텔레그램도 보내는데, 화면 배너는
  `liveTabPanel` 안에 있던 탓에 2026-08-31 라이브 탭 제거(`60ab72b7`)의 **부수 결과**로
  사라졌다(커밋 메시지에 언급 없음). 사용자 결정으로 **공식 은퇴** — 고아 CSS 47줄을 지우고,
  생산자 계약(텔레그램)만 남기고, «반쯤 남은 표면»을 막는 시험으로 바꿨다.
  🔴그 시험이 바로 남은 흔적 하나(app.js 의 선례 인용 주석)를 잡아냈다.
  ⚠️배너를 되살릴 거면 `SSE_STATE_KEYS` 에 `execution_alert` 를 넣어야 한다.
- **`_unrealized_bp`**: 2026-09-07 에 넣고 **다음 날** `15992a40` 이 되돌렸는데 시험만 남았다.
  짝이던 `_locked_bp` 는 `server.py` 에서 **호출부가 0**이라 같이 제거(시험만 붙들고 있었다).
- 🔴**짝 없는 `}` 가 CSS 규칙 하나를 3주째 먹고 있었다.** 고아 CSS 를 지우다 발견했다.
  최상위의 `}` 는 «무시»되지 않는다 — CSS Syntax L3 은 그 자리에서 qualified rule 을 시작하고
  **다음 `{` 까지**를 prelude 로 먹으므로 선택자가 `} .asset-tabs` 가 되어 규칙이 통째로
  무효가 된다. 그래서 09-19 «코인 탭 껍데기를 걷어내라» 요청이 안쪽 버튼에만 적용된
  **반쪽 상태**였다. 괄호를 지워 규칙을 되살렸다(페이지 8px 단축, 사용자 승인).
  CSS 는 문법 오류로 죽지 않고 조용히 한 규칙만 버리므로 눈으로는 못 잡는다 →
  `test/test_styles_css_braces_balanced_20260920.py` 가 기계로 막는다(배포본에서 실패하는 것
  확인 = 공허한 시험 아님).

### 결과
대시보드 관련 파이썬 39건 + node 스모크 4건 **전부 통과**. 회귀는 같은 하네스로 확인 —
데이터 패널 네 곳의 `outerHTML` 이 배포본과 **바이트 단위 일치**, 유일한 픽셀 변화는 위
`.asset-tabs` 8px 하나다.

---

## 8. 후속 — 풋프린트 증분 (2026-09-20)

§5 에서 «SSE 로 바꾸는 쪽이 맞다»고 쓴 건 **틀렸다.** 실측으로 두 군데가 어긋났다.

### ① §5 의 숫자가 추정치였다
«12.5KB × 2.5/s = 31KB/s» 는 계산값이다. 실제로 재 보니 `/api/footprint` 는 조건부 요청
20회 중 **11회가 304**(본문 0B)라 실사용은 **9.8KB/s** 였다 — `json_compress_etag` 미들웨어가
ETag 를 붙여 주고 있어 폴링이 이미 절반은 공짜였다.

### ② SSE 는 답이 아니었다
| | 지금 폴링 | 순진한 SSE |
|---|---|---|
| 메시지 수 | 2.5회/s | ~1.5회/s (바뀔 때만) |
| 메시지당 | 12.5KB **gzip** | ~33KB **비압축** |

🔴`api_events` 는 `enable_compression()` 을 **안 부른다**. §3.5 에서 SSE payload 를 줄인
근거가 바로 그 사실인데 여기서는 반대로 작용한다 — 전송방식만 바꾸면 5배 나빠진다.

### 진짜 레버는 증분이었다
내용이 실제로 바뀐 건 400ms 간격 19번 중 12번이고, 바뀌는 건 **거의 언제나 맨 오른쪽 봉
하나**다(닫힌 봉은 5분에 한 번). 48봉을 통째로 보내는 게 낭비이지 폴링이 낭비가 아니다.
`/api/supply-1s` 가 이미 쓰는 `?since=` 를 그대로 가져왔다.

**서버** — `?since=<봉시각>` → `time >= since` 인 봉만, `full` 플래그를 payload 에 실는다.
- 🔴`>=` 다. 봉 경계에서 늦게 온 체결이 **직전 봉**에 들어가므로 클라가 `최신봉 − 1봉` 으로
  물어 두 봉을 받는다. `>` 로 바꾸면 그 체결이 영영 안 보인다.
- 🔴`ready=False`(백필 중)면 **무조건 전량**. 백필은 REST 로 몇 분에 걸쳐 *과거* 봉을 채우므로
  증분을 주면 클라가 그 갱신을 영영 못 받는다. 재연결로 ready 가 내려가면 자동 재동기화된다.
- `updated`(마지막 체결 시각)를 **뺐다** — 읽는 곳이 없는데 체결마다 바뀌어 **ETag 를 매번
  깨뜨리고 있었다**. 빼면 조용한 구간의 폴링이 본문 0B 로 끝난다.

**클라** — 봉을 Map 에 누적하고 꼬리 두 봉만 요청. `payload.full` 이면 Map 을 갈아끼운다.
코인·창이 바뀌면 캐시 키가 달라져 전량부터. **폴링 주기(400ms)도 렌더 게이트(400ms)도 안
건드렸다** — 그래서 지연은 1ms 도 안 늘어난다.

### 실측 ① 실서버 (배포 후, gzip + 조건부 요청) ← **이게 실제 수치다**
`62db709a` 배포 뒤 라이브 서버에서 쟀다. **같은 20초 창 안에서 A(전량)와 B(증분)를 한 번씩
번갈아** 친다 — 두 팔이 같은 체결 흐름을 보게 해서 장 활동 교란을 없앤다(304 횟수가 9/9 로
같게 나온 것이 그 방증이다). 두 팔은 각자 ETag·커서를 들고 간다(브라우저 두 탭과 같은 상태).

| 창 | A 옛(매번 전량) | B 새(증분) | 절감 |
|---|---|---|---|
| 1h (12봉) | 5,687 B/s | **1,593 B/s** | **3.6배** |
| 4h (48봉) | 17,624 B/s | **1,936 B/s** | **9.1배** |

헤더 포함 6,257→2,161(2.9배) · 18,194→2,504(7.3배).
단건 응답(gzip) **8,507B → 641B (13배)**. 시간당 탭 하나 기준 4h 탭이 63.4MB → **7.0MB**.

🔴**순차로 재면 안 된다.** 처음엔 A 와 B 를 20초씩 앞뒤로 쟀는데 3.4배/5.0배가 나왔다.
같은 날 A 팔만 해도 7.2~17.6 KB/s 로 흔들린다(체결이 많으냐 적으냐) — 그 차이가 그대로
«절감»으로 읽힌다. 교대 측정이 아니었으면 4h 이득을 절반으로 과소보고할 뻔했다.

### 실측 ② 목서버 (개발 중 · **비압축** 기준, 참고용)
| 창 | 배포본 | 증분 | |
|---|---|---|---|
| 1h (12봉) | 15,198 B/s | **1,932 B/s** | −87% |
| 4h (48봉) | 57,479 B/s | **1,987 B/s** | −97% |

요청 수는 그대로(20초에 35~40회 = 2/s). `ScriptDuration` 도 0.199→0.155s(1h)로 덤으로
줄었다(파싱할 JSON 이 줄어서).
⚠️여기 −87%/−97% 는 **비압축이라 부풀려진 값**이다. 실서버 gzip 기준은 −72%/−89% 다 --
gzip 이 전량 payload 의 반복성을 잘 먹어(33KB→8.5KB) 증분의 상대 이득이 그만큼 줄어든다.
방향과 자릿수는 맞았지만, **압축 없는 하네스 숫자를 그대로 인용하면 안 된다**는 기록으로 남긴다.

### 회귀 검증
- 배포본(`4c9a3a9d`)과 같은 하네스 대조: **1h·4h 둘 다 DOM diff 0**, 증분 2회 실행도 동일.
- 전이 구간을 따로 쳤다. 불변식 = «증분으로 쌓은 상태 == 전량 한 방»:
  정상 폴링 · 창 1h→4h · 4h→1h · `ready=False`(백필) **네 경우 모두 equal=true**, 콘솔 오류 0.
- `test/test_footprint_since_incremental_20260920.py`(8건): `>=` 경계 · ready 강제전량 ·
  창 우선 절단 · 잘못된 `since`(`abc`/빈값/`1e9`/음수/40자리) 전부 200 · **클라가 실제로 꼬리를
  묻는지**까지. 서버 원문에 규칙 세 줄이 그대로 있는지도 검사해 사본 부패를 막는다.

### 덤 — §7 의 «전부 통과»가 불완전했다
§7 을 쓸 때 쓴 파일 목록(`grep -l "dashboard.server"` 등)이 **파일 하나를 놓쳤다**.
`test_evidence_metalabel_bar_match_20260914.py` 가 `_same_bar()`(09-16 증거신호와 함께 제거)를
찾다 **수집 단계에서 예외를 던져 pytest 실행 전체를 중단**시키고 있었다(실패보다 나쁘다).
retire 했다. 이번엔 목록을 짐작하지 않고 `pytest test/ --collect-only` 로 전수 확인했다:
남은 수집 오류 3건은 대시보드와 무관하다(`ccxt` 미설치 2건 · 순환 import 1건).
대시보드 관련 시험은 **70건 통과**(§7 의 «39건»은 그 불완전한 목록 기준이었다).

---

## 9. 후속 — 캔들 SVG 봉별 캐시 (2026-09-20)

§5 에서 «증분 렌더가 정답이지만 1,300줄 함수라 지금 손대면 위험»이라고 미뤄 둔 것.
전면 diff 렌더가 아니라 **봉 하나를 `<g>` 로 묶어 캐시**하는 국소 변경으로 풀었다.

### 먼저 전제부터 쟀다 (안 그러면 헛수고였다)
«바뀌는 건 맨 오른쪽 봉 하나»는 **데이터** 얘기지 렌더 얘기가 아니다. 창의 고저가 바뀌면
`yMin/yMax` 가 움직여 모든 셀이 자리를 옮기고, 음영은 `maxBuy/maxSell` 로 정규화되며,
행 크기는 ATR 로 정해진다. 그래서 **기하가 얼마나 자주 바뀌는가**가 이 작업의 전제다.

임시 프로브를 넣고 틱 스트림으로 30초 돌렸다 → **렌더 124회 중 기하 변경 0회**(12봉·48봉 모두).
가격이 창의 고저 밴드 안에 머무는 동안은 축도 음영 기준도 고정이다. 돌파하면 바뀌고, 그때는
전부 다시 그리는 것이 맞다.

🔴**첫 측정은 «90% 가 바뀐다»였고 그건 하네스 인공물이었다.** 픽스처가 몇 시간 전 것이라
`updateSnapshotCandleLive()` 가 만든 «지금» 봉이 이력보다 한참 앞섰고, 5분 이상 벌어지면
재요청 게이트가 열려 [재요청 → 리셋 → 다시 추가]가 매 렌더 진동했다(`candles.length` 가 매번
바뀜). 목서버가 봉 격자를 유지한 채 시각을 현재로 당기도록 고친 뒤에야 답이 나왔다.
**낡은 픽스처로는 «무엇이 자주 바뀌는가»를 못 잰다.**

### 구현
봉마다 `<g>` 하나. 키 = 기하 서명(18축) + 봉 인덱스 + `levels` **배열 객체 자체** + 캔들 OHLC.
- ⭐객체 동일성이 공짜 키다: §8 의 `?since=` 증분 덕에 **안 바뀐 봉은 폴링 사이에 같은 배열을
  그대로 들고 있다**. 해시를 만들 필요가 없다. (진행 중인 봉은 `footprintMergeLive` 가 매번 새
  배열로 갈아끼우므로 늘 미스 — 맞는 동작이다.)
- 🔴델타 라벨과 POC 점은 **캐시 밖**이다. 델타 y 는 앞선 봉들의 충돌회피 결과(`deltaBoxes`)에
  의존하는 사슬이라 중간 봉 하나만 바뀌어도 뒤쪽이 전부 틀어진다. POC 점은 봉을 가로지르는
  폴리라인이 쓰므로 건너뛴 봉에서도 나와야 한다. 둘 다 노드 하나뿐이라 매번 만들어도 싸다.
- 창 밖 봉은 캐시에서 축출한다.

### 실측 (소크 20초 × 2회 · 틱 스트림 구동 · `origin/main` 대비)
| 창 | ScriptDuration | TaskDuration |
|---|---|---|
| 1h (12봉) | 0.357 → **0.227s** (−36%) | 1.625 → **1.364s** (−16%) |
| 4h (48봉) | 0.604 → **0.345s** (−43%) | 2.112 → **1.565s** (−26%) |

`LayoutDuration`/`RecalcStyle` 은 거의 그대로다 — `<g>` 를 매번 다시 붙이므로 레이아웃 일감은
비슷하고, 줄어든 건 **JS 의 노드 생성·속성 설정**이다. 힙도 같이 내려갔다(48봉 5.4 → 2.2MB).

### 회귀 검증
- ⭐**핵심: 캐시 경로 == 캐시 비우고 다시 그린 경로.** 같은 페이지에서 `_barCache` 를 비우고
  다시 그려 `outerHTML` 을 비교했다 → **12봉·48봉 모두 바이트 동일**. 기하를 흔들면(가짜 고가
  +50) 전부 무효화되고, 되돌리면 원래 DOM 으로 정확히 복구된다.
- `origin/main` 대비 DOM 비교: 풋프린트는 차이가 **`<g>` 래퍼뿐**(속성 없는 순수 구조 요소 —
  내부 rect/text/title 전부 바이트 동일), **청산맵 모드는 diff 0**.
- 🔴픽셀 비교는 여기서도 못 썼다: 같은 빌드끼리 대조군이 **2.36%** 흔들려 빌드 간 차이(2.19%)
  보다 크다. 이 화면에서 신뢰 가능한 자는 DOM 비교뿐이다.
- 🔴비교 중 **두 번 하네스가 틀려서** 가짜 차이를 봤다: ①기준 목서버에만 시각 보정이 빠져 두
  포트가 다른 데이터를 봤다 ②그 사이 `origin/main` 이 전진해 기준 파일에 내 워크트리에 없는
  규칙(`.manual-entry-row.entry-hold`, 딱 10px)이 들어 있었다.
  **차이를 보면 코드보다 하네스를 먼저 의심한다.**
- `test/test_footprint_bar_cache_20260920.py`(7건·26 subtest): 기하 키 18축 전부 · 봉 신원과
  OHLC · 델타/POC 가 캐시 밖인지 · 셀과 캔들 테두리가 캐시 안인지 · 축출 · **호출부가 하나라는
  전제**(같은 `<g>` 를 두 svg 에 붙이면 «복사»가 아니라 **이동**이다).
  `origin/main` 에서 6/7 실패 확인 = 공허한 시험이 아니다.

### 남은 것 → §10 에서 처리
가격 플롯 바깥(격자·눈금·레짐 리본·레인·밀도)은 여전히 매번 새로 만든다.
*(«값어치가 없다»고 적었는데 **그 판단이 틀렸다** — 아래 §10 에서 재 보니 정상 상태 노드의
대부분이 거기였다. 셀만 보고 나머지를 눈대중했다.)*

---

## 10. 후속 — 가격 플롯 바깥 계층 캐시 (2026-09-20)

§9 에서 «셀에 비하면 작으니 값어치가 없다»고 미룬 것. **재 보니 틀렸다.**

### 먼저 어디에 남았는지 쟀다
셀 캐시가 다 맞은 정상 상태에서 렌더당 만들어지는 노드:

| 구성 | 노드/렌더 | 내역 |
|---|---|---|
| 풋프린트 4h | 372 | rect 114 · title 160 · text 70 |
| 청산맵 1h | 574 | **rect 301**(밀도 히트맵) · title 152 |

🔴**첫 측정은 이것도 과소였다**(각각 238 / 347). 레짐·OI/청산 레인·밀도 이력 픽스처에 시각
보정이 빠져 그 층들이 거의 안 그려지고 있었다. §9 에서 캔들·풋프린트만 보정해 놓고 나머지를
잊은 것이다. **하네스는 고칠 때마다 «남은 것»을 다시 물어야 한다.**

### 구현 — `cachedLayer(name, sig, draw)`
층 하나를 `<g>` 로 묶고 «기하 + 봉 시각 + 그 층의 데이터 신원»으로 캐시한다.
여섯 층: `density` · `grid`(격자+x눈금) · `regime` · `spans`(전환·게이트) · `oiLane` · `liqLane`.

- ⭐데이터 신원은 `objToken()` — WeakMap 기반 객체 신원이다. 이 화면의 payload 는 폴링마다
  **통째로 교체**되므로 «같은 객체면 같은 내용»이 성립한다. 내용 해시가 필요 없다.
- 각 층의 입력은 **15~300초**마다 바뀐다(OI 15s · 청산맵/cm 60s · 레짐 300s). 렌더는 2.5Hz 다.
- 🔴**가격 라벨과 이벤트 삼각형은 캐시하지 않는다.** 전자는 현재가, 후자는 그 봉의 고가/저가에서
  y 가 나와 틱마다 움직인다. 키에 안 넣고 캐시하면 **그대로 굳는다**.
- 덤: `liquidationDensityHistory()` 를 payload 신원으로 memoize 했다 — 렌더마다 스냅샷 9개 ×
  ~115빈 = 1,000여 개 객체를 새로 만들고 있었다.
- 델타 라벨도 봉별로 캐시했다(§9 에서 «사슬 때문에 캐시 밖»이라 둔 것). 🔴사슬 해시는 필요
  없었다 — **`dY` 가 이미 그 사슬의 결과**라, (글꼴·x·dY·라벨·봉시각·매수·매도) 여섯이면
  노드의 입력 전부다. 산수는 여전히 매번 돌고 **노드만** 재사용한다.

### 실측
| 구성 | 노드/렌더 | 1회 렌더 |
|---|---|---|
| 풋프린트 1h | 125 → **21** (−83%) | 3.69 → **2.66ms** |
| 풋프린트 4h | 372 → **27** (−93%) | 8.31 → **3.03ms** |
| 청산맵 1h | 574 → **169** (−71%) | 5.56 → **2.35ms** |

소크 20초 × 2 (틱 스트림): ScriptDuration 1h 0.172 → **0.142s**(−17%) · 4h 0.277 → **0.163s**(−41%).

청산맵에 남은 169 는 **평범한 캔들 루프**다(72봉 × 꼬리+몸통 = 144). 같은 봉별 캐시를 얹을 수
있지만 청산맵은 렌더 주기가 1Hz(2.5Hz 아님)라 절감이 0.1% 수준이다 — 이번엔 안 한다.
풋프린트에 남은 27 은 가격 라벨·호버층·마커·POC 선으로, 전부 틱마다 바뀌거나 한 자릿수다.

### 회귀 검증
- **캐시 경로 == 캐시 비우고 다시 그린 경로**: 풋프린트 1h·4h, 청산맵 1h **전부 바이트 동일**.
  기하를 흔들면 무효화되고 되돌리면 복구된다.
- `origin/main` 대비 DOM: 세 구성 모두 **`<g>` 래퍼 외 차이 0**.
- ⭐**내가 쓴 시험이 내 코드의 불일치를 잡았다**: `density` 층만 가변 외부 변수(`densityG`)를
  거쳐 붙고 키가 변수 뒤에 숨어 있었다. 시험을 느슨하게 하지 않고 **코드를 일관되게** 고쳤다
  (`drawDensitySeg(into, …)`, 키를 호출부에 인라인).
- 대시보드 관련 파이썬 **82건** + node 스모크 4건 통과 · `check_appjs_dict_integrity` 통과.
