# Odyssey2 — 데이터 및 리소스 관리 (2026-08-13)

이 문서는 Odyssey2 서브 프로젝트(`docs/model_contracts/odyssey2_eth_live_injection_contract_20260813.md`)에서 실제로 만지거나 검토한 리소스를 모은 목록이다. Odyssey(1)의 리소스는 `docs/model_contracts/odyssey_eth_h48qual_data_resources_20260812.md`에 이미 정리돼 있고, 대부분(라이브 번들, 예측 CSV, zigzag 라벨 등) 그대로 재사용한다 — 여기는 Odyssey2가 **새로** 만지는 것만 추가한다.

**새 리소스를 만질 때마다 그 턴에 행을 추가/갱신할 것** — 나중으로 미루지 않는다.

## 코드 (스크립트 · 문서)

| 파일 | 용도 | 상태 |
|---|---|---|
| `scripts/research_eth_omega461_atr_tpsl_recalibration_pilot_20260813.py` | ATR TP/SL 재보정 파일럿(Odyssey1 말미, Odyssey2 착수 전) | 완료, 부정 결과 |
| `scripts/ops/systemd/eth-exithead-shadow.service` | exit_head 섀도우봇 재부팅 복구용 유닛(미설치) | 대기 |
| `scripts/research_eth_omega461_regime_specific_quality_threshold_20260813.py` | Odyssey2 #1: 레짐별 quality_threshold | 완료, 혼재된 결과로 기준선 미달 |
| `scripts/build_eth_ensemble_epistemic_context_features_20260813.py` | Odyssey2 #2: h48qual/zig075 앙상블 불일치(epistemic MI)를 사이징 GBM `--risk-context-feature-dir`용 CSV로 추출 | 완료(양쪽 다), std≈0.0003~0.003로 신호 자체가 매우 작음을 직접 확인 |
| `scripts/train_eval_omega461_gbdt_exit_head_liveatr_20260813.py` | Odyssey2 #4: h48qual exit_head용 라이브ATR 데이터셋 재구축(TabM `full1500`과 동일 시드/후보수, 레퍼런스 대조 포함) + 레짐별 LightGBM 3개 학습 | 완료 |
| `scripts/research_eth_omega461_gbdt_exit_head_val_20260813.py` | Odyssey2 #4: G0 자체검증 + `GBDTExitHeadWrapper`(duck-typing 런타임 주입) + 컴포넌트/포트폴리오 VAL 비교 + 게이트 판정 | 완료, 게이트 실패(컴포넌트 PnL·MDD 둘 다 악화) |
| `scripts/research_eth_omega461_gbdt_exit_head_oos_20260813.py` | Odyssey2 #4: VAL 게이트 통과 시에만 실행되는 1회용 OOS 확인(gate_pass를 report.json에서 직접 읽어 미통과 시 RuntimeError로 중단) | 게이트 실패로 미실행(코드 레벨에서 확인) |
| `scripts/train_eval_omega461_tcn_exit_head_liveatr_20260813.py` | Odyssey2 #5(우선순위 큐 최종): #4의 `_build_dataset`을 import 재사용해 동일 데이터셋 확보 + row_i 복원/대조 + 윈도우 표준화 + `TCNExitClassifier`(Phase1 아키텍처+HP탐색 채택값 재사용, position-state MLP 브랜치 신규) 정의 + 레짐별 3개 학습 + torch 번들 저장 | 완료 |
| `scripts/research_eth_omega461_tcn_exit_head_val_20260813.py` | Odyssey2 #5: G0 자체검증 + `TCNExitHeadWrapper`(duck-typing) + `_predict_exit_prob_one_windowed`/`replay_exit_variant_windowed`/`greedy_replay_windowed`(무수정 원본의 이름바꾼 복사본, 포트폴리오는 `IS_WINDOWED` 마커로 모델별 동적 분기) + 컴포넌트/포트폴리오 VAL 비교 + 게이트 판정 | 완료, 게이트 실패(컴포넌트 PnL 부호반전·MDD 둘 다 악화) |
| `scripts/research_eth_omega461_tcn_exit_head_oos_20260813.py` | Odyssey2 #5: VAL 게이트 통과 시에만 실행되는 1회용 OOS 확인(gate_pass를 report.json에서 직접 읽어 미통과 시 RuntimeError로 중단) | 게이트 실패로 미실행(코드 레벨에서 확인) |
| `scripts/research_eth_omega461_queue_pressure_exit_threshold_20260814.py` | Odyssey2 #7(문헌 스카우팅 #6의 1위 후보 구현): h48qual exit_head 모델은 TabM 라이브ATR 그대로 두고 `EXIT_THRESHOLD`만 zig075 대기압력에 조건부 — G0/G0b 자체검증 + `greedy_replay_queue_pressure`(무수정 원본의 이름바꾼 복사본) + 대기압력 마스크 교차검증 + VAL 3후보 스윕 + 게이트 판정 + OOS 단일 확인 전부 단일 스크립트 | 완료, VAL 게이트는 threshold=0.80만 통과했으나 OOS에서 반전(부정 결과) |
| `scripts/research_eth_omega461_risk_controlled_exit_fallback_20260814.py` | Odyssey2 #8(문헌 스카우팅 #6의 2위 후보 구현, arXiv:2605.06479 Joshi/Wang/Hassani/Dobriban): h48qual exit_head 모델은 TabM 라이브ATR 그대로 두고, 이미 학습된 GBDT exit_head(#4)를 논문 Theorem 3.1 threshold 정책의 fallback π*로 재사용 — `_risk_controlled_action`(Δ(x) 스코어) + `_calibrate_threshold`/`_bumped_risk`(Algorithm 1 유한표본 calibration, VAL 전용) + `replay_exit_variant_risk_controlled`/`greedy_replay_risk_controlled`(무수정 원본의 이름바꾼 복사본) + G0/G0b 자체검증 + calibration + VAL 3후보 스윕(이중 게이트) + OOS 단일 확인 전부 단일 스크립트 | 완료, VAL은 eps_frac=0.90(τ̂=0.9995)가 이중 게이트 전부 통과했으나 OOS에서 포트폴리오 반전(부정 결과) |
| `scripts/eth_omega461_multiwindow_confirmation_gate_20260814.py` | 방법론 변경(후보 아님): `eth_val_oos_regime_mismatch_investigation_20260813.md`의 권고(4개 이상 부호 섞인 독립 구간에서 확인)를 코드화한 재사용 모듈 — `load_all_windows`(2025q1/q2/q3+val+oos_q1+oos_q2 6개 창 로더)/`verify_windows`/`align_frame_and_predictions`(범용 프레임-예측CSV 정렬, `_align_frame_and_predictions`/`_align_frame_and_oos_predictions`를 `split` 파라미터로 일반화)/`run_portfolio_variant`(baseline-shape 전용, 후보별 개입 로직 없음)/`summarize_multiwindow`(단일터치 OOS-Q1+OOS-Q2 판정 헬퍼) + G0 자체검증(창 로딩, VAL·OOS-Q1 재현, 2025분기 이중경로 재현, `_metrics`↔`_duration_gated` 동치성) + 대기압력/risk-controlled 6창 소급 스트레스테스트 전부 단일 스크립트 | 완료, G0 전부 통과(`gate_pass_g0=true`), 소급적용으로 기존 판정 불변 확인(`this_module_changes_prior_verdict=false`) |
| `scripts/research_eth_omega461_conformal_kelly_sizing_scale_20260814.py` | Odyssey2 #9(문헌 스카우팅(#6) 3위 후보, arXiv:2608.01494 Ryan): h48qual/zig075 사이징 GBM(`risk_sidecar.pkl`)의 `score`(=`net_per_notional` 예측치)와 실현 `net_per_notional`의 롤링 잔차 75th 분위수를 conformal 스케일로 써 `margin = base_sweep.rs._risk_margins(...)` 직후 `margin_fraction`에 곱함(leverage/TP/SL 무관, 두 사이드카 모두 `notional_scaled_sltp=False` 확인) — `_prep_component_with_score`(`research_eth_omega461_exit_sweep_20260721.prep_component`의 이름 바꾼 복사본, `score` 필드 추가) + 2025q1~q3(VAL 이전, 스케일 고정 1.0)→VAL→OOS-Q1→OOS-Q2 연속 causal walk-forward(컴포넌트별 완전 분리, 창 경계 없이 실현 거래이력 이월) + G0(다중구간 게이트 경로+자체 walk-forward 경로 이중검증) + VAL 3그리드(scale_floor/cap) 스윕(원기준+완화기준) + `gate.summarize_multiwindow` 단일터치 OOS 헬퍼(재사용, 이번엔 VAL 실패로 미호출) + 렛저 괴리 진단(`_ledger_divergence`, exit_head가 notional을 입력피처로 쓰는 데서 오는 2차 근사 정량화) 전부 단일 스크립트 | 완료, G0 PASS, VAL 3후보 전부 원기준·완화기준 둘 다 실패(`val_winner=None`), OOS 미실행, `REJECTED_VAL_GATE` |
| `scripts/research_eth_omega461_regime_aware_exit_head_uptrend_guard_20260814.py` | Odyssey2 #11: #10이 제안만 하고 미구현한 "지속 상승 레짐에서만 h48qual을 원본 exit로 되돌리는 조건부 정책" 실제 검증. `dual_momentum`(1주 lookback, 기존 피처 무수정) 기반 causal 탐지기(rolling 2016bar fracpos, threshold=2025 Q1+Q2만으로 캘리브레이션한 90th percentile) + `prepare_regime_aware_components`(h48qual을 원본/liveATR 두 번들로 동시 준비, entry는 항상 liveATR 쪽만 사용) + `greedy_replay_regime_aware_exit_guard`(`replay_omega4_6_1_greedy_router_20260706.greedy_replay`의 이름 바꾼 복사본, h48qual 보유 중 exit_head 확률 조회 블록만 탐지기로 조건부 교체 + counterfactual 결정변경 진단) + G0a(기존 게이트 모듈 재사용)/G0b(신규 카피 가드-미부착 항등성) 이중검증 + `eth_omega461_multiwindow_confirmation_gate_20260814.load_all_windows`/`run_portfolio_variant`/`summarize_multiwindow` 재사용 6창 비교 전부 단일 스크립트 | 완료, G0 PASS, Q3 손상 부분 완화(no_gate 격차 82.4%·with_gate 32.9% 회복)·Q1/Q2 완전 보존·VAL/OOS-Q1/OOS-Q2 비악화(CONFIRMED, 대부분 원장 완전 동일) 확인, forward 지속상승 레짐 미검증이라 승격 근거는 아님 |
| `scripts/research_eth_omega461_regime_threshold_h48qual_side_aware_revival_20260814.py`, `_oos_20260814.py` | Odyssey2 #12(사용자 요청, #1 후속 부활 시도): 렛저 사이드 분해로 h48qual LONG쪽 신규거래가 with_gate 희석 원인임을 진단, SHORT는 원 실험 레짐맵(0.30/0.30/0.35) 유지·LONG만 전 그리드 독립 스윕 — 고립 스파이크(0.35) 기각, 0.65~1.01 안정 고원(LONG 사실상 차단) 채택. `build_final_action_side_aware`(사이드별 임계값 분기) + `build_portfolio`(두 컴포넌트 예측 사전 교집합으로 `portfolio_eval`의 커버리지갭 `IndexError` 우회) + G0/G0b(원본·사이드분리 경로 항등성) + 전 그리드 VAL 스윕+견고성 판정(양쪽 그리드 이웃도 통과해야 "robust") + `eth_omega461_multiwindow_confirmation_gate_20260814.load_all_windows` 재사용 OOS-Q1+OOS-Q2 단일터치 전부 두 스크립트 | 완료, G0 PASS, VAL 4지표 전부 큰 폭 개선(오늘 밤 최고, with_gate +54.88%→+98.96%)이었으나 OOS-Q1+OOS-Q2 둘 다 원기준·완화기준 전부 실패(`REJECTED_BOTH_WINDOWS_FAIL`) — SHORT 레짐맵 자체가 이미 VAL-선택편향 보유 추정, LONG 스킬부재 8번째 독립 재확인 |
| `scripts/live_eth_regime_aware_exit_guard_shadow_20260814.py` | Odyssey2 #11(레짐인지형 exit_head 지속상승장 가드) 라이브 섀도우 배포 — 실행 로그 #13. `live_eth_exithead_asymmetric_shadow_20260813.py`(h48qual=liveATR 재라벨 exit_head, zig075=원본) 위에 `SustainedUptrendDetector`(dual_momentum 기반 rolling 1주 O(1) 증분 탐지기 — threshold/week_bars는 #11 `report.json`에서 그대로 복사, 재캘리브레이션 없음)를 추가해, h48qual 보유 포지션의 exit_head 확률 조회 지점(`Omega461LiveAdapter.evaluate_exit`이 `self.components["h48qual"].exit_probability(...)`를 호출하는 지점)만 매 bar 원본/liveATR 사이에서 조건부 전환(`evaluate_exit_guarded`, TP/SL·`EXIT_THRESHOLD`=0.95는 `adapter.evaluate_exit`와 완전 동일 경로 재사용). 원본 exit_head는 `trading_bot.py`의 실제 라이브 기본 h48qual 번들/사이드카(`FINAL_GOVERNOR_OMEGA4_6_1_H48QUAL_{BUNDLE,SIDECAR}_PATH`)를 별도 `_Component`로 그대로 재사용(신규 아티팩트 없음). entry/사이징·zig075는 완전 동결. | 완료, 서버 상시 배포·관찰 개시 |
| `scripts/verify_eth_regime_aware_exit_guard_shadow_detector_20260814.py` | 위 스크립트 배포 전 자체검증(dev에서 실행): `SustainedUptrendDetector`(증분)와 #11의 `_rolling_dual_momentum_score`(배치, 원본 함수 그대로 재사용)를 2025 전체연도(105,101bar)·2026 재구축본(57,601bar) 양쪽에서 (1) 전체 causal 재생, (2) 4개 임의 지점에서 seed()+update() 재개 재생(라이브의 콜드스타트/재시작 코드경로와 동일 패턴)으로 대조 | 완료, 두 CSV·모든 재생 방식 전부 완전 일치(NaN정렬·점수·활성화 플래그 mismatch=0) — 결과: `tmp/causal_regen_20260516/eth_omega461_regime_aware_exit_guard_shadow_20260814/detector_verification_report.json` |
| `scripts/research_eth_omega461_selective_conformal_risk_control_20260814.py` | Odyssey2 #14(문헌 스카우팅(#6) 4위 후보, arXiv:2512.12844 Xu/Guo/Wei "Selective Conformal Risk Control") — h48qual exit_head 모델은 TabM 라이브ATR 그대로 두고, 1단계 선별(`_selective_risk_controlled_action`, select_threshold=기존 `EXIT_THRESHOLD`=0.95 고정, "선별됨"={a0=1}과 일치하도록 설계) → 2단계 위험제어(#8의 `_risk_controlled_action`/`_calibrate_threshold`/`_bumped_risk` Algorithm 1을 무수정 재사용, 선별된 부분집합에만 적용)의 진짜 2단계 파이프라인 구현. `replay_exit_variant_selective_risk_controlled`/`greedy_replay_selective_risk_controlled`(무수정 원본의 이름바꾼 복사본) + G0(오케스트레이터 지정 4수치, `eth_omega461_multiwindow_confirmation_gate_20260814.run_portfolio_variant` 재사용)/G0b(2단계 무력화)/G0c(1단계 무력화, 신규) 자체검증 + calibration(선별 전용 부분집합) + 진단 대조(select_threshold=0.50, 게이트 미적용) + VAL 3후보 스윕(이중 게이트) 전부 단일 스크립트 | 완료, G0/G0b/G0c PASS, calibration이 선별 표본(52/13,330=0.39%) 협소화로 3개 eps 전부 실행불가능(feasible=0/46)해져 논문 fallback(τ̂=0)이 불일치율을 5.66%→81.13%로 악화, VAL 3후보 전부 원기준·완화기준 둘 다 실패(`val_winner=None`) — OOS 미실행(방법론상 VAL 통과 시에만 개방) |
| `scripts/research_eth_omega461_zig075_exit_threshold_recalibration_20260814.py` | Odyssey2 #15(우선순위 큐 6번 항목, "zig075 exit_head 개선" 중 별도 exit_threshold 축): h48qual exit_head 모델은 오늘 밤 확정 baseline(`asymmetric_tabm_liveatr`, `exit_threshold=0.95`)에 완전 고정, zig075는 direction_head/quality_head/encoder/exit_head 가중치 전부 원본 동결 — `exit_threshold` 숫자만 {0.80, 0.85, 0.90, 0.92, 0.95, 0.97, 0.98, 0.99} 그리드로 스윕(재학습 없음, 이름 바꾼 복사본조차 불필요 — `replay_omega4_6_1_greedy_router_20260706.greedy_replay`가 이미 컴포넌트별 `exit_threshold`를 설정값에서 읽으므로 설정 딕셔너리 오버라이드만으로 개입 완료). G0(포트폴리오 레벨, VAL·OOS-Q1) + zig075 컴포넌트 단독 그리드(`sweep.run_grid` 재사용, 2026-07-21 저장 파일과 교차검증) + 포트폴리오 8점 그리드(원기준/완화기준 이중 게이트) + 그리드 이웃 로버스트니스 판정(고립 스파이크 명시적 기각, #12 원칙 재사용) + 디제너레이트 동률(baseline과 바이트 단위 동일) 판정(후보 자격에서 별도 제외, 이번 실험에서 새로 도입) 전부 단일 스크립트 | 완료, G0 PASS, VAL 8점 그리드에서 로버스트하며 동시에 baseline과 실질적으로 다른 후보 0개(`val_winner=None`) — 유일한 실질 개선(0.80)은 이웃(0.85) 실패로 고립 스파이크 기각, 나머지 통과 지점은 전부 baseline과 동률 — OOS 미실행(`oos_opened=false`) |
| `scripts/train_eval_omega461_gittins_retirement_exit_head_20260814.py` | Odyssey2 #16(문헌 스카우팅(#6) 5위/최종 후보, arXiv:2405.01157 Dhankhar/Mishra/Bodas, 서버 학습): GBDT(#4)의 `_build_dataset`(seed=260813, max_candidates=1500)을 무수정 재사용 + `_build_transitions`(candidate별 순차 bar 구조에서 `(s_t,r_t,s_{t+1})` TD 전이 복원, 보상=`exit_path_unrealized` bar 증분) + `DGN`(paper Eq 9-11의 (s,x) 쌍 입력 신경망, 참조상태=미니배치 자기자신, 타겟망 대각선 읽기로 M(x) 구현) + 레짐별 3개 학습(bull/bear/chop, route_prob 가중) + `_mem_check`(서버 메모리 안전장치, `run_jm_full_retrain_seed_robustness_20260813.sh` 패턴 재사용) 전부 단일 스크립트, `--device cuda` | 완료, GPU 사용(RTX 3070Ti, ~160 steps/sec), 총 wall time ~8.9분, `dataset_reference_check` 3지표 전부 일치, 메모리 안전 중단 0회 |
| `scripts/research_eth_omega461_gittins_index_exit_head_20260814.py` | Odyssey2 #16 dev 평가: G0(컴포넌트 2종 + `eth_omega461_multiwindow_confirmation_gate_20260814.run_portfolio_variant` 재사용 포트폴리오 VAL/OOS-Q1 4종) + `_predict_retirement_value_one`(은퇴가치 주입, `_predict_exit_prob_one`과 동일 시그니처) + `replay_exit_variant_gittins`/`greedy_replay_gittins`(무수정 원본의 이름바꾼 복사본, TCN(#5)의 `IS_WINDOWED` 마커 패턴과 동일하게 `is_gittins` 마커로 컴포넌트별 동적 분기) + never-trigger 진단 패스 기반 4점 VAL 그리드 스윕(원기준/완화기준/컴포넌트 가드레일) + OOS 단일터치(승자 있을 시) 전부 단일 스크립트 | 완료, G0 PASS(6지표 전부 일치), VAL 4개 그리드점 전부 게이트+가드레일 결정적 실패(`val_winner=None`) — OOS 미실행(`REJECTED_VAL_GATE`) |

| `scripts/research_eth_omega461_evidence_veto_exit_overlay_20260814.py` | Odyssey2 #18(증거 신호 Candidate C, 하드 변형): `orthogonal_combo`(정의는 증거연구 계열에서 무수정 재사용) 발화 시 h48qual 숏 강제청산 — `build_signal`/`prepare_evidence_veto_components`/`_prep_liveatr_only`(#21·#22가 그대로 import 재사용) + `greedy_replay_evidence_veto_exit`(무수정 원본의 이름바꾼 복사본) | 완료, VAL 결정적 기각(-29.92%p) |
| `scripts/build_eth_evidence_signal_context_features_20260814.py` | Odyssey2 #20: 증거 신호 6개를 연속값 그대로 사이징 GBM `--risk-context-feature-dir`용 CSV로 추출(#2/#3와 동일 확장점) | 완료, 양쪽 컴포넌트 부정 결과 |
| `scripts/research_eth_omega461_evidence_veto_exit_overlay_soft_variant_20260815.py` | Odyssey2 #21: #18의 즉시청산을 N-bar exit 임계값 완화로 교체(`greedy_replay_evidence_veto_soft_exit`만 신규, 신호·컴포넌트 준비는 #18에서 import). 사전등록 3×3 그리드를 VAL에서 먼저 전부 실행 후 승자 1개만 OOS 단일터치 | 완료, VAL 통과 셀은 전부 no-op·실개입 셀은 -18.18%p, OOS `REJECTED_SIGN_MISMATCH` |
| `scripts/diagnose_eth_omega461_evidence_intervention_surface_ceiling_20260815.py` | Odyssey2 #22(후보 아님, 구조 진단): 증거 신호 주입 계열의 **개입 표면**(두 컴포넌트×양방향으로 범위 확대)과 **거래모집단 고정 사후천장**(실제 청산 ∪ 발화 bar 중 사후 최선) + **매칭 랜덤 대조**(발화와 동수의 bar를 같은 보유구간에서 균등추출, 20회, seed 20260815) 측정. G0a(기준선 4수치 재현)/G0c(거래별 수익률 대수 재구성 대조) 자체검증 포함. 신호 정의·창 로딩·리플레이는 #18/게이트 모듈에서 전부 import 재사용 | 완료, G0 전부 PASS — 개선율이 6/6 창에서 랜덤과 사실상 동일, 이 축 종결 권고 |

## 모델 아티팩트 (Odyssey2가 새로 만든 것, 전부 `tmp/causal_regen_20260516/` 하위, 라이브 미배포)

| 리소스 | 위치 | 용도 | 상태 |
|---|---|---|---|
| h48qual 사이징 GBM, fresh-predict baseline | `omega4_2_trade_risk_sidecar_20260622_odyssey2_freshpredict_baseline_20260813/` | 앙상블-피처 변형과 공정 비교하기 위한 baseline(원 배포 사이드카와 프레임 구성이 미세하게 달라 별도 재현) | 학습 중 |
| h48qual 사이징 GBM, epistemic 피처 추가 | `omega4_2_trade_risk_sidecar_20260622_odyssey2_ensemble_epistemic_20260813/` | epistemic_direction/epistemic_quality 피처 추가 | 학습 중 |
| zig075 사이징 GBM, fresh-predict baseline / epistemic 피처 추가 | 동일 명명 규칙, zig075 번들 기준 | 위와 동일 목적 | 학습 중 |
| `tmp/causal_regen_20260516/eth_{h48qual,zig075}_ensemble_epistemic_context_20260813/` | `{train,validation,oos}_context_features.csv` | 사이징 GBM 컨텍스트 피처 입력 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_regime_specific_quality_threshold_20260813/` | `report.json`, `univariate_sweep_val.csv` | 레짐별 threshold 실험 산출물 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_gbdt_exit_head_liveatr_20260813/h48qual/gbdt_exit_bundle.pkl` | 레짐별(bull/bear/chop) LightGBM exit_head 3개 + `base_cols`/`pos_cols` 계약(pickle, torch 번들 아님) | 완료, 라이브 미승격(부정 결과) |
| `tmp/causal_regen_20260516/eth_omega461_gbdt_exit_head_liveatr_20260813/report.json` | 데이터셋 재구축 레퍼런스 대조 + 레짐별 학습 진단(val AUC≈0.998) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_gbdt_exit_head_val_20260813/report.json` | G0 자체검증 + 컴포넌트/포트폴리오 VAL 비교 + 게이트 판정(`gate_pass=false`) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_tcn_exit_head_liveatr_20260813/h48qual/tcn_exit_bundle.pt` | 레짐별(bull/bear/chop) `TCNExitClassifier` state_dict 3개 + `base_cols`/`pos_cols`/`market_scaler`/`pos_scaler`/`arch` 계약(torch 번들) | 완료, 라이브 미승격(부정 결과) |
| `tmp/causal_regen_20260516/eth_omega461_tcn_exit_head_liveatr_20260813/report.json` | 데이터셋 레퍼런스 대조 + row_i/cur_ 값 대조 진단 + 레짐별 학습 진단(val AUC≈0.997~0.998) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_tcn_exit_head_val_20260813/report.json` | G0 자체검증 + 컴포넌트/포트폴리오 VAL 비교 + 게이트 판정(`gate_pass=false`) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_queue_pressure_exit_threshold_20260814/report.json` | G0/G0b + 대기압력 발생빈도 진단 + VAL 3후보 스윕 + 게이트 판정 + OOS 단일 확인(`gate_pass=true`이지만 `oos_survives=false`) 전부 포함 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_queue_pressure_exit_threshold_20260814/portfolio_ledger_{val_g0b_degenerate_thr095,val_qp_thr0.80,val_qp_thr0.85,val_qp_thr0.90,oos_baseline_tabm_liveatr,oos_qp_thr0.80}.csv` | 거래 원장(diagnostic, 참고용) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_risk_controlled_exit_fallback_20260814/report.json` | G0/G0b + Algorithm 1 calibration(n=13,330, eps 3후보) + VAL 3후보(이중 게이트) + OOS 단일 확인(`gate_pass=true`, `oos_gate_original_survives=false`, `oos_gate_relaxed_survives=false`) 전부 포함 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_risk_controlled_exit_fallback_20260814/{component,portfolio}_ledger_val_eps{0.90,0.70,0.50}.csv`, `portfolio_ledger_val_g0b_tau_never_switch.csv`, `component_ledger_oos_candidate.csv`, `portfolio_ledger_oos_{baseline_tabm_liveatr,candidate}.csv` | 거래 원장(diagnostic, 참고용 — OOS 반전 원인이 h48qual 자신의 거래가 아니라 zig075 슬롯 재순환임을 확인한 근거) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_multiwindow_confirmation_gate_20260814/report.json` | G0 자체검증 4단계(창 검증/asymmetric_tabm_liveatr 재현/2025분기 이중경로 재현/`_metrics`↔`_duration_gated` 동치성) + 대기압력·risk-controlled 6창 소급 스트레스테스트(`summarize_multiwindow` 결과, strict·완화 두 기준) 전부 포함 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_multiwindow_confirmation_gate_20260814/portfolio_ledger_{2025q1,2025q2,2025q3,val,oos_q1,oos_q2}_{baseline_both_original,asymmetric_tabm_liveatr,qp_baseline_degenerate095,qp_candidate_thr080,rc_baseline_never_switch,rc_candidate_eps090}.csv`, `_aligned_{train,validation,oos}_{h48qual,zig075}_predictions.csv` | 거래 원장(diagnostic, 참고용) + 정렬된 예측 CSV(이 모듈 전용 출력 디렉터리, 대기압력/risk-controlled 원 스크립트의 OOS-Q1 산출물과 분리돼 있어 덮어쓰지 않음) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_conformal_kelly_sizing_scale_20260814/report.json` | G0(게이트모듈 경로+자체 walk-forward 경로 이중검증) + 컴포넌트별 캘리브레이션 진단(n_calib_trades/q_ref) + VAL 3그리드(원기준+완화기준+렛저 괴리 진단) 전부 포함(`val_winner=None`이라 OOS 섹션은 미생성) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_conformal_kelly_sizing_scale_20260814/{2025q1,2025q2,2025q3,val,oos_q1,oos_q2}/_aligned_{train,validation,oos}_{h48qual,zig075}_predictions.csv` | 정렬된 예측 CSV(창별 하위디렉터리 — 2025q1/q2/q3가 `split="train"`을 공유해 파일명이 충돌하는 문제를 발견해 창별 하위디렉터리로 분리, 다중구간 게이트 모듈 자체는 무수정) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_regime_aware_exit_head_uptrend_guard_20260814/report.json` | G0a/G0b(4개 지표 정확 일치) + 탐지기 캘리브레이션(Q1+Q2-only p75/p90/p95)·6창 활성화율 + entry-side 진단(원본/liveATR 번들 dec/margin/leverage 동일성) + 6창 비교(원본/재라벨/조건부정책 no_gate·with_gate) + 가드 발동·실제 결정변경 bar 수 + 비악화 판정(strict·완화 두 기준) 전부 포함 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_regime_aware_exit_head_uptrend_guard_20260814/portfolio_ledger_{2025q1,2025q2,2025q3,val,oos_q1,oos_q2}_{baseline_both_original,asymmetric_tabm_liveatr,regime_aware_guard}.csv`, `_aligned_{train,validation,oos}_{h48qual,zig075}_predictions.csv` | 거래 원장(diagnostic, 참고용) + 정렬된 예측 CSV(이 스크립트 전용 출력 디렉터리, 다른 실험 산출물과 분리) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_regime_aware_exit_guard_shadow_20260814/detector_verification_report.json` | 라이브 섀도우 배포 전 자체검증 산출물: 증분 vs 배치 탐지기 전체 재생 + seed/resume 재생 대조 결과(두 CSV 전부) | 완료, 전부 일치(mismatch=0) |
| `tmp/causal_regen_20260516/eth_omega461_selective_conformal_risk_control_20260814/report.json` | G0(오케스트레이터 지정 VAL/OOS-Q1 4수치)/G0b(2단계 무력화)/G0c(1단계 무력화) + calibration(선별 부분집합 전용, eps 3후보 전부 feasible=0/46) + 진단 대조(select_threshold=0.50) + VAL 3후보(이중 게이트) 전부 포함(`val_winner=None`이라 OOS 섹션은 `oos_opened=false`로만 기록, 미생성) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_selective_conformal_risk_control_20260814/{component,portfolio}_ledger_val_eps{0.90,0.70,0.50}.csv`, `portfolio_ledger_val_g0b_tau_never_switch.csv`, `portfolio_ledger_{val,oos_q1}_asymmetric_tabm_liveatr.csv` | 거래 원장(diagnostic, 참고용) + G0 재현용 baseline 원장 | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_zig075_exit_threshold_recalibration_20260814/report.json` | G0(포트폴리오 레벨, VAL·OOS-Q1) + zig075 컴포넌트 단독 그리드(2026-07-21 저장 파일과의 교차검증 포함) + 포트폴리오 8점 그리드(원기준/완화기준) + 로버스트니스·디제너레이트 동률 판정 + `val_winner=null`/`final_verdict="REJECTED_VAL_GATE"` 전부 포함(OOS 미개방이라 OOS 섹션 없음) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_zig075_exit_threshold_recalibration_20260814/portfolio_ledger_val_g0_zig075_et095.csv`, `portfolio_ledger_oos_q1_g0_zig075_et095.csv`, `portfolio_ledger_val_val_zig075_et{0.80,0.85,0.90,0.92,0.95,0.97,0.98,0.99}.csv`, `_aligned_{train,validation,oos}_{h48qual,zig075}_predictions.csv` | 거래 원장(diagnostic, 참고용) + 정렬된 예측 CSV(이 스크립트 전용 출력 디렉터리) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_gittins_retirement_exit_head_20260814/h48qual/gittins_retirement_bundle.pt` | 레짐별(bull/bear/chop) `DGN` state_dict 3개(타겟망) + `base_cols`/`pos_cols`/`all_cols`/`scaler`(mean/std)/`arch`/`gamma`/`target_tau` 계약(torch 번들, 서버 학습, `handoff.sh pull`로 dev 회수) | 완료, 라이브 미승격(부정 결과) |
| `tmp/causal_regen_20260516/eth_omega461_gittins_retirement_exit_head_20260814/report.json` | 서버 학습 로그: `dataset_reference_check`, transitions/candidate-split 진단, 전문가별 train/val TD-loss 곡선 + elapsed_sec, `_mem_check` 8회 기록(가용 메모리·GPU 메모리) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_gittins_index_exit_head_20260814/report.json` | dev 평가: G0(컴포넌트 2종+포트폴리오 4종) + `m_distribution_diagnostic`(never-trigger 진단 패스 M(x) 분포, 사후선택 없는 4점 그리드 근거) + VAL 4후보 스윕(원기준/완화기준/가드레일) + `val_winner_key=null`/`final_verdict="REJECTED_VAL_GATE"`(OOS 미개방이라 OOS 섹션 없음) | 완료 |
| `tmp/causal_regen_20260516/eth_omega461_gittins_index_exit_head_20260814/portfolio_ledger_val_*.csv`, `_aligned_val_{h48qual,zig075}_predictions.csv` | 거래 원장(diagnostic, 참고용, VAL 4개 그리드점 각각) + 정렬된 예측 CSV | 완료 |

| `tmp/causal_regen_20260516/eth_omega461_evidence_veto_exit_overlay_20260814/`, `..._soft_variant_20260815/` | #18·#21 `report.json` + 거래 원장(diagnostic) | 완료, 둘 다 부정 결과 |
| `tmp/causal_regen_20260516/eth_{h48qual,zig075}_evidence_signal_context_20260814/`, `omega4_2_trade_risk_sidecar_20260622_odyssey2_{h48qual,zig075}_evidence_signal_ctx_20260814/` | #20 컨텍스트 피쳐 CSV + 재학습된 사이징 사이드카 | 완료, 라이브 미승격(부정 결과) |
| `tmp/causal_regen_20260516/eth_omega461_evidence_intervention_surface_ceiling_20260815/report.json`, `trade_surface_{2025q1,2025q2,2025q3,val,oos_q1,oos_q2}.csv`, `counterfactual_ledger_{창}_{always_first_fire,hindsight_best_fire}.csv` | #22 감사 산출물: 창별 표면/천장/랜덤대조 + 거래별 발화·반사실 수익률 원장. **반사실 원장은 미래 정보를 의도적으로 쓴 상한 계산물이라 전략 성과로 인용 금지** | 완료 |

## 재사용 중인 Odyssey(1) 리소스 (그대로 재사용, 재수집 안 함)

- 라이브 h48qual/zig075 번들 + 사이드카 (경로: Odyssey1 레지스트리 "라이브 프로덕션 번들" 절)
- `research_eth_omega461_exit_sweep_20260721.py`(`prep_component`/`replay_exit_variant`), `replay_omega4_6_1_greedy_router_20260706.py`(`greedy_replay`) — 컴포넌트/포트폴리오 리플레이 하네스
- h48qual exit_head 라이브ATR 재라벨 데이터셋/번들 (`scripts/research_eth_omega461_exit_head_liveatr_relabel_20260813.py` 산출물)
- 오토인코더(139→16) 학습 스크립트/latent — Odyssey1 `eth_h48qual_autoencoder_latent_mi_r2_gate_20260812.md` 산출물, 재사용 시 TRAIN 윈도우 일치 여부 확인 필요
- 앙상블 불일치(epistemic MI) 추출 로직 — `scripts/diagnose_eth_h48qual_ensemble_disagreement_20260811.py`(Depeweg MI 분해, `.mean(dim=1)` 풀링 전 k=8 멤버 출력 필요)

## 인프라

| 리소스 | 위치 | 용도 | 상태 | 주의사항 |
|---|---|---|---|---|
| GPU 서버 핸드오프 | `scripts/ops/handoff.sh` | 재학습은 dev(GPU 없음) 대신 서버에서 | 활성 | 서버는 라이브 트레이딩 박스와 공유 — 한 번에 다 밀어넣지 않는다([[feedback_gpu_backlog_offload_to_server]]) |

## 미검증 후보 / 보류

- CNN 캔들차트 exit_head 재적용 — 낮은 우선순위(계약 문서 트리아지 참고)
- One-vs-rest exit_head 재적용 — 구조적으로 대응 안 됨, 스킵
- MFE 분위수 회귀 세 번째 재적용 — 이미 두 컨텍스트에서 실패, 명확한 새 컨텍스트 없으면 보류
