# ETH V자반등 경제라벨 자동매매 규격 (2026-09-02)

배선 전 규격서. 근거 전문은 `docs/homer/v_rebound_open_issues_20260901.md` **20절**.
라이브 코드는 `scripts/live_eth_v_rebound_econ_autotrade_signal_20260902.py`(판단 산출 전용,
`trading_bot.py` 미배선).

⚠️**기존 배포 신호와 다른 모델이다.** `live_eth_sweep_v_rebound_signal_20260829.py`(대시보드
표시 전용)는 라벨이 "모양"(1.5×ATR 튐 + giveback≤0.20)이고 매매 엣지가 입증되지 않았다.
이 후보는 라벨이 **"이 진입이 비용 후 이익인가"**(결과)다.

---

## 1. 모델

| 항목 | 값 |
|---|---|
| 라벨 | `(open[i+1] 진입 → 트레일링 SL5.0/ARM1.5/Trail0.1×ATR → 비용 10bp) > 0` |
| 학습 라벨률 | 0.7585 (롱 0.7571 / 숏 0.7573 — 방향 대칭) |
| 피쳐 | Tier0 22 + rsi = **23** (⚠️154피쳐 확장은 전 구성에서 악화 → 제외) |
| 모델 | TabPFN **5시드 앙상블**, 시드당 컨텍스트 18,000행 |
| 시드 | 20260829 / 141592 / 271828 / 577215 / 20260902 |
| 동결 아티팩트 | `data/labels/eth_5m_v_rebound_econ_label_20260902/tabpfn_train_context_frozen_econ_5seed_20260902.csv` |
| 학습 구간 | TRAIN < 2025-09-01 (348,474행 중 시드별 추출) |

라벨 산출 셀 `(5.0, 1.5, 0.1)`은 **사전 지정**이다 — 9-10에서 매 봉 모델과 giveback 모델이
독립적으로 같은 값을 고른 교차수렴 지점. VAL에서 고르면 라벨이 평가셋을 훔쳐본다.

## 2. 서빙 규격 (VAL 선정 → OOS·HOLDOUT 각 1회 검증. 재조정 금지)

    임계값   p >= 0.8158        (5시드 앙상블 평균 확률, VAL 상위 5%)
    진입     다음 봉 시가        (지연 1~2봉까지 강건함이 실측됨)
    손절     entry ∓ 5.0 × ATR
    무장     ±1.5 × ATR 도달 시 트레일 개시
    트레일   0.1 × ATR           (한 방향으로만 조임)
    한도     동시보유 5

## 3. 검증 결과 (전부 방향뒤집기 대조군 통과)

| 구간 | n | 건/일 | 기대값 | 누적 | 뒤집기 | 최대DD | 연속손실 |
|---|---|---|---|---|---|---|---|
| VAL (선정) | 1,851 | — | +3.63bp | +6,714bp | — | −4,279bp | 11 |
| OOS (1회) | 1,383 | 15.71 | +7.98bp | +11,031bp | +0.32bp | −5,032bp | 13 |
| **HOLDOUT (1회)** | **1,987** | **13.18** | **+6.09bp** | **+12,108bp** | **−4.15bp** | **−8,949bp** | **15** |

승률 78.0% / 손익비 0.346 (HOLDOUT). **평균이익 42.2bp, 평균손실 122.1bp** — 음의 왜도.

**실행 강건성 (OOS)**: 진입 1봉 지연 +8.93bp / 2봉 +7.56bp · 비용 12/15/20bp에서
+6.05/+3.05/−1.95bp(**손익분기 ≈18bp**) · 슬리피지 왕복 2/4/6bp에서 +6.05/+4.05/+2.05bp ·
동시보유 5/3/1에서 +8.05/+9.94/+9.15bp.

## 4. ⭐사이징 (측정치에서 유도)

트레이드당 **notional 비중 f** 를 계좌 대비로 정한다. bp는 notional 기준이므로:

| f (notional) | 5개월 수익 | **최대DD** | 연속 15패 | 단일 손절 | 레버3 마진/건 | 5건 동시 마진 |
|---|---|---|---|---|---|---|
| 5% | +6.1% | −4.5% | −0.92% | −0.061% | 1.67% | 8.3% |
| **10% (권장)** | **+12.1%** | **−8.9%** | −1.83% | −0.122% | 3.33% | **16.7%** |
| 15% | +18.2% | −13.4% | −2.75% | −0.183% | 5.00% | 25.0% |

CLAUDE.md 선물 사이징 계약: `notional = margin_fraction × leverage`. 레버 3 기준
f=10%면 `margin_fraction = 0.0333`, 5건 동시 시 계좌 마진 16.7%.

**권장 f=10%** — 최대DD를 계좌의 9% 안에 두면서 5개월 +12%(연 ~29% 환산). HOLDOUT 실측
최대DD가 그대로 재현된다는 가정이며, **미래 낙폭이 이보다 클 수 있다.**

⚠️**한도 3이 OOS에서 낙폭 39% 낮고 건당 기대값은 더 높았다**(+9.94bp, DD −3,103bp).
그러나 VAL이 고른 값은 5다. 한도를 3으로 바꾸려면 **OOS를 보고 고르는 것**이 되므로,
바꾸려면 사이징 정책으로서 별도 근거를 세워야 한다.

## 5. ⚠️ 알려진 리스크

1. **레짐 적응은 하지만 완벽하지 않다.** HOLDOUT 롱 비중 64.8%(하락장이던 VAL/OOS는 3~11%)로
   방향을 바꾸는 것이 확인됐다. 그러나 2026-06(시장 −21.95%)에 **72.6% 롱**으로 들어가
   그 달 −3.25bp. **월 단위로 크게 틀릴 수 있다.**

   | 월 | 시장 | 모델 롱 비중 | 월 기대값 |
   |---|---|---|---|
   | 2026-04 | +7.26% | 66.3% | +18.87bp |
   | 2026-05 | −11.32% | 43.1% | −6.12bp |
   | 2026-06 | −21.95% | 72.6% ❌ | −3.25bp |
   | 2026-07 | +18.27% | 68.2% | +7.87bp |
   | 2026-08 | +31.45% | 76.4% | +15.42bp |

2. **음의 왜도.** 승률 78%지만 지면 3배로 잃는다. 손절이 5.0×ATR(≈125bp)로 넓다.
3. **HOLDOUT 소진.** 이 풀 정의로는 재노출 불가. 다음 검증은 실시간 섀도우뿐이다.
4. **비용 민감도.** 손익분기 ≈18bp. 스프레드 확대·펀딩 악화가 겹치면 마진이 얇아진다.

## 6. 배선 전 남은 것

- [ ] **실시간 섀도우 운용** (가상주문, 최소 2~4주) — HOLDOUT이 소진됐으므로 유일한 추가 검증
- [ ] `trading_bot.py` 배선 + 크로스심볼 노출 캡 연동
- [ ] 사이징 최종 결정 (f, 동시보유 한도)
- [ ] 킬스위치 기준 (월 손실 한도, 연속손실 한도)

⚠️**자동매매 활성화는 사용자의 명시적 결정 사항이다.** 이 문서와 라이브 스코어러는
그 결정을 위한 준비물이며, 어떤 주문도 내지 않는다.
