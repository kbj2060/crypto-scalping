# ETH V자반등 경제라벨 자동매매 규격 (2026-09-02)

배선 전 규격서. 근거 전문은 `docs/homer/v_rebound_open_issues_20260901.md` **20절**.
라이브 코드는 `scripts/live_eth_v_rebound_econ_autotrade_signal_20260902.py`(판단 산출 전용,
`trading_bot.py` 미배선).

⚠️**기존 배포 신호와 다른 모델이다.** `live_eth_sweep_v_rebound_signal_20260829.py`(대시보드
표시 전용)는 라벨이 "모양"(1.5×ATR 튐 + giveback≤0.20)이고 매매 엣지가 입증되지 않았다.
이 후보는 라벨이 **"이 진입이 비용 후 이익인가"**(결과)다.

---

## 1. 모델

| 항목 | 값 |
|---|---|
| 라벨 | `(open[i+1] 진입 → 트레일링 SL5.0/ARM1.5/Trail0.1×ATR → 비용 10bp) > 0` |
| 학습 라벨률 | 0.7585 (롱 0.7571 / 숏 0.7573 — 방향 대칭) |
| 피쳐 | Tier0 22 + rsi = **23** (⚠️154피쳐 확장은 전 구성에서 악화 → 제외) |
| 모델 | TabPFN **5시드 앙상블**, 시드당 컨텍스트 18,000행 |
| 시드 | 20260829 / 141592 / 271828 / 577215 / 20260902 |
| 동결 아티팩트 | `data/labels/eth_5m_v_rebound_econ_label_20260902/tabpfn_train_context_frozen_econ_5seed_20260902.csv` |
| 학습 구간 | TRAIN < 2025-09-01 (348,474행 중 시드별 추출) |

라벨 산출 셀 `(5.0, 1.5, 0.1)`은 **사전 지정**이다 — 9-10에서 매 봉 모델과 giveback 모델이
독립적으로 같은 값을 고른 교차수렴 지점. VAL에서 고르면 라벨이 평가셋을 훔쳐본다.

## 2. 서빙 규격 (VAL 선정 → OOS·HOLDOUT 각 1회 검증. 재조정 금지)

    임계값   p >= 0.8158        (5시드 앙상블 평균 확률, VAL 상위 5%)
    진입     다음 봉 시가        (지연 1~2봉까지 강건함이 실측됨)
    손절     entry ∓ 5.0 × ATR
    무장     ±1.5 × ATR 도달 시 트레일 개시
    트레일   0.1 × ATR           (한 방향으로만 조임)
    한도     동시보유 5

## 3. 검증 결과 (전부 방향뒤집기 대조군 통과)

| 구간 | n | 건/일 | 기대값 | 누적 | 뒤집기 | 최대DD | 연속손실 |
|---|---|---|---|---|---|---|---|
| VAL (선정) | 1,851 | — | +3.63bp | +6,714bp | — | −4,279bp | 11 |
| OOS (1회) | 1,383 | 15.71 | +7.98bp | +11,031bp | +0.32bp | −5,032bp | 13 |
| **HOLDOUT (1회)** | **1,987** | **13.18** | **+6.09bp** | **+12,108bp** | **−4.15bp** | **−8,949bp** | **15** |

승률 78.0% / 손익비 0.346 (HOLDOUT). **평균이익 42.2bp, 평균손실 122.1bp** — 음의 왜도.

**실행 강건성 (OOS)**: 진입 1봉 지연 +8.93bp / 2봉 +7.56bp · 비용 12/15/20bp에서
+6.05/+3.05/−1.95bp(**손익분기 ≈18bp**) · 슬리피지 왕복 2/4/6bp에서 +6.05/+4.05/+2.05bp ·
동시보유 5/3/1에서 +8.05/+9.94/+9.15bp.

## 4. ⭐사이징 (측정치에서 유도)

트레이드당 **notional 비중 f** 를 계좌 대비로 정한다. bp는 notional 기준이므로:

| f (notional) | 5개월 수익 | **최대DD** | 연속 15패 | 단일 손절 | 레버3 마진/건 | 5건 동시 마진 |
|---|---|---|---|---|---|---|
| 5% | +6.1% | −4.5% | −0.92% | −0.061% | 1.67% | 8.3% |
| **10% (권장)** | **+12.1%** | **−8.9%** | −1.83% | −0.122% | 3.33% | **16.7%** |
| 15% | +18.2% | −13.4% | −2.75% | −0.183% | 5.00% | 25.0% |

CLAUDE.md 선물 사이징 계약: `notional = margin_fraction × leverage`. 레버 3 기준
f=10%면 `margin_fraction = 0.0333`, 5건 동시 시 계좌 마진 16.7%.

**권장 f=10%** — 최대DD를 계좌의 9% 안에 두면서 5개월 +12%(연 ~29% 환산). HOLDOUT 실측
최대DD가 그대로 재현된다는 가정이며, **미래 낙폭이 이보다 클 수 있다.**

⚠️**한도 3이 OOS에서 낙폭 39% 낮고 건당 기대값은 더 높았다**(+9.94bp, DD −3,103bp).
그러나 VAL이 고른 값은 5다. 한도를 3으로 바꾸려면 **OOS를 보고 고르는 것**이 되므로,
바꾸려면 사이징 정책으로서 별도 근거를 세워야 한다.

## 4-b. ⭐실행 방식 — peg 메이커 진입 권고 (2026-09-02 실측)

`scripts/research_eth_v_rebound_maker_entry_recovery_20260902.py`.

**지정가 오프셋(가격 개선)은 실패한다.** 앵커 쪽으로 물러나면 건당 기대값은 소폭 오르지만
체결률이 무너져 총수익이 줄어든다(VAL 상위2% 기준):

| 오프셋 | 체결률 | n | 건당 | 총 bp |
|---|---|---|---|---|
| 0 (시장가) | 100.0% | 1,405 | +12.12bp | **+17,029** |
| −0.10×ATR | 83.0% | 1,166 | +12.34bp | +14,391 |
| −0.25×ATR | 66.8% | 939 | **+13.57bp** | +12,746 |
| −0.50×ATR | 42.5% | 597 | +12.35bp | +7,371 |

되돌림을 기다리면 그냥 가버리는 건들을 놓친다. VAL도 오프셋 0을 골랐다.

**반면 메이커 수수료는 확실히 이득이다.** OOS 동일 조합:

| 비용 | 기대값 | 총 | 뒤집기 |
|---|---|---|---|
| 테이커 10.0bp | +6.14bp | +15,935bp | −9.19bp |
| **메이커진입 8.11bp** | **+8.03bp** | **+20,842bp** | −7.30bp |

**+31% 개선.** 저장소 peg-maker 섀도우 실측이 **체결률 100% · 평균 3.11bp**이므로
(`eth_maker_fill_shadow_realized_cost_checkpoint_20260824`), **오프셋 없이 현재가에 붙이는
peg 지정가**로 체결률을 잃지 않고 수수료만 절감할 수 있다.

**⇒ 실행 권고: peg 메이커 진입(오프셋 0). 청산은 트레일링 스톱이라 구조상 테이커.**
⚠️승격 판정은 여전히 테이커 10.0bp 기준이다(수수료 우대 가정 금지 규칙).
⚠️이 실험은 동시보유 한도를 적용하지 않아 n=2,596이다 — 3절 포트폴리오 수치(n=1,383)와
직접 비교하지 말 것.

## 4-c. ⭐룩어헤드/오염 감사 (2026-09-02, 섀도우 전 관문)

`scripts/audit_eth_v_rebound_econ_lookahead_contamination_20260902.py`.
이 파이프라인은 2026-09-02에 새로 만든 것이라 기존 Tier0 감사가 커버하지 않는 경로가 있었다 --
특히 **피쳐가 VAL_END와 무관하게 항상 전체 CSV로 계산된다**(`build_all_bar_frame()`은
`_tier0.SOURCE`를 통째로 읽는다). 즉 HOLDOUT 데이터가 물리적으로 같은 계산에 들어간다.

### ⭐T1 절단 재계산 — ✅ 통과 (결정적)

데이터를 시점 T에서 자르고 피쳐를 **처음부터 다시 계산**해 전체 계산본과 대조.
후방 전용이면 t≤T의 값이 비트 단위로 같아야 한다. 내부 코드(`compute_indicators`/
`add_creative_*`/`add_broad_*`/`add_causal_columns`)를 읽지 않고 전 경로를 한 번에 검증한다.

| 절단점 | 시각 | 비교 행 | 결과 |
|---|---|---|---|
| 126,211 | 2025-03-13 | 2,000 | ✅ 차이 0 |
| 182,306 | 2025-09-24 | 2,000 | ✅ 차이 0 |
| 238,400 | 2026-04-07 | 2,000 | ✅ 차이 0 |
| 272,056 | 2026-08-02 | 2,000 | ✅ 차이 0 |

**전 피쳐가 후방 전용.** 전체 CSV로 계산해도 오염이 없다.

### ✅T6 단일피쳐 방향 AUC — 통과, 인과 서명 확인

| 피쳐 | 다음봉 | 직전봉 |
|---|---|---|
| delta_z | 0.4846 | **0.8305** |
| ret3_z | 0.4685 | **0.7304** |
| p_fast | 0.4657 | **0.7019** |
| ndi(최대편차) | 0.5306 | 0.4116 |

0.55 초과 없음. **직전봉 AUC ≫ 다음봉 AUC** — 피쳐가 "방금 일어난 일"을 재고 미래를 못
본다는 정확한 서명이다. 부수: 다음봉 AUC가 대체로 0.465~0.485로 0.5보다 약간 낮다(약한
평균회귀 — V자반등 논리와 정합).

### ✅T4 피쳐 위생 — 통과
23개, 금지 컬럼(`triggers`/`label`/`y`/`net_bp` 등) 혼입 0, 중복 0.

### ℹ️T2 라벨 창의 split 경계 침범 — 15행 (0.004%)
E0 라벨은 진입 후 최대 200봉을 보므로 TRAIN 끝자락 라벨이 VAL 가격을 쓸 수 있다(purge 미적용).
실측 **TRAIN 348,474행 중 15행**(최대 침범 91봉) -- `exit_pos`가 최대 호라이즌이 아니라
**실제 청산봉**이라 대부분 일찍 끝나기 때문. 18,000행 컨텍스트에 들어갈 기댓값이 1행 미만이라
실질 영향 없음. **다만 purge를 적용하지 않았다는 사실은 명시한다.**

### ⚠️T3 TabPFN 배치 의존성 — 정량화 완료: 호출의 0.31%, PnL의 3.1%

청크 크기에 따라 확률이 달라진다(순서 역전은 0 → batch-statistic 정규화로 추정).
백테스트는 20,000행씩, **라이브는 6행씩** 예측하므로 불일치가 실재한다.

⚠️**측정 설계 주의**: 전 행(51,840)을 6행 청크로 재예측하면 시드당 8,640회 호출이 필요한데
**TabPFN은 호출마다 18,000행 컨텍스트를 재인코딩**하므로 비용이 예측 행 수가 아니라 **호출
횟수**에 비례한다(호출당 4초+, 실측 86분에 첫 시드도 미완). 대신 **상한을 재고 해석적으로
적용**했다 -- 작은 표본에서 δ를 측정하고, `|p−CUT| ≤ δ`인 행만 재예측한다. 전수 재예측과
논리적으로 동등하면서 호출이 수백 배 적다.

| | 값 |
|---|---|
| 앙상블 max\|Δp\| (6행 vs 일괄, 600행 표본) | **7.987e-05** (중앙 1.4e-05) |
| 뒤집힐 수 있는 행 (해석적 상한) | 46건 |
| **실제 뒤집힌 건** | **8건 / 호출 2,619건 = 0.31%** |
| 그 거래들의 PnL 합 | +343.6bp = 전체 누적(+11,031bp)의 **3.1%** |

**판정: 문서화하고 진행.** 0.31% 때문에 패딩(라이브 배치를 고정 크기로 채워 배치 통계를
안정화) 같은 새 아티팩트를 도입할 가치가 없다. ⚠️단 **라이브가 유리한지 불리한지는 미확정**
-- 측정 스크립트가 "백테스트에만 있는 호출"과 "라이브에만 있는 호출"의 PnL을 합산해 냈다.
크기 상한만 3.1%로 확정됐다.

⭐**섀도우 러너가 라이브 청크로 돌고 있으므로 이 효과를 포함한 실제 괴리가 자동 측정된다** --
백테스트 기대값(+7.98bp)과 섀도우 실현값의 차이를 볼 때 이 3.1%를 감안할 것.

### ⚠️T5 동일 봉 양방향 동시 호출 — 1.00% → **수정·재배포 완료 (2026-09-02)**
OOS 2,593봉 중 **26봉**에서 롱·숏이 동시에 임계값을 넘는다. 그대로 두면 라이브에서 헤지
포지션이 열린다. 라이브 스코어러에 **확률 높은 쪽만 취하고 동률이면 스킵**하는 규칙을 추가했다.
⚠️백테스트는 양방향을 허용한 상태로 측정됐으므로, 같은 규칙 적용 시의 차이를 별도 확인해야 한다.

**배포 이력**: 수정본을 서버에 반영하고(md5 `b37bc135…` 로컬=서버 일치, 파싱 검증) 섀도우
러너를 재기동했다(구 원장 9건은 `v_rebound_econ_shadow_state.json.bak_pre_t5fix_20260902T051456Z`로
아카이브). 이제 쌓이는 원장에는 헤지 포지션이 섞이지 않는다.

## 5. ⚠️ 알려진 리스크

1. **레짐 적응은 하지만 완벽하지 않다.** HOLDOUT 롱 비중 64.8%(하락장이던 VAL/OOS는 3~11%)로
   방향을 바꾸는 것이 확인됐다. 그러나 2026-06(시장 −21.95%)에 **72.6% 롱**으로 들어가
   그 달 −3.25bp. **월 단위로 크게 틀릴 수 있다.**

   | 월 | 시장 | 모델 롱 비중 | 월 기대값 |
   |---|---|---|---|
   | 2026-04 | +7.26% | 66.3% | +18.87bp |
   | 2026-05 | −11.32% | 43.1% | −6.12bp |
   | 2026-06 | −21.95% | 72.6% ❌ | −3.25bp |
   | 2026-07 | +18.27% | 68.2% | +7.87bp |
   | 2026-08 | +31.45% | 76.4% | +15.42bp |

2. **음의 왜도.** 승률 78%지만 지면 3배로 잃는다. 손절이 5.0×ATR(≈125bp)로 넓다.
3. **HOLDOUT 소진.** 이 풀 정의로는 재노출 불가. 다음 검증은 실시간 섀도우뿐이다.
4. **비용 민감도.** 손익분기 ≈18bp. 스프레드 확대·펀딩 악화가 겹치면 마진이 얇아진다.

## 6. 배선 전 남은 것

- [ ] **실시간 섀도우 운용** (가상주문, 최소 2~4주) — HOLDOUT이 소진됐으므로 유일한 추가 검증
- [ ] `trading_bot.py` 배선 + 크로스심볼 노출 캡 연동
- [ ] 사이징 최종 결정 (f, 동시보유 한도)
- [ ] 킬스위치 기준 (월 손실 한도, 연속손실 한도)

⚠️**자동매매 활성화는 사용자의 명시적 결정 사항이다.** 이 문서와 라이브 스코어러는
그 결정을 위한 준비물이며, 어떤 주문도 내지 않는다.
